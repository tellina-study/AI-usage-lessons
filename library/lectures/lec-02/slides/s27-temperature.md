---
id: s27
type: case_study
section: "Раздел 4. Сэмплинг и генерация"
duration_min: 2.5
assertion: "Температура — делитель логитов: она меняет остроту выбора, но не знания модели"
learning_goal: "Точная механика temperature — формула T=0⇒argmax, P^(1/T) при T>0 — и почему T влияет, хотя порядок токенов не меняется; top-p/top-k вторым эшелоном"
learning_outcomes: [LO4, LO7]
chapter_ref: "§4.2 (chapter-part2.md) [for-slide-s27]"
visual_brief: "Верх — явная формула в Ocean rounded box: «T=0 ⇒ выбор = argmax P(токен)»; «T>0 ⇒ сэмплинг из P^(1/T)-образного распределения (логиты делятся на T до softmax)». 3 версии распределения из s26 side-by-side ниже: T→0 — один столбец «яблоко» 1.0 (argmax), T=1 — исходное распределение, T=1.5 — сглаженное, хвост приподнят. Под ними тонкая строка про top-p (отрез по массе) и top-k (отрез по числу). Отдельный callout «почему T влияет, а порядок токенов не меняется»: выбор — не «топ-1», а случайная выборка ПРОПОРЦИОНАЛЬНО вероятностям; T меняет сами вероятности (заостряет/выравнивает), не их ранжирование. Бейдж «живое демо: один запрос, T=0 ×10 и T=1.5 ×10». Gold на панели T=1 (стандарт)."
interaction: live_comparison
---

# Visible content

## Title bar
«Температура — делитель логитов: меняет остроту выбора, не знания»

## Body
[Формула — верхний блок, Ocean rounded box]

**T = 0** ⇒ выбор = **argmax P(токен)** — берём самый вероятный, без сэмплирования
**T > 0** ⇒ сэмплинг из **P^(1/T)**-образного распределения — логиты делятся на T до softmax

[3 распределения side-by-side, Ocean rounded boxes]

**T → 0** (argmax)
[Один столбец: яблоко ≈ 1.0]
Выбор самого вероятного. Почти одинаковые ответы — «почти» разберём на следующем слайде.

**T = 1** (стандарт) *(gold)*
[Исходное распределение]
Сэмплирование пропорционально вероятностям модели. Естественная вариативность.

**T = 1.5** (сглаживание)
[Распределение приплюснуто, хвост приподнят]
Редкие токены получают реальные шансы — от удачных находок до бессвязности.

[Отдельный callout — почему T влияет, хотя порядок не меняется]
**Порядок токенов по вероятности одинаков при любой T. Но выбор — не «взять топ-1», а случайная выборка пропорционально вероятностям — а T перекраивает сами вероятности: заостряет (T<1) или выравнивает (T>1) их значения.**

[Строка второго эшелона]
**top-p** — отрез хвоста по вероятностной массе · **top-k** — по числу кандидатов. Основная ручка — температура; эти две — тонкая настройка.

[Бейдж живого сравнения]
**Живой прогон:** один и тот же запрос — 10 раз при T=0 и 10 раз при T=1.5.

[Строка действия]
**Что делать:** T под задачу — 0–0.3 для кода и классификации, 0.7+ для генерации текста (детали — таблица на следующем слайде).

## Speaker notes

Эти ручки вы знаете — уточним механику до точной формулы. При T=0 выбор — это argmax P(токен): детерминированно берём токен с максимальной вероятностью, без всякого сэмплирования. При T>0 модель сэмплирует из распределения, преобразованного по правилу P в степени 1/T — технически это означает, что сырые логиты делятся на T перед тем, как пройти через softmax и превратиться в вероятности.

Здесь стоит остановиться на вопросе, который часто вызывает путаницу: порядок токенов по вероятности не меняется от температуры — самый вероятный токен остаётся самым вероятным при любой T. Тогда почему температура вообще на что-то влияет, если ранжирование одно и то же? Ответ в том, что выбор токена — это не «взять топ-1 из списка», а случайная выборка, где вероятность выбора каждого токена пропорциональна его вероятности в распределении. А температура меняет сами эти вероятности, не их порядок: при T меньше единицы распределение заостряется — лидер получает ещё больше массы вероятности, аутсайдеры теряют шансы почти до нуля; при T больше единицы распределение выравнивается — разрыв между лидером и остальными токенами сокращается, и у токенов, которые раньше были почти невозможны, появляется реальный шанс быть выбранными. Именно поэтому T=0 — предельный случай: заострение доведено до предела, и вероятность лидера становится равна единице — argmax.

Второй эшелон: top-p отрезает хвост по вероятностной массе — сэмплируем из минимального набора самых вероятных токенов с суммарной вероятностью не меньше p; top-k отрезает по числу кандидатов. Практика не изменилась: основная ручка — температура, эти две — тонкая настройка.

Теперь живой прогон: один и тот же запрос — «придумай название для сервиса заметок» — десять раз при нуле и десять раз при полутора. Первый ряд даёт почти идентичные ответы — «Заметкин», «Заметкин», «Заметкин», изредка меняется разве что окончание; слово «почти» здесь несёт больше смысла, чем кажется, и ему посвящён следующий слайд. Второй ряд — разброс от удачных находок до бессвязицы: «МыслеПоток», «конспект-туман на рассвете», «блокнот дыхания чисел» (примеры иллюстративные — свой разнобой вы увидите за минуту).

Ценность этого эксперимента, если повторите его сами, не в подтверждении теории, а в калибровке руки: вы увидите, как выглядит хвост распределения вашей конкретной задачи и на какой температуре он начинает протекать в ответы. И обратное упражнение: если ваши промпты в проде работают на температуре по умолчанию — выясните, какая она у вашего провайдера; значения по умолчанию у всех разные и меняются, а осознанный выбор здесь стоит одну строку кода.
