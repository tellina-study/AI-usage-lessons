---
id: s04b
type: assertion_visual
section: "Раздел 0. Введение"
duration_min: 2.5
assertion: "Поток данных в LLM: слова → токены → векторы → LLM → векторы → токены → слова"
learning_goal: "Keystone: 7-стадийный inference-конвейер целиком до первого погружения; повторяется прогрессом на всех дивайдерах"
learning_outcomes: [LO1]
chapter_ref: "§Введение [for-slide-s04b]"
visual_brief: "Горизонтальный 7-стадийный конвейер: Текст → Токены → Векторы → LLM (gold center) → Распределение → Токен → Текст; под каждой стадией короткая подпись. Петля авторегрессии: стрелка от [Токен] назад ко входу конвейера с подписью «токены генерируются по одному; каждый добавляется ко входу». Под конвейером — 4 подкарточки Раздел 1/2/3/4 со стадиями, которые они разбирают. Gold callout: «слова — только на границах; внутри — векторы»."
---

# Visible content

## Title bar
«Поток данных в LLM — туда и обратно»

## Body
[Горизонтальный конвейер, 7 стадий в Ocean rounded boxes; центральная стадия LLM — gold]

```
[Текст] → [Токены] → [Векторы] → [LLM] → [Распределение] → [Токен] → [Текст]
«Привет»  [Прив][ет]  vec₁, vec₂  внимание  p(токен | контекст)  выбран    ответ
```

[Под каждой стадией — короткая подпись: «слова», «id из словаря», «числа», «инференс», «вероятности», «выбор», «обратно в текст»]

[Петля авторегрессии: стрелка от [Токен] назад ко входу конвейера, подпись: «токены генерируются по одному; каждый добавляется ко входу»]

[Под конвейером — 4 подкарточки]
**Раздел 1** — Текст → Токены · **Раздел 2** — Токены → Векторы · **Раздел 3** — LLM: внимание · **Раздел 4** — Распределение → Токен

[Gold callout снизу]
**Слова — только на границах; внутри — векторы.**

## Speaker notes

Это опорная схема всей лекции — конвейер инференса, путь одного запроса от текста до ответа. Посмотрим на него целиком, до первого погружения; дальше мы будем возвращаться к нему на входе в каждый раздел.

Слева — ваш текст. Первое преобразование — токенизация: текст режется на токены, идентификаторы из словаря модели; это Раздел 1. Дальше каждый токен превращается в вектор — список чисел, с которыми нейросеть умеет считать; это Раздел 2. В центре — сама модель: механизм внимания решает, какие части контекста важны для следующего шага; это Раздел 3. На выходе модель выдаёт не ответ, а распределение вероятностей по всему словарю; из него выбирается один очередной токен, он приклеивается к контексту — и цикл повторяется; это Раздел 4. Последний шаг — выбранные токены собираются обратно в текст.

Главное наблюдение: слова существуют только на границах конвейера — на входе и на выходе. Всё внутри — операции над векторами. И обратите внимание на петлю в схеме: токены генерируются по одному, каждый выбранный токен добавляется ко входу — и конвейер прокручивается заново; ответ строится последовательными оборотами этой петли.

Эта ось — не только про сегодня. Все последующие лекции курса лягут на неё же: RAG — это управление тем, что попадает в контекст до конвейера; агенты — цикл вокруг конвейера; оптимизация стоимости — экономика конвейера. Сегодняшняя лекция движется по этой же оси — каждый раздел раскрывает свой участок конвейера вместе с границами применимости.
