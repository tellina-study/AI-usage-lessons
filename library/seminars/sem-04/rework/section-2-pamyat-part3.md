---
section: 2
title: "Ступень 2: память — Кейс 2.3 (операционная задачная память) + пересчёт таймингов"
stage: 2
part: 3
parts: 3
duration_min_this_part: 10.75
slot_lec03: "Слот 1 — «Память» (§4.5–4.6); критерий усложнения памяти из §5.2b"
status: rework-draft-v6
supersedes: "section-2-pamyat-part3.md v5 (сценарий вокруг негативной договорённости «не трогать email-валидацию»; целевой ответ — дисциплина синхронной записи; провал без меры снижения риска; исследование без мостика от проблемы). Раунд правок владельца 2026-09-23 по кейсу 2.3."
---

# Кейс 2.3. Операционная (задачная) память (~10,75 мин)

> **Переписан под контракт стиля и повествования** (см. `rework/section-0-otkrytie.md` §B): из
> gold-рамки проблемного слайда снята строка «Файл был. Практика была. Не хватило одного: вписать
> мелкую договорённость в лог в момент, когда она прозвучала» — это **дословно целевой ответ**
> слайда разбора (§A.4), сказанный до голосования, из-за чего голосование обессмысливалось
> полностью; с дивайдера снята классификация исхода; из студенческого текста убраны «реплики»
> (вокабуляр чата) и «заметьте это слово»; даты файла задачи приведены к сквозному календарю.

> **Раунд правок владельца, 2026-09-23** (четыре замечания по кейсу 2.3, см. §A.8): сценарий
> перестроен вокруг позитивной договорённости («делать так») вместо негативной («не делать X»);
> в исследование добавлен явный мостик от проблемы к предмету измерения; целевой ответ
> переформулирован с человеческой дисциплины на инструкцию агенту в `CLAUDE.md`; в провал добавлена
> мера снижения риска (сверка плана + строка «Сверено: дата»), выведенная из условия уже
> цитируемого источника, без привлечения новых.

**Продолжение раздела 2** — часть 2 (`section-2-pamyat-part2.md`) закончилась мостиком: до сих
пор речь шла о памяти, которая живёт навсегда (решения команды). Этот кейс — про другую ось:
память **одной задачи**, актуальную ровно до её завершения. И, как и Кейс 2.1, это дешёвая
универсальная гигиена (R5-0): практику «файл на задачу» заводят на старте задачи, а не после того,
как кто-то уже потерял нить.

## A. План кейса 2.3

### A.1. Состояние репозитория на входе

`DECISIONS.md` из Кейсов 2.1–2.2 не меняется — этот кейс не про него. В репозитории уже действует
практика «файл на задачу» — команда завела её как общее правило с первого дня работы над
`signup-landing`, той же логикой, что и `DECISIONS.md` в Кейсе 2.1: дёшево завести конвенцию сразу,
не дожидаться боли. Конкретно для многошаговой задачи этого кейса файл создаётся **в момент
постановки задачи**, до первого шага, а не реактивно, когда кто-то уже запутался: `.tasks/active/
signup-wizard.md` с планом из шести шагов существует с самого начала первой сессии.

`[Захвачено реально, живой многошаговой задачи владельца этот кусок больше не ждёт: практика «файл на задачу» существует в демо-репозитории — `.tasks/README.md`, файл текущей задачи `.tasks/active/2026-09-24-dostroit-dugu-seminara-4.md` (статус на каждом шаге, строка «Сверено» с машинной датой, дословный вывод команд вместо «всё работает»), архив `.tasks/done/` и блок «Задачная память» из трёх строк в `CLAUDE.md`; коммит `14fa124` в `tellina-study/signup-landing-demo`, ветка `seminar-4-arc`.
Две границы, которые нельзя стереть вместе с пометкой: задача визарда — условная фактура (настоящий файл ведёт другую задачу), а «с первого дня работы» из абзаца выше репозиторием не подтверждается — в дне-0-коммите `.tasks/` нет, практика заведена на своём кейсе, задним числом в git она не дописывается (README демо-репозитория называет это расхождение со слайдом s07 прямо).]`

### A.2. Проблема — конкретная, на материале `signup-landing`

> Команда завела практику «файл на задачу» с первого дня работы над `signup-landing` — как и
> `DECISIONS.md` в Кейсе 2.1, это дешёвая гигиена, которую заводят сразу, не дожидаясь, что
> кто-то потеряет нить многошаговой задачи. Разработчик ставит агенту задачу на несколько сессий:
> разбить форму `signup-landing` на пошаговый визард (имя → email → подтверждение), сохранив всю
> текущую валидацию. По заведённой практике агент перед началом сам создаёт
> `.tasks/active/signup-wizard.md` с планом из шести шагов.
>
> Первая сессия закрывает шаги 1–2. По ходу шага 2 становится видно, что значения полей нужно где-то
> держать между шагами, и в разговоре — не по плану — принимается решение, **как именно это делать**:
> состояние визарда хранить одним объектом в `sessionStorage` под ключом `signup-wizard`, чтобы
> перезагрузка страницы на третьем шаге не сбрасывала уже заполненные поля. В плане шаг 3 записан
> одной строкой — «Состояние текущего шага (JS)»; как это делать, там не сказано. Договорённость
> живёт ровно до конца этой задачи: после визарда это просто деталь реализации, не правило проекта.
> В «Лог хода» она не попадает — ничто в этот момент не требует её записывать. Сессия обрывается на
> середине шага 3: контекст сжимается.
>
> Вторая сессия. Файл задачи — на месте, он был там с самого начала, это не сюрприз и не забытая
> вещь. Агент открывает его, видит план и отметки «сделано» на шагах 1–2 — всё выглядит актуальным
> и заслуживающим доверия. Про `sessionStorage` в файле нет ни строки: сжатие контекста случилось
> раньше, чем кто-то её записал. Агент делает шаг 3 самым коротким способом — держит состояние в
> переменной модуля, никуда его не сохраняя. Переключение шагов работает, тесты шага 5 зелёные.
> Расхождение всплывает на шаге 6, при ручной проверке полного прохождения: перезагрузка страницы
> на третьем шаге очищает форму — ровно то, ради чего в первой сессии выбрали `sessionStorage`.
> Шаг 3 переделывается целиком, вместе с тем, что успели на него завязать.
>
> На входе во вторую сессию у агента есть ровно то, что лежит в файле: шесть шагов плана и две
> отметки о выполнении. Договорённость про хранение состояния осталась в контексте первой сессии,
> которого больше нет.

> **Что убрано со слайда-проблемы и почему.** Строка-итог в gold-рамке «Файл был. Практика была.
> Не хватило одного: вписать мелкую договорённость в лог в момент, когда она прозвучала» — это
> дословно целевой ответ слайда разбора («строка в `CLAUDE.md`… файл не нужно создавать — нужно
> сказать агенту, когда в него писать»). Прочитанная до голосования, она не подсказывала ответ, а
> заменяла его, и вопрос на следующем слайде терял смысл целиком. На слайде вместо неё —
> состояние: что у агента есть на входе во вторую сессию и чего в этом нет. Диагностика ниже
> остаётся в методическом файле — она верна, просто звучит после голосования.

**Диагностика — и это то место, где рамка кейса меняется относительно интуитивного «агент начал с
нуля».** Файл был, план был, часть прогресса честно отражена. Боль не в отсутствии файла — боль в
том, что **файл, заведённый проактивно, не обновлялся синхронно с ходом работы**, и именно это
опаснее отсутствия файла вообще: разработчик доверяет плану, который выглядит авторитетным, и не
перепроверяет то, что в нём должно было быть, но не попало. Отдельно стоит заметить, чего в
сценарии **не** произошло: никто ничего не забыл и не поленился. Практика заведена, файл ведёт
агент, план он отмечает сам — но ни одна инструкция не говорит ему дописывать в лог то, о чём
договорились в разговоре, а не в плане. Пробел не в добросовестности людей, а в том, что этой
строки нет в инструкциях.

Всё это прямо предвещает количественный результат исследования (§A.4): устаревший план вреднее
отсутствия плана — не потому что план вреден сам по себе, а потому что доверие к нему отключает
бдительность.

Почему этого не могли закрыть Кейсы 2.1–2.2. `DECISIONS.md` (в любой форме) — про решения,
которые остаются в силе навсегда, для всех будущих задач. Ход одной конкретной многошаговой
задачи — не решение проекта, это статус: что уже сделано, что дальше, какие мелкие договорённости
приняты именно в рамках неё. После завершения задачи всё это можно выбросить. И даже если обе
практики — `DECISIONS.md` и «файл на задачу» — заведены проактивно с одного и того же дня 0, они
решают разные вопросы: ни одна из них сама по себе не говорит, **когда** в файл дописывается то,
что изменилось по ходу разговора. Практика отвечает на вопрос «где это лежит», а не «в какой
момент туда попадает».

Это триггер третьего, отдельного вида памяти: **не «что решено навсегда», а «что происходит
прямо сейчас, в одной задаче, между её собственными сессиями» — и остаётся ли записанное о ней
синхронным с реальностью.**

### A.3. Открытый вопрос классу (дословно) + варианты

> «Файл задачи существовал с самого начала, план в нём выглядит актуальным — шаги 1–2 отмечены
> сделанными. И всё равно вторая сессия делает шаг 3 не так, как договорились в первой.
> **Выберите, что нужно сделать иначе — из карточек ниже — так, чтобы файл задачи не
> вводил в заблуждение, а реально отражал ход работы. Обоснуйте выбор.**»

**Карточки-варианты на слайде (пять):**

`надеяться на контекстное окно — не сжимать и не закрывать сессию` · `записать план в
DECISIONS.md` · `правило в CLAUDE.md: дописывать договорённость в файл задачи сразу, как она
прозвучала` · `завести папку на задачу с отдельным файлом под каждый шаг` · `попросить агента
«продолжи с того места, где мы остановились»`

*(Слайд без единой цифры и без ссылки на исследование — грунтовка и разбор идут только после
голосования, §A.4.)*

### A.4. Исследование и разбор

Голосование прошло — теперь можно раскрывать источники. Порядок изложения важен: сначала мостик
от проблемы к предмету исследования, затем практика (что реально делают), потом количественный
риск (что может пойти не так), потом честная оговорка про распространённость.

**Мостик: файл задачи — это и есть письменный план, по которому работает следующая сессия.**
Эта связка нужна явно, иначе переход от «потеряли договорённость» к исследованиям про планы
выглядит подменой темы. Агент, открывающий вторую сессию, не помнит первую — он реконструирует
задачу целиком из того, что записано в файле. Всё, что в файл не попало, для него не «забыто»,
а **не существует**: у него нет способа отличить «про это договорились, но не записали» от «про
это не было речи». Отсюда два следствия, и оба важны для разбора карточек:

1. Полнота файла задачи — это не аккуратность ради аккуратности. Это буквально полнота плана,
   по которому будет действовать исполнитель, не имеющий других источников.
2. Значит, вопрос кейса — частный случай измеримого вопроса: что происходит с работой агента,
   когда план, которым он руководствуется, неполон или разошёлся с реальностью. **Именно это и
   измеряли** — сначала в виде практики, которая удерживает план свежим, затем в виде цифр о том,
   чего стоит несвежий план.

**Практика — от Pokémon до recitation.**

- **Anthropic, инженерный блог.** «Structured note-taking» — тот же паттерн, что и в Кейсе 2.2,
  но применённый к ходу одной задачи, не к решениям проекта. Показательный кейс — агент играет в
  Pokémon, ведя простые структурированные заметки (счётчики, текущая цель) через тысячи шагов,
  переживая сбросы контекста без потери прогресса.
- **Manus, техника «recitation».** Агент непрерывно переписывает `todo.md` в конец контекста
  («мягкое управление вниманием» — модель охотнее следует тому, что видела последним). На задачах
  порядка пятидесяти вызовов инструментов это снижает уход от цели (goal drift).
- **Реальный паттерн из практики.** Проект metasphere-agents (открытый код) реализует прямой
  аналог: файл на задачу в `.tasks/active/`, с планом/статусом/логом, который архивируется в
  `.tasks/done/` по завершении.

**Риск, ради которого этот кейс вообще существует: устаревший план вреднее отсутствия плана.**
Evaluating Plan Compliance (`arXiv:2604.12147`, 21 120 траекторий, SWE-bench Verified/Pro): наличие
плана в среднем повышает долю решённых задач — но **«bad plans hurt performance more than no plan
at all»**: плохой план хуже отсутствия плана вообще. Комплаенс агента к собственному плану падает
примерно на 13 процентных пунктов на более сложном бенчмарке. Это прямо объясняет, почему файл,
заведённый правильно, всё равно подвёл в нашем сценарии: не потому что практика плохая, а потому
что план, переставший отражать реальность, хуже, чем полное отсутствие плана — он выглядит
авторитетным именно тогда, когда вводит в заблуждение. Полная картина, включая второй источник
(100% провала по устаревшему плану без проверки), — на слайде провала (§A.7), где ей и место как
количественному ядру ограничения.

**Разбор пяти карточек зала — на фоне этой грунтовки:**

| Вариант | Где это работает | Почему здесь ещё рано — и что вместо |
|---|---|---|
| «Не сжимать, не закрывать сессию» | Для задачи, целиком укладывающейся в одну короткую сессию | Контекстное окно конечно; на многошаговой задаче рано или поздно случится `/compact` или разрыв сессии — это не стратегия, а отсутствие стратегии |
| «Записать план в `DECISIONS.md`» | Никогда — не тот слой памяти | `DECISIONS.md` — про «почему» для решений, которые остаются в силе навсегда; статус одной задачи туда не относится. Смешение испортит оба журнала — ровно та ошибка, которую Кейс 2.1 разбирал на карточке про упавший тест |
| «Правило в `CLAUDE.md`: договорённость — сразу в файл задачи» | Ровно наш случай — файл уже есть с первого дня, не хватило строки в инструкциях о том, **когда** в него писать | Целевой ответ для этого масштаба; см. решение ниже. Файл не нужно создавать — нужно, чтобы тот, кто его ведёт, дописывал по ходу, а не «когда будет время». Адресат правила — агент, ведущий файл, а не память разработчика |
| «Папка на задачу с файлом под каждый шаг» | На задачах с параллельными подзадачами, несколькими агентами/субагентами, или когда лог одного файла сам разрастается до неудобного объёма | Оверинжиниринг для одной линейной задачи здесь и сейчас — и не решает саму проблему синхронности записи. Паттерн реально масштабируется до папки — это раскрывается в Лекции 3 подробнее; в этом семинаре упрощаем до файла |
| «Попросить "продолжи с того места"» | Внутри одной непрерывной сессии, до сжатия контекста | После `/compact` или в новой сессии буквально нечего продолжать сверх того, что записано в файле, — а записано было не всё. Тот же механический факт, что «агент сам запомнит» в Кейсе 2.1 |

Целевой ответ — **строка в `CLAUDE.md`, обязывающая агента дописывать договорённость в файл задачи
в тот момент, когда она прозвучала**, а не по завершении шага или сессии. Формулируется как
инструкция тому, кто и так ведёт этот файл, — не как напоминание разработчику не забыть дописать
руками. И сразу с оговоркой из Кейса 1.2: правило в тексте инструкций — просьба, а не закон, так
что срабатывать оно будет не всегда. Направление от этого не меняется: просьба, адресованная
исполнителю, который ведёт файл каждой сессией, — механизм, а человеческая памятливость —
нет. Плюс явное уточнение, что это упрощённая версия паттерна, масштабируемого до папки.

### A.5. Конкретное решение: что добавляется в репозиторий

Файл на задачу — план, лог хода, результаты — уже существует с первого шага задачи, это не новое
явление этого кейса. Новое здесь — **три строки в `CLAUDE.md`**, то есть в инструкциях, которые
агент читает в начале каждой сессии. Они адресованы тому, кто ведёт файл, и говорят, **когда** в
него писать:

```markdown
## Задачная память
- На каждую многошаговую задачу — файл `.tasks/active/<задача>.md`:
  план, лог хода, результаты.
- Договорённость, принятую по ходу работы, дописывать в «Лог хода»
  сразу, тем же ответом, а не по завершении шага или сессии.
- В начале и в конце сессии сверять план с состоянием репозитория
  и ставить строку «Сверено: <дата>».
```

Третья строка — мера против устаревшего плана, её обоснование в §A.6; первые две закрывают
сценарий §A.2. Никакой ручной работы за человеком эти строки не закрепляют: файл ведёт агент,
и правило говорит ему, в какой момент дописывать, — разработчик ничего не пишет и ничего не
обязан помнить.

Что получается в самом файле задачи (фактура визарда — иллюстративная; сама практика и её
артефакты захвачены реально: `.tasks/active/`, `.tasks/done/` и тот же блок в `CLAUDE.md`,
коммит `14fa124`) — это файл, в котором правило сработало: мид-сессионная договорённость про
хранение состояния попала в лог до того, как сессия оборвалась:

```markdown
# Задача: разбить форму на пошаговый визард

## План
1. [x] Разметка трёх шагов (имя / email / подтверждение)
2. [x] Перенос текущей валидации без изменений
3. [ ] Состояние текущего шага (JS)
4. [ ] Кнопки «назад»/«далее»
5. [ ] Тесты на переключение шагов
6. [ ] Ручная проверка полного прохождения

## Лог хода
- 2027-01-26: шаги 1–2 закрыты. Состояние визарда держим одним объектом в
  sessionStorage, ключ signup-wizard: перезагрузка на шаге 3 не должна
  сбрасывать заполненные поля. Решено по ходу шага 2, в плане этого нет.
- 2027-01-28: начат шаг 3, не закончен — контекст сжат в середине.

## Результаты (заполняется по завершении)
—

## Сверено
2027-01-28, начало сессии: план сверен с репозиторием, расхождений нет.
```

Разница между этим файлом и файлом из сценария §A.2 — не в структуре и не в том, что файл появился
позже: разница ровно в одной строке лога, записанной вовремя, до сжатия контекста, а не после.
Это и есть содержательный урок кейса: артефакт один и тот же, решает не наличие файла, а момент
записи — и момент записи задаётся инструкцией, а не памятью участников.

Честная граница, та же, что в Кейсе 1.2: эти строки — просьба текстом. Агент их читает и **может**
выполнить; гарантии, что дописал каждый раз, они не дают. Именно поэтому третья строка (регулярная
сверка) стоит рядом с первыми двумя: одна просьба страхует другую, но ни одна из них не барьер.

По завершении задачи файл переносится в `.tasks/done/` (или удаляется, если ценности после
задачи он не несёт) — это архивирование, а не хранение навсегда: главное отличие от
`DECISIONS.md`, который **никогда** не архивируется и не удаляется.

### A.6. Провал / ограничение

**Количественно подтверждённый риск: устаревший план вреднее отсутствия плана.** Это тот же
источник, что кратко прозвучал в §A.4 как объяснение механизма, — здесь он раскрыт полностью,
как и полагается количественному ядру ограничения.

- Evaluating Plan Compliance (`arXiv:2604.12147`, 21 120 траекторий): «bad plans hurt performance
  more than no plan at all» — прямая количественная поддержка. Комплаенс к собственному плану
  падает примерно на 13 п.п. на более сложном бенчмарке: чем сложнее задача, тем менее надёжно
  агент реально следует тому, что сам же записал в план.
- Fresh Memory, Stale Plans (`arXiv:2609.03340`): без проверки актуальности плана на зависимостях
  исполнитель действовал по устаревшему плану в **100%** из 30 контролируемых прогонов. Файл
  задачи, который никто не обновляет по ходу работы, — не нейтральный артефакт, а источник
  систематически неверных действий.
- Практический вывод для нашего файла: пункт плана, ставший неактуальным (например, после
  дополнительного решения в `DECISIONS.md`, отменяющего часть подхода), должен быть либо
  вычеркнут, либо помечен как устаревший в момент, когда это стало известно, — не оставлен
  как есть до следующей сессии.

**Что из этого следует делать — мера, а не только предостережение.** Условие эксперимента Fresh
Memory, Stale Plans названо в самом результате: 100% — это доля прогонов **без проверки
актуальности плана**. То есть измерена не неизбежность провала, а цена пропуска одного конкретного
шага. Отсюда прямая мера, и это третья строка того же блока в `CLAUDE.md` (§A.5):

> В начале **и в конце** сессии сверять план с состоянием репозитория и ставить строку
> «Сверено: `<дата>`».

Три детали, каждая из которых делает работу:

1. **«И в конце», а не только в начале.** Сверка в начале защищает текущую сессию; сверка в конце
   защищает следующую — именно она дописывает то, что выяснилось под конец, пока контекст ещё
   цел. В нашем сценарии сессия оборвалась на середине шага 3, и сверки в конце не случилось —
   но сверка в начале второй сессии сравнила бы план с кодом и показала, что шаг 3 начат, а как
   именно его делать — в файле не сказано. Это не гарантия, но это место, где расхождение видно.
2. **Явная строка «Сверено: дата».** Без неё следующая сессия не может отличить свежий план от
   плана трёхнедельной давности: и тот и другой выглядят одинаково уверенно — тот самый механизм,
   из-за которого плохой план вреднее его отсутствия. Отметка о свежести превращает невидимое
   свойство файла в видимое.
3. **Сверка с репозиторием, а не с памятью.** Сверяется план с тем, что реально в коде и в
   истории изменений, — источник, переживающий сжатие контекста.

Ограничение меры называем сразу, по формуле Кейса 1.2: это снова просьба текстом. Она снижает
риск, а не снимает его; в терминах Manus (§A.4) сверка — тот же приём удержания плана свежим,
что и «recitation», только между сессиями, а не внутри одной. Механического барьера, который
не даст начать работу по несверенному плану, мы сегодня не ставим — это материал Семинара 5.

**Кейс-стади «Index Sickness» — сильный, но n=1.**

Реальный проект, 391 сессия за месяц: по мере роста правил агент терял связь с реальной
семантикой проекта — правила накапливались, противоречили друг другу, и агент начинал следовать
букве вместо духа. Решение — физическое разделение базовых, стабильных инструкций и append-only
лога текущей сессии (тот же принцип разделения слоёв, что мы проводили весь раздел: стабильное
отдельно от текущего). Результат — минус 75% объёма AI-инструкций, ноль рецидивов за
последующие 150 сессий. Честно: это один проект, один автор, action research, не контролируемый
эксперимент — показательная иллюстрация механизма, не статистическое доказательство того, что так
будет всегда.

**Практика пока нишевая — сказать прямо.**

Harness Engineering study (2853 реальных репозитория): паттерн «файл/папка на задачу» — не
мейнстрим, доминируют статические context-файлы. Это не «все так делают, а вы отстали» — это
практика, которую продвигают отдельные лидеры, и она пока не устоялась как норма индустрии.

### A.7. Критерий «здесь ещё рано» — переход от файла к папке

Чек-лист: если верно большинство пунктов — одного файла на задачу достаточно, папка не нужна.

- Задача линейная: шаги идут по порядку, не параллельно.
- Работает один агент (без субагентов/параллельных веток) и один разработчик.
- Лог хода помещается в разумный файл (сотни строк, не тысячи) без потери читаемости.
- Результатов и артефактов на выходе немного — не нужен отдельный файл на каждый.

Если хотя бы половина неверна — переход к папке на задачу (`.tasks/active/<task>/plan.md`,
`log.md`, `results/`) оправдан. Это уже за пределами сегодняшнего занятия — подробно
раскрывается в Лекции 3.

### A.8. Слайды кейса 2.3

**Fix R5-3 (вопрос до исследования, весь раунд v5).** До этого фикса исследование (Pokémon/Manus
+ plan-compliance статистика) звучало в речи фасилитатора между сценарием и вопросом (см. B.3
предыдущей версии этого файла), хотя формальной строки в таблице «Слайды кейса» v4 под это не
было заведено — тот же пробел предыдущих QA-проходов, что и в кейсе 2.2 (см. §R5-3). Новый
порядок: проблема → **вопрос** (без единой цифры) → [голосование] → **исследование + разбор**,
слитые в один новый слайд/заход 2.3.3/2.3.3b с собственным таймингом.

**Fix R5-0 (день 0, кейс 2.3).** Проблема (2.3.1) переписана: практика «файл на задачу» заведена
с первого дня, файл создан на старте задачи, а не реактивно посреди работы. Боль сместилась с
«агент начинает с нуля» на «файл устарел в одном месте, которое имело значение, и это опаснее
отсутствия файла». Решение (2.3.4) переписано: новое — не файл, а строка в инструкциях агента.

**Правки раунда владельца (2026-09-23), кейс 2.3 — четыре.** (1) Сценарий проблемы (2.3.1)
перестроен вокруг **позитивной** договорённости — «состояние визарда хранить одним объектом в
`sessionStorage`», то есть «делать так», вместо прежней негативной «не трогать email-валидацию»:
механизм кейса (мелкая мид-сессионная договорённость не попадает в файл, теряется при сжатии,
всплывает провалом на следующей сессии) сохранён целиком, изменилась только фактура. (2) В
исследование (2.3.3) добавлен явный мостик «файл задачи — это и есть письменный план следующей
сессии; чего в нём нет, для агента не существует» — без него переход от проблемы к цифрам про
планы читался как смена темы. (3) Целевой ответ по всему кейсу переформулирован с «дисциплины»
(человек не забывает дописывать) на **инструкцию агенту в `CLAUDE.md`** — механизм, а не
человеческая памятливость; с явным эхом формулы кейса 1.2 «правила в промпте — просьбы». (4) В
провал (2.3.5) добавлена мера снижения риска — сверка плана с репозиторием в начале **и в конце**
сессии плюс строка «Сверено: дата» в файле, выведенная из условия уже цитируемого источника
`arXiv:2609.03340` (100% — это доля прогонов *без проверки актуальности*).

| № | Слайд | Содержимое | Материал |
|---|---|---|---|
| 2.3.0 | Разделитель кейса 2.3 | «Кейс 2.3 · операционная память», смена оси; тег темы «память одной задачи», без классификации исхода | — |
| 2.3.1 | **[СЛАЙД-ПРОБЛЕМА]** Сценарий-боль: файл задачи существовал с начала, но мид-сессионная договорённость о том, **как** делать шаг 3 (`sessionStorage`), в лог не попала до сжатия контекста | файл выглядит достоверным и всё равно вводит в заблуждение; тесты зелёные, расхождение всплывает на ручной проверке; диагностика «устаревший план опаснее отсутствия плана» | фактура визарда иллюстративная; сама практика и её артефакты захвачены реально — `.tasks/README.md`, `.tasks/active/`, `.tasks/done/` + блок «Задачная память» в `CLAUDE.md`, коммит `14fa124` в `tellina-study/signup-landing-demo`, ветка `seminar-4-arc` |
| 2.3.2 | **[СЛАЙД-ВОПРОС]** Открытый вопрос + пять карточек-вариантов | БЕЗ таблицы разбора, БЕЗ цифр, БЕЗ подсветки целевого ответа | — |
| 2.3.3 | **[СЛАЙД-ИССЛЕДОВАНИЕ, Fix R5-3, новый]** Мостик «файл задачи = письменный план следующей сессии» → Pokémon/Manus/metasphere-agents практика → краткое упоминание риска устаревшего плана | грунтовка §A.4, звучит после голосования — впервые получила собственный слайд и тайминг; мостик добавлен раундом владельца | — |
| 2.3.3b | **[СЛАЙД-РАЗБОР]** Таблица разбора пяти позиций + целевой ответ | мелким шрифтом вопрос сверху для контекста; таблица §A.4 целиком, идёт сразу за 2.3.3 как один смысловой заход | — |
| 2.3.4 | Решение: блок из трёх строк в `CLAUDE.md` + `.tasks/active/signup-wizard.md`, в котором правило сработало | пример файла задачи — иллюстративная фактура визарда поверх реально захваченной практики; пояснение «дело не в файле, а в моменте записи, и момент задаётся инструкцией» + честная граница «просьба, не закон» + «упрощение до файла, масштабируется до папки — Лекция 3» | захвачено реально: блок «Задачная память» из трёх строк в `CLAUDE.md` + `.tasks/active/2026-09-24-dostroit-dugu-seminara-4.md` + `.tasks/done/`, коммит `14fa124` в `tellina-study/signup-landing-demo`, ветка `seminar-4-arc`; иллюстративна только фактура визарда |
| 2.3.5 | Провал/ограничение: устаревший план хуже отсутствия плана (13 п.п., 100% на 30 прогонах, полная версия) **+ мера: сверка начало/конец сессии и строка «Сверено: дата»** + «Index Sickness» n=1 + практика пока нишевая | — | — |
| 2.3.6 | Критерий «файл vs папка» + мостик к разделу 3 | чек-лист §A.7 | — |

### A.9. Тайминг кейса 2.3 (пересчитан после Fix R5-3 + Fix R5-0)

| Шаг | Мин |
|---|---|
| 2.3.0 — разделитель кейса | 0,5 |
| 2.3.1 — проблема: план на месте, договорённости в файле нет | 1,0 |
| 2.3.2 — открытый вопрос + карточки (Fix R5-3: без исследования, звучит раньше) | 1,25 |
| 2.3.3 + 2.3.3b — **исследование + разбор**, единый заход после голосования (Fix R5-3, новый явный тайминг; +0,5 — мостик «файл задачи = письменный план», раунд владельца) | 2,75 |
| 2.3.4 — решение: блок из трёх строк в `CLAUDE.md` + файл задачи + пояснение про масштабирование до папки | 1,0 |
| 2.3.5 — **провал/ограничение:** устаревший план хуже отсутствия плана (полная версия) + **мера: сверка и строка «Сверено»** (+0,25, раунд владельца) + Index Sickness | 3,25 |
| 2.3.6 — критерий «файл vs папка» + мостик к разделу 3 | 1 |
| **Итого кейс 2.3** | **10,75** |

Рост с 9 (значение после review-раунда v4, Fix 4) до 10 — тот же механизм, что в кейсе 2.2:
исследование (Pokémon/Manus/metasphere-agents практика) и раньше звучало в речи фасилитатора (см.
`B.3` предыдущей версии этого файла), но не имело собственной строки в таймингах. Fix R5-3
переставляет его после вопроса и впервые выделяет ему честный тайминг (объединённый с разбором в
2,25 мин, было 1,25 вопрос + 1,25 разбор = 2,5 без явного исследования; стало 1,25 вопрос отдельно
+ 2,25 исследование+разбор вместе). Fix R5-0 не меняет тайминг — меняется содержание проблемы
(2.3.1) и решения (2.3.4), не их длительность. Полная версия количественного риска (13 п.п., 100%
на 30 прогонах) остаётся в провале (2.3.5) без сокращения — в исследовании (2.3.3) звучит только
краткое упоминание, обосновывающее целевой ответ.

**Рост с 10 до 10,75 — раунд владельца (2026-09-23).** Два добавленных куска, оба содержательные,
оба честно оплачены: мостик «файл задачи — это и есть письменный план следующей сессии» в
исследовании (+0,5 мин, 2.3.3) и мера против устаревшего плана — сверка в начале и в конце сессии
плюс строка «Сверено: дата» — в провале (+0,25 мин, 2.3.5). Перестройка сценария на позитивную
договорённость и переформулировка целевого ответа с «дисциплины» на строку в `CLAUDE.md` тайминг
не меняют. Ни один провал, ни один критерий и ни одна честная оговорка не сокращены.

**Неприкосновенное:** «bad plans hurt performance more than no plan at all» с цифрами (это
единственный слой провала во всём разделе 2 с количественной, а не только качественной, опорой на
конкретный риск устаревания) и честная оговорка про нишевость практики. **Первый кандидат на
урезание:** «Index Sickness» — можно свернуть до одной фразы с цифрой (−75%, 0 рецидивов за 150
сессий), не рассказывать устройство кейса подробно.

---

## B. Методическая глава — Кейс 2.3

### B.1. Вход в кейс: смена оси, план на месте — договорённости в файле нет

Фасилитатор явно проговаривает, что это другой тип памяти, не следующая ступень сложности той же
самой, и сразу называет, что практика «файл на задачу» — тоже дешёвая гигиена дня 0:

> «Всё, что мы обсуждали до сих пор в этом разделе, — про решения, которые остаются в силе
> навсегда. Теперь — другой вопрос: что происходит с памятью **одной** задачи, пока она ещё не
> закончена?
>
> У нас в команде заведено правило, такое же дешёвое и такое же обязательное с первого дня, как
> журнал решений из первого кейса: на каждую многошаговую задачу — свой файл. Разработчик ставит
> агенту задачу на несколько сессий: разбить форму на пошаговый визард — имя, email, подтверждение,
> сохранив всю текущую валидацию. По этому же правилу агент перед началом сам создаёт файл задачи
> с планом из шести шагов.
>
> Первая сессия закрывает шаги один-два. По ходу второго шага становится видно, что значения полей
> надо где-то держать между шагами, и в разговоре — не в плане — решают, как именно: состояние
> визарда хранить одним объектом в `sessionStorage`, под ключом `signup-wizard`, чтобы перезагрузка
> страницы на третьем шаге не сбрасывала заполненные поля. В плане третий шаг записан одной
> строкой: «состояние текущего шага». Как его делать — там не сказано. В лог хода договорённость
> не попадает: ничто в этот момент не требует её записывать. Сессия обрывается на середине
> третьего шага — контекст сжимается.
>
> Вторая сессия. Файл задачи на месте — он был там с самого начала, это не сюрприз. Агент его
> открывает, видит план, видит, что шаги один-два отмечены сделанными — выглядит убедительно и
> актуально. Про `sessionStorage` в файле нет ни строки: сжатие случилось раньше, чем кто-то её
> записал. Агент делает третий шаг самым коротким способом — держит состояние в переменной модуля.
> Переключение шагов работает, тесты зелёные. Расхождение всплывает на шестом шаге, при ручной
> проверке: перезагрузка на третьем шаге очищает форму — ровно то, ради чего в первой сессии
> выбрали `sessionStorage`. Третий шаг переделывается целиком.
>
> На входе во вторую сессию у агента есть ровно то, что лежит в файле: шесть шагов и две отметки.
> Всё остальное было в контексте первой сессии, которого больше нет.»

*(Снято: «Файл был. Практика была. Не хватило одного: вписать мелкую договорённость в лог в момент,
когда она прозвучала» — дословный целевой ответ разбора, сказанный до голосования.)*

### B.2. Вопрос классу, дословно

Вопрос задаётся точно по карточке (§A.3). Фасилитатор зачитывает вопрос и пять карточек, вызывает
2 голоса — и **не приводит на этом слайде ни одной цифры и ни одной ссылки на источник**. Одна
реакция стоит подготовки заранее, но фиксируется вслух без комментария по существу:

**«Дописать в DECISIONS.md»** звучит похоже на верный ответ из Кейса 2.1 — фасилитатор просто
фиксирует вариант и переходит дальше, комментарий — на следующем слайде.

### B.3. Голоса прозвучали — от Pokémon до разбора

Порядок изложения важен: сначала мостик от проблемы к предмету исследования, затем практика (что
реально делают), потом количественный риск (что может пойти не так) в объёме, достаточном, чтобы
обосновать целевой ответ — полная версия риска переносится на слайд провала.

> «Сначала одна связка, без неё дальше не сойдётся. Файл задачи — это и есть письменный план, по
> которому работает следующая сессия. Агент, который её открывает, первую сессию не помнит: он
> восстанавливает задачу целиком из того, что записано в файле. И у него нет способа отличить
> «про это договорились, но не записали» от «про это не было речи» — то, чего в файле нет, для
> него не забыто, его просто нет. Значит, наш вопрос — это вопрос о том, что происходит, когда
> план, по которому действует исполнитель, неполон или разошёлся с реальностью. Это измеряли.
>
> Прежде чем разбирать ваши ответы — прецедент этого паттерна из инженерного блога Anthropic:
> агент играет в Pokémon тысячи шагов подряд, переживая сбросы контекста, — и справляется благодаря
> простым структурированным заметкам: счётчики, текущая цель. У Manus похожая техника называется
> "recitation": агент непрерывно переписывает список дел в конец контекста — модель охотнее
> следует тому, что видела последним. На задачах порядка полусотни вызовов инструментов это
> заметно снижает уход от цели.
>
> И вот почему в нашем сценарии файл всё равно подвёл, хотя был заведён правильно и с первого дня.
> Есть измерение на двадцати одной тысяче ста двадцати траекториях: план в среднем помогает. Но
> дословная цитата авторов — "плохой план вреднее отсутствия плана вообще". Файл, который перестал
> отражать реальность, не нейтрален — он создаёт ложную уверенность именно там, где нужна
> бдительность. Полную картину с цифрами покажу через несколько минут, на слайде провала — сейчас
> достаточно понимать механизм.»

Дальше — разбор карточки про `DECISIONS.md`, зафиксированной ранее без комментария:

> «Обратите внимание — файл задачи это не то же самое, что журнал решений. `DECISIONS.md` — про
> то, что остаётся в силе навсегда, для всех будущих задач. Ход этой конкретной задачи — не
> решение проекта, это статус: что сделано, что дальше, что мелкое мы успели по ходу решить. Когда
> задача закончится, всё это можно выбросить. Совсем другой тип памяти, не следующая ступень той
> же лестницы.»

Целевой ответ — не «завести файл» (он уже есть) и не «не забывать дописывать»:

> «Файл был. Практика была. Не хватило одной строки — но не в логе, а в `CLAUDE.md`: дописывать
> договорённость в файл задачи сразу, как она прозвучала, а не "когда будет время". Адресат этой
> строки — не разработчик, а агент, который этот файл и так ведёт. Он читает инструкции в начале
> каждой сессии, он отмечает шаги, он пишет лог; ему просто не было сказано, **когда** писать.
> Решение этого кейса — сказать это в файле инструкций, а не полагаться на то, что кто-то вспомнит
> сам.
>
> И ровно та же оговорка, что в кейсе про гейт готовности: правило в тексте — просьба, не закон.
> Оно сработает не каждый раз. Разница в том, что просьба адресована исполнителю, который открывает
> эти инструкции каждую сессию, — а человеческая внимательность не масштабируется вообще никак.»

### B.4. Решение: не новый файл, а строка в инструкциях

> «Один файл на задачу: план, лог хода, результаты — он уже был с первого шага, это не новость
> этого кейса. Новость — вот этот блок в `CLAUDE.md`, три строки. Первая описывает сам файл. Вторая
> говорит, когда в него писать: договорённость, принятую по ходу работы, дописывать в лог сразу,
> тем же ответом. Третья — сверка плана с репозиторием в начале и в конце сессии, к ней вернёмся
> через минуту, когда посмотрим цифры.
>
> Справа — что из этого получается в самом файле задачи: договорённость про `sessionStorage`
> попала в лог до того, как сессия оборвалась. Одна и та же структура файла, одна и та же
> практика — разница ровно в том, записали вовремя или нет. И записывает это агент, по инструкции,
> а не вы по памяти.
>
> И явная оговорка: это упрощённая версия паттерна. В реальной практике — и в Лекции 3 подробнее —
> это масштабируется до **папки** на задачу: отдельный план, отдельный лог, отдельная папка
> результатов, особенно когда задача не линейная. Для сегодняшнего занятия одного файла достаточно
> — не усложняем.»

### B.5. Провал: полная картина риска, «Index Sickness» и честная оговорка про нишевость

> «Обещанные цифры. Двадцать одна тысяча сто двадцать траекторий, SWE-bench: наличие плана в
> среднем помогает. И тут же прямая цитата авторов: "плохой план вреднее отсутствия плана вообще".
> Комплаенс агента к собственному плану падает примерно на тринадцать пунктов на более сложном
> бенчмарке — чем сложнее задача, тем меньше агент реально следует тому, что сам же записал.
>
> А вот эксперимент с более жёстким условием: тридцать контролируемых живых рабочих сценариев, где
> план не проверялся на актуальность по ходу работы. Устаревший план выполнялся буквально **во
> всех тридцати случаях**. Файл задачи, который никто не обновляет, — не нейтральная бумажка. Это
> источник систематически неверных действий.»

Сразу за цифрой — мера, а не только предостережение (третья строка блока из §A.5, её обоснование
становится понятным именно здесь):

> «Прочитайте условие этого эксперимента ещё раз: сто процентов — это прогоны **без проверки
> актуальности плана**. Измерена не неизбежность провала, а цена пропуска одного шага. Значит,
> шаг надо вернуть — и это третья строка того блока, который мы только что записали: сверять план
> с состоянием репозитория в начале и в конце сессии и ставить в файле строку «сверено» с датой.
>
> Три вещи в этой формулировке не случайны. Первое — «и в конце». Сверка в начале защищает эту
> сессию, сверка в конце защищает следующую: она дописывает то, что выяснилось под конец, пока
> контекст ещё цел. Второе — сама строка «сверено, дата». Без неё следующая сессия не отличит
> свежий план от плана трёхнедельной давности — выглядят они одинаково уверенно, и это тот самый
> механизм, из-за которого плохой план вреднее отсутствия плана. Дата делает свежесть видимой.
> Третье — сверять с репозиторием, а не с памятью: код и история изменений переживают сжатие
> контекста, память — нет.
>
> И граница этой меры — та же, что у гейта готовности из первого раздела: это снова просьба
> текстом. Риск она снижает, но не снимает. У Manus, о котором говорили пару слайдов назад, тот же
> приём работает внутри одной сессии — они переписывают список дел в конец контекста; сверка — то
> же самое между сессиями. Механического барьера, который не даст работать по несверенному плану,
> мы сегодня не ставим.»

Дальше — «Index Sickness», честно помеченный как n=1:

> «Есть кейс-стади, очень близкий к нашей ситуации: реальный проект, триста девяносто одна сессия
> за месяц. Авторы наблюдают, как по мере роста правил агент теряет связь с реальной семантикой
> проекта — правила накапливаются, начинают противоречить друг другу, и агент следует букве вместо
> духа. Решение — то же самое разделение, которое мы проводим весь раздел: стабильное отдельно от
> текущего, базовые инструкции отдельно от append-only лога сессии. Результат — минус семьдесят
> пять процентов объёма AI-инструкций, ноль повторений проблемы за следующие сто пятьдесят сессий.
>
> Сразу честно: это один проект, один автор, это называется action research, а не контролируемый
> эксперимент. Хорошая иллюстрация механизма — не доказательство, что так будет у всех.»

И последнее, что нужно сказать прямо, а не спрятать в сноску:

> «Паттерн "файл или папка на задачу" сейчас — не мейнстрим. Исследование почти трёх тысяч реальных
> репозиториев показывает, что доминируют статические context-файлы, а не операционная память
> задачи. Практику продвигают отдельные лидеры — Anthropic, Manus, — но это ещё не общепринятая
> норма. Если вы придёте на работу и не увидите такого файла ни у кого — это не значит, что вы
> что-то делаете не так, это значит, что индустрия пока не договорилась.»

### B.6. Критерий «файл vs папка» и мостик к разделу 3

Критерий — голосом, по чек-листу (§A.7).

> «Подведём черту под разделом памяти. Три кейса — три разных ответа на вопрос "что происходит,
> когда сессия заканчивается": решение остаётся навсегда в плоском логе, решений становится
> слишком много и они обрастают структурой, а ход одной задачи живёт ровно до её конца в отдельном
> файле. Первое и третье мы заводим сразу, на дне 0, потому что это дёшево. Второе — реактивный шаг,
> потому что заранее нельзя знать, вырастет ли именно ваш журнал решений до нужного объёма.
>
> Но все три — **текст**, который агент читает и **может** учесть. Именно «может», а не «учтёт».
> Дальше в этом занятии мы посмотрим, что происходит, когда прочитать становится недостаточно и
> нужно что-то, что агент не может обойти при всём желании.»

---

## D. Пересчёт таймингов раздела 2 — в `section-2-pamyat-timings.md`

Таблицы по разделу 2 целиком (v3 → v4 → v5 → раунд владельца 2026-09-23) вынесены туда при
превышении лимита в 600 строк (CLAUDE.md § Document Size Limit); собственный тайминг кейса 2.3
остаётся здесь, в §A.9 — **10,75 мин**.