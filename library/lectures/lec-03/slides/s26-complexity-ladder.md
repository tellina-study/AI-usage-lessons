---
id: s26
type: process
section: "Раздел 5. Фреймворк решения"
duration_min: 2
assertion: "Лестница сложности: оставайся на нижней ступени, поднимайся только под требование задачи; нижняя ступень — обычный код без ИИ"
learning_goal: "Лестница сложности как инструмент (LO7 payoff) + правило движения"
learning_outcomes: [LO7]
chapter_ref: "§5.1 [for-slide-s26]"
visual_brief: "Лестница 6 ступеней (bottom-aligned, снизу вверх). У каждой стрелки-перехода — требование, которое его открывает. Правило-плашка «каждый подъём — обмен, не улучшение». Gold — нижняя ступень «обычный код (без ИИ)»."
interaction: none
---

# Visible content

## Title bar
«Лестница архитектурной сложности»

## Body
[Лестница 6 ступеней снизу вверх, bottom-aligned, Ocean rounded boxes; у каждой стрелки — требование]

6. **Multi-agent** ↑ *подзадачи широко-параллельны И независимы И высокоценны*
5. **Агент** (цикл plan→act→check→iterate, с budget/loop-лимитами) ↑ *задача непредсказуема И ценность оправдывает кратные стоимость/риск*
4. **Workflow** (предопределённые пути) ↑ *задача многошаговая, но последовательность известна заранее*
3. **RAG / контекст-инжиниринг** (для малого стабильного корпуса — длинный контекст + кэш) ↑ *знание большое И меняющееся И провенанс И приватное*
2. **Один вызов LLM** (промпт; + CoT, + few-shot) ↑ *нужен NL / неструктурированный ввод / неточное соответствие*
1. **Обычный код (без ИИ)**  *(gold-акцент — точка отсчёта)*

[Правило-плашка, низ]
**Оставайся на самой нижней ступени, которая закрывает требования; поднимайся только при явно сформулированном требовании. Каждый подъём — это ОБМЕН (возможности ↔ стоимость, латентность, аудируемость, поверхность атаки), а не улучшение.**

## Speaker notes

Соберём всё пройденное в одну структуру — лестницу архитектурной сложности. Каждая ступень — это архитектура, которую мы разобрали. Снизу вверх: обычный код без ИИ; один вызов модели с промптом, возможно с пошаговым рассуждением и примерами; RAG или контекст-инжиниринг, а для малого стабильного корпуса — длинный контекст с кэшированием; workflow с предопределёнными путями; агент с динамическим циклом и обязательными лимитами на бюджет и итерации; и наверху мульти-агент.

Правило движения по лестнице — несущее правило всей лекции: оставайтесь на самой нижней ступени, которая закрывает требования задачи; поднимайтесь на следующую только при явно сформулированном требовании, которого текущая ступень не закрывает. Каждый подъём оплачивается новыми режимами отказа, стоимостью, латентностью, ухудшением аудируемости и новой поверхностью атаки. «Поднялся, потому что мог» — это технический долг; «поднялся, потому что вот требование, не закрываемое ступенью ниже» — инженерное решение. Заметьте, что нижняя ступень — обычный код без ИИ: лестница архитектур AI-систем начинается с «возможно, ИИ здесь вообще не нужен».

Соберём, какое именно требование открывает каждый подъём — это переход от лестницы-картинки к лестнице-инструменту. Код в один вызов: появилось требование обрабатывать естественный язык, неструктурированный ввод, нечёткое соответствие, которого детерминированными правилами не выразить. Один вызов в RAG: появилось требование к знанию, которого нет в весах и которое большое, и меняющееся, и требующее провенанса, и приватное одновременно. В workflow: задача стала многошаговой, но последовательность шагов известна заранее. Workflow в агента: последовательность принципиально зависит от промежуточных результатов так, что заранее её выписать нельзя, и ценность задачи оправдывает кратные стоимость, латентность и потерю аудируемости. Агент в мульти-агент: подзадачи широко-параллельны, независимы и высокоценны настолько, что выигрыш параллелизма перекрывает кратный рост токенов и хрупкость согласования. Каждая стрелка — это не «улучшение», а обмен: больше возможностей в обмен на стоимость, латентность, аудируемость, поверхность атаки. Инженер, владеющий этим навыком, умеет назвать обе стороны обмена, а не только левую.
