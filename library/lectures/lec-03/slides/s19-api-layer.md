---
id: s19
type: assertion_visual
section: "Раздел 4. Агенты"
duration_min: 2.5
assertion: "3 механизма делают модель компонентом системы (structured output / function calling / prompt caching); MCP стандартизует подключение (N×M→N+M), но удобство подключения не равно безопасности подключаемого"
learning_goal: "API-слой (3 механизма) + MCP (N×M→N+M, поворот доверия) на assertion-уровне (разбор и приватность → notes/глава)"
learning_outcomes: [LO7]
chapter_ref: "§4.1 [for-slide-s19]"
visual_brief: "3 равные карточки, по одному тезису на механизм. Плашка снизу: «ни один не делает модель надёжнее». Gold — связка-вывод. Числа экономии НЕ на видимом слое."
interaction: none
---

# Visible content

## Title bar
«API-слой: модель как компонент системы»

## Body
[3 равные карточки в Ocean rounded box, по одному тезису]

**Structured output**
модель обязана вернуть ответ строго по схеме (JSON), а не свободный текст
→ выход = **валидные данные, не текст для парсинга** → модель *встраиваемая*

**Function calling** *(вызов функций / инструментов)*
модель возвращает «вызови инструмент X с аргументами Y»; **исполняет ваш код, не модель**
→ модель *активная* в системе

**Prompt caching**
переиспользовать вычисленное состояние неизменного префикса
→ не переплачивать за повторяемую часть → модель *экономически жизнеспособная*

[Плашка-вывод, низ]
**Ни один из трёх не делает модель надёжнее или аудируемее — они расширяют, что модель может, не меняя её недетерминированной природы. Правило лестницы не отменяется.**  *(gold-акцент)*

## Speaker notes

Чтобы AI стал частью системы, нужны три механизма API-слоя. Первый — structured output, структурированный вывод: режим, в котором модель обязана вернуть ответ строго по заданной схеме, например JSON, а не свободный текст. Технически схема компилируется в ограничение на сэмплинг, поэтому модель не может выдать структурно невалидный ответ; выход превращается из текста, который надо парсить регулярками, в гарантированно валидные по форме данные. Это делает модель встраиваемой. Тонкость: гарантируется валидность формы, но не содержания — поле может быть фактически неверным. Второй — function calling: модель вместо ответа текстом может вернуть запрос «вызови инструмент X с аргументами Y», а исполняет вызов ваш код, не модель. Модель лишь формулирует намерение — это делает её активной и будет ключевым для безопасности. Третий — prompt caching: переиспользование уже вычисленного состояния неизменного префикса, чтобы не пересчитывать его каждый раз; кэшируется состояние, а не текст ответа. Это делает модель экономически жизнеспособной при объёме и делает full-context для малого стабильного корпуса реальным конкурентом RAG. Ни один из трёх не делает модель надёжнее или аудируемее — они расширяют возможности, не меняя недетерминированной природы, и правило лестницы не отменяют.

Остаётся вопрос, как инструмент к модели подключается. До конца 2024 года каждая интеграция решала это по-своему. MCP, Model Context Protocol, — открытый стандарт единого способа подключать инструменты к модели; метафора — «USB-C для инструментов». Суть в арифметике: при N моделях и M инструментах наивно нужно N умножить на M интеграций, а MCP сводит это к N плюс M. Но здесь критический поворот: стандартизация подключения не означает безопасность подключаемого и даже усугубляет проблему доверия. Каждый подключённый инструмент — это код в вашем окружении и канал, через который в контекст попадает недоверенный текст. «Подключить за минуту» и «доверять подключённому» — разные вопросы; MCP решил первый и сделал второй острее. Удобство подключения — не аргумент за подключение.
