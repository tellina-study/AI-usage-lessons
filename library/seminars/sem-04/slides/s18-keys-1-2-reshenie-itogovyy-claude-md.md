---
id: s18
type: code_artifact
duration_min: 1.5
assertion: "Итоговый файл — тринадцать строк: критерий готово, гейт-предупреждение, честная оговорка предела; AGENTS.md — симлинк на тот же файл"
learning_goal: "Решение кейса 1.2 — реальный итоговый CLAUDE.md, механика загрузки, симлинк AGENTS.md и острый край cp -R"
visual:
  pattern: code_artifact
  primary: "Итоговый CLAUDE.md целиком (13 строк) слева. Справа — три механические детали одной строкой каждая: файл грузится целиком каждую сессию; @-импорты не экономят контекст; AGENTS.md — симлинк с острым краем cp -R."
---

# Итоговый CLAUDE.md — тринадцать строк

## Assertion

Итоговый файл — тринадцать строк: критерий готово, гейт-предупреждение, честная оговорка предела; `AGENTS.md` — симлинк на тот же файл.

## Visual

```markdown
# CLAUDE.md

signup-landing: статический лендинг с формой заявки на демо-урок.
Готово = npm run build код 0, npx playwright test зелёный,
форма реально отправляет заявку на проде.

## Safety / scope boundaries
- Никогда не запускать деплой на прод без явного запроса.

## Build, test, verify
- To verify: открыть dist/index.html после сборки, вручную
  отправить форму с заполненными и с пустыми полями.
```

Справа — три механические детали:

1. Файл грузится в начало каждой сессии целиком (официальный ориентир — до 200 строк, наш итог — 13).
2. `@`-импорты организуют файл, но не экономят контекст — импортированное всё равно грузится целиком.
3. `AGENTS.md` — симлинк на тот же файл (`ln -s CLAUDE.md AGENTS.md`). Острый край: `cp -R` без `-P` на macOS разыменовывает симлинк во вторую копию молча — копировать через `cp -a` / `git clone` / `git archive`.

## Speaker notes

«Вот итог кейса 1.2: тринадцать строк — критерий готовности, гейт-предупреждение вручную проверить доставку, честная граница на деплой. Файл грузится в начало каждой сессии целиком, официальный ориентир — до двухсот строк, у нас — тринадцать. `@`-импорты организуют файл, но контекст не экономят — импортированное всё равно грузится целиком.

И мы заводим `AGENTS.md` как симлинк на тот же файл — один источник правды для разных движков. Острый край: `cp -R` без `-P` на macOS разыменовывает симлинк во вторую реальную копию молча — копировать через `cp -a`, `git clone` или `git archive`.»
