---
id: s05c
type: assertion_visual
section: "Раздел 1. Промпт и его границы"
duration_min: 3
assertion: "Протокольные роли system/user/assistant ≠ роль-персона; приоритет system — склонность (~63,8%), не граница; STI подделывает роль"
learning_goal: "Развести роль-персону и протокольные роли; показать, что приоритет system — статистическая привычка, а не защита, и назвать конкретную уязвимость (STI)"
learning_outcomes: [LO7]
chapter_ref: "§1.2 [for-slide-s05c]"
verify_day_of: true
---

# Visible content

## Title bar
«Протокольные роли: не персона, а разметка диалога»

## Body
[Верхняя плашка — различение]
**Роль-персона** («ты — юрист») — текст внутри сообщения, влияет на тон
**Протокольная роль** `system` / `user` / `assistant` — разметка «кто говорит», структура диалога

[Слева — зачем нужны + приоритет]
Структура диалога · управление поведением · **выученный приоритет** system-роли
GPT-4o соблюдает приоритет ≈ **63,8%** случаев; спец-тюнинг поднял 84,1%→94,1% — но **не 100%**

[Справа — механика + уязвимость]
Модель принимает **один плоский поток токенов**; роли — спецтокены chat-шаблона (ChatML, Llama, Anthropic — свои форматы)
**Special Token Injection (STI):** строка-подделка роли во внешнем контенте → ChatInject: ASR AgentDojo 5,18%→32,05%, на Llama-4 до 88,3%

[Gold callout, низ]
**Приоритет системной роли — склонность, не граница безопасности.** Защита: экранировать спецтокены во входящем контенте + проверять chat-шаблон локальной модели.

## Speaker notes

В промптинге слово «роль» имеет два независимых значения, и их постоянно смешивают. Первое — роль-персона в тексте промпта: «ты — юрист», «ты — редактор». Это обычное содержимое сообщения, оно влияет на тон и глубину изложения, и его разбор мы уже закончили. Второе значение — протокольные роли system, user и assistant. Это не текст внутри сообщения, а разметка самого диалога: «кто говорит». Роль-персона отвечает на вопрос «в каком тоне»; протокольная роль — «чья это реплика: системы, пользователя или самой модели». Смешение этих двух смыслов — источник и путаницы, и целого класса уязвимостей, поэтому разведём их явно.

Зачем протокольные роли вообще нужны? Три причины. Первая — структура диалога: модель должна отличать инструкцию разработчика от реплики пользователя и от собственных прошлых ответов, иначе многоходовой разговор превращается в неразмеченную кашу. Вторая — управление поведением: в системную роль кладут постоянные ограничения, действующие поверх всего разговора. Третья — выученный приоритет: модель обучают относиться к системным инструкциям как к более авторитетным. Слово «выученный» здесь ключевое: это статистическая привычка, а не архитектурная гарантия. Исследование Geng и коллег показало, что GPT-4o подчиняется приоритету системной инструкции лишь примерно в 63,8% случаев; специальная тренировка на 16 бенчмарках подняла устойчивость с 84,1% до 94,1% — но и это не 100%. Вывод: приоритет системной роли — склонность, а не граница безопасности.

Как это устроено физически. Модель получает один плоский поток токенов, а не структурированный объект с полями. Список сообщений собирает chat-шаблон, который оборачивает каждое сообщение специальными токенами разметки (в ChatML — `<|im_start|>system … <|im_end|>`). Форматы у семейств разные и меняются между версиями. Отсюда уязвимость: если во внешний контент — веб-страницу, файл, письмо, вывод инструмента — попадёт текст, похожий на разметку роли, модель может принять его за собственную реплику или системную инструкцию. Это Special Token Injection, подделка роли. Работа ChatInject измерила её на агентных сценариях: средний attack success rate на бенчмарке AgentDojo вырос с 5,18% до 32,05%, а на отдельных моделях — на Llama-4 — с 50,1% до 88,3%. Та же работа показала: просьба к модели «не поддаваться» почти не защищает.

Защита строится не на надежде, а на двух системных мерах: экранировать спецтокены во входящем недоверенном контенте до подачи в модель и проверять chat-шаблон для локальных моделей — чужой шаблон даёт тихую деградацию. Здесь мы разобрали устройство протокольных ролей; полный разбор prompt injection как класса атак агента — в разделе про безопасность агента.
