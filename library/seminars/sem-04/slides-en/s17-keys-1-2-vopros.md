---
id: s17
type: reflection_question
duration_min: 0.5
assertion: "The gate line has been there from the very start and did not work — pick from five options what to do next so that it actually works in the next session"
learning_goal: "The open question of case 1.2 (asked BEFORE the evidence) — a question slide with a ready-made set of options, NOT a single number, NO source, NO breakdown table"
visual:
  pattern: question_with_option_cards
  primary: "The question large at the top, in a gold frame. Below — five option cards verbatim: \"I'll rewrite the line in caps / harsher\" · \"I'll duplicate the line as a second bullet\" · \"I'll set up a separate checklist file next to it\" · \"I'll hand the check to a mechanism outside the instruction text\" · \"I'll live with it — it won't always work, and that is a normal margin of error\". Not a single number, no breakdown table, no target answer."
---

# The gate did not work — what next

## Assertion

The gate line has been there from the very start and did not work — pick from five options what to do next so that it actually works in the next session.

## Visual

The question verbatim, in a gold frame:

> "You have had the gate line in CLAUDE.md from the very start, and the agent's behavior still diverges from it for the second time in a row — the request has again not been checked by hand. Pick what you will do next — from the cards below — so that it actually works in the next session."

Below — five option cards:

`I'll rewrite the line in caps / harsher` · `I'll duplicate the line as a second bullet` · `I'll set up a separate checklist file next to it` · `I'll hand the check to a mechanism outside the text` · `I'll live with it — it won't always work, that's normal`

Not a single number, no reference to a source. There is no breakdown table and no target answer on the slide — they are on the next one.

## Speaker notes

A pause, 2–3 voices — with not a single number on screen. Every option voiced is recorded the same way, including "I'll rewrite it in caps": "Noted, we'll come back to this in a minute."
