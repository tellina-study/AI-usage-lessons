---
id: s22
type: problem_scenario
duration_min: 1.0
assertion: "Разработчик выносит правила тестов в tests/CLAUDE.md и ждёт, что агент подхватит его сам, когда работает именно в этой папке — логично звучит, но стоит проверить, что там реально происходит с загрузкой"
learning_goal: "Проблема кейса 1.3 — сценарий выноса правил в подпапку, сжато под сокращённый бюджет"
visual:
  pattern: problem_scenario
  primary: "Дерево репозитория: tests/ подрос — пачка тестов, договорённость по паттерну именования и Page Object. Справа — решение разработчика: файл tests/CLAUDE.md в подпапке, ожидание автозагрузки. Нижняя строка: «Логично звучит. Давайте проверим, что там на самом деле происходит с загрузкой»."
---

# Правила тестов — в отдельный файл в подпапке?

## Assertion

Разработчик выносит правила тестов в `tests/CLAUDE.md` и ждёт, что агент подхватит его сам, когда работает именно в этой папке — логично звучит, но стоит проверить, что там реально происходит с загрузкой.

## Visual

Месяц спустя `tests/` подрос: пачка тестов, своя договорённость — как называть файлы, какой паттерн использовать (Page Object).

Решение разработчика: «Вынесу эти правила в отдельный `tests/CLAUDE.md` — короче, релевантнее контексту, корневой файл не разбухнет.» Кладёт файл в подпапку, ожидая, что агент подхватит его автоматически, когда будет работать именно там.

Нижняя строка: **«Логично звучит. Давайте проверим, что там на самом деле происходит с загрузкой.»**

## Speaker notes

«Прошёл месяц. `signup-landing` подрос: в `tests/` накопилась пачка тестов, появилась своя договорённость — как называть файлы, какой паттерн использовать. Разработчик решает: вынесу это в отдельный `tests/CLAUDE.md`, короче, ближе к делу, корневой файл не разбухнет. Кладёт файл в подпапку и ждёт, что агент подхватит его сам, когда будет работать именно в `tests/`. Логично звучит. Давайте проверим, что там на самом деле происходит с загрузкой.»
