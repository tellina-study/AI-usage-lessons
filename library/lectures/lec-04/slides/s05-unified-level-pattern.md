---
id: s05
type: assertion_visual
section: "Раздел 1. Уровни A и B"
duration_min: 1.5
assertion: "Цена ошибки растёт вместе с автономией A→D: тот же провал на уровне D стоит несравнимо дороже, чем на A — поэтому точка обязательного человека смещается и ужесточается"
learning_goal: "Несущий контентный принцип (не meta): радиус поражения растёт по A→D → гейт под выросшую цену ошибки. Ранний посев → собирается в матрице s29 (ось «цена ошибки»)"
learning_outcomes: [LO1, LO7]
chapter_ref: "§1.1 [for-slide-s05]"
visual_brief: "4 столбца A→D с растущей blast-radius-плашкой (bottom-aligned, высота ↑ A→D). Под каждым — что задевает ошибка + где человек контролирует. Gold — D-апекс + принцип-callout."
interaction: none
---

# Visible content

## Title bar
«Чем выше автономия — тем больше радиус поражения ошибки»

Подзаголовок: поэтому точка обязательного человека не исчезает с подъёмом — она смещается и ужесточается.

## Body
[4 столбца A→D в Ocean rounded box, растущая blast-radius-плашка bottom-aligned]

| Уровень | Радиус ошибки задевает | Человек контролирует |
|---|---|---|
| **A** автодополнение | одна строка | на каждом нажатии Tab |
| **B** мелкие задачи | функция или фикс | ревью результата до интеграции |
| **C** кодинг-агент | много файлов сразу | ревью PR до merge |
| **D** оркестратор *(gold-апекс)* | прод-данные, необратимое | только вход и выход — между автономно |

[Gold callout, низ]
**Тот же провал на уровне D стоит несравнимо дороже, чем на A.** Поднимать автономию имеет смысл только под требование задачи — и только с гейтом под выросшую цену ошибки.

## Speaker notes

Прежде чем разбирать уровни по отдельности, зафиксируем несущий принцип, который будет действовать на каждом из них и который объясняет вторую половину центрального вопроса.

Чем выше уровень автономии, тем больше радиус поражения ошибки. На уровне A ошибка — это одна неверная строка, которую человек видит в момент написания. На уровне B это уже целая функция или фикс, который человек ревьюит после генерации. На уровне C это много файлов сразу, и человек видит только итоговый pull request. На уровне D в радиусе действия агента оказываются продакшен-данные и необратимые операции, а человек контролирует только вход — постановку — и выход — слияние и выкладку; всё, что между, происходит автономно.

Отсюда прямое следствие: точка, где человек обязателен, не исчезает с подъёмом по лестнице — она смещается и ужесточается. На уровне A контроль распределён по каждому нажатию клавиши; на уровне D он должен быть сконцентрирован в архитектурном гейте, потому что цена пропущенной ошибки несоизмеримо выше. Тот же по сути провал — модель сгенерировала правдоподобное, но неверное — на уровне A стоит одной правки, а на уровне D может стоить стёртой продакшен-базы за девять секунд.

Практический вывод, который мы будем применять весь курс: каждый подъём по автономии оплачивается ослаблением человеческого контроля над тем, что попадает в систему. Поэтому поднимать автономию имеет смысл только тогда, когда этого требует задача, и только если под выросшую цену ошибки поставлен соответствующий гейт. Эта ось — цена ошибки — позже станет решающей в матрице выбора уровня.
