---
id: s04b
type: assertion_visual
section: "Раздел 1. Токенизация"
duration_min: 1
assertion: "Поток данных в LLM: слова → токены → векторы → LLM → векторы → токены → слова"
learning_goal: "Roadmap всего inference-конвейера перед детальным разбором каждого этапа"
learning_outcomes: [LO1]
chapter_ref: "§1 [for-slide-s04b]"
visual_brief: "Горизонтальный 7-stage pipeline с двусторонним показом туда-обратно. Этапы: 1) текст → 2) токены → 3) векторы → 4) LLM (внимание+forward) → 5) распределение → 6) токен → 7) текст. Стрелки направлены вправо. Над каждым этапом — иконка. Под каждым — короткая подпись."
---

# Visible content

## Title bar
«Поток данных в LLM — туда и обратно»

## Body
[Sub-title 16pt italic]
*Каждый из этих этапов мы разберём отдельно. Сейчас — карта целиком.*

[Горизонтальный pipeline, 7 стадий, в Ocean rounded boxes]

```
[Текст]  →  [Токены]  →  [Векторы]  →  [LLM]  →  [Распределение]  →  [Токен]  →  [Текст]
«Привет»   [Прив][ет]    vec₁, vec₂   внимание    p(token | контекст)  выбран     «Здравствуй»
                                       +forward
```

[Под каждым этапом — короткая подпись: «слова», «id из словаря», «numeric», «inference», «вероятности», «sample», «de-tokenize»]

[Gold callout снизу]
**Только границы — слова. Внутри модели — векторы. Сегодня разберём 4 центральных этапа: 1-3-4-5.**

## Speaker notes

Прежде чем углубляться в детали каждого этапа, посмотрим на полную картину — что происходит между вашим запросом и ответом модели. Это карта, к которой мы будем возвращаться: с правой стороны каждого следующего раздела вы будете видеть, какая именно стадия этого pipeline сейчас в фокусе.

Слева — ваш входной текст: например, `«Привет, мир»`. Первое преобразование — **токенизация**: текст разрезается на единицы из словаря модели; `«Привет»` превращается в, условно, `[Прив][ет]`. Эти токены — целые числа, id из словаря. Это первый из четырёх этапов, и именно ему посвящён ближайший Раздел 1.

Следующий шаг — **embedding lookup**: каждому id токена сопоставлен выученный вектор в памяти модели. Слово исчезает; остаётся последовательность векторов. Это входит в Раздел 2.

Дальше — **LLM-инференс**: серия слоёв с механизмом внимания, который мы разберём в Разделе 3. Здесь модель «думает» — но думает она над векторами, не над словами. Из этой обработки на выходе появляется ещё один вектор — кандидат на следующий токен.

Этот вектор-кандидат превращается в **распределение вероятностей** по всему словарю модели: «насколько правдоподобно, что следующий токен — этот? а этот? а вот этот?». Из распределения выбирается **один токен** через сэмплинг — это Раздел 4.

И последний шаг — **обратное преобразование**: id токена через де-токенизацию превращается обратно в текст. Этот шаг почти симметричен токенизации в самом начале, и обычно его относят к тому же первому этапу.

Главное наблюдение для практики: «слово» появляется только на границах — на входе и на выходе. Всё, что происходит внутри модели — это операции над векторами. Сегодняшняя лекция — про эти внутренние операции, без формул, но с инженерным пониманием, что и где происходит.
