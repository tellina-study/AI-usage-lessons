---
id: s22
type: assertion_visual
section: "Раздел 3. Механизм внимания"
duration_min: 3
assertion: "Кэш промптов — ставка на повторное использование: чтение 0.1× ставки, запись дороже входа, один изменившийся токен инвалидирует всё после него"
learning_goal: "Экономика prompt caching: тарифная структура, требование точного префикса, правило «стабильное — в начало»"
learning_outcomes: [LO4, LO6]
chapter_ref: "§3.4 (chapter-part2.md) [for-slide-s22]"
visual_brief: "Слева 55% — Ocean rounded box с тарифной схемой: три плашки «чтение 0.1× (новейшие — 0.025×)» (gold), «запись 1.25–2×», «без кэша 1×»; под ними мини-кейс $45 000 → $8 000 (−82%) как две колонки-бары. Справа 45% — схема префикса: полоса из блоков «системный промпт | инструкции | документы | ⚡timestamp | вопрос», блок timestamp красным крестом рушит зелёную заливку всех блоков после него. Внизу gold callout «Стабильное — в начало, переменное — в конец». Mini-poll бейдж у блока timestamp."
interaction: mini_poll
verify_day_of: true
---

# Visible content

## Title bar
«Кэш промптов — ставка на повторное использование, а не скидка»

## Body
[Слева — тарифная структура + кейс, Ocean rounded box]

**Тарифы (сентябрь 2026):**
- Чтение из кэша — **0.1× базовой ставки входа** (новейшие модели — до 0.025×)
- Запись в кэш — **1.25–2× ставки** — дороже обычного входа
- Окупается со второго-третьего попадания; уникальные запросы без общего префикса от кэша только дорожают

**Кейс:** 50 000 анализов документов в месяц — **$45 000 без кэша → $8 000 с кэшем (−82%)**

[Справа — схема точного префикса]

**Условие: точное совпадение префикса.** Один изменившийся токен инвалидирует кэш для всего, что после него.

[Полоса: системный промпт → инструкции → документы → ⚡«Сегодня 2026-09-05 14:23» → вопрос]

**Мини-опрос:** добавили динамический timestamp в начало системного промпта — что происходит с кэшем?

[Gold callout]
**Правило компоновки: стабильное — в начало (промпт, инструкции, примеры, документы), переменное — в конец.**

## Speaker notes

KV-cache живёт внутри одной генерации. Провайдеры сделали следующий шаг: если два разных запроса имеют одинаковый префикс — тот же системный промпт, инструкции, документы, — его K/V можно переиспользовать между запросами. Это кэширование промптов, и с 2025–2026 годов это главный рычаг оптимизации стоимости LLM-нагрузок.

Обратите внимание на структуру тарифов: чтение из кэша — десятая часть базовой ставки входа, у новейших моделей — до одной сороковой, но запись стоит дороже обычного входа — от 1.25 до 2 ставок в зависимости от времени жизни. Кэш не бесплатен — это ставка на повторное использование: окупается со второго-третьего попадания, а нагрузка из уникальных запросов только дорожает. Масштаб эффекта реален: кейс на 50 тысяч анализов документов в месяц — 45 тысяч долларов без кэша против 8 тысяч с кэшем, экономия 82 процента.

Главное техническое условие — точное совпадение префикса: кэш срабатывает, только если всё до контрольной точки совпадает байт-в-байт. Один изменившийся токен инвалидирует кэш для всего, что после него. Теперь вопрос залу: вы добавили в начало системного промпта строку «Сегодня такое-то число и время». Что с кэшем? Правильно: ни один запрос никогда не попадёт в кэш — вся нагрузка платит полную ставку, а при явной записи ещё и надбавку за запись. Классический самострел.

Отсюда правило компоновки: стабильное — в начало, переменное — в конец. И минутный аудит для действующего конвейера: посмотрите поля учёта кэша в ответах API — если при стабильном системном промпте чтения из кэша нулевые, вы почти наверняка чините это перестановкой промпта за один вечер. Для многошаговых агентов кэш — не оптимизация, а условие рентабельности: к этому вернёмся в следующей лекции.
