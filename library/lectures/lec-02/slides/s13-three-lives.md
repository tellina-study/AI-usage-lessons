---
id: s13
type: assertion_visual
section: "Раздел 2. Эмбеддинги"
duration_min: 2.5
assertion: "Эмбеддинги — не только внутренность инференса: поиск и RAG работают на отдельной embedding-модели"
learning_goal: "Три жизни эмбеддинга от практики поиска (term-lock «три жизни»); какая «жизнь» работает в RAG; переиндексация не привязана к чат-LLM"
learning_outcomes: [LO1, LO6]
chapter_ref: "§2.2 [for-slide-s13]"
visual_brief: "Строка-тезис под заголовком: «когда вы строите поиск или RAG, вы пользуетесь третьей жизнью термина — отдельной embedding-моделью». 3 вертикальные карточки: (1) Входная таблица — внутри инференса; статическая, вектор [кот] один во всех предложениях; (2) Контекстуальные представления — внутри инференса; после слоёв внимания, вмешан контекст; (3) Векторы для поиска — самостоятельный инструмент (gold-обводка, пометка «это и есть ваш поиск/RAG»): отдельная embedding-модель, не внутренности чат-LLM. Внизу gold callout: «обновили чат-LLM → переиндексировать базу НЕ надо: индекс живёт в координатах embedding-модели»."
---

# Visible content

## Title bar
«Эмбеддинги — не только внутренность инференса, но и инструмент поиска»

## Body
[Строка-тезис под заголовком]
Когда вы строите поиск или RAG, вы пользуетесь третьей жизнью термина — отдельной embedding-моделью.

[3 вертикальные карточки — три жизни термина «эмбеддинг»]

**1. Входная таблица** *(внутри инференса)*
Статическая: вектор `[кот]` один и тот же в любом предложении. Выборка по id, контекста ещё нет.

**2. Контекстуальные представления** *(внутри инференса)*
После слоёв внимания: вектор каждой позиции обновлён с учётом окружения. Именно они несут «понимание» модели.

**3. Векторы для поиска** *(gold-обводка — «это и есть ваш поиск/RAG»)*
Самостоятельный инструмент: вектор целого текста от **отдельной embedding-модели** — не внутренности вашего чат-LLM. Свой продукт, своё обучение, свои лидерборды.

[Callout внизу, gold]
**Обновили чат-LLM → переиндексировать базу НЕ надо: индекс живёт в координатах embedding-модели.**

## Speaker notes

Эмбеддинги нужны не только для инференса — это ещё и самостоятельный инструмент поиска. Слово «эмбеддинг» в инженерном обиходе обозначает три разные сущности; разложим три жизни термина и посмотрим, какой из них вы пользуетесь, когда строите поиск.

Жизнь первая: входная таблица — статическая таблица «токен → вектор» на входе LLM. Вектор токена «кот» в ней один и тот же в любом предложении. Жизнь вторая: контекстуальные представления. Последовательность входных векторов проходит через десятки слоёв внимания, и на каждом слое вектор каждой позиции обновляется с учётом окружения — на выходе вектор позиции «кот» уже не равен табличному, в него вмешан контекст. Именно эти представления несут «понимание» модели; входная таблица — лишь стартовая точка. Обе эти жизни — внутренности инференса. А третья жизнь — ваш рабочий инструмент: когда вы вызываете embedding-API для поиска или RAG, вы получаете вектор целого текста от отдельной модели, специально обученной под поиск и сравнение. Это не внутренности вашего чат-LLM: embedding-модель — самостоятельный продукт, и чат-модель с embedding-моделью могут быть от разных вендоров — это нормальная практика.

Правило разграничения: речь про стоимость и окно — это токены и входная таблица; про «что модель поняла» — контекстуальные представления; про поиск и векторные базы — выходные эмбеддинги отдельной модели.

И типовые ошибки склейки, ради которых различение вводится. «У нас подписка на чат-модель — зачем платить за embedding-модель?» — затем, что чат-модель векторов для поиска не отдаёт. «Возьмём embedding-модель того же вендора — они совместимее» — совместимости здесь нет как категории: векторы сравниваются только друг с другом внутри одной embedding-модели, LLM их вовсе не видит. И зеркально: «обновили LLM — надо переиндексировать базу» — не надо; переиндексация нужна ровно тогда, когда меняется сама embedding-модель.
