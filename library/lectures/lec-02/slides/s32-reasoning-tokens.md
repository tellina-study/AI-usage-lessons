---
id: s32
type: assertion_visual
section: "Раздел 4. Сэмплинг и генерация"
duration_min: 3.5
assertion: "Reasoning-токены не видны, но тарифицируются как output — по самой дорогой ставке, с раздуванием 3–10×"
learning_goal: "Reasoning-токены: тот же цикл + невидимая платная часть; adaptive/effort вместо ручных бюджетов; summarized thinking"
learning_outcomes: [LO4, LO6]
chapter_ref: "§4.7 (chapter-part2.md) [for-slide-s32]"
visual_brief: "Верх — схема: тот же авторегрессионный цикл (мини-версия s31), но выход раздвоен: широкая серая полоса «черновик „для себя“ — в ответ не попадает, в счёт попадает» (×3–10) и узкая teal полоса «видимый ответ». Низ слева — ценовой экспонат: три относительных бара o4-mini / o3 / o3-pro, подпись «o3-pro = 3.6× o3 = 18× o4-mini — при сопоставимой длине видимых ответов» (без абсолютной цифры; gold на 18×). Низ справа — две плашки: «управление: адаптивное мышление / effort вместо ручных бюджетов» и «в интерфейсе — пересказ рассуждения (summarized), не сырые токены». Gold callout про бюджет."
---

# Visible content

## Title bar
«Reasoning-токены не видны — но тарифицируются как output»

## Body
[Верх — цикл с раздвоенным выходом]

Тот же авторегрессионный цикл — но перед видимым ответом модель генерирует **черновик «для себя»**: в ответ не попадает, **в счёт попадает** — по ставке output-токенов, с учётом в `max_tokens`.

**Раздувание: 3–10×** объёма видимого ответа — без естественного потолка.

[Низ слева — ценовой экспонат]
**o3-pro: 3.6× дороже o3, 18× дороже o4-mini** *(gold)* — при сопоставимой длине видимых ответов; разницу делает объём рассуждения.

[Низ справа — две границы, плашки]
- **Управление:** адаптивное мышление / `effort` вместо ручных бюджетов — удобно, но стоимость запроса стала менее предсказуемой
- **«Ход рассуждений» в интерфейсе — пересказ** (summarized), не сырые токены: строить аудит решений на «показанных мыслях» нельзя

[Gold callout]
**Закладывайте в бюджет невидимую часть 2–5× видимого ответа — и сверяйте по полю usage в ответе API, где reasoning-токены видны как строка расхода.**

## Speaker notes

Reasoning-модели — o-серия OpenAI, расширенное мышление Claude, Deep Think у Gemini — не меняют цикл, который мы только что собрали. Они делают другое: перед видимым ответом модель генерирует тем же авторегрессионным циклом токены рассуждения — черновик «для себя», который в ответ не попадает. Утверждение «не видно — значит, не оплачивается» закрывается одной строкой из документации биллинга: reasoning-токены тарифицируются как output-токены, по самой дорогой ставке, и учитываются в лимите max_tokens.

Масштаб стоит прочувствовать в числах. Объём невидимого рассуждения раздувается в три-десять раз относительно видимого ответа, без естественного потолка. В типичной агентной нагрузке o3-pro на максимальной глубине рассуждения обходился порядка 280 долларов в месяц — в три и шесть десятых раза дороже o3 и в восемнадцать раз дороже o4-mini, — при том что видимые ответы всех трёх сопоставимы по длине; разницу делает именно объём рассуждения. Планируя бюджет, закладывайте невидимую часть в два-пять раз больше видимого ответа — и перепроверяйте по полю usage в ответе API, где эти токены видны как строка расхода.

Ещё две границы. Первая: то, что вы видите как «ход рассуждений» в интерфейсе, — пересказ: провайдеры по умолчанию отдают summarized thinking, отдельный сгенерированный текст, а не сырые токены; строить на нём аудит решений нельзя. Вторая: управление сместилось от ручных бюджетов к адаптивности — модель сама решает, сколько думать. Это удобно — и это же означает, что стоимость запроса стала менее предсказуемой: на массовой обработке прижимайте effort вниз явно, а «думающий» режим включайте там, где сложность это оправдывает. Дефолтная глубина часто избыточна: если вы не измеряли зависимость качества от effort на своей задаче — с высокой вероятностью переплачиваете вдвое.
