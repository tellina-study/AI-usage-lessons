# Reader (Rendered) Report — Лекция 4 — 2026-08-10

Scope: only s13a–s13g (7 new slides, tooling landscape / Skills / MCP-for-coding-agents / steering-files / task-log patterns), read in context of surrounding Раздел 2 (s10 divider → s11 → s12 → s13a-g → s13 → s14 divider). Mode: `rendered` — PNG snapshots (rendered fresh via `render_slides_png_workaround.py`, positions 15-22 in `lec-04.pptx`) + speaker notes from `slides/s13*.md`. I am reviewing two weeks after the lecture, no lecturer present, self-study for РК.

## Slide s13a — Три категории инструментов (Agentic IDE / CLI-агент / Фреймворк)
- **Понимаю из слайда + notes без лекции:** да.
- **Что забыл / не помню без преподавателя:** ничего structurally missing — the "категория важнее бренда" callback to §0.4 register is legible even without remembering §0.4 verbatim, because the sentence restates the logic inline ("agentic IDE выигрывает при визуальном контроле... CLI-агент — при автоматизации...").
- **Где speaker notes недостаточны:** notes are connected prose, ~280 words, no layout description. One soft gap: "субагент" (gold callout + notes) is used with zero setup anywhere earlier in this lecture — I checked s01-s09, it's not there. It's a real concept (delegation to isolate context/blast-radius) but a first-time viewer meets the term cold, defined only by the one gold-callout example. It works as a self-contained mini-definition ("делегирует узкий подшаг... чтобы не засорять контекст и изолировать радиус поражения"), so it clears the bar, but barely — no explicit "новый термин" framing, it reads as if I should already know it.
- **Где визуал самодостаточен, где — нет:** visual (3 cards + 2 callouts) is self-sufficient; icons are decorative not semantic-load-bearing, so no information is lost without live narration.
- **2-Weeks-After Retention:** да — I can restate "3 categories differ by WHERE the agent lives, orthogonal to the A→D ladder" from the PNG alone.
- **Vocabulary check:** "субагент" — used without inline "что это" framing before its first real use (see above). "headless" — used once in notes without gloss, but low-stakes (context makes it inferable: "headless-окружение (CI, удалённый сервер)" — actually this IS glossed inline, fine).
- **Recommendation:** Add one clause flagging субагент as new vocabulary, e.g. "агент может использовать субагента — вспомогательный экземпляр той же модели, которому делегируется..." Minor, not blocking.

## Slide s13b — Skill = директория с SKILL.md
- **Понимаю из слайда + notes без лекции:** да.
- **Что забыл / не помню без преподавателя:** nothing blocking. Notes explicitly anchor back to a prior general definition ("Мы уже вводили skill в общем виде...") — this is exactly the kind of predecessor-slide callback the reader-simulator exception clause wants, though it doesn't cite a slide number. I could not independently verify which earlier slide that was without grep — a `(см. sNN)` pointer would remove the ambiguity for a student two weeks out.
- **Где speaker notes недостаточны:** notes are solid, ~230 words, fully readable prose, no "слева/справа" leakage.
- **Где визуал самодостаточен, где — нет:** self-sufficient — anatomy (SKILL.md/scripts/references), scope table, and 3 SWE examples are all legible from the PNG without narration.
- **2-Weeks-After Retention:** да.
- **Vocabulary check:** none new left undefined. "SWE" itself (Software Engineering, used as an adjective "SWE-примера") is never expanded anywhere in the deck's visible text I could find — low risk since ИУ6 audience knows it, but technically an unglossed acronym.
- **Recommendation:** add `(см. sNN)` slide pointer for "skill в общем виде" back-reference.

## Slide s13c — 5 категорий MCP-серверов
- **Понимаю из слайда + notes без лекции:** частично.
- **Что забыл / не помню без преподавателя:** the notes open with "MCP как протокол... уже разобраны раньше" and "принцип least-privilege уже разобран раньше" — both callbacks point to material introduced in **Lecture 3** (verified: `lec-03/slides/s20-mcp-standard.md`, `s21-agent-loop.md`, `s23-agent-failures.md` establish MCP + least-privilege), not to an earlier slide in *this* lecture. Two weeks after Lecture 4, a student who has since also sat through parts of the syllabus may not immediately recall that MCP and least-privilege were Lecture 3 content rather than assumed general knowledge. The slide itself never restates what MCP stands for (no inline "Model Context Protocol" or one-line gloss) — it relies entirely on cross-lecture memory.
- **Где speaker notes недостаточны:** this is the one slide in the set where the "отсылка к утерянной живой речи" failure mode from the checklist is real, even though phrased as a callback rather than "как я говорил": "уже разобраны раньше" without naming the lecture or slide is functionally the same gap for self-study purposes as a live-lecture-only reference.
- **Где визуал самодостаточен, где — нет:** the 5-category table + least-privilege examples are visually self-sufficient on their own terms (the mechanics are legible without MCP's underlying definition) — but the reader is left assuming they already know what MCP is.
- **2-Weeks-After Retention:** частично — the taxonomy (5 categories, scope-is-a-separate-decision) is retained fine; the underlying "what is MCP" prerequisite is not re-anchored.
- **Vocabulary check:** **MCP is used as a load-bearing term with zero inline definition on this slide or anywhere earlier in lec-04's own slide set.** This is a P1 per the Vocabulary Check rule: the acceptable exception requires "явная отсылка к слайду sNN" — this callback names neither a slide number nor even the source lecture. A first-time acronym expansion ("MCP — Model Context Protocol, протокол..." ) is nowhere in lec-04's visible or notes text for these 7 slides.
- **Recommendation:** add one-clause gloss reminder, e.g. "MCP (Model Context Protocol, разобрали в Лекции 3) — единый способ подключить агента к внешним системам" — cheap fix, closes the gap for both intra-course and standalone readers.

## Slide s13d — Steering-файл: анатомия
- **Понимаю из слайда + notes без лекции:** да.
- **Что забыл / не помню без преподавателя:** none — this slide is a genuinely well-built definitional unit: 4 components, README/CONTRIBUTING comparison table with explicit contrast axis, versioning callout. Nothing requires having heard the lecturer.
- **Где speaker notes недостаточны:** notes explicitly forward-reference the next slide ("presence paradox, к которому вернёмся на следующем слайде отдельно") — this is a *good* pattern (names what's coming, not a vague "as discussed"), and it delivers: s13e picks it up immediately.
- **Где визуал самодостаточен, где — нет:** self-sufficient. The README-vs-steering comparison table is the strongest visual in the set — clean contrast axis (аудитория/назначение/частота чтения), no live narration needed.
- **2-Weeks-After Retention:** да.
- **Vocabulary check:** "Kiro от AWS" is named in notes only (not visible body) as the source of the term "steering" — fine as color, doesn't carry definitional weight, and isn't asserted as something the student needs to know.
- **Recommendation:** none — this is a model example.

## Slide s13e — Presence paradox + Honest Lying
- **Понимаю из слайда + notes без лекции:** частично, with the substantial finding: да.
- **Presence-paradox finding (RCT null result):** the citation format itself is good practice — author, arXiv ID, year, explicit 3-condition/2-setting methodology, and the actual numeric-shaped finding ("не даёт статистически значимого прироста... стоимость и число шагов, как правило, растут") plus the one documented exception. This is **not** a name-drop — I can restate the finding, its scope, and its one caveat from the PNG alone. This is the kind of citation the course's own baseline/counterfactual mandate wants: it names the comparison conditions (NONE vs LLM-generated vs developer-written) rather than a bare "research shows X".
- **Honest-Lying citation:** thinner. It gives authors + arXiv ID + year and one mechanism sentence ("self-authored заметки могут не исправлять неверное убеждение, а закреплять его через повторные попытки"). This is enough to state the *risk* and *why* (same failure mode as self-authored memory generally), but there is no concrete example of what a "закреплённая ошибка" looks like in a steering-file context — the abstract mechanism is given, but no worked instance (e.g., "agent wrote 'auth uses JWT' after one failed guess, next session inherited it as fact and never re-checked"). The chapter may have this; the slide+notes pair does not. This is the closer of the two to "name-drop with substance but no instantiation" — it clears the bar (there IS a mechanism explained, not just a name), but a P2 concrete-example gap remains.
- **Где speaker notes недостаточны:** the Honest Lying paragraph in notes is one sentence longer than the visible callout but doesn't add a worked example either — notes and visual say almost the same thing in slightly different words, which is fine for redundancy but doesn't close the concreteness gap.
- **Где визуал самодостаточен, где — нет:** self-sufficient for presence-paradox; partially self-sufficient for Honest Lying (mechanism clear, instance missing).
- **2-Weeks-After Retention:** да for presence-paradox null result + "when steering file NOT needed" criterion (gold callout, maximum visibility, does exactly what §5's schema rules ask — this is not a schema slide but the callout plays the same self-sufficiency role). да, partial, for Honest Lying — I'd retain "self-editing steering files is risky because errors can calcify," but might not reconstruct *why* mechanically without re-reading.
- **Vocabulary check:** "self-authored" appears in the visible gold-callout text in English ("self-authored правки") — this is flagged separately by the anti-anglicism mandate (§5.8 of the presentation-build README), not strictly my checklist's job, but worth noting since it's the exact kind of unglossed English term the course's own rule targets. It is used twice in this slide (title of Honest Lying box: "риск self-authored правки"; body: "self-authored заметки", "self-authored память в целом") without a Russian gloss anywhere on the slide.
- **Recommendation:** (1) one added clause with a concrete Honest-Lying instance ("например: агент один раз ошибочно решил, что тест X не работает из-за Y, записал это в файл, следующая сессия унаследовала вывод без перепроверки"); (2) Russify "self-authored" → "самостоятельно написанные/self-authored (заметки, которые агент пишет сам)" per §5.8 — this is a cross-artifact concern the orchestrator's deep-latin-token scan should also catch.

## Slide s13f — Три паттерна журнала задач
- **Понимаю из слайда + notes без лекции:** да.
- **Что забыл / не помню без преподавателя:** nothing blocking. The teal callout at top does useful work distinguishing this from steering-file and self-authored memory — a good example of pre-empting a "wait, isn't this the same as s13d?" confusion before it happens.
- **Где speaker notes недостаточны:** notes are ~260 words, connected prose, and go slightly further than the visible body (e.g., adding "тот же класс проблемы, что деградация точности при росте контекста" as an implicit gloss for "context rot") — that's good, it partially compensates for the unglossed term (see vocabulary below).
- **Где визуал самодостаточен, где — нет:** self-sufficient. One render-vs-source discrepancy I noticed: the rendered PNG's Pattern-1 card shows an extra clause not present in the `s13f-*.md` source markdown — "план отдельно от лога выполнения" appears under "tasks/TASK-123/ с notes.md, log.md, черновиками" in the render but isn't in the reviewed markdown body. This is a build-pipeline / consistency-checker concern, not a reader-comprehension blocker (the added clause is harmless and consistent with the pattern), but flagging since drift between `slides/*.md` source-of-truth and rendered PPTX visible text is exactly what `consistency-checker` should catch.
- **2-Weeks-After Retention:** да — I can restate each pattern's strength + failure mode from the PNG alone.
- **Vocabulary check:** **"context rot"** — used in visible body ("файл не помещается в контекст агента целиком (context rot)") with no inline Russian gloss anywhere on this slide's visible text. The English term appears parenthetically as if it's assumed known. Notes soften this slightly ("тот же класс проблемы, что деградация точности при росте контекста") but that clause is in speaker notes, not on the visible slide — and per the checklist, undefined terms in visible body are P1 regardless of whether notes later paraphrase it. This is the second unglossed-acronym/anglicism instance in the 7-slide set (after "self-authored" on s13e).
- **Recommendation:** add inline Russian gloss on the visible card itself: "(context rot — деградация точности модели при разрастании контекста)" — one clause, matches what notes already say, just needs to move onto the visible artifact.

## Slide s13g — Сравнение 3 паттернов (таблица 4×3)
- **Понимаю из слайда + notes без лекции:** да.
- **Что забыл / не помню без преподавателя:** nothing blocking — this slide is explicitly the resolution slide for s13f, and it reads that way on its own.
- **Где speaker notes недостаточны:** notes (~270 words) are strong — they don't just restate the table, they add the "no single right answer, it's an axis-weighing exercise, not a hedge" framing and two worked examples (sequential team → Pattern 2 valid; low-task-count team → Pattern 1 valid), matching what the visible teal callout already states. Good redundancy without being repetitive filler.
- **Где визуал самодостаточен, где — нет:** self-sufficient. This is a `schema_matrix`-style 4-column × 3-row table; fill rate is 100% (no empty cells), which passes the Schema Readability checklist's fill-rate bar even though this slide is typed `comparison` rather than `schema_matrix` in frontmatter.
- **2-Weeks-After Retention (this is the key test — can I actually APPLY the decision criteria to a new situation?):** да, with one caveat. I can genuinely walk into a fresh scenario ("my team runs 3 parallel agent sessions on different tickets, we're 8 people") and correctly derive "parallelism axis is decisive here → avoid Pattern 2" using only the table + callout. The two worked examples in the callout ("team working strictly sequentially" / "small number of tasks, low parallelism") give me two of the three decision poles concretely; the third (moderate-complexity + parallelism-decisive → converges to Pattern 3, mentioned only in the *speaker notes*, not the visible slide: "команды... для которых решающей осью оказывается устойчивость к параллелизму при умеренной сложности задачи... чаще сходятся к плоской общей директории") is missing from the visible artifact. A student re-deriving this from PNG alone (without notes) would correctly rule out Pattern 2 in the parallel-team case but wouldn't be pointed at Pattern 3 as the typical resolution — they'd have to infer it themselves from the table's own columns, which is a reasonable but non-trivial inferential step.
- **Vocabulary check:** none new — all terms carried over from s13f, already covered there.
- **Recommendation:** consider promoting the third worked example (parallelism-decisive + moderate complexity → Pattern 3 typical) from speaker notes into the visible teal callout, to make the three-way decision criterion fully self-contained on the slide itself, not just in notes. Not blocking — the two-example version is already enough to apply the general reasoning pattern.

## Сводка

- Слайдов self-contained (из 7 в scope): **6 / 7** (s13a, s13b, s13d, s13e, s13f, s13g — all "да" or "да, with minor P2 gaps"; s13c is "частично" due to unglossed MCP + cross-lecture-only callback).
- Self-containedness ratio (7-slide scope): 86% — above the 85% production threshold, but the denominator is small (7 slides), so this should not be read as a full-deck verdict; see Structural Blockers below for what specifically needs fixing.
- Слайдов, требующих преподавателя/предыдущей лекции для полного понимания: **1** (s13c — assumes MCP already known, doesn't restate it).
- Топ-3 правки speaker notes / visible body:
  1. **s13c: add inline MCP gloss** ("MCP (Model Context Protocol, Лекция 3) — единый способ подключить агента к внешним системам") — closes the one P1 vocabulary gap that actually blocks a cold read.
  2. **s13f: move "context rot" gloss from speaker notes onto the visible card** — currently the definition-adjacent clause exists but is notes-only; the visible slide has the bare English term unglossed.
  3. **s13e: add one concrete worked instance of Honest-Lying calcification** (currently mechanism-only, no example) + Russify "self-authored" → gloss it inline (cross-cutting with §5.8 anti-anglicism mandate).
- Vocabulary issues: **2 terms without inline definition on visible body** — MCP (s13c, P1 — blocks cold comprehension) and context rot (s13f, P1 per strict rule — English term unglossed in visible body, even though notes partially compensate). Additionally 1 anglicism-adjacent instance not strictly a vocabulary-checklist item but worth flagging: "self-authored" (s13e, visible gold callout, twice) — unglossed English term in RU-audience visible body, cross-references presentation-build README §5.8.

## Structural Blockers (для slides не self-contained)

Of 1 self-contained-fail slide (s13c) + 2 partial-P1-vocabulary slides (s13e Honest-Lying concreteness, s13f context rot):

- **Notes-fixes** (just expand notes / move existing notes content onto visible slide, ~1 clause each): **s13c** (add MCP one-line gloss), **s13f** (promote context-rot gloss from notes to visible card).
- **Schema redesign:** none — no schema-readability failures found in this 7-slide set; s13g's 4×3 table passes fill-rate and is well within projector-readability norms at this zoom.
- **Vocabulary fixes** (inline term definitions needed): **s13c** (MCP), **s13f** (context rot), **s13e** (Russify "self-authored" per §5.8).
- **Structural cuts:** none — no slide in this set needs deletion or relegation to live-only. All 7 pull their weight for self-study; the fixes needed are all small (single-clause) additions, not redesigns.

## Cross-cutting observations (beyond the 7-slide scope, worth flagging to orchestrator)

- **"субагент" (s13a) and "MCP" (s13c) are the two terms in this batch that a genuinely cold reader (never took Lecture 3, or forgot it) would stumble on.** Both are real, useful callbacks to Lecture 3 content, but neither slide restates the term inline — they assume retained cross-lecture memory, which is a stronger assumption than the checklist's exception clause technically allows (the clause wants "слайд sNN, помните" — a same-deck slide pointer, not a same-course-different-lecture pointer).
- **The s13f/PNG vs s13f/markdown discrepancy** ("план отдельно от лога выполнения" appearing in the render but not the reviewed source md) is a build-consistency issue, not a reader-comprehension one — flagging for `consistency-checker` / orchestrator's own pre-gate diff, not actionable by me as a reader.
- Overall, **this batch of 7 new slides is meaningfully stronger on self-containedness than the median pre-existing slide I'd expect from a first pass** — teal "distinguish from adjacent concept" callouts (s13a vs autonomy ladder, s13f vs steering-file/memory) are doing real anti-confusion work, and the two-worked-example pattern in s13g's callout is a good model for what "apply the criteria yourself" should look like elsewhere in the deck.
