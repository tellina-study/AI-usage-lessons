# Render log — Лекция 2 v2.0, БАТЧ 2 (Разделы 3–5, 26 слайдов) — issue #183

Дата: 2026-09-05. Ветка: `issue-183-lec02-v2`.
Builder: `rendered/build_lec02.py` — дополнен 26 функциями батча 2; dict
BUILDERS теперь 46 слайдов, полный дек. Convert — канонический wrapper
`source /home/harness/.local/lo-portable-env.sh` (mcp-limitations [#172-2]),
PDF → PNG через `pdftoppm -r 100`. PowerPoint MCP по-прежнему не
зарегистрирован — python-pptx full-rebuild путь батча 1 ([#71-1]).

## Asset-итерации (до слайдовых)

- **s19-chart** (QuickChart POST, 980×420): веса внимания на 8 токенов
  предложения из s18 («мышь» 0.40 gold, сумма = 1) — принят с 1-й итерации;
  связность с матрицей s18 вместо абстрактных 8 токенов из visual_brief.
- **s25-chart** (QuickChart POST, 980×480): NoLiMa — 13 баров, 2 teal выше /
  11 MID ниже gold-пунктира 50%. **Найдена limitation:** label у
  chartjs-plugin-annotation в QuickChart v4 не рендерится (добавлено в
  mcp-limitations [#183-2]); подпись «11 из 13 — ниже 50%» наложена
  текстовым слоем pptx поверх PNG.
- **s41-hero** (свой SVG → PyMuPDF, solid fills only по [#183-1]):
  вантовый мост «конвейер Лекции 2 → агентный граф Лекции 3», muted ocean
  `#b8c9db` + gold ванты; композиция паттерна s01 — hero 9.9″×5.11″ ≈ 51%
  площади фоновым слоем, карточки 2×2 поверх, пилон и ванты читаются в
  центральной щели. Attribution не нужна (собственная графика).
- **s41-icons**: Lucide search/settings/plug/refresh-cw → recolor `#065A82`
  → PNG 96px через PyMuPDF (rsvg-convert в env нет; strokes рендерятся
  чисто).

## Итерации (Generate → Convert → Inspect → Fix; полный rebuild на итерацию)

3 полных цикла: iter-1 все 46 стр., iter-2 перерендер стр. 23–41,
iter-3 полный перерендер + финальный accept. Визуальный осмотр каждой
batch-2 страницы на каждом цикле.

### Iter 1 — осмотр 26 PNG, найдено 10 замечаний

| Слайд | (a) что найдено | (b) что изменено в iter 2 |
|---|---|---|
| s19 | title 2-я строка «Key / Value» срезана боксами; beam-треугольник закрыл заголовок «Фонарик…»; chart вылез за низ бокса и налез на карточки 1–3 | title 23→20pt (влез в 1 строку), боксы y1.3; beam заменён на 3 gold-луча (plain_line); chart 6.6→5.6″ внутрь бокса |
| s20 | title 2-я строка срезана верхним боксом; «null»?» think-pause обрезан низом бокса | title 22→20pt y0.32; top box y1.12 h2.45; думайте-блок y2.82 h0.7; каскадный сдвиг нижних боксов/callout |
| s23 | title 2-я строка «полезное» срезана | title 25→24pt (1 строка), chart box и полюса y1.5, каскад вниз |
| s25 | title 2-я строка «нет» срезана; практика-строка упиралась в нижний край | title 24pt/1 строка; панели y1.45; практика 10.5pt y6.7 |
| s26 | title 2-я строка «словаря» срезана | title 24pt/1 строка; боксы y1.5, каскад |
| s30 | title 2-я строка срезана; «постфактум» обрезан низом бокса | title 23pt/1 строка; левый бокс h4.25, сравнение y4.48; правые плашки h0.76 |
| s32 | текст «авторегрессионный» впритык к краю чипа; caption «по ставке output…» лёг ВНУТРЬ серой полосы | чип w2.7/11.5pt; полоса h0.62, caption вынесен под полосу y3.24 |
| s35 | чип «следующий токен» — кривой перенос (2-я строка вправо) | одна строка → два centered-параграфа text_runs |
| s36 | title 2-я строка «порядка» срезана колонками | title 24pt/1 строка; колонки y1.5, IMO/шкала каскад вниз |
| s37 | title 2-я строка «жульничают сами» частично под карточкой 1 | title y0.35 h0.9; карточки y1.52 |

Паттерн итерации: батч-1 конвенция `slide_title` (y0.5 h1.0) не
рассчитана на 2-строчные заголовки при контенте с y≈1.3 — у 8 слайдов
заголовок конфликтовал с первым боксом. Лечение: либо ужать шрифт до
1 строки (s23/s25/s26/s30/s36), либо явно опустить контент (s20/s37).

### Iter 2 — перерендер стр. 23–41: все 10 фиксов легли чисто

Остаточные замечания (найдены активным поиском): s20 — нижние колонки с
пустой нижней третью (visual mass); s31 — цикл прижат вверх, ~1″ пустоты
снизу.

### Iter 3 — полный перерендер, финальный accept

- s20: нижние боксы h2.05→1.85, callout поднят — масса выровнена.
- s31: цикл y2.15→2.35, stop-строка и caption вниз — вертикальный баланс.
- Батч-1 (стр. 1–20): без регрессий, все страницы получили «N/46».
- 5-Second Test по всем 26 batch-2 PNG — PASS (main message = assertion;
  сильнейшие: s28 «80/1000», s22 timestamp-самострел, s38 таблица M1–M6).
- Projector-check 50%: минимальные шрифты — 9.5–10.5pt в ячейках матрицы
  s18 (7×7 значений — плотность неизбежна), 11pt в таблице s38, 10.5pt
  якоря-курсив s41. Всё — вторичный слой (значения/якоря), заголовки и
  тезисы ≥12.5pt. Отмечено для критиков.

## Ключевые решения по слайдам

- **Дивайдеры s18a/s26a/s35a:** тот же `section_divider` + `pipeline_bar`
  батча 1; s18a active=LLM, s26a active={Распределение, Токен}, s35a — все
  7 стадий gold + объединяющая gold-рамка (новый параметр `frame_bar`).
- **s18:** матрица 8×8 — нативная таблица (паттерн s15 батча 1), заливка
  `_ocean_scale`, ячейка «она»→«мышь» GOLD; правая колонка — 3 свойства.
- **s21:** схема K/V-плашек (teal, «уже посчитаны — лежат в кэше») + Q gold
  у нового токена + gold LEFT_ARROW «сверка со всеми K/V»; ниже prefill/
  decode в 2 боксах.
- **s22:** тарифные плашки (чтение gold-tint / запись) + кейс-бары
  $45k→$8k (−82% gold); справа вертикальный exact-prefix стек: 3×teal «✓
  кэш» → gold «динамический timestamp !» → grey «× мимо кэша»; мини-опрос.
- **s23:** нативные лог-бары (log10-ширины) вместо QuickChart — полный
  контроль подписей; Scout-полюс — SOFT_GREY с dashed-обводкой
  («заявлено»), YandexGPT — контраст снизу.
- **s28:** экспонат «80 / 1000» 64pt gold + цепочка 4 пронумерованных
  плашек-причин + 2 плашки решение/цена.
- **s29:** нативная таблица сценариев 4×5 (mono-заголовки параметров, T=0
  gold / 0.9–1.2 teal) + 2 плашки effort/verbosity с gold-чипом «2026».
- **s30:** мини-бары маскирования (2 из 5 SOFT_GREY с «×») + «100%» gold;
  ограничения — 4 плашки; retrieval-бейдж «Вопрос залу».
- **s31:** цикл 5 боксов + gold-чип «вход» (точка входа), возврат
  LEFT_ARROW под боксами «возврат к шагу 1 — цикл повторяется»; шаг 2
  gold (это и был конвейер).
- **s32:** раздвоенный выход (teal «видимый ответ» / серый «черновик» с
  gold «×3–10») по visual_brief источника (не «айсберг» deck.yaml — source
  wins); относительные бары o4-mini/o3/o3-pro (18× gold), без абсолютных $.
- **s35:** конвейер 4 стадий + gold-чип «следующий токен» + loop; 4
  плашки-наложения TEAL_TINT+gold-обвод с dashed-коннекторами к стадиям.
- **s36:** 2 лагеря + IMO-полоса gold-tint + ценовая шкала (полоса с
  LIGHT/DEEP концами) + Kimi-строка (gold «при цене на ~80% ниже»).
- **s38:** нативная таблица 7×3, M-бейджи GOLD_TINT (единственное место с
  M-метками кроме s02a), zebra-строки; перекличка с чек-листом s01.
- **s39:** 4 ветки-строки «условие → ответ» (ответ teal bold) + gold-tint
  «Иначе»; справа лестница 2 ступеней + расчёт $10 000 vs $1 180 (gold).
- **s40:** 2 колонки с чипами 3 уровней причинности: человек 3×«✓» teal;
  модель «✓ / ~ / ×» (teal / outline / grey) — уровень 1, частично 2, не 3.
- **s41:** hero-мост (см. asset-итерации) + 4 карточки с Lucide-иконками;
  gold-якорь на MCP-карточке (кэш), gold «Лекция 3:» в заголовке.
- **s42:** паттерн Lec-1 s31 — SURFACE фон, «Q&A» 140pt DEEP, «Спасибо»
  36pt italic, заглушка контактов; без gold (санкционировано паттерном
  qa_minimal — единственный слайд без gold-акцента).
- **Page numbers:** глобально «N/46» на всех 46 слайдах (page_number
  расширен параметром total; бокс расширен под «46/46»).

## Compliance-сканы (по visible-слою PPTX + notes, все 46 слайдов)

- Anti-leak grep (`[VERIFY-DAY-OF]`, `[FACT-CHECK]`, `LO[1-9]`, `§N.N`,
  `→ sNN`, «Лектору», «Вы здесь», `N мин`, «методическ», «педагогическ»,
  ⏱/⏰): **0 hits** в visible и в notes.
- Deep latin-scan (deep_latin_scan.py) по visible: **218 occurrences /
  132 unique** на весь 46-слайдовый дек. Разбор remainder: brand/model
  names (GPT-, Kimi, Qwen, Fable, Llama, DeepSeek, Grok, SWE-bench,
  Hugging Face, vLLM, RTX…), glossary_lock термины (KV-cache, top-p,
  temperature, max_tokens, prompt caching, chat-, batch-, reasoning-…),
  API-литералы chat-шаблона (system/user/assistant/role/content,
  `<|im_start|>`), параметры (effort, verbosity, seed, strict mode, $ref,
  argmax, prefill/decode, Q/K/V), tokenization-примеры (strawberry,
  cranberry, cat, low/er/new/est/wid) и бенчмарк-имена (NoLiMa, Verified,
  Pro, Elo, IMO). Narrative-англицизмов вне keep-list не найдено; 2×
  «inference» — батч-1 (s02/s06), уже учтено в его логе.
- Gold ≥1×/слайд: подтверждён на 25 из 26; исключение s42 (qa_minimal —
  паттерн Lec-1 без gold, сознательно).
- Ocean-motif: на каждом content-слайде ≥1 ocean_box/производный.
- Speaker notes: дословно из «## Speaker notes» источников; 181–278 слов
  на слайд (все в 150–300).

## Отклонения source ↔ deck.yaml (не правились, No Extra Content — решение оркестратору)

1. Теги дивайдеров: источники всех трёх дают «4 разбора · 2 провала»;
   deck.yaml — «5 разборов · 2 провала» (s18a/s26a) и «3 разбора · 2
   провала» (s35a). Рендер по источникам.
2. s26a sub-title: источник «Сэмплинг и генерация», deck.yaml — «Сэмплинг».
   Рендер по источнику.
3. s18a frame-фраза источника длиннее версии deck.yaml (рендер по
   источнику, 18pt чтобы влезала в 2 строки).
4. s32: deck.yaml pattern `iceberg_billing_schema`; visual_brief источника —
   «раздвоенный выход» (не айсберг). Рендер по источнику.
5. s41: deck.yaml упоминает «мини-блок: 4 коротких эксперимента» — в
   visible-источнике его нет (эксперименты в notes). Рендер по источнику.
6. s42: паттерн требует «без footer» — оставлен единый page number
   «46/46» ради сквозной пагинации (микро-отклонение, легко убрать).

## Известные компромиссы (для критиков)

1. LibreOffice-превью: лёгкие drop-shadow на rounded-rect + DejaVu вместо
   Arial — артефакты конвертера ([#54-render]), в реальном PowerPoint
   отсутствуют; не чинилось (по brief).
2. s18: значения матрицы 10pt / заголовки колонок 9.5pt — ниже гайдовых
   минимумов; это референс-значения (главный сигнал — gold-ячейка и
   заливка), заголовок и вывод ≥13pt.
3. s25: подпись «50% базовой точности» на chart-пунктире не отрендерилась
   QuickChart'ом ([#183-2] — новая limitation) — компенсирована
   pptx-текстом «11 из 13 — ниже 50%» на белом поле чарта.
4. pipeline_bar «Распределение» 9.5pt — компромисс батча 1, унаследован
   дивайдерами батча 2.
5. `rendered/lec-02-notes.pdf` в репо остался от v1.8 (35 слайдов) — его
   перегенерация в brief батча 2 не входила; отметить оркестратору
   (tools/presentation-build/notes_pages_pdf.py).
6. EN-артефакты (`build_lec02_en.py`, `lec-02-en.pptx`) не трогались —
   v2.0 EN-трек отдельной фазой после approve RU (Bilingual Rule).

## Топ-слайды

- **Сильнейшие:** s22 (тарифы + timestamp-самострел — самый «инженерный»
  слайд дека, 5-сек message мгновенный) и s28 (экспонат 80/1000 +
  причинная цепочка — закрытие M4 читается без слов).
- **Слабейшие:** s18 (матрица 8×8 информационно плотная, значения 10pt —
  живёт за счёт gold-ячейки, но с 5-го ряда числа не читаются) и s35
  (4 плашки-наложения + dashed-коннекторы дают лёгкий «паутинный» шум;
  альтернатива — цветовое кодирование плашек по стадиям без линий).
