VERDICT: REVISE

# Presentation Critic Report — Лекция 4 «AI в разработке ПО» — 2026-05-16 (v1)

Issue #99 · ветка `issue-99-lec-04-software-production` · 32 слайда · vision-enabled (все 32 PNG просмотрены).
Эталон стиля: `library/lectures/lec-03/rendered/snapshots/`.

## Сводка

- Всего слайдов: 32
- **P0 issues (блокеры): 1** (deck-wide `[VFY]`/`[VERIFY]`-маркеры на видимом слое — L2-прецедент квалифицирует как P0)
- **P1 issues (важные): 6** (см. ниже; counter-check выполнен)
- P2 issues (мелочи): 5

**Counter-check (mandatory):** P0 присутствует → verdict не может быть APPROVE. Кроме того 6 P1 → по правилу «5+ P1 ⇒ REVISE». Verdict = **REVISE** (не REJECT: контент методически корректен, арка сильная, дефекты — системный leak видимого слоя + 3 нечитаемых chart'а, всё чинится одной revision-итерацией без пересмотра концепта).

---

## Вердикт по 2 известным кандидатам (designer-REPORT)

### Кандидат 1 — s32 «LO4» на видимом слое → **ПОДТВЕРЖДЁН, P1**

`slides/s32-bridge-seminar-qa.md` строка 24, секция **## Body** (НЕ speaker notes):
> «…— место полного освоения **LO4**. Для каждого кейса…»

На PNG s32 жёлтый ДЗ-блок выводит: «Задание — Семинар 4 … Для каждого кейса: уровень + ≥2 причины по осям + ≥1 условие смены + явное "где человек обязателен"». В отрендеренной версии designer **сократил** видимую строку и фразу «место полного освоения LO4» на PNG **не видно** — но source-md по-прежнему содержит «LO4» в Body. Это нарушение anti-pattern #37 (LO codes на student-видимом слое) на уровне source-of-truth: при следующем ре-рендере / правке риск регрессии. Квалификация: **P1** (не P0 — на текущем PNG не виден; но source обязан быть чистым: переписать строку Body на «место полного освоения учебной цели применения», LO-код оставить только в frontmatter `learning_outcomes`).

### Кандидат 2 — forward/cross-refs «(sNN)» / «§X.X» / «Раздел N» на видимом слое → **ПОДТВЕРЖДЁН, P1 (cross-deck pattern)**

Это **не точечный, а deck-wide системный leak**. Подтверждено и в source-md (греп Body-секций), и **визуально на PNG**:

| Слайд | Видно на PNG (Body) | Anti-pattern |
|---|---|---|
| s03 | `(§5.1)`, `(§5.2)`, `(§4.3)`, `(§4.7)`, `(Раздел 4)` в таблице-мэппинге | #38 §-ref |
| s04 | `§1.4 §2.3 §3.5 §4.4/§4.7 §5.2 Brooks/DORA` — буквально подписи 5 chip-карточек | #38 §-ref (грубо: §-коды = единственный текст карточек) |
| s06 | «Рамка уровня A» (ок), но footer и блоки используют §1.1 косвенно | #38 (мягко) |
| s07 | `«почти правильный» код (s08)` в правой нижней карточке | #39 forward-ref |
| s09 | `(методология №1 — Раздел 5)` + `(строгое определение — s25)` в плашке-мосте | #39 forward-ref + #38 |
| s11 | `…ревью PR и merge (s13)` + `…на незнакомом коде (s12)` + `(callback Лекции 3 §4.3)` | #39 + #38 |
| s19 | `(Brooks, Раздел 5)` | #38 |
| s20 | `(§2.3-деградация + FP-шум)`, `(GitClear)` | #38 |
| s21 | `…частично; полный возврат на s23` (footer/плашка) | #39 forward-ref |
| s23 | `…мост Л3 §4.6`, `§4.7` в канон-карточках | #38 |
| s24 | первый столбец = `§1.4 / §2.3 / §3.4–3.5 / §4.4/4.7` (§-коды как контент-ячейки) | #38 (грубо) |
| s30 | `(перенос Лекции 3 §5.2)` | #38 |
| s32 | `матрица §6.1`, `чек-лист §6.4` в мост-блоке | #38 |

Квалификация: **P1 cross-deck redundancy/leak pattern** (13+ слайдов). Для студента в зале `§4.7`, `(s13)`, `Раздел 5` — методические крестики-ссылки, не несущие смысла; они засоряют видимый слой и выглядят непрофессионально (никто из эталона lec-03 так не делает). Fix: все §-refs / (sNN) / «Раздел N» / callbacks переносятся в `speaker_notes`; на видимом слое — человеческая формулировка («как в Лекции 3», «дальше разберём строго», «риск — почти правильный код» без «(s08)»).

---

## P0 — блокеры

### s12 / s21 / s22 / s27 / s28 — `[VFY-day-of]` / `[VFY]` маркеры на видимом слое
**Severity:** P0
**Issue:** Footer-плашки выводят буквально `Числа [VFY-day-of] …` (s12), `Доли [VFY], концептуально стабильно` (s21), `Проценты [VFY], направление стабильно` (s22), `Adoption-доли [VFY-day-of]` (s27), `…числа [VFY-day-of]` (s28). Это внутренний production-маркер «проверить в день показа», не контент для аудитории.
**Anti-pattern:** #36 (`[VERIFY-DAY-OF]`/`[FACT-CHECK]` visible в body). Прецедент Лекции 2 R1 Phase 7 квалифицировал идентичную проблему как **P0** (s16, s27). Plan-v2-final §7 forbidden прямо: «vendor/benchmark-числа как незыблемые → `[VFY]`/notes» — то есть маркер живёт в notes, НЕ на слайде.
**Recommendation:** Удалить токены `[VFY]`/`[VFY-day-of]` с видимого слоя на всех 5 слайдах. Caveat сформулировать человеческим языком («числа на конец 2025, лидер меняется еженедельно» — без скобочного маркера). Сам verify-флаг — только в `verify_day_of_items` (он уже есть в deck-part2.yaml) и в speaker_notes.
**Visual evidence:** s12 footer «Числа [VFY-day-of]: SWE-bench лидеры меняются еженедельно…»; s27 footer «…Adoption-doli [VFY-day-of]»; s28 footer «AGENTS.md/CLAUDE.md adoption-числа [VFY-day-of]…». Прямо читается на PNG 13–14pt.

---

## P1 — важные

### s03 — таблица-мэппинг перегружена §-кодами и мелким текстом
**Severity:** P1
**Issue:** 4×2 mapping-таблица с §5.1/§5.2/§4.3/§4.7/Раздел 4 как часть видимых ячеек; шрифт ячеек ~12–13pt, на 50% zoom (задние ряды) текст «Кодинг-агент (уровень C) = ровно этот цикл, применённый к коду» нечитаем. 5-сек тест: главную мысль («A→D = лестница сложности Л3») считываешь только из gold-плашки внизу, не из таблицы.
**Recommendation:** Убрать §-коды из ячеек (→ notes), укрупнить текст ячеек до ≥14pt, сократить формулировки до 4–6 слов. Левая колонка — без «§5.1»-якорей, только смысл блока.
**Visual evidence:** Плотная сине-серая таблица, 4 строки, текст под колонкой «Проекция на разработку ПО» переносится на 2 строки мелким кеглем.

### s04 — chip-карточки «где человек обязателен» подписаны только §-кодами
**Severity:** P1
**Issue:** 5 карточек-якорей содержат буквально «§1.4 / §2.3 / §3.5 / §4.4·§4.7 / §5.2 (Brooks/DORA)» с микроподписями «почти правильный / merge / деструктив + perception-gap / безопасность / —». Студент видит §-коды крупно, смысл — мелко. Это инверсия иерархии: методический указатель крупнее содержания.
**Recommendation:** Карточки подписать смыслом крупно («"почти правильный" код», «merge», «деструктив без гейта», «уязвимость + утечка», «essential vs accidental»); §-коды убрать с видимого слоя целиком.
**Visual evidence:** s04 — 5 голубых chip-боксов, верхняя строка каждого = §-код 18pt+, нижняя = слово 11pt.

### s32 — «LO4» в source Body (см. Кандидат 1) — P1
Деталь выше. Fix — переписать строку Body, LO-код только во frontmatter.

### Forward/cross-ref leak pattern (см. Кандидат 2) — P1
Системный, 13+ слайдов. Деталь выше.

### s13 — GitClear chart нечитаем (axis/legend микро-кегль)
**Severity:** P1
**Issue:** Bar-chart «GitClear 211M строк (2020→2024)» — подписи осей, легенда (3 серии) и значения настолько мелкие, что нечитаемы даже на полном render, не говоря о 50% zoom. Anti-pattern #22 (projector-distance illegibility, axis <14pt). Числовая суть (клоны 8,3→12,3%, рефакторинг 24,1→9,5%, churn 5,5→7,9%) на графике не считывается — она есть только в антипаттерн-карточке слева текстом.
**Recommendation:** Либо заменить chart на 3 крупных стат-числа со стрелками тренда (↑клоны ↓рефакторинг ↑churn), либо перестроить QuickChart с font ≥16pt для labels, ≤2 серии, прямой подписью значений на барах. Текущий chart = декорация, не доказательство (anti-pattern base #4 наоборот: визуал не работает на assertion).
**Visual evidence:** s13 правый блок — серо-голубые группы баров, легенда нечитаемой строкой сверху, ось Y без читаемых делений.

### s12 / s17 — sparse / illegible bar charts
**Severity:** P1
**Issue:** s12 — bar-chart всего из 2 столбцов (Verified ~88% / Pro ~64%) в большой пустой рамке: 5-сек тест проходит только по тексту-плашке справа, не по графику; разрыв ~24 п.п. визуально не подчёркнут (бары не выровнены к общей базе с явной дельтой). s17 — bar-chart с нечитаемыми микроподписями осей, смысл «эффект монотонно убывает» из графика не считывается.
**Recommendation:** s12 — заменить на пару mega-stat (88,7% / 64,3%) + явная gold-дельта «−24 п.п.» крупно; bar-chart из 2 значений не оправдан. s17 — либо убрать chart (смысл несёт текст «+56% / +7–22% / −19% одной функцией»), либо сделать наклонную линию-тренд с 3 крупными точками и подписями.
**Visual evidence:** s12 — два изолированных бара (тёмно-синий, золотой) в широкой рамке, ~60% площади пусто; s17 — 3 бара с нечитаемой осью.

---

## P2 — косметика

### s07 — «Граница А -- В» в заголовке (двойной дефис вместо тире/стрелки)
**Severity:** P2
**Issue:** Assertion-заголовок рендерится «Граница А -- В — первое реальное делегирование…» — литеральный `--` вместо `↔` или `—`. Выглядит как невыправленный markdown.
**Recommendation:** Заменить `--` на `↔` («Граница A↔B») или em-dash.

### s06 — bar-chart без подписей значений
**Severity:** P2
**Issue:** 3 бара (Лаборатория +56 / Поле / Знакомое легаси −19) без числовых подписей на барах; числа есть только в тексте справа. Визуал работает слабее, чем мог бы.
**Recommendation:** Подписать значения прямо на барах (крупно, ≥14pt).

### s08 — donut «66%» хорош, но левый блок «70/80%-проблема» — 3 длинных булета мелким кеглем
**Severity:** P2
**Issue:** Левая Ocean-box: 3 буллета по ~2 строки ~13pt. На 50% zoom — на грани читаемости.
**Recommendation:** Сократить буллеты до 1 строки каждый или укрупнить до 15–16pt.

### s24 / s29 — первый столбец matrix содержит §-коды как идентификаторы строк
**Severity:** P2 (контентно — часть P1 leak pattern, но визуально отдельная мелочь)
**Issue:** s24 столбец «Точка» = «1·§1.4 / 2·§2.3 / 3·§3.4–3.5 / 4·§4.4/4.7»; читается как технический индекс. s29 — ось-строки нормальные (словами), это ок.
**Recommendation:** s24 — заменить столбец на «1 / 2 / 3 / 4» или короткое имя точки («Почти правильный код» и т.д.) без §.

### s10 / s14 / s18 — section dividers: gold подчёркивание под «РАЗДЕЛ N» вместо teal accent line (отличие от lec-03 s02)
**Severity:** P2
**Issue:** В lec-03 cover/divider accent-line под меткой раздела — teal `#1C7293`/teal. В lec-04 dividers (s10/s14/s18) подчёркивание gold `#F0AB00`. Это сознательный gold-маркер раздела (заявлен в deck.yaml visual-brief «gold-маркер Раздел N»), консистентен внутри lec-04 и не нарушает палитру — поэтому P2 «note», не дефект. Иерархия и читаемость dividers — чистые, эталону соответствуют (гигантская soft-outline цифра справа, roadmap-bar 7 карточек, текущая gold). Расхождение с lec-03 минимальное и осознанное.
**Recommendation:** Принять как есть (gold-маркер раздела — заявленное дизайн-решение Л4); фиксировать только если владелец хочет 1:1 с lec-03.

---

## Schema Readability — таблица pass/fail (§5.5)

| Слайд | Subtype | Fill ≥75% | Шрифт ≥14pt body | Axis/labels inside | 5-сек тест | Projector 50% | Verdict |
|---|---|---|---|---|---|---|---|
| s03 | schema_matrix (mapping) | PASS | **FAIL** (~12–13pt) | n/a | **FAIL** (смысл только из gold-плашки) | **FAIL** | **FAIL → P1** |
| s07 | comparison (2-col) | PASS | PASS | n/a | PASS | borderline | PASS (но leak `(s08)`) |
| s11 | schema_cycle | PASS | PASS (шаги крупно) | start/continue explicit (есть «↻ повторяется», iterate→plan) | PASS | PASS | PASS (leak `(s12)(s13)§4.3`) |
| s12 | schema_matrix (stat-pair) | **FAIL** (2 бара, ~60% пусто) | PASS | n/a | borderline (текст несёт) | borderline | **FAIL → P1** |
| s15 | schema_pipeline | PASS | PASS | RIGHT_ARROW MSO, owners RU-only | PASS | PASS | **PASS** (лучший schema) |
| s20 | schema_matrix (2-col) | PASS | PASS | n/a | PASS | PASS | PASS (leak `(§2.3)(GitClear)`) |
| s24 | schema_matrix (summary) | PASS | PASS (крупная таблица) | header inside | PASS | PASS | PASS (но §-коды в столбце «Точка» → P2) |
| s25 | schema_matrix (methodology) | PASS | PASS | n/a | PASS (gold solid «vibe-coding отвергается» внизу) | PASS | **PASS** |
| s27 | schema_matrix (2-col) | PASS | PASS | n/a | PASS («exhausted bottleneck» gold) | PASS | PASS (leak `[VFY-day-of]` → P0) |
| s29 | schema_matrix (decision 5×4) | PASS (≥75% заполнено) | PASS (~14pt, слова без крипто-сокращений) | axis-метки строк/столбцов внутри, иконка на столбец | PASS (нижняя gold-плашка во всю ширину — «детерминированная → обычный код без AI» доминанта) | PASS (50% — текст ячеек на грани, но читаем; gold-плашка читается уверенно) | **PASS** (пик плотности — выдержан; gold solid vs teal solid различимы — НЕ color-only, есть текст в ячейках) |

**Вывод по schema:** s29 (пик плотности, 5×4) и s25 — спроектированы хорошо, проходят §5.5. s15 — эталонный pipeline. Провалы: **s03** (микро-кегль + смысл не в схеме), **s12** (2-бар sparse chart). s24 — пройден, но §-коды в первом столбце (P2). designer-self-acceptance на s03/s12: геометрия/alignment ок, но concept-level «считывается ли главная мысль из самой схемы за 5 сек» — НЕ выполнено (смысл несёт текст рядом, не визуал) — классический designer blind spot.

---

## 5-секундный тест — провалившие слайды

| Слайд | Проходит? | Комментарий |
|---|---|---|
| s03 | **FAIL** | Главную мысль («A→D = лестница Л3») берёшь из gold-плашки, не из таблицы; таблица — шум §-кодов |
| s04 | **FAIL** | Видишь 5 §-кодов крупно — не понимаешь, что это «5 точек возврата к человеку» без чтения мелких подписей |
| s12 | borderline-FAIL | 2 голых бара ни о чём; смысл «88 vs 64, доверие обратно незнакомости» — только текстом справа |
| s13 | **FAIL** | Chart нечитаем; мысль «скорость↑ качество↓» — только из текст-карточки |
| s17 | borderline-FAIL | Chart с микроосями; «измеряй, не ощущай» считывается из заголовка+gold-плашки, не из графика |
| s24 | PASS (но столбец §) | Таблица крупная, «риск → контроль» читается; §-коды мешают, но не блокируют |
| Остальные 26 | PASS | Сильные assertion-заголовки + gold-акцент; s15/s25/s29/s16/s30 — образцовые |

**Итого 5-сек: 4 жёстких FAIL (s03, s04, s13) + s17, и 2 borderline (s12).** Все привязаны к двум корневым причинам: (а) §/forward-ref leak забивает видимый слой, (б) 3 нечитаемых chart'а.

---

## Cross-deck issues

1. **Палитра/motif — PASS.** Ocean (#21295C/#065A82/#1C7293) + teal #028090 + gold #F0AB00 выдержаны на всех 32. Off-palette цветов = 0. Ocean rounded box (surface #F4F7FA, stroke #1C7293, radius 12) на каждом content-слайде. Gold ≥1× на каждом слайде (проверено визуально: s01 контраст «−20 vs +19», s08 «66%», s16 «95/100», s25 «vibe-coding отвергается», s29 нижняя плашка, и т.д.). Cover s02 — без motif (корректно, эталону lec-03 s02 соответствует: giant outline «04», roadmap-bar 7 карточек, текущая gold). Dividers s10/s14/s18 единым шаблоном, soft-outline цифра.

2. **Иллюстрации/визуальная нагрузка — в целом PASS, 3 исключения.** Каждый content-слайд несёт реальную визуальную работу (icon-карточки, схемы, таблицы, charts, cycle, pipeline). Иконки семантичны и одного семейства (Lucide-стиль, Ocean recolor): {} Structure / щит Constraints / колба Tests на s09; цикл-иконка s11; колба s19; восклицание s16/s21. Исключения — s12/s13/s17, где chart формально присутствует, но не работает как доказательство (см. P1) — это нарушение приёмочного критерия владельца «настоящая визуальная нагрузка» (chart-декорация ≠ нагрузка).

3. **Системный leak видимого слоя (P0+P1) — главная cross-deck проблема.** `[VFY]` на 5 слайдах (P0), §-refs/(sNN)/«Раздел N»/callbacks на 13+ слайдах (P1), «LO4» в source s32 Body (P1). Это не точечные дефекты, а единый паттерн: designer оставил методическую «арматуру» (cross-links из chapter) на видимом слое вместо переноса в speaker_notes. Anti-patterns #36/#37/#38/#39 — все четыре сработали. Эталон lec-03 этого не делает (проверено: lec-03 s02/s10 — чистый видимый слой). Forbidden plan-v2-final §7 прямо запрещает CVE/`[VFY]`/тайминг на видимом слое — частично нарушено.

4. **Forbidden §7 на видимом слое — частично PASS.** «Лектору»/«Вы здесь»/тайминг-минуты — НЕ найдены (0, хорошо). CVE-номера — НЕ на видимом слое (s23 footer: «CVE-номер и точный механизм/хронология — в главе» — корректно вынесено). Vendor-pricing — нет. Код >3 строк — нет (s09 «structure+constraints+tests» — концепты, не код). Subtitle-инициатива — нет. Локальный binding — нет (audience «универсально без локального binding» соблюдён). **Нарушено только: `[VFY]`-маркеры (§7 «→[VFY]/notes», не на слайд) — P0.**

5. **Cross-slide redundancy — низкая, PASS.** METR появляется на s01 (hook, нераскрыт) и s17 (раскрытие) — это запланированная reveal-пара (plan §2.1, GATE-0 Q1), НЕ дубль: s01 даёт 3 числа + «гипотеза, не данные», s17 даёт механизм + протокол измерения.差异化 достаточна. Bar-charts: s06/s12/s13/s17 — разные данные, не дублируют (но 3 из них нечитаемы — отдельная P1). Assertion-заголовки уникальны, парафраз-дублей нет. «Рамка §1.1 для уровня X» повторяется s06/s07/s11/s15 — это запланированный единый паттерн (s05 вводит рамку явно), консистентность, не redundancy.

6. **Нарратив/переходы — сильный, PASS.** Арка A→B→C→D→«не только код»→методологии→фреймворк читается; dividers s10/s14/s18 + narrative_bridge явные; s01 hook ↔ s17 payoff ↔ s24 свод ↔ s29–s31 framework — связная нить. Reveal-пары парные (s01→s17). Курсовая привязка LO1/LO4/LO7 + мост Семинар 4 (s32) на месте. Curriculum relevance: каждый слайд отвечает «зачем в Л4» (первая отраслевая, проекция рамки Л3) — orphan-слайдов нет, concept-jump нет (термины inline-defined: SWE-bench s12, mutation s19, slopsquatting s22, CWE s21, perception-gap s01).

7. **AI-failure/judgment share — PASS (не предмет critic, но подтверждаю визуально).** Failure/ограничение/критерий-плотность на слайдах высокая и распределена по всем 6 разделам (s08/s12/s13/s16/s17/s21/s22/s23/s24/s25/s26/s27/s29/s30) — single-artifact concentration отсутствует. Тон anti-hype, «магической пилюли» нет, «где НЕ AI» явно (s30/s29 нижняя плашка). Это сильная сторона deck'а.

---

## Финальный вердикт

**VERDICT: REVISE**

- **P0: 1** — `[VFY]`/`[VFY-day-of]` на видимом слое (s12, s21, s22, s27, s28). L2-прецедент = P0. Блокирует показ.
- **P1: 6** — (1) s32 «LO4» в source Body; (2) deck-wide §/forward-ref leak 13+ слайдов; (3) s03 schema микро-кегль + смысл не в схеме; (4) s13 GitClear chart нечитаем; (5) s12/s17 sparse/illegible charts; (6) s04 chip-карточки = только §-коды.
- **5-сек провалы: 4 жёстких (s03, s04, s13, s17) + 2 borderline (s12).**

### Топ-5 правок (приоритет для revision-итерации)

1. **[P0] Удалить все `[VFY]`/`[VFY-day-of]` с видимого слоя** (s12/s21/s22/s27/s28). Caveat — человеческим языком; флаг — только в `verify_day_of_items` deck.yaml + speaker_notes. Один греп-проход + 5 правок footer.
2. **[P1] Снять §-refs / (sNN) / «Раздел N» / callbacks с видимого слоя на 13+ слайдах** (s03/s04/s07/s09/s11/s19/s20/s21/s23/s24/s30/s32). Перенести в `speaker_notes`; на слайде — смысловая формулировка без кода. Плюс s32 Body «LO4» → «учебной цели применения».
3. **[P1] s13 GitClear chart** — заменить на 3 крупных стат-числа со стрелками тренда (↑клоны 8,3→12,3 / ↓рефакторинг 24,1→9,5 / ↑churn 5,5→7,9) ИЛИ перестроить QuickChart font ≥16pt. Сейчас chart-декорация, не доказательство.
4. **[P1] s12 / s17 charts** — s12 → пара mega-stat (88,7% / 64,3%) + gold-дельта «−24 п.п.» крупно (2-бар chart не оправдан); s17 → убрать chart или наклонная линия-тренд с 3 крупными точками.
5. **[P1] s03 / s04 укрупнить и очистить** — текст ячеек/карточек ≥14pt, §-коды убрать, смысл крупно; s04 карточки подписать словами, не §-кодами.

После этих 5 правок (одна batched revision-итерация, без пересмотра концепта/арки — они сильные) deck выходит на APPROVE-WITH-POLISH или CLEAN. P2-список (s07 `--`, s06/s08 кегль, s24 столбец §, divider gold-note) — опционально в той же итерации.
