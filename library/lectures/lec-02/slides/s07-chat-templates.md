---
id: s07
type: assertion_visual
section: "Раздел 1. Токенизация"
duration_min: 2.5
assertion: "Роли system/user/assistant — те же токены в общем потоке, а не защищённая структура"
learning_goal: "Chat-шаблоны и специальные токены; слой 1 закрытия M2 (полное закрытие — s20, forward-ref в notes)"
learning_outcomes: [LO1, LO6]
chapter_ref: "§1.3 [for-slide-s07]"
visual_brief: "Слева — структурированный диалог {role, content}; стрелка «chat-шаблон» → плоский поток токенов с выделенными спецтокенами <|im_start|>system … <|im_end|> (моноширинно, спецтокены teal). Справа — карточка риска: внешний контент со строкой, похожей на разметку роли, вливается в тот же поток (подделка — gold-обводка). Caption: вторая половина ответа — в разделе про внимание."
---

# Visible content

## Title bar
«Роли system/user/assistant — те же токены в общем потоке»

## Body
[Слева — схема преобразования]

Структурированный диалог `{role, content}`
↓ **chat-шаблон**
Плоский поток токенов (моноширинно, спецтокены teal):
`<|im_start|>system` Ты помощник… `<|im_end|>`
`<|im_start|>user` Объясни… `<|im_end|>`

[Справа — карточка риска, gold-обводка]
**Подделка:** внешний контент (веб-страница, файл, письмо) со строкой, похожей на разметку роли, вливается в тот же поток токенов — отдельного «защищённого канала» для ролей нет.

[Caption мелким]
*Почему роль при этом работает — и почему подделанная работает так же — вторая половина ответа в разделе про внимание.*

## Speaker notes

Вы знаете, что сообщения в API имеют роли: system, user, assistant. Опытные пользователи часто представляют роли как структурные поля — метаданные, которые модель получает «отдельным каналом». Такого канала не существует: роли — это те же токены в общем линейном потоке.

Прежде чем список сообщений попадает в модель, он прогоняется через chat-шаблон — правило сборки одной плоской последовательности токенов из структурированного диалога. В формате ChatML, ставшем фактическим стандартом, сообщение оборачивается специальными токенами — служебными записями словаря, не встречающимися в обычном тексте. В экосистеме Hugging Face шаблон хранится буквально как Jinja-шаблон в файле настроек токенизатора — его можно открыть и прочитать. Деталь: имя роли — обычная строка после спецтокена, а не отдельный спецтокен; именно поэтому провайдеры добавляли новые роли без перевыпуска словаря. И практический след: локальная модель, запущенная с чужим chat-шаблоном, не выдаёт ошибку — она просто заметно глупеет; проверка шаблона — первый пункт диагностики.

Следствие для всех, кто строит агентов: если роль — последовательность токенов, то текст, похожий на разметку роли, может попасть в поток из внешнего контента. Атаки класса Special Token Injection — подделка роли: в страницу или файл вставляют строку вида «начало реплики assistant», и модель в части случаев верит. Исследования показывают, что «кто говорит» модель определяет в значительной мере стилистически, а не по формальной метке. Защита строится на экранировании и детекции спецтокенов до подачи в модель. Здесь мы закрыли половину вопроса: роль — не защищённая структура. Почему она вообще работает — увидим в разделе про внимание; разбор prompt injection целиком — в Лекции 3.
