---
id: s18
type: process
duration_min: 1.5
assertion: "Агент = чат + оркестратор + внешняя память + инструменты"
learning_goal: "Архитектура агента — что добавляется к чату; decision loop"
learning_outcomes: [LO1, LO4, LO7]
references: [weng-2023-agents, anthropic-2024-effective-agents, anthropic-mcp-2024]
visual:
  pattern: agent_react_pipeline
  primary: "Eyebrow-пилюля «АГЕНТ» сверху. Redesign (issue #153): линейный ReAct-pipeline План→Действие→Наблюдение→Рефлексия (с англ. глоссом Plan/Act/Observe/Reflect мелким шрифтом под каждым блоком) с явной петлёй возврата (вместо hub-and-spoke v3.2). USER слева инициирует цикл; Память и Инструменты подписаны как ресурсы, к которым обращается шаг «Действие», с подписанными коннекторами «использует»; explicit loop-back стрелка «продолжить» (Рефлексия→План). Issue #155 QA fixes #189/#190/#191: USER-круг поднят так, что его вертикальный центр совпадает с центром ряда pipeline-блоков (было заметно ниже); loop-back коннектор у «Рефлексии» перевёрнут на UP_ARROW (поток идёт из Рефлексии вверх в горизонтальную gold-полосу, а не в Рефлексию); стрелка «стоп → результат пользователю» теперь идёт L-образным маршрутом — горизонтальный отрезок ниже pipeline-блоков (не пересекает их текст), затем короткий вертикальный riser вверх, стрелка-наконечник упирается точно в нижний край (поднятого) USER-круга."
---

# Агент = чат + оркестратор + внешняя память + инструменты

## Assertion

Агент = чат + оркестратор + внешняя память + инструменты.

## Visual

Eyebrow-пилюля «АГЕНТ» в левом верхнем углу — единый паттерн с s15/s16/s17/s19/s19a. Redesign (issue #153, замена слабой v3.2 hub-and-spoke схемы): USER-иконка слева отправляет цель в линейный горизонтальный pipeline из 4 стадий — План → Действие → Наблюдение → Рефлексия (мелкий англ. гросс «(Plan)/(Act)/(Observe)/(Reflect)» под каждым названием), каждая стадия — блок с RIGHT_ARROW-коннектором к следующей. Под стадией «Действие» — teal-tinted подпись «Инструменты: API, файлы, code, search» с подписанным коннектором «использует». Под стадией «Наблюдение» — подпись «Память: vector DB, файлы, логи», тоже с коннектором «использует». От «Рефлексии» идёт явная gold loop-back стрелка назад к «Плану», подписанная «продолжить — цикл повторяется» — визуально показывающая цикл. От «Рефлексии» также идёт стрелка «стоп → результат пользователю» — результат возвращается пользователю. Ссылка Yao et al. 2022 (arXiv:2210.03629) внизу.

**Issue #155 Round 2 QA fixes (#189, #190, #191):**
- **#189** — USER-круг был заметно ниже ряда pipeline-блоков (центр circle ~4.10" vs центр pipeline-ряда ~3.225" по вертикали в 7.5"-высоком слайде). Поднят так, что вертикальный центр USER-круга совпадает с центром блоков «План/Действие/Наблюдение/Рефлексия»; подпись «Пользователь» под кругом сдвинулась автоматически (вычисляется от `user_y`).
- **#190** — коннектор loop-back-полосы у блока «Рефлексия» был `DOWN_ARROW` (визуально читался как «полоса подаёт в Рефлексию»), развёрнут в `UP_ARROW` — теперь явно показывает, что поток идёт ИЗ «Рефлексии» вверх в горизонтальную gold-полосу «продолжить — цикл повторяется», которая затем несёт его влево к «Плану».
- **#191** — стрелка «стоп → результат пользователю» раньше заканчивалась в воздухе на уровне старой (нижней) позиции USER. Перестроена L-образным маршрутом: горизонтальный отрезок идёт ниже pipeline-блоков на прежней высоте (не режет текст блоков), затем короткий вертикальный riser (`UP_ARROW`) поднимается точно к нижнему краю нового (поднятого) USER-круга. Подпись сдвинута вправо от riser'а, чтобы не обрезаться.

## Speaker notes

Агент — следующий слой после чата. По сравнению с чатом у агента появляется три дополнительных компонента, которых у чата не было.

Первый компонент — оркестратор. Логика управления: план, decision loop, критика. Оркестратор смотрит на цель пользователя, разбивает её на шаги, выбирает, какой инструмент применить на каждом шаге, и решает, когда остановиться.

Второй — внешняя память. В отличие от чата, у агента есть память за пределами одной сессии: vector database (база данных, оптимизированная для поиска по сходству эмбеддингов; подробнее — позже в курсе), файловая система с промежуточными результатами, лог собственных предыдущих действий.

Третий — инструменты. Возможность вызывать внешние API, читать и писать файлы, выполнять код, делать веб-поиск. Каждый инструмент — это функция с описанием: что делает, какие параметры принимает, что возвращает. Оркестратор выбирает, какой инструмент применить на каждом шаге.

Цикл работы агента, базовая модель ReAct — Reasoning plus Acting. Шаг план: оркестратор и LLM формулируют план действий. Шаг act: выбирается и вызывается инструмент с конкретными параметрами. Шаг observe: результат инструмента наблюдается, записывается в память, передаётся обратно в LLM. Шаг reflect: LLM обдумывает результат — цель достигнута или нет, нужен ли следующий шаг. Если цель достигнута или итераций было слишком много — остановка. Иначе — следующий шаг.

Каноническая формулировка из работы Лилианы Венг: Agent равен LLM плюс Memory плюс Planning плюс Tool Use. Канонические продукты-агенты: Claude Code от Anthropic — агент в терминале для разработки. Devin от Cognition Labs — облачный коддинг-агент. OpenAI Operator — агент для веб-задач. AutoGPT — open-source автономный агент.
