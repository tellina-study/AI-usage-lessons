---
section: 2
title: "Ступень 2: память — три кейса"
stage: 2
parts: 3
duration_min_total: 33.0
duration_min_this_part: 11.75
slot_lec03: "Слот 1 — «Память» (§4.5–4.6); критерий усложнения памяти из §5.2b"
status: rework-draft-v5
supersedes: "section-2-pamyat.md v4 (порядок проблема→исследование→вопрос→разбор, рамка кейса 2.1 реактивная); REWORK-REQUIREMENTS-v5.md §R5-0 (рамка дня 0 для кейса 2.1) + §R5-3 (вопрос до исследования, все кейсы)"
---

# Раздел 2. Ступень 2: память (три кейса, ~32,75 мин)

**Карта раздела и части.** Раздел расколот на 3 файла — каждый кейс достаточно велик сам по
себе (сценарий, вопрос, исследование+разбор, решение, провал, критерий), чтобы уложить все три
в один файл ≤600 строк.

| Файл | Содержимое | Тайминг |
|---|---|---|
| `section-2-pamyat.md` (этот файл) | Обзор раздела + **Кейс 2.1** — плоский файл, заведённый на дне 0 (реестр решений) | 11,75 мин |
| `section-2-pamyat-part2.md` | **Кейс 2.2** — структурированная вики-память | 11,25 мин |
| `section-2-pamyat-part3.md` | **Кейс 2.3** — операционная задачная память + пересчёт таймингов всего раздела | 10 мин |

## Обзор. Почему три кейса и как они связаны

Раздел закрывает первый слот экипировки из Лекции 3 — **память** (§4.5–4.6): то, что агент
помнит **между** сессиями, в отличие от контекста одного разговора. В v3 это был один кейс
(`DECISIONS.md`). В v4 — три кейса, по восходящей сложности. В v5 к этому добавилась вторая ось
(R5-0, согласовано с владельцем): не только «сложнее», но и «когда заводить» —

1. **Кейс 2.1 — плоский файл, заведённый на дне 0.** Реестр решений команды заводится сразу,
   вместе со `spec.md`, README и файлом инструкций — как дешёвая универсальная гигиена, не как
   реакция на боль. Работает, пока влезает в контекст целиком и решений — десятки, не сотни.
2. **Кейс 2.2 — структурированная вики-память.** Тот же реестр решений, но разросшийся настолько,
   что плоский лог перестал быть удобным: нужны темы и перекрёстные ссылки. Это уже не гигиена дня
   0 — это реактивный шаг масштабирования, который нельзя предсказать заранее (зависит от того, как
   конкретно вырастет именно ваш проект).
3. **Кейс 2.3 — операционная (задачная) память.** Другая ось деления: не «что решено навсегда», а
   «что происходит прямо сейчас в одной многошаговой задаче» — план, ход, результат, живущие ровно
   до конца задачи. Практика «файл на задачу» — тоже дешёвая гигиена дня 0, как и в кейсе 2.1.

Кейсы 2.1 и 2.2 — про один и тот же артефакт (реестр решений) на двух стадиях роста, но с разным
происхождением: 2.1 заведён проактивно, 2.2 — реактивный ответ на рост. Кейс 2.3 — про
принципиально другой вид памяти: не долгоживущий, а задачно-ограниченный, тоже заведённый
проактивно. Это стоит явно проговорить в конце раздела как две разные оси, а не три ступени одной
лестницы — план собирает их в одну таблицу в конце `section-2-pamyat-part3.md`.

**Место в харнесе (терминология Лекции 3, для всего раздела).** Все три кейса — один и тот же слот
экипировки, «память» (§4.5), в отличие от файла инструкций (раздел 1) — того, что вы знали
**заранее**. Раздел идёт по двум разным логикам одновременно: кейсы 2.1 и 2.3 — дешёвая гигиена
дня 0, кейс 2.2 — специфичный для проекта рост, триггер которого нельзя предсказать заранее и
нужно дожидаться. Раздел идёт вторым не потому, что вся память реактивна (это больше не так с v5),
а по естественному порядку повествования: сначала фиксируется, что заводится вместе с файлом
инструкций (раздел 1) и день-0-гигиеной памяти (2.1, 2.3), и только потом — что из заведённого
день-0-по-умолчанию перестаёт справляться и требует специфичного для проекта решения (2.2). Этот
разворот уже объяснён в разделе 0 — здесь его не нужно повторять.

---

## Индекс частей раздела 2

Раздел разбит на четыре файла (лимит 600 строк на документ, CLAUDE.md § Document Size Limit).
Читать по порядку:

1. **`section-2-pamyat.md`** — этот файл: обзор раздела + §A «План кейса 2.1».
2. **[`section-2-pamyat-part1b.md`](section-2-pamyat-part1b.md)** — §B «Методическая глава кейса 2.1».
3. **[`section-2-pamyat-part2.md`](section-2-pamyat-part2.md)** — Кейс 2.2 «Структурированная вики-память», §A и §B.
4. **[`section-2-pamyat-part3.md`](section-2-pamyat-part3.md)** — Кейс 2.3 «Операционная память», §A и §B, плюс §D «Пересчёт таймингов раздела 2».

Контракт стиля и повествования, под который переписаны все три кейса, сформулирован в
[`section-0-otkrytie.md`](section-0-otkrytie.md) §B.

---

# Кейс 2.1. Плоский файл, заведённый на дне 0 (реестр решений)

> **Переписан под контракт стиля и повествования** (см. `rework/section-0-otkrytie.md` §B): из
> gold-рамки проблемного слайда снята формулировка «Завести практику — не то же самое, что
> воспользоваться ею в момент, когда это имеет значение» — это половина целевого ответа слайда
> разбора, сказанная до голосования; с дивайдера снята классификация исхода; сценарий приведён к
> сквозному календарю (конец первой недели, а не «третья сессия за две недели»); дата записи в
> `DECISIONS.md` приведена к дню, когда решение записали (2026-09-26), а не ко дню создания
> репозитория; из студенческого текста убраны «обратите внимание», «голосование прошло — теперь
> можно раскрывать карты» и предсказания за зал.

> **Правка v6 (две, по замечаниям владельца).**
> **(1) Механизм загрузки разведён.** Слайд исследования утверждал, что `DECISIONS.md` «грузится в
> контекст целиком, каждую сессию». Это неверно и противоречило собственной таблице слоёв (§A.7,
> строка «Что грузится» — «ничего автоматически»). Автоматически грузятся только корневой и
> родительские `CLAUDE.md`/`AGENTS.md` — задокументированный механизм, разобранный в кейсе 1.3.
> `DECISIONS.md` — обычный файл репозитория: агент читает его по своей инициативе либо по явной
> ссылке. Presence paradox переносится на него в условной, но всё-или-ничего форме: цена возникает
> в каждой сессии, где файл прочитан, и файл читается целиком. Обратная сторона названа честно как
> открытый вопрос: в итоговом `CLAUDE.md` кейса 1.2 (пятнадцать строк) ссылки на `DECISIONS.md`
> нет, то есть гарантированного пути от автозагружаемого слоя к журналу решений сейчас нет.
> **(2) Таблица слоёв переставлена и расширена.** Таблица (§A.7) переезжает на место перед слайдом
> разбора — она контекст для разбора, а не его послесловие, — и получает четвёртую колонку
> «сложная система памяти» (внешний сервис, векторная или графовая база). Четвёртая колонка мягко
> предвещает кейс 2.2 («сложнее» не равно «лучше»), не называя его целевого ответа; опора —
> материал контрпримера из `section-2-pamyat-part2.md`, без переноса цифр на этот слайд.

## A. План кейса 2.1

### A.1. Состояние репозитория на входе

`DECISIONS.md` — как `spec.md`, README и файл(ы) инструкций (раздел 1) — заведён с первого дня
работы над `signup-landing`, по общему правилу дня 0 (R5-0): то, что дёшево завести сразу,
заводится сразу, не дожидаясь боли. На входе в этот кейс файл в репозитории **уже существует** —
пустой append-only лог с одной строкой формата («Append-only log of why, in date order.») и без
единой содержательной записи: решения по проекту уже принимались, но фиксировать их в файл пока
не сложилось привычки. К концу этого кейса в файле появится первая реальная запись.

`[TODO: имена captures используют старую сквозную нумерацию «stage 1…7» из v3 (7 ступеней); после
слияния разделов 1 и 2 в 6 кейсов эту нумерацию, вероятно, придётся пересчитать — решение
оркестратора при сборке, не этого файла.]`

**Захваченная история репозитория этому соответствует буквально** (сверка 2026-09-24, публичный
репозиторий `tellina-study/signup-landing-demo`, ветка `seminar-4-arc`). Пустой журнал решений
заведён в самом первом коммите дня 0 (`10699a2`), рядом с `CLAUDE.md`/`AGENTS.md`; первая
содержательная запись приходит отдельным поздним коммитом (`94c5378`) — ровно та пересъёмка, о
которой раньше стоял открытый вопрос. Дерево на итоге кейса 1.2 (`21-tree-stage1.txt`) уже
показывает `DECISIONS.md` в списке файлов, то есть рассказ «файл существует с первого дня»
проверяется командой, а не принимается на слово.

### A.2. Проблема — конкретная, на материале `signup-landing`

> Конец первой недели работы над `signup-landing`. Разработчик просит агента доработать валидацию
> формы: заказчик прислал заявку с опечаткой в домене почты, и форма её приняла. Агент читает код и
> предлагает подключить стороннюю библиотеку валидации форм — ту самую, которую **обсуждали и
> отклонили несколько дней назад**: форма из двух полей, хватает нативных `required`/`pattern` и
> двадцати строк собственного кода, лишняя зависимость и лишние килобайты в сборке того не стоят.
>
> Агент предлагает её так, будто вопрос никогда не поднимался. Потому что для него он и не
> поднимался: разговор, в котором решение было принято, закончился, а его содержимое нигде не
> осталось.

**Диагностика — и это ровно то место, где рамка кейса меняется относительно интуитивного «забыли
завести файл».** Файл уже есть. `DECISIONS.md` лежит в репозитории с первого дня, точно так же,
как `spec.md` и README — команда завела его проактивно: раз это дёшево, заводим сразу, не ждём
боли. И тем не менее нужной записи в нём нет. Потому что решение о валидации родилось не за
столом, где кто-то открыл `DECISIONS.md`, а на бегу, в потоке рабочего разговора — а между «мы это
обсудили и решили» и «кто-то реально дописал строку в файл» есть шаг, который не делается сам
собой.

> **Что убрано со слайда-проблемы и почему.** Строка «Завести практику — не то же самое, что
> воспользоваться ею в момент, когда это имеет значение» — это половина целевого ответа разбора
> (§A.4: дело не в создании файла, а в дисциплине записи), произнесённая до голосования. На слайде
> вместо неё — состояние: файл заведён на дне 0, записи об этом решении в нём нет, модель между
> вызовами не хранит ни бита. Вывод собирается после голосования. В этом методическом файле
> формулировка остаётся — она верна, просто её место не на проблемном слайде.

Это честнее и жёстче старой версии кейса: не «у нас не было инфраструктуры» (уязвимая формулировка
— зал спросит «а почему не завели раньше»), а «у нас была инфраструктура с первого дня, и её всё
равно не хватило» — потому что зафиксировать решение в момент принятия требует отдельной
дисциплины, а не только наличия файла, в который можно писать.

Почему этого не мог закрыть раздел 1. Файл инструкций — то, что вы знали **заранее** и
сформулировали сами, до всякой работы. Решение «не подключаем библиотеку валидации» заранее не
существовало: оно появилось как вывод из конкретного обсуждения на той же первой неделе. Место, куда его
можно было записать, уже существовало (`DECISIONS.md`, заведён на дне 0) — но то, что место есть,
не значит, что им воспользовались в момент, когда решение родилось.

### A.3. Открытый вопрос классу (дословно) + варианты

> «Вы второй раз за эту неделю объясняете агенту, почему в этой форме нет сторонней библиотеки
> валидации — то самое решение, которое уже отклонили. **Выберите, куда записать это решение — из
> карточек ниже — так, чтобы не объяснять его в третий раз, и обоснуйте, почему не в тот файл
> инструкций, который мы только что завели.**»

**Карточки-варианты на слайде (пять):**

`дописать в CLAUDE.md` · `сказать агенту «запомни это»` · `записать в DECISIONS.md — он уже есть` ·
`поставить систему памяти` · `ничего — сам запомнит`

*(Слайд без единой цифры и без ссылки на исследование — грунтовка и разбор идут только после
голосования, §A.4.)*

### A.4. Исследование и разбор — presence paradox, применённый к памяти

Прежде чем разбирать сами варианты, полезно
связать задачу с тем, что занятие уже устанавливало в кейсе 1.1.

**Presence paradox, перенесённый на память.** Специального нового измерения не потребовалось — это
прямой перенос уже установленного в занятии механизма. В кейсе 1.1 разобран контролируемый
эксперимент Gloaguen et al. (arXiv:2602.11988): сам факт присутствия текста в контексте создаёт
стоимость (+20–23% cost), независимо от того, помогает ли он решить задачу. Занятие называет это
presence paradox (термин определён на слайде исследования кейса 1.1).

**Сначала — механизм загрузки, и его нельзя смешивать.** Автоматически грузится файл инструкций:
по официальной документации Claude Code корневой `CLAUDE.md`/`AGENTS.md` и все родительские
попадают в контекст при старте сессии сами (разобрано в кейсе 1.3, вместе с on-demand-поведением
вложенных файлов). `DECISIONS.md` под это правило **не попадает**: это обычный файл репозитория,
такой же, как `src/main.js`, — ни один инструмент не подаёт его в контекст в начале сессии. Агент
открывает его, если сам решил открыть, либо если на него явно сослались: в запросе или
строкой-указателем из файла инструкций.

Перенос на `DECISIONS.md` поэтому **условный, но всё-или-ничего**: цена возникает не в каждой
сессии, а в каждой, где файл прочитан, — и прочитан он целиком, по одной релевантной строке файл
не читают. Тридцать записей о деплое, валидации и UI-мелочах оплачиваются полностью в тот момент,
когда нужна одна из них. Десять записей — цена небольшая; тридцать — уже заметная. Механика
загрузки всех слоёв сведена в таблицу §A.7, которая показывается **до** разбора карточек.

**Честная оговорка.** Прямого измерения presence paradox именно на append-only логах решений не
проводилось — это перенос механизма на новый объект, а не отдельное новое исследование. Так это и
подаётся залу: не как новое доказательство, а как применение уже знакомого.

**Обратная сторона той же механики — называется здесь, а не замалчивается.** Запись, которую агент
не открыл, не стоит ничего и не даёт ничего. В итоговом `CLAUDE.md` кейса 1.2 (пятнадцать строк,
слайд решения кейса 1.2) ссылки на `DECISIONS.md` нет — то есть гарантированного пути от
автозагружаемого слоя к журналу решений в нашем репозитории сейчас не существует. Дыра закрывается
одной строкой-указателем в файле инструкций (именно указателем: ссылкой на файл, а не содержимым
решения — иначе это ровно тот вариант «допишу в `CLAUDE.md`», который разбирается ниже как
смешение слоёв). Окупается ли эта строка своей ценой присутствия, здесь не измерено — на слайде
она подаётся как открытый вопрос, а не как предписание.

Эта грунтовка держит в голове зала неявный вопрос: если запись не бесплатна, точно ли туда идёт
вообще всё, что можно туда положить? (Отвечает на это корзина «никуда» в ходе 1 co-building'а,
§A.5.)

**Разбор пяти карточек зала — на фоне этой грунтовки:**

| Вариант | Где это работает | Почему здесь ещё рано — и что вместо |
|---|---|---|
| «Допишу в `CLAUDE.md`» | Механически сработает: файл читается каждую сессию | Смешивает два разных по природе слоя — стабильные правила, написанные заранее, и растущий лог решений. На десятке решений файл выйдет за ориентир в 200 строк и начнёт работать против себя, как в разделе 1. Вместо: развести слои по авторству |
| «Ничего, агент сам запомнит» | Нигде: это не настройка, которую забыли включить | Модель не имеет состояния между вызовами **по конструкции**: без записи на диск ничего не переживает конец сессии. Вместо: решить, кто и куда пишет — вы или он |
| «Скажу агенту "запомни это"» | Для личного предпочтения — идеально | **Половина ответа.** Уходит в авто-память: машинно-локальный слой, который не синхронизируется между машинами и не проходит код-ревью. Для командного решения этого мало. Вместо/вдобавок: то, что важно команде, — в репозиторий |
| «Запишу в `DECISIONS.md` — он уже есть» | Для командного решения с обоснованием — именно то, что нужно | **Вторая половина ответа.** Файл уже заведён (§A.1) — дело не в создании, а в дисциплине: писать «почему», а не «что» (что уже есть в истории изменений), и заметить в моменте, что решение достойно записи. Плюс открытый край: файл не грузится сам, агент откроет его по своей инициативе или по явной ссылке |
| «Поставлю систему памяти (векторную/графовую)» | На десятках-сотнях записей, нескольких пользователях, при потребности в поиске по смыслу | Рано на порядок: живые замеры показывают, что на малых корпусах простой файл регулярно конкурентен со сложными системами, а иногда обгоняет их (§A.8). Вместо: плоский файл, пока история влезает в контекст целиком |

Целевой ответ — **оба механизма разом, с разделением ролей**, и именно это разделение собирается
на следующем шаге.

### A.5. Совместное формирование: сортировка по трём корзинам, затем сборка записи

**Ход 1. Три корзины.** На слайде — три пустые корзины: `авто-память` · `DECISIONS.md` ·
`никуда`. И пять карточек-фактов, все с нашего же проекта. Фасилитатор берёт карточку и
спрашивает зал: «в какую?»

| Карточка | Корзина | Почему |
|---|---|---|
| «Не подключаем стороннюю библиотеку валидации — форма из двух полей» | `DECISIONS.md` | Командное решение с обоснованием, должно быть видно всем и переживать смену машины |
| «Разработчик просит отвечать коротко, без преамбул» | `авто-память` | Личное предпочтение, не решение по проекту; никому в команде не нужно, машинно-локальное — то, что надо |
| «Обработчик формы лежит в `src/main.js`» | `никуда` | Выводимо из кода за секунду. Запись такого факта — чистый расход контекста |
| «24 сентября тест упал из-за таймаута, увеличили ожидание» | `никуда` (или журнал задачи — см. Кейс 2.3) | Это факт, а не решение; он уже есть в истории изменений. В `DECISIONS.md` идёт «почему», а не «что» |
| «Своего бэкенда не делаем — заявка уходит на внешний сервис приёма форм» | `DECISIONS.md` | Решение, неотличимое в коде от недоделки. Именно то, что раздел 0 отметил как невидимое |

По корзине «никуда» один раз проговаривается механика: запись, выводимая из кода, не бесплатна —
индекс памяти грузится в каждую сессию, у него жёсткий лимит, и место в нём конечное.

**Ход 2. Собираем запись.** Формат — дата, решение одной строкой, один-три предложения «почему».
Фасилитатор спрашивает по частям: «дата?», «формулировка решения одной строкой?», «причина?» — и
проявляет на слайде запись, которая реально лежит в демо-репозитории.

### A.6. Конкретное решение: что добавляется в репозиторий

Механизм остаётся тем же, что и в предыдущих версиях кейса, — просто один из двух компонентов уже
был на месте с первого дня, а не создаётся здесь впервые.

1. **Авто-память рантайма** — живёт **вне репозитория**, в каталоге памяти проекта на машине
   разработчика. Устройство: индекс `MEMORY.md` (в каждую сессию грузятся первые 200 строк или
   25 КБ — что наступит раньше) плюс тематические файлы, которые агент создаёт сам и читает по
   требованию. Типы записей: предпочтения пользователя, обратная связь и подтверждённые подходы,
   текущая работа проекта, указатели на внешние источники. Команда «запомни: не подключаем
   стороннюю библиотеку валидации, форма из двух полей, хватает нативного HTML5» создаёт
   тематический файл именно в этом слое — этот файл действительно новый, его не было до этого
   кейса.
2. **`DECISIONS.md`** — git-трекнутый append-only лог «почему», заведённый ещё на дне 0 (§A.1).
   Новое здесь — не файл, а первая содержательная запись в нём. Реальный захваченный файл
   (`assets/captures/33-code-DECISIONS.md.txt`):

```markdown
# Decisions

Append-only log of why, in date order.

## 2026-09-26 — не подключаем библиотеку валидации форм
Форма — два поля (имя, email), хватает нативных `required`/`pattern` в `index.html`.
Сторонняя библиотека — лишняя зависимость и лишние килобайты в сборке ради этого объёма.
```

> **Дата записи — сквозной календарь.** Запись датируется днём, когда её реально написали (конец
> первой недели, 2026-09-26), а не днём создания репозитория (2026-09-20). Прежняя дата
> противоречила самому сценарию кейса: решение обсуждали позже дня 0, и весь смысл развилки в том,
> что до сегодняшнего дня оно на диск не попадало.

Дерево после кейса 2.1 — `assets/captures/22-tree-stage2.txt`. На слайде показывается **содержимое
файла**, а не диафф его создания (см. §A.11 — почему диафф-capture сейчас не используется на
слайде).

### A.7. Описание решения: как это работает механически (четыре слоя)

**Место слайда изменилось (правка v6):** таблица показывается **перед** разбором карточек, а не
после решения — она контекст, на который разбор опирается, а не его послесловие. Различает слои не
формат (первые три — markdown), а три вещи: кто инициирует запись, кто её потом увидит и по какому
механизму она вообще попадает в контекст.

| | Файл инструкций (раздел 1) | Авто-память | `DECISIONS.md` | Сложная система памяти |
|---|---|---|---|---|
| Кто автор | вы, заранее | агент, по ходу | вы, в момент решения | сервис: извлекает сам из хода работы |
| Где живёт | в репозитории, git | вне репозитория, на этой машине | в репозитории, git | вне репозитория, в отдельной базе или сервисе |
| Что грузится | целиком, автоматически, каждую сессию | индекс: первые 200 строк / 25 КБ | ничего автоматически — обычный файл, агент читает по своей инициативе или по явной ссылке | ничего автоматически — сервис сам подбирает, что подать по запросу |
| Проходит код-ревью | да | нет | да | нет: в репозитории ничего не лежит, показывать на ревью нечего |
| Переживает смену машины | да | нет | да | да, если сервис доступен с обеих |
| Что туда идёт | стабильные правила и границы | предпочтения, подтверждённые подходы, контекст вне кода | решения и их «почему» | то, что сервис счёл достойным записи |

**Четвёртая колонка — что она делает в кейсе 2.1 и чего не делает.** Она закрывает карточку
«поставлю систему памяти», которая на голосовании звучит всегда и до сих пор разбиралась только
словами. Логика колонок продолжается ровно та же: у внешнего сервиса автор записи — не человек,
живёт она вне репозитория, в контекст попадает по retrieval-запросу, ревью не проходит вовсе (в
git нет ни файла, ни диффа), смену машины переживает — это её единственное преимущество над
авто-памятью. Чего колонка **не** делает: не приводит цифр и не называет целевого ответа кейса
2.2. На слайде она несёт одну оценочную строку — «выигрыш не автоматический: на малых корпусах
простой файл регулярно её обходит», — опирающуюся на уже отобранный материал контрпримера
(`section-2-pamyat-part2.md`, бенчмарк Letta на LoCoMo); сами числа остаются там, где они уже
стоят: на слайде провала кейса 2.1 (§A.8) и в исследовании кейса 2.2.

Четыре механических детали, которые стоит назвать вслух:

- **Индекс памяти — только индекс.** Одна строка на запись со ссылкой на тематический файл. Это
  не стилистический вкус, а следствие жёсткого лимита: за 200 строк содержимое физически не
  попадает в сессию.
- **Агент сам пропускает** то, что выводимо из кода, — архитектуру, пути файлов, найденные
  исправления. Если он всё-таки это пишет, это сигнал раздутия, а не полезности.
- **Сжатие контекста** (`/compact`) переживают только файлы на диске. То, что было сказано лишь в
  разговоре, при сжатии может исчезнуть — и это ещё одна причина, почему «объясню ещё раз в чате»
  не работает даже внутри одной длинной сессии. (Эта же деталь — прямая предпосылка Кейса 2.3.)
- **Усложнение слоя само по себе результата не улучшает.** Переход к внешней системе памяти меняет
  механизм подачи в контекст, но не гарантирует, что подано будет лучше, — на малых корпусах
  простой файл регулярно обходит такие системы (замеры — §A.8).

### A.8. Провал / ограничение

**Слой 1 — отравление памяти: однажды попавшее обслуживается как доверенный факт бессрочно.**

Задокументированный кейс — **SpAIware** (Johann Rehberger, сентябрь 2024, настольное приложение
ChatGPT под macOS). Механика: агент прочитал внешний текст, в который была вложена инструкция; эта
инструкция попала в **долговременную память**, пережила конец разговора и активировалась в
последующих сессиях, включая непрерывную эксфильтрацию переписки на сервер атакующего. Вектор
эксфильтрации производитель закрыл в версии 1.2024.247 (сентябрь 2024) — но структурный принцип
остался, и он не про конкретный продукт: **память по умолчанию читается как доверенный контекст, а
не как данные.**

Масштаб того же класса, для базы сравнения: отравление процедурной памяти замаскированными под
безобидные файлы шаблонами (MemoryGraft, 2025) захватило **около 48%** всех последующих обращений
к памяти — при доле до отравления, равной нулю, — и держалось до ручной чистки; в смежном
эксперименте по отравлению поискового хранилища оказалось достаточно **пяти** подложенных
документов, чтобы управлять выводом системы более чем в 90% случаев.

Практический вывод для нашего файла: всё, что попадает в память **из внешнего, непроверенного
текста**, должно быть помечено как данные, а не как инструкция. Записываете вы — фиксируйте своё
решение. Записывает агент по итогам чтения чужого issue или страницы — это уже другой уровень
доверия.

**Слой 2 — «память есть» не равно «память помогает».**

Здесь важно не продать память как улучшение по умолчанию, и цифры это позволяют.

- Независимый прогон одной популярной системы памяти на бенчмарке долговременной памяти: **с
  включённой памятью — 46,0%, с выключенной — 57,6%** на том же наборе задач. База сравнения —
  та же самая система без памяти. Включение памяти сделало результат **хуже** на 11,6 пункта.
- В живом сравнении шести систем памяти против тривиального варианта «положить всё содержимое
  сессии в один файл» часть систем проиграла этому плоскому файлу **на 25–67 пунктов**, а одна —
  проиграла даже голой модели вообще без памяти. Честная оговорка: корпуса в этом сравнении
  маленькие, целиком помещаются в контекстное окно — то есть условия структурно выгодны плоскому
  файлу. Поведение на больших корпусах не протестировано ни для одной из систем. Но именно малый
  корпус — это ровно наш случай сегодня.
- Дискреционность записи: в живом прогоне инструмента памяти на 24 задачах модель **на 4 задачах
  (17%)** явно отказалась записывать нужный факт, посчитав его слишком мимолётным или не
  относящимся к делу, а один и тот же диалог, поданный дважды, один раз факт сохранил, другой раз
  — нет. Это не баг: модель по устройству сама решает, что достойно записи. Для инженера это
  означает, что полнота памяти не гарантирована, и если факт критичен — его надо записать руками,
  а не надеяться на суждение модели.

### A.9. Критерий «здесь ещё рано» — и граница к Кейсу 2.2

Чек-лист: если верно большинство пунктов — сложная система памяти не нужна, плоского файла
достаточно.

- Задача одноразовая, второй сессии по этому контексту не будет.
- Весь нужный контекст помещается в одно контекстное окно без потерь.
- Правила проекта стабильны и укладываются в файл инструкций короче 200 строк — раздел 1
  справляется один.
- Никто не повторяет вручную одну и ту же правку или уточнение из сессии в сессию.
- Нет нескольких участников, которым нужно делиться контекстом асинхронно.
- Факт выводим из кода или истории изменений.

**Граница к следующему кейсу — честно называем её здесь, а не молчим.** Плоский `DECISIONS.md`
перестаёт справляться не мгновенно и не из-за размера файла самого по себе, а когда одновременно
верны два условия: записей набралось настолько много, что найти нужную линейным чтением дольше,
чем вспомнить и переобъяснить решение заново — и между записями появились связи, которые в
плоском append-only логе физически некуда положить (решение №7 отменяет часть решения №2, но
рядом с №2 об этом ни слова). Это ровно ситуация Кейса 2.2 — переходим к ней сразу за этим
слайдом.

### A.10. Слайды кейса 2.1

**Fix R5-3 (вопрос до исследования, весь раунд v5).** До этого фикса кейс 2.1 шёл в порядке
проблема → исследование → вопрос → разбор — зал видел цифры presence paradox ДО голосования, что
фактически подсказывало ответ. Новый порядок: проблема → **вопрос** (без единой цифры) →
[голосование] → **исследование + разбор**, слитые в один смысловой заход. Технически это
достигается перестановкой содержимого слайдов 2.1.2 и 2.1.3 (было: 2.1.2=исследование,
2.1.3=вопрос; стало: 2.1.2=вопрос, 2.1.3=исследование) и явным слиянием исследования с разбором в
2.1.3/2.1.3b.

**Fix R5-0 (день 0, только кейс 2.1).** Проблема (2.1.1) переписана: `DECISIONS.md` заведён
проактивно на дне 0, боль — не в отсутствии файла, а в том, что дисциплина фиксации решения в
моменте не сработала. Решение (2.1.5) переписано: новое — не файл, а первая запись в нём.

**ПЕРЕСТАВЛЕНО (правка v6, замечание владельца):** было 2.1.3 (исследование) → **2.1.3b (разбор)**
→ 2.1.4 → 2.1.5 → **2.1.6 (таблица трёх слоёв)** → 2.1.7; стало 2.1.3 (исследование) →
**2.1.6 (таблица, теперь четыре колонки)** → **2.1.3b (разбор)** → 2.1.4 → 2.1.5 → 2.1.7.
В терминах текущих имён файлов слайдов: было `s27 → s28 → s29 → s30 → s31 → s32`, стало
**`s27 → s31 (4 колонки) → s28 → s29 → s30 → s32`**. Логические номера 2.1.N ниже оставлены
неизменными как стабильные идентификаторы содержимого — физическая перенумерация
`deck.yaml` и имён файлов `slides/*.md` делается оркестратором одним проходом после всех
параллельных правок раздела, не в этом файле.

| Порядок | № | Текущий файл | Слайд | Содержимое | Материал |
|---|---|---|---|---|---|
| 1 | 2.1.0 | `s24` | Разделитель раздела 2 + тег кейса | «Раздел 2 · память» → «Кейс 2.1 · плоский файл»; тег темы «журнал решений проекта», без классификации исхода | — |
| 2 | 2.1.1 | `s25` | **[СЛАЙД-ПРОБЛЕМА]** Сценарий + состояние репозитория | отклонённое решение всплывает снова; `DECISIONS.md` заведён на дне 0, записи об этом решении в нём нет. БЕЗ вопроса, БЕЗ карточек, БЕЗ формулировки вывода | — |
| 3 | 2.1.2 | `s26` | **[СЛАЙД-ВОПРОС]** Открытый вопрос + пять карточек-вариантов (Fix R5-3) | БЕЗ таблицы разбора, БЕЗ цифр, БЕЗ подсветки целевого ответа | — |
| 4 | 2.1.3 | `s27` | **[СЛАЙД-ИССЛЕДОВАНИЕ]** Два механизма загрузки + presence paradox в условной форме (правка v6) | §A.4: «грузится само» (файл инструкций, кейс 1.3) против «не грузится само» (`DECISIONS.md` — обычный файл); честная оговорка про перенос механизма; открытый вопрос про отсутствие ссылки в итоговом `CLAUDE.md` кейса 1.2 | — |
| 5 | **2.1.6** | **`s31`** | **[ПЕРЕСТАВЛЕН СЮДА]** Таблица **четырёх** слоёв: файл инструкций / авто-память / `DECISIONS.md` / сложная система памяти (правка v6) | §A.7: кто автор, где живёт, что грузится, проходит ли ревью, переживает ли смену машины, что туда идёт; четвёртая колонка без цифр и без целевого ответа кейса 2.2 | — |
| 6 | 2.1.3b | `s28` | **[СЛАЙД-РАЗБОР]** Таблица разбора пяти позиций + целевой ответ («оба механизма разом») | мелким шрифтом вопрос сверху для контекста; таблица §A.4 целиком, опирается на только что показанные слои и их не переобъясняет | — |
| 7 | 2.1.4 | `s29` | Co-building: три пустые корзины + пять карточек-фактов → заполненные корзины | сортировка вживую, с подписью «почему "никуда" — тоже ответ» | — |
| 8 | 2.1.5 | `s30` | Собранная запись `DECISIONS.md` + дерево кейса 2.1 | реальный файл и реальное дерево; показывается содержимое, не диафф создания (Fix R5-0) | `33-code-DECISIONS.md.txt`, `22-tree-stage2.txt` |
| 9 | 2.1.7 | `s32` | Провал: отравление памяти + «память не обязательно помогает» | SpAIware, 48%, 5 документов; 46,0% против 57,6%; 17% отказов записи | — |
| 10 | 2.1.8 | `s33` | Критерий «ещё рано» + граница к Кейсу 2.2 | чек-лист §A.9 + явная формулировка «когда плоский файл перестаёт справляться» | — |

**Почему таблица слоёв ушла вперёд.** Разбор карточек (2.1.3b) целиком построен на различении
слоёв: «допишу в `CLAUDE.md`» отвергается как смешение слоя правил со слоем лога, «скажу "запомни
это"» — как уход в машинно-локальный слой, «поставлю систему памяти» — как переход на слой, который
здесь ещё рано. Пока таблица шла после решения, разбор вводил все эти различения устно и
задним числом, а таблица оказывалась их повторением. В новом порядке разбор ссылается на уже
показанные колонки — и четвёртая карточка голосования («поставлю систему памяти») впервые получает
на слайде тот же разрез, что и остальные три.

### A.11. Материалы: что захвачено, что ждёт живой сессии

- **Захвачено реально:** `33-code-DECISIONS.md.txt` (итоговый файл, снят с `94c5378`),
  `21-tree-stage1.txt` / `22-tree-stage2.txt` (деревья до/после кейса 2.1, сняты с `1019546` и
  `94c5378`).
- **Прежнее расхождение с рамкой дня 0 (Fix R5-0) закрыто.** Раньше здесь числился отдельный
  захват диффа создания, где `DECISIONS.md` появлялся «new file mode», то есть файл и первая
  запись одним коммитом на этой ступени: **реактивное** появление, а не день-0. В пересобранном
  публичном репозитории этого расхождения больше нет: пустой журнал заведён в день-0-коммите
  `10699a2`, новой на этой ступени является только запись (`94c5378`).
  Захват диффа удалён вместе с панелью, которая его показывала; на слайде 2.1.5 остаётся только
  содержимое файла (`33-code-DECISIONS.md.txt`), а «файл существует с первого дня» проверяется
  деревом `21-tree-stage1.txt` либо командой `git ls-tree -r --name-only 10699a2`. Опись захватов
  и опорные коммиты дуги — `assets/captures/README.md`.
- **Ждёт реальной сессии владельца** — позиции №5 и №6 в `assets/captures/TODO-capture.md`: вывод
  `/memory` со списком файлов памяти (включая созданный тематический файл про валидацию форм) и
  вывод `/context` в новой сессии, где видно, что индекс памяти реально загрузился.
  **Важно:** попытка захватить эту сцену из другой сессии рабочего пространства не удалась и не
  должна повторяться — агент обоснованно отказался записывать в постоянную память инструкцию,
  пришедшую не от настоящего собеседника. Это корректное поведение, а не препятствие, которое надо
  обходить. Кадр может снять только владелец в собственной живой сессии.
- **Имитацию терминала не рисовать.** Пока кадров нет, слайды 2.1.5–2.1.6 (решение, таблица трёх
  слоёв) держатся на реальном `DECISIONS.md` и на таблице трёх слоёв — авто-память описывается
  словами, без поддельного вывода `/memory`.

### A.12. Тайминг кейса 2.1 (пересчитан после Fix R5-3 + Fix R5-0 + правки v6)

| Шаг | Мин |
|---|---|
| 2.1.0 — тег кейса | 0,5 |
| 2.1.1 — проблема: сценарий-боль + диагностика «файл есть, дисциплины не было» (Fix R5-0) | 0,5 |
| 2.1.2 — открытый вопрос + карточки (Fix R5-3: переставлен раньше исследования) | 1,0 |
| 2.1.3 — **исследование:** два механизма загрузки + presence paradox в условной форме (правка v6) | 1,0 |
| **2.1.6 — таблица четырёх слоёв (переставлена сюда, правка v6)** | **1,0** |
| 2.1.3b — **разбор** пяти карточек + целевой ответ, сразу за таблицей | 1,25 |
| 2.1.4 — co-building: три корзины + сборка записи | 1,75 |
| 2.1.5 — решение: первая запись в уже заведённом файле + дерево | 0,75 |
| 2.1.7 — **провал:** отравление памяти + «память не обязательно помогает» | 3 |
| 2.1.8 — критерий «ещё рано» + граница к Кейсу 2.2 | 1,0 |
| **Итого кейс 2.1** | **11,75** |

До правки v6 сумма держалась на 11,5 с раунда v4: Fix R5-3 переставил и слил слайды
2.1.2/2.1.3/2.1.3b без изменения суммарного времени, Fix R5-0 менял содержание диагностики (2.1.1)
и решения (2.1.5), но не их длительность.

**Правка v6 добавляет +0,25 мин (11,5 → 11,75), и это честнее, чем подогнать сумму.** Разложение:
слитый заход 2.1.3+2.1.3b (2,25) разделён перестановкой таблицы на 1,0 (исследование) + 1,25
(разбор) — сумма та же; блок 2.1.5–2.1.6 (1,5) распался на 0,75 (решение, дерево) + 1,0 (таблица,
переехавшая вперёд), то есть таблица подорожала на 0,25 — в ней стало четыре колонки вместо трёх,
и четвёртая проговаривается вслух впервые. Если 0,25 мин нужно вернуть, кандидат на урезание уже
назван ниже и не меняется: ход 2 co-building'а (сборка записи `DECISIONS.md`) показывается готовым.
Решение о компенсации — за оркестратором при сведении тайминга всего раздела.

> **Флаг оркестратору (не правка этого файла).** Сводный пересчёт тайминга раздела 2 живёт в §D
> `section-2-pamyat-part3.md` — файле кейса 2.3, над которым работает другая сессия. Там сумма
> раздела указана по старому значению кейса 2.1 (11,5); после правки v6 её нужно поднять на +0,25.
> Здесь это только помечено, чтобы не править чужой кейс параллельно.

**Неприкосновенное:** оба слоя провала и явная граница к Кейсу 2.2. **Первый кандидат на
урезание при нехватке времени в аудитории:** ход 2 co-building (сборка записи `DECISIONS.md`) —
формат простой, можно показать готовым, сохранив сортировку по корзинам как основной co-building.

---

Методическая глава кейса 2.1 (§B) — в
[`section-2-pamyat-part1b.md`](section-2-pamyat-part1b.md); см. индекс частей в начале файла.
