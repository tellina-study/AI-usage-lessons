# Known Limitations

- Single maintainer — no concurrent editing workflows needed
- No real-time sync — manual trigger via skills
- Oxigraph is in-memory by default — data persists only if configured with file backend
- knowledge-rag index must be rebuilt if export format changes
- draw.io MCP export quality depends on diagram complexity
