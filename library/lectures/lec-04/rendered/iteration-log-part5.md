# Iteration Log — Лекция 4, Part 5

Продолжение `iteration-log-part4.md` (лимит 600 строк на документ).
Здесь — финальный сборочный проход раунда 6 (issue #162): слияние пяти
веток блоков в одну колоду и сквозная проверка всех 58 слайдов.

<!-- Навигация по журналу итераций Лекции 4 -->
| Файл | Что внутри |
|---|---|
| `iteration-log.md` (part 1) | v4-переспина (40 слайдов), окружение/тулчейн, раунды 1–4 |
| `iteration-log-part2.md` (part 2) | Round-5 (мемы) · **Round-6 блок 1** (контекст/инструкции) |
| `iteration-log-part3.md` (part 3) | **Round-6 блок 2** (инструментарий) · **блок 3** (тестирование) |
| `iteration-log-part4.md` (part 4) | **Round-6 блок 4** (ревью + доставка) · **блок 5** (закрытие) |
| `iteration-log-part5.md` (part 5) | **Round-6 сборочный проход** — слияние пяти веток + сквозная проверка колоды |

Файлы разбиты по лимиту 600 строк на документ (CLAUDE.md § Document Size
Limit). Хронология сквозная: part 1 → part 2 → … → part 5.


## Round-6 — сборочный проход: слияние пяти веток + сквозная проверка (2026-09-23)

Ветка `hc/lesson4-de299d-0fac50ac`, база — коммит `02c70b4` (57 слайдов).
Пять блоков round-6 правились параллельно в изолированных worktree; здесь они
сведены в одну колоду, пересчитаны счётчики и прогнана проверка всего дека
(а не только 21 затронутого слайда).

### Порядок слияния и контрольный счёт после каждого шага

Сливали по одной ветке, с полной пересборкой из исходников и smoke-тестом
после каждой (бинарники `.pptx`/`.pdf` НИКОГДА не мержились как файлы —
всегда перегенерировались из слитых `.py`/`.md`/`.yaml`):

| Шаг | Ветка | Конфликты | Слайдов после |
|---|---|---|---|
| 1 | `…block1-context` | нет (fast merge) | 58 |
| 2 | `…block2-tooling` | `iteration-log-part2.md`, `.pptx`, `.pdf` | 58 |
| 3 | `…block3-testing` | + `deck.yaml`, `deck-part2.yaml`, `build_lec04_v4.py` | 57 |
| 4 | `…block4-review-delivery` | `iteration-log-part2.md`, `.pptx`, `.pdf` | 57 |
| 5 | `…block5-closing` | `deck.yaml`, `iteration-log-part2.md`, `.pptx`, `.pdf` | **58** |

### Разрешение конфликтов

- **`slides_band2.py` (блоки 1+2), `slides_band3.py` (блоки 3+4),
  `slides_band4.py` (блоки 4+5), `slides_band1.py`, `_helpers.py`** — git
  смержил построчно без конфликтов. Это не принималось на веру: после каждого
  слияния прогонялась пофункциональная сверка — файл резался по `^def `, и
  каждая функция, изменённая какой-либо веткой, сравнивалась побайтно с
  версией именно той ветки. Результат: `s14/s14b/s17b/s18/s18b/s19` = блок 1,
  `_down_arrow/s20b/s20c/s20d/s20e/s20f` = блок 2, `s21/s23/s25b/s25c` =
  блок 3, `s27/s28/s31/s33/s33b` = блок 4, `s03/s03b/s37b/s38/s39/s40` =
  блок 5 — 100% совпадение, ни одна правка не потеряна и не перекрыта.
  Для `_helpers.py` (там правились словари `URLS`/`SLIDE_REFS`, а не функции)
  сверка шла построчно: все добавленные каждой веткой строки присутствуют,
  удалённые — удалены (`SLIDE_REFS["s22"]` снят блоком 3).
- **`build_lec04_v4.py`** — список `builders` смержился корректно сам
  (вставка `b2.s14b`, удаление `b3.s21`, вставка `b1.s03b`); вручную сведены
  docstring (склеены описания всех пяти блоков) и два ассерта (`57`/`56` →
  **`58`**).
- **`deck.yaml` / `deck-part2.yaml`** — `total_slides` и `totals.slides`
  сведены арифметически: 57 +1 (s14b) −1 (s21) +1 (s03b) = **58**.
- **`iteration-log-part2.md`** — конфликты разрешались сохранением ОБЕИХ
  сторон (записи блоков идут подряд, ничего не выброшено); у блока 3 уровни
  заголовков понижены (`#`/`##` → `##`/`###`) под конвенцию файла.
- **`.pptx` / `.pdf`** — бинарные конфликты не разрешались как файлы: после
  каждого слияния `python3 build_lec04_v4.py` + `./render.sh`, затем
  `git add` перегенерированного артефакта.

### Пересчёт счётчиков после слияния

- `deck.yaml total_slides` = **58**, `deck-part2.yaml totals.slides` = **58**,
  объявленных `- id:` записей = **58**, слайдов в `lec-04.pptx` = **58**,
  страниц в `lec-04.pdf` = **58**. Сходится точно.
- `slide_times_sum_min` **пересчитан заново: 130,5 → 149,5**. Прежнее значение
  копилось приращениями и разошлось с фактической суммой `duration_min` по
  записям ещё ДО этого раунда (на базовом коммите фактическая Σ = 147,5 при
  объявленных 130,5). Вклад round 6: +3 (s14b) −3 (s21) +2 (s03b).
- `ai_failure.strict_in_slides` в `deck.yaml` содержал s21 и `count: 21`, хотя
  блок 3 уже поправил долю — рассогласование снято: список без s21,
  `count: 20` (сходится с 20 записями `in_bucket: true`). Доля пересчитана
  **20/48 ≈ 42%** (знаменатель 47 → 48: −s21, +s14b, +s03b — оба новых слайда
  out-of-bucket). Порог L4 (≥30%) держится с запасом. `partial_out` дополнен
  `s03b` и `s14b`.

### Порядок слайдов: deck.yaml ↔ отрендеренный PPTX

Проверялось не по памяти: сборка запускалась с перехватом `load_notes(sid)`,
что даёт точную последовательность id по позициям рендера, и сравнивалось с
порядком `- id:` в `deck.yaml` + `deck-part2.yaml`. Найдены и исправлены
**два реальных расхождения порядка** (оба — до этого раунда, не регрессия
слияния):

1. `- id: s09b` стоял ПОСЛЕ `- id: s10`, хотя его собственный комментарий и
   рендер помещают его между `s09` и `s10`. Перенесён.
2. `- id: s20` (70%-проблема) стоял ПЕРЕД кластером `s20b…s20f`, а рендер
   ставит его ПОСЛЕ; вдобавок его поле `file:` было копией соседнего
   (`s20-harness-gate.md`). Запись перенесена после `s20f`, `file:` исправлен
   на `slides/s21-70-percent-problem.md` — это ровно та конвенция
   «deck-id N → файл s(N+1)», по которой живут все соседние записи.

После правок позиционное соответствие полное: запись i ↔ слайд i для всех 58.

### Сквозные проверки всего дека (58 слайдов, не только затронутых)

- **Grep'ы по CLAUDE.md** (scaffold / LO-коды / `§N` / forward-ref `→ sNN` /
  тайминг `N мин` / методические мета-комментарии) по видимому слою PPTX **и**
  по заметкам докладчика: **0 / 0 / 0** в обоих слоях.
  Единственный найденный хит — `§1–§6` на p53 (сводная матрица) в видимом
  тексте и в заметках, плюс `§2.4` в заметке p14. Оба существовали до раунда,
  но правило ENFORCED — заменены на «разделов 1–6» / «в разделе про
  архитектуру» (смысл тот же, ссылка перестала быть «внутренней»).
- **Deep latin-token scan** — сравнивался не абсолютный счёт (в этой колоде
  латиница законна: бренды, URL, листинги кода, английские названия
  источников в машинном блоке «Источники:»), а **дельта к базовому коммиту**.
  Видимый слой: 552 → 503 уникальных токена (−49; 57 новых, 106 ушло).
  Заметки: 491 → 447 (−44; 43 новых). Все 57 новых — легитимные: ключевые
  слова Gherkin (`Given/When/Then`), уровни C4 (`Container/Component`),
  Structurizr-DSL из листинга s14b (`person/softwareSystem/container/inzh`),
  имена инструментов (`pytest/ruff/mypy/eslint/vitest/tsc/pnpm/adr-lint/
  structurizr-cli`), имена веток из схемы worktree (`wt-a/wt-b/wt-c`),
  названия источников (`Stack Overflow Developer Survey`, `DORA — State of
  AI-assisted Software Development`). Регрессии по англицизмам нет — колода
  стала чище базовой.
- **Осиротевшие ссылки на удалённый слайд.** Найдена одна, которую блок 3 не
  мог увидеть из своего worktree: на слайде risk-triad (p56) ось вероятности
  подписывалась «растёт с незнакомостью задачи (ось SWE-bench Pro)», а
  SWE-bench вводился ровно на удалённом слайде — после удаления термин
  оставался в колоде ровно один раз и без объяснения. Заменено на
  самодостаточное «растёт с незнакомостью задачи и кодовой базы» (в
  `slides_band4.py` и в зеркале `slides/s39-risk-triad.md`). Больше вхождений
  SWE-bench в видимом слое и заметках нет: 0/0.
- **Визуальная выборка** (не по самоотчётам блоков): p04 (новый s03b), p19
  (новый s14b), p32 (перестроенный worktree блока 2), p33 и p34 (окрестность
  удалённого слайда — переход «70%-проблема → дивайдер Раздела 4» связный),
  p41 (третий кейс ревью, блок 4), p56 (исправленная ось), p58 (закрытие,
  hero ≈45% площади, ссылки на семинар нет). Переполнений, потерянных
  иконок и разъехавшихся боксов не обнаружено.

### `speech.md` — снята повисшая ссылка на удалённый слайд

Точечно, без ресинхронизации речи в целом (она по-прежнему на 41 секцию
против 58 слайдов — отдельная задача, как и во все прошлые раунды):
удалена секция `## [s22] — Провал: бренд и бенчмарк ≠ дисциплина`, из
`slides_covered` убран `s22`, удалён пункт подготовки «Открыть swebench.com
и labs.scale.com — переверить числа s22», из сводки strict-in убрано
«бренд≠дисциплина / anti-hype (s22)». Добавлена запись в changelog.
Q&A-ответ про SWE-bench Verified в конце файла **оставлен**: он не ссылается
на слайд и работает сам по себе как запасной ответ.

### Разбиение журнала итераций

`iteration-log-part2.md` после слияния всех пяти записей вырос до 1472 строк
при лимите 600 (CLAUDE.md § Document Size Limit). Разбит на три файла:
part 2 (шапка + Round-5 + блок 1, 514 строк), part 3 (блоки 2 и 3, 450),
part 4 (блоки 4 и 5 + этот раздел). В начало каждого файла и в part 1
добавлена одинаковая таблица-навигация. `iteration-log.md` (part 1, 1093
строки) **не трогали** — он превышал лимит ещё до этого раунда, это отдельная
уборка.

### Что сознательно оставлено как есть (и почему)

- **`[VFY-day-of]` — 50 вхождений на 34 слайдах** в заметках докладчика. Это
  НЕ запрещённый скаффолд-маркер `[VERIFY-DAY-OF]`, а сложившаяся с round-1
  пометка волатильного факта, генерируемая машинным блоком «Источники:».
  Существует до этого раунда, не является регрессией слияния; правка задела
  бы 34 слайда. Вынесено владельцу отдельным решением.
- **Дрейф `slides/*.md` ↔ `slides_band*.py`** и **устаревшие поля `file:` в
  `deck.yaml`** (27 записей указывают на файлы, переименованные ещё при
  сдвиге нумерации v4.1; порядок при этом корректен — проверено выше).
  Прецедент «не чиним старый дрейф» держится с round-5. Исправлено только
  одно поле `file:`, которое мешало навести порядок записей (`s20`).
- **EN-колода** (`slides_band3_en.py`, `slides-en/`, `deck.en.yaml`) — не
  синхронизировалась. `build_lec04_v4_pub.py` был превентивно поправлен
  блоком 3 и собирается без ошибки (40 слайдов); бинарник `lec-04-pub.pptx`
  намеренно НЕ перезаписан, чтобы не публиковать полусинхронизированную
  EN-версию. Полная EN-синхронизация — отдельная задача.

### Перегенерированные артефакты

`lec-04.pptx` (58), `lec-04.pdf` (58 страниц), `snapshots/slide-01…58.png`
@150dpi, `lec-04-notes.pdf` (58 слайдов → 105 страниц; блок 1 явно отложил
его пересборку до сборочного прохода, чтобы пять веток не перезаписывали
один бинарник).
