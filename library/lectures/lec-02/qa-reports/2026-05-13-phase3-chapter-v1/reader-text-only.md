# Reader-Simulator Report (mode=text-only) on Chapter Лекции 2 v1.0 — 2026-05-13

VERDICT: APPROVE-WITH-POLISH

## Сводка как студента (1 paragraph)

Я прочёл главу — и в целом, **впервые за курс мне понятно**, что значит «модель внутри». Лекция 1 повисла на красивой метафоре «слоёного пирога» и не сказала, что внутри слоя «модель»; эта глава по сути закрывает мой долг по Лекции 1. К концу я могу проговорить четыре этапа (токенизация → эмбеддинг → внимание → сэмплинг), для каждого назвать практическое следствие, и три «почему» из §5.3 первой лекции у меня действительно отвечены — особенно «strawberry» и «разные ответы». Что НЕ зашло одинаково хорошо: §3.2 (объяснение роли через attention) — для меня неубедительное, оно само себе сказало «это упрощение, а на самом деле может быть in-context steering или RLHF», и я после этого не понимаю, на что мне опираться — это объяснение через механизм или нет? Несколько мест я перечитывал (cosine similarity формулу, embedding-таблица vs выходные эмбеддинги, авторегрессионный цикл шаг 2). И есть несколько незнакомых терминов, которые мне как студенту 3 курса встречаются впервые и не объясняются inline. Но никаких настоящих P0-блокеров я не вижу — глава **читаемая и работающая**.

## Где я теряюсь (P0 / P1 by reading flow)

**P1 — §1.1, параграф 2 («Размер словаря у современных моделей»).** Я только что прочитал «токен — id из словаря, словарь зафиксирован». И тут же — `o200k_base`, `cl100k_base`, SentencePiece, ~200к, ~100к, ~128к. Это **обрушение** на читателя в первый момент знакомства с темой: четыре имени собственных в одном абзаце, и ни одно не нужно для понимания. Я не знаю, должен ли я их запомнить (если да — это перегруз; если нет — зачем они тут?). Достаточно «у GPT-4o порядка 200к, у LLaMA порядка 128к — порядок десятки-сотни тысяч».

**P1 — §1.2, параграф 2 («Идея алгоритма проста»).** Алгоритм BPE описан в одном плотном предложении: «начинаем с алфавита, итеративно объединяем пары, добавляя в словарь». Я перечитал три раза. Я **не понял**, как именно «low / lower / newest / widest» превращается в `low / er / new / est / wid`. Какие пары и почему — не показано. План v2 явно сказал «БЕЗ пошаговой итерации», и я понимаю мотив (P1-2 fix), но текущая формулировка оставляет меня без интуиции вообще. Либо нужно одно-два конкретных предложения «пара `e+r` встречается часто → объединяем в `er`; пара `n+e` встречается часто → `ne`; ...» (даже без полной трассы), либо снять метафору алгоритма совсем и оставить только «выученный однажды компромисс».

**P1 — §1.3, параграф 4 (FACT-CHECK warning).** Здесь я как студент **сбит с толку**: глава только что три абзаца строила объяснение «модель ошибается в strawberry — она не видит букв», а тут говорит «эксперимент работает не всегда, современные топ-модели могут отвечать правильно». Это **противоречит примеру**, на котором держится весь раздел. Я не понимаю: показывает ли strawberry слепоту к буквам в 2026 или нет? Параграф пытается сказать «структурный факт остаётся, даже если в конкретном чате ответ правильный — там внутри тула», но это сказано в скобках и я не уверен. Это место **снижает доверие** ко всему §1.3, и я как студент его перечитал три раза.

**P1 — §2.1, параграф 5 («Различение, важное для практики»).** Различие между «внутренними эмбеддингами» (внутри LLM) и «выходными эмбеддингами» (для RAG) — для меня **переключение системы**, и я его упустил с первого чтения. Я думал, что у LLM есть «эмбеддинги», и читал §2.2-§2.4 как продолжение. Только перечитав, понял, что в §2.2-§2.4 (cosine similarity, RAG, поиск) речь идёт про **второй тип**, а в §3 (attention) — про первый. Это разные сущности, и переход между ними неявный. Я перечитал §2.1 и потом снова §2.2.

**P1 — §3.2, оговорка про in-context steering и RLHF.** Это **самое уязвимое место главы для меня**. Глава обещала «ответ через механизм, а не интуицию». В §3.2 механизм есть (role-токены → высокий вес в attention), и я готов был это принять. Но дальше написано: «На исследовательском уровне дискуссия о том, **что именно** делает роль в промпте, не закрыта». Как я как студент должен это понимать? Это объяснение, которое я могу написать в РК, или нет? Если меня спросят на экзамене «почему промпт с ролью работает лучше», я должен сказать «role-токены имеют высокий attention-вес» — или «attention, но возможно in-context steering, возможно RLHF, дискуссия открыта»? Второй ответ я не вспомню через 2 недели; первый — потенциально неточный, как утверждает сам автор. Чувствую, что мне нужна **более твёрдая позиция**: «вот рабочее объяснение для целей курса, нюансы — в Лекции 12». Сейчас оговорка слишком развёрнутая и подрывает уверенность.

**P1 — §4.4, шаг 2 «Forward pass».** «Полный проход контекста через все слои модели — токенизация (на самом деле она уже сделана для всего, кроме нового последнего токена, и кэшируется), эмбеддинги, все слои attention, финальный слой → распределение». Это самое плотное предложение во всей главе. Я перечитал. «Уже сделана и кэшируется» — это для меня **отвлекающая деталь**, она ничего не объясняет, я даже не понимаю, что такое «кэшируется» в этом контексте (KV-кэш? но это термин из forbidden additions). Я бы просто написал «forward pass = все четыре этапа, которые мы прошли в §§1-4, одним проходом» — без скобки про кэш.

**P2 — §4.2, параграф про логиты.** «Технически температура — это коэффициент, на который делят логиты перед softmax (логиты — внутренние «сырые» оценки вероятностей до нормализации); делать формулу здесь не будем, важно следствие». Слова «логиты» и «softmax» брошены и не нужны — я их не использую ни в чём дальше. Зачем они тут? Я знаю, что план говорит «без формул», и это правильно. Но тогда зачем «логиты», «softmax», «нормализация»? Это **footer-tax** — деталь, которая ничего не добавляет.

**P2 — §1.4, конец параграфа 3 («тот же документ на русском займёт примерно 40 тысяч токенов»).** Числовой пример хороший, но я **не понимаю**, как из «0.5 токена на символ» получилось 40 тысяч — я сначала думал «50% от 80 тысяч символов = 40 тысяч токенов», и это сошлось — ОК. Но мог бы пропустить логику, если бы числа не совпали. Полстроки расчёта помогли бы.

**P2 — §2.2, параграф «Технический момент про cosine similarity».** Формула `(A · B) / (||A|| · ||B||)`, скалярное произведение, длины векторов, единичные векторы, ортогональные — это **компактный курс линейной алгебры**, который я как 3-курсник проходил, но он внезапно вставлен после illustrative-таблицы и до следующего раздела. Я не уверен, нужен ли он мне для понимания дальнейшего. Если нужен — нормально; если не нужен (а кажется, что не нужен — §2.3 и §2.4 без формулы работают) — можно сжать до «cosine similarity = мера, чем ближе к 1, тем похожее».

**P2 — §5.3, decision tree.** «small transformer-классификатор (BERT-base fine-tuned)» — что такое BERT-base? Знаю BERT смутно по Лекции 1. «Fine-tuned» — что это? Об этом упоминалось в Forbidden additions «pretraining vs fine-tuning vs RLHF — другая лекция», и я понимаю, что глубоко не раскрывать. Но термин «fine-tuned» брошен **без disclaimer** в decision tree, и я как студент 3 курса не знаю, что мне с этим делать.

## Promise §5.3 cohesion

**4 концепта (Лекция 1 §5.3 обещала):** токены / эмбеддинги / attention / температура.

- **Токены — covered.** Раздел 1 целиком; concept introduction sequence чистый.
- **Эмбеддинги (векторные представления) — covered.** Раздел 2 целиком; терминологически вижу алиас «векторное представление» в §2.1.
- **Механизм внимания (attention) — covered.** Раздел 3 целиком; метафора «фонарик», три ключевых факта.
- **Температура — covered.** §4.2 + сводная таблица §4.3.

**4 / 4 концептов — payoff'ены.** Промис Лекции 1 я как студент чувствую выполненным.

**3 «почему» (Лекция 1 §5.3):** роль / буквы / разные ответы.

- **Почему промпт с ролью лучше?** §3.2 + §5.2 первое «почему». **Payoff частичный** — формальный механизм дан (role-токены → attention-вес), но оговорка про in-context steering / RLHF меня подрывает (см. P1 выше). Хочу более твёрдую позицию.
- **Почему AI плохо считает буквы?** §1.3 + §5.2 второе «почему». **Payoff convincing**, особенно через `[straw][berry]` визуализацию. Это самое крепкое место главы.
- **Почему один и тот же запрос даёт разные ответы?** §4.2 + §5.2 третье «почему». **Payoff convincing** — distribution → sampling → T параметр, прямая логика. Числа на распределении («съел яблоко 0.32, пиццу 0.19...») делают это конкретным.

Where payoff cards live: §5.2 collects all three в одном месте — это работает как summary. После §5.2 я могу ответить на 3 «почему» близко-к-памяти, **кроме первого** (роль) из-за P1 выше.

## Concept introduction sequence audit

| Термин | Где впервые | Где определён | Issue? |
|---|---|---|---|
| токен | §1.1, абз 1 | §1.1, абз 1 | ✅ ОК |
| BPE | §1.2 заголовок | §1.2, абз 1 (полная расшифровка) | ✅ ОК |
| эмбеддинг | §2.1, абз 1 | §2.1, абз 1 (алиас «векторное представление») | ✅ ОК |
| attention / механизм внимания | §3.1, абз 1 | §3.1, абз 1 (метафора фонарика + 3 факта) | ✅ ОК |
| cosine similarity | §2.2, абз 2 | inline («мера угла между векторами; значение от −1 до 1, ближе к 1 — более похожи») | ✅ ОК, потом formal в §2.2 |
| PCA | **не встречается в главе** | — | ✅ Removed |
| argmax | §4.2, T=0 параграф | inline («операция argmax — возьми тот, у которого вероятность максимальна») | ✅ ОК |
| autoregressive / авторегрессионный | §4.4 заголовок и абз 2 | §4.4 («буквально само-регрессирующий; модель опирается на свои предыдущие выводы») | ✅ ОК |
| top-p / top-k | §4.2 | inline («параметр, отрезающий хвост»; «параметр-предел количества кандидатов») | ✅ ОК |
| **k-means** | §2.3 | inline («итеративный алгоритм, разбивающий точки на заданное число групп») | ✅ ОК |
| **embedding-таблица** | §2.1, абз 2 | inline в том же абзаце | ⚠️ работает, но термин используется и в §3.1, и не помечен как «вспомните» |
| **softmax** | §4.2, скобка | **НЕ определён** | ⚠️ P2 — брошен в footer-tax детали |
| **логиты** | §4.2, скобка | inline («внутренние сырые оценки вероятностей до нормализации») | ⚠️ P2 — определение есть, но всё равно лишнее |
| **inductive bias** | §3.4, конец абз 3 | **НЕ определён** | ⚠️ **P1 — мне как студенту 3 курса термин знаком из ML-курса, но не все одногруппники проходили — disclaimer одной фразой «склонность обучения предпочитать определённые гипотезы» был бы полезен** |
| **fine-tuned** | §5.3, decision tree | **НЕ определён** | ⚠️ P2 — см. выше |
| **stemming** | §2.4, абз 2 | **НЕ определён** | ⚠️ P2 — для 3-курсника знаком, но один человек спросит «что это» |
| **RLHF** | §3.2 (обучение с подкреплением через обратную связь) | inline в скобках | ✅ ОК, развёрнуто |
| **edge-устройство** | §5.3, decision tree | **НЕ определён** | ⚠️ P2 — упоминается как «edge-устройства с ограниченным compute», понятно из контекста, но термин брошен |
| **batching** (как причина T=0 микро-вариативности) | §4.2 | inline («серверный батчинг») | ⚠️ Marginal, понятно из контекста |

**Summary audit:** sequence чистый для core 4 концептов. Issue — несколько ML-терминов брошены без inline disclaimer в decision tree §5.3 (fine-tuned, BERT-base, edge-устройство, inductive bias). Это меня как студента не блокирует, но добавляет когнитивную нагрузку именно там, где должно быть «всё сложить» — в conclusion разделе.

## Cross-references к Lec-1 helpfulness

| Ref | Где | Helpful or skipping? |
|---|---|---|
| §3.2 (модель = stateless inference) | Введение, §4.4 | ✅ **Helpful callback**. Я помню эту схему. §4.4 уточняет («stateless шаги → stateful процесс») — это для меня прозрение. |
| §3.3 + §3.3.1 (цикл чата, контекстное окно) | Введение, §3.3 | ✅ **Helpful**. §3.3 здесь углубляет (квадратичная стоимость) — естественно. |
| §3.4.1 (Agent loop) | §5.5 | ✅ **Helpful**, короткая отсылка. |
| §4.2 (локально vs облако) | Введение, §4.5 | ✅ **Helpful**. §4.5 не повторяет, только добавляет архитектурный угол — на одной странице. |
| §4.8 (3 уровня причинности Пёрла) | Введение, §5.4 | ✅ **Helpful, важный**. §5.4 даёт **механизм** к концепту Лекции 1. Это лучший cross-ref в главе. |
| §5.3 (3 «почему») | Введение, §5.2 | ✅ **Helpful — payoff card в §5.2 закрывает обещание**. |
| §1.3 (трансформер 2017, self-attention high-level) | Введение | ✅ **Helpful**, короткая отсылка «вы это знаете как чёрный ящик». |
| §1.4 (задача × модальность) | §5.3 | ✅ **Helpful**, §5.3 расширяет. |
| §3.3 PARTS (Persona / Action / Recipe / ...) | §3.2 | ⚠️ **Marginal skipping**. Я не помню, что в §3.3 первой лекции были PARTS — я помню там что-то про чат-цикл. Это конкретно та внутренняя противоречивость Lec-1 §5.2 vs §3.3, отмеченная в плане. Не блокирует, но cite «Лекция 12 PARTS-каркас» без явного пояснения мне сейчас ничего не говорит. |
| §2.2 (MCP, 2024) | §5.5 | ✅ **Helpful**, мне напомнил. |

**Overall:** Cross-references **в основном helpful**, не skipping. Я не должен открывать Lec-1 chapter, чтобы понять Лекцию 2. Единственный мутный момент — PARTS в §3.2 (но это не блокер, а напоминание про будущее).

## 3 cross-cutting frames — student perspective

- **§4.5 local/cloud:** **adequate**, не overload. 3 абзаца, конкретные примеры моделей (Ollama, llama.cpp, Llama 3.1 8B vs OpenAI / Anthropic), trade-off ясный (приватность × скорость × контекст × качество). Callback на Lec-1 §4.2 короткий и правильный. Для меня этот frame **работает как закрытие** — я понимаю, что архитектура не меняется, меняется размер.

- **§5.3 ML vs LLM:** **adequate, но самое слабое из трёх**. Decision tree из 4 веток ясный, но **внутри веток вброшены ML-термины без disclaimer** (XGBoost, LightGBM, CatBoost, BERT-base fine-tuned, decision trees, rule-based) — я их слышу из ML-курса, но без inline definition. Для студента, у которого ML-курс ещё не был, это может быть overload. Я как студент-проходивший понял; одногруппник без ML может зависнуть.

- **§5.4 Human vs AI («attention ≠ понимание»):** **adequate, factual tone сработал**. Side-by-side человек / AI и связь с тремя уровнями Пёрла — это лучший cross-cutting frame главы. Я после §5.4 могу сформулировать «модель работает на уровне 1, частично на 2, не на 3» — и теперь у меня есть **механистическое основание** (attention = статистика, не каузальный граф). Это сильное место.

**3 frames в сумме = adequate, не overload.** Каждый занимает 1-2 страницы, не разрушает арку четырёх этапов, и каждый имеет cite в Лекцию 1 без повтора. Хорошо.

## Self-check вопросов usefulness

**5 секций self-check (Раздел 1, 2, 3, 4, 5).** Я как студент посмотрел на каждую — **могу ли я ответить из памяти после reading?**

- **Раздел 1 (3 вопроса):** ✅ Могу. Вопрос 1 (`strawberry` vs `cat`) — повторяет ключевой пример главы; retrieval practice работает. Вопрос 2 (три типа задач из «слепоты к буквам») — могу перечислить (подсчёт, опечатки, регистр) из §1.3. Вопрос 3 (batch на 50к запросов) — это **transfer**, не recall, и хороший: я должен применить знание про токенизацию RU vs EN к конкретному кейсу с trade-off.
- **Раздел 2 (3 вопроса):** ⚠️ **Вопрос 1 (эмбеддинг токена vs эмбеддинг предложения)** — это **проверка различия, которое я плохо усвоил с первого чтения** (см. P1 выше про §2.1 параграф 5). Self-check вопрос точно бьёт в слабое место, и это **хорошо как retrieval-test**, но плохо, если различие в тексте было неявным. Вопрос 2 (cosine = 0.62, что делать) — отличный, потому что заставляет меня сказать «зависит от распределения в задаче», что я как раз вытащил из §2.2 параграфа про эмпирические пороги. Вопрос 3 (когда full-text лучше) — хороший transfer.
- **Раздел 3 (4 вопроса):** ✅ Могу. Особенно вопрос 4 (lost in the middle, миллион токенов, куда положить инструкцию) — этот вопрос **показывает мне, что я не усвоил**, если у меня нет твёрдого ответа «начало или конец». Хороший retrieval.
- **Раздел 4 (4 вопроса):** ✅ Вопрос 1 (классификация жалоб, T, max_tokens, system prompt) — отличный применительный вопрос, прямо из таблицы §4.3. Вопрос 4 (локально vs облако) — transfer, и мне он понятен.
- **Раздел 5 (3 вопроса):** ✅ Вопрос 1 (четыре этапа + следствия) — recall, сводка LO1. Вопрос 2 (три «почему» одной фразой) — recall LO7. Вопрос 3 (три задачи по decision tree) — отличный синтез.

**Overall self-check usefulness:** **высокая**. 17 вопросов в сумме, retrieval + transfer mix, прямо бьют в LO. Это полезная подготовка к РК.

**Один маленький gap:** в Self-check Раздел 4 нет вопроса прямо на авторегрессионную генерацию (вопрос 2 близок, но он скорее про оркестратор / stateless ↔ stateful, чем на сам loop). Не блокер.

## Tone & motivation

**Хочу ли я продолжать читать после §1?** ✅ **Да.**

Тон **explanatory-engineering** работает — я как студент 3 курса не чувствую ни «магии LLM» (хайповый стиль), ни сухого академизма. Глава **разговаривает со мной**: фразы «полезное мысленное упражнение», «попробуйте сами угадать», «представьте, что вы стоите в комнате», «удобная и недозированная метафора» — это **читаемая проза**, не bullet-points-disguised-as-prose. Я отвлекался от текста реже, чем боялся.

Самое мотивирующее место — **§1.3 «Почему AI плохо считает буквы»**. После него я хочу продолжать; вижу, что глава **отвечает на вопрос, который у меня уже был** (странности ChatGPT). Это hook, который держит до §3.2 (роль) и §4.2 (T).

Самое скучноватое место — **§4.3 «Четыре ручки API»**. Таблица + 4 параграфа развёрнутого комментария про каждый сценарий. Контент полезный, но темп резко падает после энергичного §4.2. Я перечитывал не из-за непонимания, а потому что глаз скользил. Если бы можно было сжать четыре поясняющих параграфа на 30% — было бы динамичнее. Не блокер.

Самое скучное место **во всём контексте** — **§1.2 параграфы 4-5 (WordPiece, SentencePiece, Hugging Face Tokenizers).** Дополнительные имена собственные без use в дальнейшей главе. После энергичного §1.1 и интригующего §1.3 это **обременение**.

Никакого «wow-style» / алармизма / триумфализма я не вижу — это плюс. Соответствует Lec-1 tone.

## Domashka clarity

**§5.5 задание (s27) — могу ли я как студент 3 курса самостоятельно выполнить?** ✅ **Да, с одной оговоркой.**

- Шаги 1-3 прозрачны: возьми задачу, запусти на T = 0, 0.7, 1.5 по три запуска каждой T, принеси разбор на A4.
- Рекомендованный playground: HF Inference Playground, Llama-3-8B-Instruct. Резервы: Together.ai, Ollama. **Это спасает меня** от ситуации «HF лёг — что теперь?», потому что fallback'и явные.
- Disclaimer «не подойдут ChatGPT Free / Claude.ai (нет T-slider в UI)» — **полезный**. Без него я бы попробовал ChatGPT и обнаружил, что параметра нет, и не понял почему.
- Бонус-челлендж (`сколько 'р' в "строгая регуляризация"`) — opциональный, ясный.

**Оговорка:** что значит «одностраничный разбор любой формат»? Это документ Word? Markdown? Скан рукописного? Не указано. Я как студент бы написал в любом формате, но **формальный требуемый формат не задан**. Это P2 — не блокер, но 30 секунд preview clarification полезны.

**Вторая оговорка:** Llama-3-8B-Instruct рекомендован «для unified apples-to-apples сравнения», но **не сказано почему** именно эта модель и **могу ли я взять другую**, если у меня есть предпочтение. Это P2, не блокер.

## Bridge к Лекции 3

**§5.5 §28 предисловие к Лекции 3 — adequately preview?** ✅ **Да.**

4 концепта Лекции 3 (RAG, Tools / function calling, MCP, Agent loop) перечислены с inline disclaimer'ом для каждого — это правильно. Я как студент после §5.5 могу ответить на быстрый вопрос «что в Лекции 3», и каждый концепт у меня уже есть якорь:

- **RAG** — узнаю из §2.3, §2.4 (semantic search база).
- **Tools / function calling** — узнаю из §1.1 (внешний инструмент для арифметики), §1.3 (внешний инструмент для посимвольной работы).
- **MCP** — узнаю из Lec-1 §2.2 (Anthropic 2024).
- **Agent loop** — узнаю из Lec-1 §3.4.1.

Bridge ясный: «все 4 надстраиваются над single-shot inference из §4.4». Это правильный nexus — я понимаю, как глава 2 (single-shot inference) переходит в главу 3 (расширения).

Multimodal (CLIP) упомянут в одной строке как «там же» — adequate, не overload.

## Cognitive load tracking

Где плотные переходы / можно усилить prose:

- **§1.1 → §1.2 (плотно)** — между «токен = id из словаря» и «BPE — компромисс между алфавитом и словарём» переход быстрый. Я успел переварить §1.1, но мостовое предложение типа «но как именно этот словарь возникает? Алгоритм называется BPE» помогло бы. Сейчас §1.2 начинается прямо «Алгоритм, которым строятся словари...» — это работает, но без явного моста с предыдущей секцией.
- **§2.1 → §2.2 (плотно, см. P1 выше)** — переключение между «внутренние эмбеддинги внутри LLM» и «выходные эмбеддинги для RAG / sentence similarity» происходит в одном параграфе §2.1, и я как читатель его пропустил с первого захода. Нужен **усиленный мост** в §2.2: «Дальше в этом разделе мы говорим именно про выходные эмбеддинги».
- **§3.2 длинный (5+ параграфов)** — это самый длинный sub-section в главе. Я перечитал не один раз. Внутри есть worked example (`Кот съел мышь...`) + упражнение (`Программа упала...`) + role-effect + softer phrasing про in-context steering. **Это много в одном sub-section** — я к концу немного потерял нить «что было главным». Если разбить на §3.2a (worked example) и §3.2b (role effect) — это бы помогло.
- **§4.4 шаг 2 forward pass (см. P1 выше)** — самое плотное предложение в главе.
- **§4.3 → §4.4** — переход «4 ручки API» (§4.3) к «авторегрессионная генерация» (§4.4) у меня вызвал «зачем теперь это?». Связь есть (max_tokens из §4.3 объясняется через цикл §4.4), но **она не сказана**. Мостовое предложение «Теперь, когда мы понимаем 4 ручки, посмотрим, как они вписываются в цикл генерации» помогло бы.

Где можно облегчить prose:

- **§1.2 параграфы 4-5** — сжать WordPiece / SentencePiece / Hugging Face Tokenizers до одного абзаца, без отдельных bullet-точек.
- **§2.2 параграф про cosine similarity formal** — можно сжать или вынести как «опционально для интересующихся».
- **§4.3 четыре поясняющих параграфа** — каждый можно сжать на 30%, главное (T, system prompt, max_tokens choice) останется.

## Visual aid hints (для Phase 5-6)

Где я как читатель **хочу** визуал:

1. **§1.1 — токенизация примера.** «Сегодня я съел яблоко» → `[Сег][одня][ ][я][ ][съ][ел][ ][я][бл][око]`. Визуал «строка → разрезание на цветные блоки → последовательность id-чисел». **Plan уже говорит s01 + s05** — ✅ покрыто.

2. **§1.4 — стоимость токенизации по языкам.** Bar chart EN ~0.25, RU ~0.5, ZH ~0.8, Code ~0.4. **Plan говорит s08** — ✅ покрыто.

3. **§2.1 — embedding lookup table.** `[кот]` → таблица → вектор `[0.21, -0.45, 0.88, ...]`. Очень хочу. **Plan говорит s09** — ✅ покрыто.

4. **§2.2 — heatmap cosine similarity 5x5.** Очень помог бы (я её представляю в голове, но визуал бы закрепил). **Plan говорит s10** — ✅ покрыто.

5. **§3.1 — фонарик в комнате с токенами + bar chart распределения весов под ним.** Метафору фонарика я нашёл лёгкой к восприятию, но визуал был бы the next step. **Plan говорит s14** — ✅ покрыто.

6. **§3.2 — worked example `Кот съел мышь...` со стрелками разной толщины.** Очень хочу — это первый «механизм видно». **Plan говорит s15** — ✅ покрыто.

7. **§3.3 — bar chart progression контекстного окна (4k → 200k → 1M).** Хочу — числа на лестнице запомнятся лучше. **Plan говорит s16** — ✅ покрыто.

8. **§3.4 — U-shape curve accuracy vs position.** Хочу — без графика «U-shape» — это просто слова. **Plan говорит s17** — ✅ покрыто.

9. **§4.1 — bar chart распределения вероятностей** (яблоко 0.32, пиццу 0.19, ...). **Plan говорит s18** — ✅ покрыто.

10. **§4.2 — три копии bar chart распределения при T=0 / T=0.7 / T=1.5.** **Plan говорит s19** — ✅ покрыто.

11. **§4.3 — таблица 4 сценариев × 4 параметров.** Хочу её в slide прямо как есть. **Plan говорит s20** — ✅ покрыто.

12. **§4.4 — замкнутый цикл шаг 1 → 2 → 3 → 4 → 5 → 1.** Хочу — диаграмма закроет нить. **Plan говорит s21** — ✅ покрыто.

13. **§5.1 — таблица «этап → следствие» (4 строки).** Хочу — это лучшая сводная карточка. **Plan говорит s23** — ✅ покрыто.

14. **§5.3 — decision tree ML vs LLM.** Хочу — текст decision tree без визуала тяжелее. **Plan говорит s25** — ✅ покрыто.

**Один новый visual hint, не учтённый в плане:**

- **§2.1 — наглядное различие «внутренние эмбеддинги» vs «выходные эмбеддинги».** Это место в тексте у меня вызвало P1 (см. выше), и визуал «embedding table внутри LLM (картинка LLM с зум-врезкой на таблицу) | output embedding model (отдельный квадрат — модель, на выход вектор)» закрыл бы концептуальный разрыв. **Currently этот контраст plan не покрывает явно** (s09 — что такое эмбеддинг, без разделения internal/output). Подумайте, нужен ли это в s09 или это лекторская заметка.

## Топ-5 фиксов для chapter v1.1

1. **§3.2 in-context steering / RLHF оговорка — сузить и сделать твёрже.** «Рабочее объяснение: role-токены имеют существенный вес в attention-распределении. Альтернативные исследовательские объяснения (in-context steering, RLHF role-priming) — упоминание в Лекции 12 / Дальнейшее чтение». Сейчас оговорка подрывает уверенность в payoff'е первого «почему».

2. **§1.3 FACT-CHECK warning — переформулировать так, чтобы не противоречил основному примеру.** Сейчас я как студент после warning не уверен, ошибается ли модель в `strawberry` или нет. Возможные формулировки: «Если в момент чтения этот пример уже не воспроизводится — это связано с тем, что современные топ-модели внутри вызывают код или явную цепочку рассуждений; **сам один-проход через нейросеть к посимвольному ответу не приводит — это структурный факт, не зависящий от того, какой ответ вы получили в конкретном чате**». Сейчас warning хочет это сказать, но слишком мягко.

3. **§1.1 параграф 2 — сократить имена собственные.** Убрать `o200k_base`, `cl100k_base`, SentencePiece из первого знакомства с токеном. Достаточно «GPT-4o — около 200к, LLaMA — около 128к, порядок десятки-сотни тысяч». Подробности — в §1.2 (если нужны) или в Дальнейшем чтении.

4. **§2.1 параграф 5 «внутренние vs выходные эмбеддинги» — вынести как явный мост в §2.2.** Сейчас различие зарыто в последнем абзаце §2.1; нужно явно сказать в начале §2.2: «Дальше в этом разделе мы говорим про **выходные эмбеддинги** — те, которые используются внешне для поиска и RAG. Внутренние эмбеддинги токенов работают по той же геометрической логике, но скрыты внутри модели.»

5. **§4.4 шаг 2 forward pass — упростить плотное предложение.** Убрать скобку «(на самом деле она уже сделана для всего, кроме нового последнего токена, и кэшируется)». Заменить на «Forward pass = весь конвейер §§1-3 (токенизация → эмбеддинг → attention) одним проходом → распределение вероятностей следующего токена». KV-кэш — это deep-dive, его план запрещает.

## Counter-check

- **Verdict-counter:** 5 P1 issues найдено. Self-containedness главы (как академического текста) **высокая** — ни один из P1 не блокирует понимание; каждый — это «можно лучше». P0 (структурный блокер) у меня нет. Verdict APPROVE-WITH-POLISH соответствует.
- **Не предлагаю «правильную» структуру** — я как студент комментирую, что я понял / не понял, не как методолог.
- **Не пишу revisions / правки** — топ-5 фиксов содержат **направление**, конкретный editing — задача Phase 4 book-editor revision, не моя.
- **Не критикую факты** — это fact-checker domain. Я отметил FACT-CHECK warning §1.3 только как читательскую проблему, не как факт-проблему.
- **Не add «Лектору»** — глава = chapter.md (academic), не speech.md. Speaker notes маркеры `[for-slide-sNN]` я игнорировал как метаданные, не как разговорный язык.
- **Tone counter:** студенческий, конкретный — да. Я говорил «я перечитал», «я зацепился», не «нужно улучшить prose» в общем виде.
- **Promise §5.3 cohesion:** 4 / 4 концепта payoff, 3 / 3 «почему» payoff (одно частично из-за §3.2 оговорки). Promise выполнен в смысле «всё на месте», требует polish в одном месте.
