---
id: s13b
type: assertion_visual
section: "Раздел 3. Fine-tune vs промпт vs RAG"
duration_min: 1.5
assertion: "Fine-tuning меняет САМИ ВЕСА модели на ваших данных — этим он принципиально отличается от промпта и RAG, которые меняют только контекст"
learning_goal: "Определение fine-tuning ДО его критики/применимости; закрыть пререквизит-gap на видимом слое (раньше был только inline в s14)"
learning_outcomes: [LO7]
chapter_ref: "§3.1 [for-slide-s14]"
visual_brief: "Сверху — определение одной фразой. В центре — мини-схема в Ocean rounded box: [предобученная модель] + [ваш датасет] → (дообучение) → [дообученные веса]. Снизу — контраст-плашка: промпт/RAG меняют КОНТЕКСТ (веса не трогают) vs fine-tuning меняет ВЕСА. Gold — слово «ВЕСА» как смысловой якорь."
interaction: none
suffix_insert_after: s13a
---

# Visible content

## Title bar
«Что такое fine-tuning»

## Body
[Определение, 16pt, под заголовком]
**Fine-tuning (дообучение)** — продолжение обучения уже готовой модели на ваших данных. В Лекции 1 это был один из *типов использования*; здесь — *архитектурный выбор*, одна из ступеней лестницы.

[Центр — мини-схема, Ocean rounded box, 3 блока + стрелки]
**Предобученная модель** *(общие веса)*  ＋  **Ваш датасет** *(примеры нужного поведения)*  →  *дообучение*  →  **Дообученные веса** *(модель уже другая)*

[Низ — контраст-плашка, 2 половины]
Промпт и RAG меняют **КОНТЕКСТ** — что подаём на вход; веса модели не трогаются, эффект живёт только в рамках запроса.
Fine-tuning меняет **САМИ ВЕСА** — изменение встроено в модель, действует всегда и стоит дороже всего, что меняет контекст.

[Gold — смысловой якорь]
Промпт/RAG = «что показать модели». Fine-tuning = «изменить саму модель».

## Speaker notes

Прежде чем что-либо говорить про то, где fine-tuning сузился, где он опасен и по каким критериям его выбирать, нужно одинаково понимать, что это такое. Fine-tuning, по-русски дообучение, — это продолжение обучения уже готовой, предобученной модели на ваших собственных данных. В Лекции 1 дообучение упоминалось мельком как один из типов использования ИИ; здесь оно появляется в своей настоящей роли — как архитектурный выбор, одна из ступеней той же лестницы сложности.

Механика на схеме: берётся предобученная модель с её общими весами, берётся ваш датасет — набор примеров того поведения, которое вы хотите получить, — и процесс дообучения сдвигает веса модели в сторону этих примеров. На выходе — уже другая модель: те же архитектура и размер, но другие числа внутри. Это и есть ключевое отличие, ради которого нужен отдельный слайд-определение. Промпт и RAG не трогают веса вообще: они меняют только контекст, то есть то, что вы подаёте модели на вход прямо сейчас. Эффект промпта или подложенного RAG-фрагмента живёт ровно в пределах одного запроса и исчезает со следующим. Fine-tuning меняет саму модель: изменение встроено в веса, действует на каждом запросе по умолчанию, и именно поэтому стоит дороже и откатывается тяжелее всего, что меняет лишь контекст. Запомните это различие как формулу: промпт и RAG — это «что показать модели», fine-tuning — это «изменить саму модель». На следующих слайдах мы будем опираться на это определение, разбирая, почему в 2026 году fine-tuning сузился до поведения, а не знания, и где он превращается из инструмента в источник проблем.
