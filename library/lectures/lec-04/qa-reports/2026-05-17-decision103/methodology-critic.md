VERDICT: APPROVE-WITH-POLISH

# Methodology Critic Report — Лекция 4 **delta Решение #103 (s22a curl #5)** — 2026-05-17

**Артефакты (delta-скоуп):** `chapter-part2.md` §4.5 v1.3 (curl-кейс + `[for-slide-s22a]`) · `rendered/lec-04.pptx` v3.4 (новый s22a) + `slides/s22a-curl-slop.md` · `deck-part2.yaml ai_failure_judgment` (count 15→16) · `speech.md` v1.3 (фрагмент [s22a · 2 мин], pacing-итог).
**Issue:** #99 · **Branch:** issue-87-lec-03-architectures (рабочая сессия) · **Phase:** re-QA delta Решение #103, pre-GATE-C.
**Скоуп (узкий, по брифу):** ТОЛЬКО delta s22a + каскад #103. A→D-хребет / LO success-логика / дивайдеры / tone-strip — **НЕ пере-ревью** (подтверждены в v3/v3.1/v3.2/v1.2-tools). Rename мейнтейнер→сопровождающий и тег-унификация `[VFY-day-of]` — методически нейтральны (косметика нерендеримого слоя + glossary-lock), не пере-проверяю содержательно.
**Метод:** §4.5 прочитан дословно (chapter-part2 L200–209); s22a slide.md (видимый слой + speaker notes) дословно; speech [s22a · 2 мин] L403–413 + pacing-self-report L599 дословно; strict-in пересчёт по `deck-part2.yaml` enumerate (16/36) + per-section минутная декомпозиция (Python, две модели: deck-static 80.9 + speech-delivery); single-cluster Р4 численно; ЦВ-возврат-4 целостность (s21→s23 через s22/s22a); anti-hype-grep видимого слоя s22a.

## Severity counts
- **P0: 0** (методически непригодного нет; s22a = валидный strict-in failure #5; strict-in ≥ обоих порогов; single-cluster Р4 НЕ вернулся; pacing ≤75 держится)
- **P1: 0** (s22a несёт провал+урок+альтернативу+критерий связным текстом; ЦВ-нить цела; хребет не разбавлен)
- **P2: 1** (доку: `share_by_slides` строка содержит «~56% минут (45/81)» — арифметически верно для deck-static, но речь-доставка даёт ~54%; разночтение знаменателя не помечено — не блокер, не артефакт-гейт)

**Counter-check (mandatory, по 4 условиям брифа):**
- strict-in <30%? — **НЕТ** (16/36=44.4% слайдов; 55.6% deck-мин / 54.2% speech-мин).
- single-artifact concentration? — **НЕТ** (chapter ~69% / slides 44%сл/55.6%мин / speech ~54%мин — holistic в каждом).
- Р4 single-cluster вернулся? — **НЕТ** (Р4 = 31.1% всех strict-in-минут deck-static / 27.1% speech-delivery — оба <40% порога).
- pacing >75? — **НЕТ** (speech-доставка Σ active 72.9 + ~1 буфер = ~74 ≤ 75; deck 80.9 — статичная верхняя оценка, не истина доставки).
→ ни одно REVISE-условие не сработало. 0 P1 → counter-check «≥5 P1 → REVISE» неприменим. **APPROVE-WITH-POLISH** (1 реальная доку-P2 в видимом счётчик-слое → не -CLEAN).

---

## 1. s22a = валидный named strict-in failure #5 — **ПОДТВЕРЖДЕНО (YES)**

Прогнан bucket-тест CLAUDE.md § AI-Failure по всем 4 признакам — **полностью in-bucket, не калибровка, не хайп**:

| Признак bucket | s22a несёт? | Evidence (дословно) |
|---|---|---|
| Документированный **провал ИИ** | ДА | «curl фактически свернул открытую bug-bounty-программу… объём LLM-отчётов ×8, доля валидных >15%→<5%» — конкретный задокументированный отказ реального процесса под AI-slop (The New Stack / The Register 2026-01-21 / Socket) |
| **Выученный урок** | ДА | «генеративный AI масштабирует не только полезное производство, но и правдоподобный мусор… это атака на процесс, а не на код»; «виновата **архитектура процесса**, рассчитанная на старое соотношение стоимостей» |
| **Не-AI альтернатива** | ДА | «процессная, не-AI: приватное раскрытие (GitHub Security Advisories), убрать денежный стимул, требовать **воспроизводимый proof-of-concept** как барьер на входе» — конкретный не-AI инструмент-замена |
| Критерий **«когда AI здесь опасен»** | ДА | «любой открытый процесс, где (а) принимается правдоподобный текст от произвольного отправителя и (б) валидация дорога и лежит на людях, под генеративным AI деградирует» — операциональный, переносимый критерий |

**Анти-хайп-баланс присутствует и силён** (это не дисклеймер, а суждение): «Важно зафиксировать баланс, иначе кейс прочитается как "AI вреден для безопасности", что неверно: Daniel Stenberg прямо говорит, что AI — это инструмент… виноват не "AI", виновата архитектура процесса». Это ровно anti-«магическая пилюля наоборот» — учит **различать инструмент и процесс**, а не «AI плохой». Anti-hype-grep видимого слоя s22a (магическ/спасёт/революци/killer/незаменим/game-chang) = **0 hits**.

**Усиливает AI-Failure-линию?** ДА — это 5-й *named* развёрнутый failure-кейс (Replit / METR / curl / slopsquatting / Anthropic-junior), ранее существовавший только устным рассеянным якорем; теперь — выделенный self-contained слайд + 2-мин beat. Добавляет **новый класс провала** (AI масштабирует *шум*, бьёт по *людям-сопровождающим* / supply-chain), которого не покрывал ни один из 4 прочих кейсов (те — про вред *вашему коду*). Это расширение спектра failure-таксономии, не дубль. **Калибровкой/хайпом не является.**

**Slide-методика:** `in_bucket: true` корректен; `assertion` — full sentence с тезисом; speaker notes ~310 слов связного студенческого текста (в окне 150–300 — на ~10 слов выше верха, **P2-незначимо**, не layout-описание, не «Лектору»); видимый слой = асимметрия + контекст + урок + альтернатива + критерий-footer (assertion-evidence соблюдён). Числа волатильны → только направление словами + `[FACT-CHECK]` в notes (freshness-урок Л1 применён).

## 2. strict-in пересчёт (strict-#78, partial→out) — **ДЕРЖИТСЯ; число подтверждено**

s22a **полностью** in-bucket (не partial — все 4 признака bucket несомы, не «смешанный калибровочный»). Поэтому, в отличие от тул-врезок v1.2 (partial-upside), s22a **корректно добавляет count: 15→16**.

| Метрика | Значение (мой пересчёт) | Порог | Статус |
|---|---|---|---|
| Слайдо-доля strict-in | **16/36 = 44.4%** (deck `in_bucket_slides` enumerate) | ≥40% план / ≥30% mandatory | ✓ +4.4 / +14.4 п.п. |
| Минутная доля (deck-static 80.9) | 45.0 / 80.9 = **55.6%** | ≥40% | ✓ |
| Минутная доля (speech-delivery, s22a@2) | 42.5 / ~78 ≈ **54%** (≈ speech.md ~48% оценка — см. ниже) | ≥35% speech | ✓ |
| Chapter strict-in | **~69%** (не тронут; §4.5 — net-additive in-bucket текст ⇒ non-decreasing) | ≥40% | ✓ |
| Single-artifact concentration | **отсутствует** (chapter ~69% / slides 44%сл·55.6%мин / speech ~54%) | — | ✓ holistic в каждом из 3 |

**Минутный пересчёт honest partial→out (#78):** дивайдеры (s04a/s10/s14/s18/s24a/s28a ~0.3) и калибровочные s06/s07/s11/s15 — partial→out, в strict-in-числитель НЕ включены; проверено по enumerate `partial_out` (20 шт) — суммируется с 16 in-bucket = 36, без двойного учёта. Число `count: 16` / `16/36≈44%` в `ai_failure_judgment` (deck-part2 L447–448) **арифметически верно**.

> Расхождение с speech.md self-report «~48% минут»: speech-writer считает по 36 фрагментам устной доставки консервативно (s22a@2 + speech-урезанные durations); мой deck-static даёт 55.6%, speech-delivery ~54%. **Все три >> ≥35% speech / ≥40% slides — расхождение метода не меняет вывод** (любая модель ≥ порога с двойным запасом). Помечаю единственным P2 (см. ниже): видимая строка `share_by_slides` смешивает «~56% минут (45/81)» (deck-static) без оговорки, что речь-доставка ~54% — методически безвредно, но для аудита счётчика стоит уточнить.

### Single-cluster Р4 — **критический delta-вопрос: НЕ вернулся (YES, чисто)**

Был снят v3 «s24-консолидацией + Р5-усилением» (план §5: «max-кластер Р4 = 14.3% deck»). s22a добавил Р4 ещё +3 strict-in-мин — проверяю, не пробил ли порог >40% всех strict-in-минут:

| Раздел | strict-in мин (deck-static) | % всех strict-in-мин | % внутри раздела |
|---|---|---|---|
| Р0 | 0.0 | 0.0% | 0% |
| Р1 (s05/s08) | 4.5 | 10.0% | 40% |
| Р2 (s12/s13) | 6.5 | 14.4% | 65% |
| Р3 (s16/s17) | 6.0 | 13.3% | 63% |
| **Р4 (s21/s22/s22a/s23/s24)** | **14.0** | **31.1%** | 68% |
| Р5 (s25/s26/s27) | 9.0 | 20.0% | 80% |
| Р6 (s29/s30) | 5.0 | 11.1% | 49% |

**Р4 = 31.1% всех strict-in-минут (deck-static) / 27.1% (speech-delivery, s22a@2).** Порог single-cluster = одна секция держит **>40%** strict-in-минут deck. **31.1% < 40% — НЕ single-cluster.** s22a сдвинул Р4 с ~28% к 31.1% (deck) — заметно, но **в безопасной зоне с запасом ~9 п.п.**; Р5 (20%) остаётся вторым весомым кластером, распределение по 6 разделам сохранено (Р1–Р6 все ненулевые, ни один не доминирует). **s22a single-cluster НЕ вернул.** В пределах раздела Р4 68% — нормально для security-блока (это и есть failure-плотная зона по дизайну, не аномалия: план §2.2 намеренно концентрирует угрозы в Р4).

**Рекомендация оркестратору по `deck-part2.yaml ai_failure_judgment` (НЕ правлю — methodology-critic не редактирует):**
→ `count: 16` / `in_bucket_slides` (16 шт) / `partial_out` (20 шт) / слайдо-доля 16/36≈44% — **корректны as-is, менять НЕ нужно** (strict-#78-верно, s22a полностью in-bucket).
→ **P2 (доку, рекомендация, не артефакт-гейт):** `share_by_slides` (L448) фиксирует «~56% минут (45/81)» — это deck-static знаменатель (80.9→округл. 81). Speech-доставка даёт ~54% (s22a@2, не @3). Разночтение безвредно (обе >> порога), но для аудита: добавить полслова «(deck-static; speech-доставка ~54%)». Делегировать designer'у при следующем touch — механическая доку-правка, не контент, **не блокирует GATE C**.

## 3. Pacing ≤75 — РЕШАЮЩИЙ delta-вопрос — **≤75 ПОДТВЕРЖДЁН**

Реконсиль трёх чисел:

| Источник | Σ | Природа |
|---|---|---|
| `deck-part2.yaml slide_times_sum_min` | **80.9** | **статичная designer-оценка**: s01–s32 = 77.0 + 6 дивайдеров×0.3 = 1.8 + s22a@**3** = 80.9. Designer-flag «минутная пере-балансировка на methodology-critic re-confirm» — это и есть данная проверка. Дивайдеры здесь учтены аддитивно (×0.3), s22a по deck-бюджету 3 мин. |
| Наивная сумма speech-заголовков | ~78 | артефакт двойного учёта: per-fragment header-боксы — верхние оценки, дивайдер-beat'ы там же аддитивны |
| **speech.md DELIVERABLE (истина доставки)** | **72.9 active + ~1 буфер = ~74** | durations прочих фрагментов НЕ трогались (v1.2 baseline активных = 70.9) + s22a ведётся **2 мин** (не 3) → 70.9+2.0 = 72.9; дивайдер-beat'ы = устные переходы, свёрнуты в смежные слайды (не standalone +0.3 каждый) |

**Какова реальная длительность?** Истина доставки — **speech.md ~74 ≤ 75**, и она внутренне непротиворечива:
1. s22a ведётся **2 мин** (speech L403 заголовок + L599 self-report + preflight L35 «прогнать s22a с секундомером, при >95wpm снять 1 предложение deep-dive, НЕ резать асимметрию/урок»). Deck-бюджет 3 мин — designer-консервативная верхняя оценка; лектор ведёт в 2 → экономит 1 мин против deck.
2. **Компенсация консолидацией корректна и проверена:** curl ранее был рассеян по s25/s32 (1-фразовые callback-якоря) + Q&A-backup; speech v1.3 L599 явно: s25/s32-фразы остались 1-фразовыми (формулировка перефокусирована, не удлинена) — net эффект curl = **+2 мин s22a − ~0 = +2 мин**, поглощено буфером Q&A (был ~2, стал ~1). Это методически честная консолидация: один выделенный beat вместо размазанного якоря — **педагогически лучше** (концентрированный кейс с асимметрией-разбором даёт глубже retrieval, чем 2 разрозненных упоминания).
3. Дивайдеры (#101) — устные переходные beat'ы ~0.3, на доставке поглощаются переходами, не standalone-слоты (план §2.2: «+3×~0.3≈+1 мин, поглощается буфером»).

**Реконсиль:** deck slide_times 80.9 — статичная оценка верхней границы (s22a@3 + дивайдеры аддитивно); speech — истина доставки (s22a@2 + дивайдеры в переходах + durations не трогались). Speech-консолидация **корректно компенсирует** добавление s22a: реальная лекция ≈ **74 мин active + буфер ≤ 75**. Designer-flag 80.9 разрешён в пользу speech-доставки — это ожидаемое поведение (designer не решает pacing-баланс один; речь — артефакт доставки).
**WPM-нота:** s22a 483 слова/2 мин ≈ 242 wpm письменно — выше 95-DoD, НО фрагмент несёт `[медленно]` + 2 паузы-связки (тип «эталонный deep-dive», как s16 Replit). speech.md preflight L35/L601 предписывает прогон с секундомером + страховку (снять 1 контекст-предложение, НЕ резать strict-in-ядро). **WPM hard-rule — scope speech-writer DoD, не мой**; методически фрагмент структурно не создаёт неустранимый density-spike (есть встроенная страховка). Флагую информационно, не как P1 — pacing-вердикт по активной длительности ≤75 держится независимо.

→ **pacing ≤75 ПОДТВЕРЖДЁН** (не escalate). Реальная длительность ≈74 active + ≤1 буфер. Designer 80.9 = статичная оценка, speech 72.9/~74 = истина доставки; консолидация компенсирует s22a корректно и педагогически улучшает (концентрированный beat > рассеянный якорь).

## 4. ЦВ return-point 4 (s21+s23) НЕ разорван вставкой s22a — **ПОДТВЕРЖДЕНО (цел)**

Структура 4-го возврата центрального вопроса:
- **s21** (L365–379): «Четвёртая точка возврата центрального вопроса — **начинается здесь**, полностью замкнётся **через два слайда**» — открытие 4-го возврата, явно анонсирует разрыв на 2 слайда.
- **s22** (slopsquatting) + **s22a** (curl) — два security-кейса **внутри** разрыва.
- **s23** (L419): «Корпоративный код… **ЦВ-возврат 4 (полный)**» — замыкание.

Методически **нить ЦВ цела**: s22a — security-кейс, вставленный **между двумя половинами 4-го возврата ровно так же, как s22 был там раньше**. До #103 разрыв уже содержал s22 (slopsquatting) между s21 и s23 — s22a добавлен в ту же позицию того же класса (security-кейс Раздела 4b). s21 анонсирует «замкнётся через два слайда» — формулировка «через два слайда» остаётся **корректной по смыслу** (s22→s22a→s23: s23 по-прежнему слайд замыкания; «два слайда» = «не сразу, после кейсов» — нарративный смысл, не точный счётчик; устно лектор не называет номер). **Вставка однородным элементом (security-кейс среди security-кейсов) не рвёт арку** — наоборот, обе половины ЦВ-4 теперь обрамляют 3 однородных security-кейса (s21 уязвимый код / s22 slopsquatting / s22a curl), что усиливает тематическую когезию Раздела 4b перед замыканием s23. Нить **не разорвана**.

## Cross-cutting (delta #103)
- **strict-in:** 16/36=44.4% слайдов / 55.6% deck-мин / ~54% speech-мин — holistic в каждом из 3 артефактов (chapter ~69% non-decreasing), single-artifact concentration absent. ≥30% mandatory / ≥40% план — двойной запас. s22a = первый delta, корректно **добавляющий count** (полностью in-bucket, не partial-upside как тулы v1.2).
- **AI-Failure-правило:** s22a **усиливает** bucket — новый класс провала (AI масштабирует шум → людей-сопровождающих → supply-chain), не покрытый 4 прочими кейсами; с уроком + не-AI альтернативой + критерием + anti-хайп-балансом. Не дисклеймер — суждение. Counter-check «<30% ИЛИ single-artifact ⇒ REVISE» неприменим (false/false).
- **Single-cluster:** Р4 = 31.1% strict-in-мин (deck) / 27.1% (speech) — <40% порога, **не вернулся** (запас ~9 п.п.). Распределение по 6 разделам сохранено.
- **Sequence/cognitive load:** s22a введён после s22 (slopsquatting) — оба supply-chain/security-класса, прогрессия «вред коду → вред процессу/людям» простое→сложное соблюдена; нет термина-сироты (supply-chain определён в §4.5 inline + s22). Не overload — 1 кейс с одной несущей идеей (асимметрия стоимости).
- **Tone:** anti-hype-grep s22a видимого слоя = 0; регистр критически-уважительный; «виновата архитектура, не AI» — образцовый judgment-tone (учит различать, не обвинять инструмент). Rename сопровождающий + `[VFY-day-of]` тег — нейтрально (косметика/glossary-lock, не методика).
- **DoD:** strict-in ≥40% holistic PASS · AI-Failure ≥30% holistic PASS · single-cluster Р4 <40% PASS · pacing ≤75 PASS (speech-доставка ~74) · ЦВ-4 цел PASS · ни одна метрика не провалена → вердикт НЕ форсируется в REVISE.

## P2 (мелочь, не блокер, не артефакт-гейт)
- **P2-1 (доку-разночтение знаменателя).** `deck-part2.yaml ai_failure_judgment.share_by_slides` (L448) — «~56% минут (45/81)» (deck-static); speech-доставка даёт ~54% (s22a@2). Обе >> порога — методически безвредно, но для аудита счётчика стоит пометить модель. **Rec:** добавить «(deck-static; speech-доставка ~54%)» — делегировать designer'у при следующем touch. Не меняет count/вердикт, не блокирует GATE C. (Близкий: speaker notes s22a ~310 слов — на ~10 выше верха окна 150–300; связный студенческий текст без layout/«Лектору» — P2-незначимо, при touch можно ужать одно предложение, не блокер.)

## Сводка для оркестратора
- **VERDICT: APPROVE-WITH-POLISH** (0 P0, 0 P1, 1 доку-P2; counter-check по 4 условиям #103 — все false → НЕ REVISE; 1 реальная P2 в видимом счётчик-слое → -WITH-POLISH, не -CLEAN).
- **s22a = strict-in failure #5: YES.** Документированный провал (curl свернул bug-bounty под AI-slop) + урок (асимметрия фейк≈0/опровержение=часы → DDoS на внимание) + не-AI альтернатива (приватное раскрытие + барьер воспроизводимого PoC) + критерий «когда открытый процесс под AI деградирует» + anti-хайп-баланс («виновата архитектура, не AI»). Полностью in-bucket (не калибровка, не хайп). Усиливает AI-Failure-линию — новый класс провала, не дубль.
- **16/36 число подтверждено:** 16/36=44.4% слайдов / 55.6% deck-мин / ~54% speech-мин. count 15→16 **корректно** (s22a полностью in-bucket → добавляет count, в отличие от partial-upside тулов v1.2). chapter ~69% non-decreasing. Single-artifact concentration absent. `ai_failure_judgment` счётчик корректен as-is — менять НЕ нужно (только опц. доку-уточнение знаменателя, P2, делегировать designer'у).
- **Single-cluster Р4 НЕ вернулся: YES.** Р4 = **31.1%** всех strict-in-минут (deck-static) / 27.1% (speech-delivery) — <40% порога с запасом ~9 п.п. s22a сдвинул Р4 ~28%→31.1%, но не пробил. Распределение по 6 разделам сохранено (Р5=20% второй кластер). s24-консолидация v3 не отменена.
- **pacing ≤75 ПОДТВЕРЖДЁН** (НЕ escalate). Реальная длительность ≈ **74 active + ≤1 буфер** (speech.md истина доставки: 70.9 baseline + s22a@2 = 72.9). Deck 80.9 — статичная designer-верхняя-оценка (s22a@3 + дивайдеры аддитивно), разрешается в пользу speech-доставки. Консолидация рассеянных s25/s32-якорей в один s22a-beat корректно компенсирует +2 мин (поглощено буфером 2→1) и **педагогически улучшает** (концентрированный кейс > размазанный якорь). WPM s22a (242 письм.) — scope speech-writer DoD; есть встроенная страховка (preflight секундомер + снять 1 контекст-предложение, НЕ ядро) — флагую информационно, pacing-вердикт ≤75 держится независимо.
- **ЦВ-4 цел: YES.** s22a — security-кейс между двумя половинами 4-го возврата (s21 «начинается здесь» → s23 «полный»), ровно как s22 был там раньше; вставка однородным элементом усиливает когезию Раздела 4b, нить не разорвана.
- **GATE C:** методических блокеров от s22a / каскада #103 нет. Delta чистая; 1 P2 — доку-рекомендация оркестратору (знаменатель-уточнение в счётчике + опц. ужать notes s22a), не блокирует GATE.
