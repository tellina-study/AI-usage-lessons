---
id: s16
type: process
duration_min: 2
assertion: "Как работает чат: 6 шагов цикла"
learning_goal: "Снять магию чата + контроль через системный промпт + ограничение контекстного окна"
learning_outcomes: [LO1, LO4]
references: [anthropic-2024-effective-agents]
visual:
  pattern: dialog_cycle_compact
  primary: "Компактная схема диалогового цикла: USER слева в двух ролях (отправитель и получатель), посередине два горизонтальных блока (Сообщение → LLM → Ответ → USER), сверху — Системный промпт, объединяющийся с Сообщением через gold-arrow вниз. Под Ответом — «⋮ следующая итерация». Справа два gold callouts: «Контроль через системный промпт» и «Ограничение — контекстное окно (128k–1M токенов)». Внизу — bottom takeaway."
---

# Как работает чат: цикл диалога

## Assertion

Как работает чат: цикл диалога.

## Visual

Компактная схема диалогового цикла. В левом столбце — иконка пользователя в двух ролях (отправитель сверху, получатель снизу). По центру — два горизонтальных блока: «Сообщение» (вход пользователя) и «Ответ» (выход модели), один над другим. Справа крупный синий блок «LLM / модель». От «Системного промпта» вверху идёт gold-стрелка вниз, объединяясь с «Сообщением» — визуально показывая, что промпт прицепляется к каждому запросу. Стрелки: USER → Сообщение → LLM (вправо), LLM → Ответ → USER (влево). Под Ответом — «⋮ следующая итерация (история накапливается)». В правом столбце два gold callouts: «Контроль через системный промпт — инженерный рычаг разработчика» и «Ограничение — контекстное окно: 128k–1M токенов; старое выпадает». Внизу bottom takeaway курсивом: «Чат — это конвейер "собрать → подать → дописать → показать", а не магия».

## Speaker notes

Чат удобно обсуждать на бытовом уровне: задал вопрос — получил ответ. Но за этим простым опытом стоит вполне конкретный технический цикл. Понимание этого цикла снимает магию и даёт два практических навыка: как контролировать чат через системный промпт и как объяснить, почему чат «забывает» старые сообщения.

Шесть шагов цикла. Первый — пользователь вводит текст в окно чата: вопрос, инструкцию, фрагмент данных.

Второй — система берёт введённый текст и присоединяет его к системному промпту. Системный промпт — это заранее заданные разработчиком инструкции: «Ты корпоративный ассистент компании X. Отвечай на русском. Не давай юридических советов. Если не знаешь — скажи "не знаю"». Дополнительно подмешивается вся текущая история чата с начала сессии.

Третий — полный собранный пакет, то есть системный промпт плюс история плюс новое сообщение, отправляется в LLM как один большой текст. Модель не «помнит» прошлый разговор — она каждый раз получает его заново как часть входа.

Четвёртый — модель генерирует ответ токен за токеном на основе вероятностного распределения следующего токена при заданном контексте.

Пятый — ответ модели дописывается в историю чата на стороне системы. С этого момента он часть контекста для всех следующих обращений в этой же сессии.

Шестой — ответ показывается в интерфейсе чата. Пользователь читает, отвечает, и цикл повторяется с шага один. Но теперь история уже больше, и LLM при следующем обращении получит весь набор: системный промпт плюс предыдущие пары вопрос-ответ плюс новое сообщение.

Из этого цикла два практических следствия. Первое — системный промпт это инженерный рычаг. Через него разработчик задаёт рамки чата: тон, роль, ограничения, обязательные форматы ответа. Один и тот же ChatGPT в роли корпоративного ассистента и в роли обычного помощника ведёт себя по-разному именно из-за системного промпта.

Второе — у каждой модели есть контекстное окно: максимальное число токенов за один запрос. Для современных моделей это десятки и сотни тысяч токенов. Когда история чата перестаёт помещаться в окно, старые сообщения выпадают. Поэтому чат «забывает» детали из начала длинного разговора.
