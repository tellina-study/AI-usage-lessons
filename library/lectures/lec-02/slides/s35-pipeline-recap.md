---
id: s35
type: assertion_visual
subtype: schema_pipeline
section: "Раздел 6. Финал"
duration_min: 2
assertion: "Мы рассмотрели, как работает модель, — соберём картину: новые темы не добавили стадий, они встроились в существующие"
learning_goal: "Recap-сборка конвейера с наложением новых слоёв (KV-cache → внимание, thinking → цикл, схема → сэмплинг, glitch-токены → токенизация); диагностическое дерево"
learning_outcomes: [LO1]
chapter_ref: "§5.1 (chapter-part3.md) [for-slide-s35]"
visual_brief: "Горизонтальный конвейер 4 стадий с RIGHT_ARROW, Ocean rounded boxes: Токенизация → Эмбеддинги → Внимание → Сэмплинг → «следующий токен» (gold) с loop-стрелкой обратно. НАД/ПОД стадиями — 4 плашки-наложения новых тем, каждая соединена пунктиром со своей стадией: «glitch-токены → свойство словаря на стадии токенизации», «KV-cache → внутри внимания; prompt caching — надстройка на границе запросов», «structured outputs → фильтр сэмплинга», «reasoning-токены → тот же цикл, часть выхода помечена „черновик“». Плашки-наложения teal с gold-обводом."
---

# Visible content

## Title bar
«Мы рассмотрели, как работает модель, — соберём картину»

## Body
[Горизонтальный конвейер 4 стадий, RIGHT_ARROW, loop-стрелка]

**Токенизация** → **Эмбеддинги** → **Внимание** → **Сэмплинг** → *следующий токен* ⟲

[4 плашки-наложения, пунктир к своим стадиям]
- **Glitch-токены** — свойство словаря на стадии токенизации
- **KV-cache** — внутри внимания; **prompt caching** — надстройка над ним на границе запросов
- **Structured outputs** — фильтр на стадии сэмплинга
- **Reasoning-токены** — тот же цикл; часть выхода помечена «черновик»

[Gold callout]
**Конвейер — диагностическое дерево: по симптому почти всегда угадывается стадия-виновник. Спрашивайте: «на какой стадии это происходит?»**

## Speaker notes

Соберём схему. Конвейер вывода — четыре стадии, замкнутые в цикл: токенизация превращает текст в идентификаторы из зафиксированного словаря; эмбеддинги — идентификаторы в выученные векторы; внимание обогащает векторы контекстом через три проекции и выдаёт распределение вероятностей следующего токена; сэмплинг выбирает один токен и дописывает его в контекст. Это тот самый чёрный ящик «модель» из Лекции 1 — теперь прозрачный.

Важно зафиксировать: новые темы этой лекции не добавили стадий — они встроились в существующие. Glitch-токены — свойство словаря на стадии токенизации. KV-cache живёт внутри стадии внимания, а кэширование промптов — коммерческая надстройка над ним на границе запросов. Structured outputs — фильтр на стадии сэмплинга: обнуление вероятностей невалидных токенов. Reasoning-токены — не новая стадия, а тот же авторегрессионный цикл, часть выхода которого помечена «черновик». Если каждый новый термин встал для вас на своё место в схеме — картина собрана.

И самый компактный способ унести эту схему с собой: конвейер работает как диагностическое дерево — по симптому почти всегда угадывается стадия-виновник. Модель «не видит» очевидного в тексте — буквы, цифры, опечатки — токенизация. Поиск приносит тематически близкий мусор — эмбеддинги и граница сходства. Ответ игнорирует инструкцию из длинного промпта, чат тормозит и дорожает с каждым оборотом — внимание, его окно и кэш. Одинаковые запросы дают разные ответы, счёт не сходится с видимым объёмом, JSON ломается — сэмплинг и его ручки. Привычка спрашивать «на какой стадии конвейера это происходит?» превращает лекцию из справочника в рабочий инструмент отладки.
