---
id: s05
type: assertion_visual
section: "Раздел 1. Промпт и его границы"
duration_min: 2
assertion: "Дефолт — один вызов с хорошим промптом; любой подъём по лестнице оплачивается требованием задачи"
learning_goal: "Точка отсчёта лестницы + распределение бремени доказательства"
learning_outcomes: [LO7]
chapter_ref: "§1.1 [for-slide-s05]"
visual_brief: "Слева — крупный блок «1 вызов = минимум стоимости + латентности + максимум предсказуемости». Справа — список «что добавляет каждый подъём»: RAG → конвейер индексации/хранилище/деградация retrieval; агент → внешние вызовы/петли/недетерминизм/поверхность атаки. Gold — фраза-правило «усложнение оплачивается требованием задачи»."
interaction: none
---

# Visible content

## Title bar
«Дефолт — один вызов»

## Body
[Слева — главный блок в Ocean rounded box]

**Один вызов LLM с хорошим промптом**
- минимальная стоимость (один проход)
- минимальная латентность (нет лишних обращений)
- максимальная предсказуемость (нет петель, нет retrieval, который тихо деградирует)

[Справа — «во что обходится каждый подъём», 2 блока]

**Добавляешь RAG** → конвейер индексации + векторное хранилище + компонент retrieval (может молча деградировать) + метрики его качества

**Добавляешь инструменты / цикл** → внешние вызовы (падают, тормозят) + петли (расходятся) + недетерминизм траектории + новая поверхность атаки

[Gold callout, низ]
**Не усложняй архитектуру без причины, выраженной в требованиях задачи. Это распределение бремени доказательства, а не примитивизм.**

## Speaker notes

Прежде чем выбирать между RAG, fine-tuning и агентами, зафиксируем точку отсчёта. Самая дешёвая, надёжная и предсказуемая AI-архитектура — это один вызов модели с хорошо составленным промптом. Один вызов — в Лекции 2 это single-shot инференс — не имеет ни внешнего поиска, ни цикла, ни инструментов: на вход промпт, на выход ответ, памяти между вызовами нет. У него минимальная стоимость, минимальная латентность и максимальная предсказуемость: нет петель, которые расходятся, нет retrieval, который тихо деградирует.

Зафиксируем границу, несущую для всего раздела: модель знает только то, что попало в промпт, плюс то, что осело в её весах при обучении. Это единственный источник информации, доступный одному вызову. Всё, чего нет ни в промпте, ни в весах, модели неизвестно, и тогда она либо честно откажется отвечать, либо сгенерирует правдоподобный, но ни на что не опирающийся текст. Именно эта граница — причина, по которой дальше появляется RAG, расширяющий то, что «в промпте», и fine-tuning, расширяющий то, что «в весах».

Инженерное правило по умолчанию: не усложнять архитектуру без причины, выраженной в требованиях задачи. Это не призыв к примитивизму, а распределение бремени доказательства. По умолчанию архитектура — один вызов. Любой переход выше требует явного обоснования: вот требование задачи, которое одним вызовом не закрывается, поэтому я добавляю RAG, инструмент или цикл. Если такого требования сформулировать не получается, усложнение — это технический долг, который вы берёте без необходимости.

Полезно зафиксировать, во что обходится каждый подъём, чтобы бремя доказательства было перечнем. Добавляя RAG, вы добавляете конвейер индексации, векторное хранилище, компонент retrieval, который может молча деградировать, и метрики его качества. Добавляя инструменты и цикл агента — внешние вызовы, которые падают и тормозят, петли, которые расходятся, недетерминизм траектории и новую поверхность атаки. Ни один из этих пунктов не появляется у одного вызова. Поэтому «один вызов по умолчанию» — не консерватизм, а отказ оплачивать инфраструктуру и режимы отказа, которых задача не требует.
