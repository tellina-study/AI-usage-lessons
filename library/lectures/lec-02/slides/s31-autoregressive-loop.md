---
id: s31
type: process
subtype: schema_cycle
section: "Раздел 4. Сэмплинг и генерация"
duration_min: 2.5
assertion: "Цикл: предсказали токен → дописали в контекст → предсказываем следующий"
learning_goal: "Авторегрессионный цикл как сборка всех стадий конвейера в единый процесс"
learning_outcomes: [LO1]
chapter_ref: "§4.6 (chapter-part2.md) [for-slide-s31]"
visual_brief: "Замкнутый цикл 5 шагов с RIGHT_ARROW и явной loop-стрелкой возврата, Ocean rounded boxes: (1) текущий контекст → (2) прямой проход (gold-обвод; подпись «токенизация → эмбеддинги → внимание») → (3) распределение на ~200k токенов → (4) сэмплинг (T / top-p / схема) → (5) токен дописан в контекст → возврат к (1). Точка входа помечена явно. Stop-условие мелким: «стоп-токен или max_tokens» — как цикл останавливается. Рядом — как цикл ломается: v3.1 (#183 round 3) реальная иллюстрация Отто Уббелоде (1909, общественное достояние) к сказке братьев Гримм «Сладкая каша» / «Горшочек каши» — гора каши, заполняющая деревню, метафора вырожденного цикла повторов без стоп-условия, с атрибуцией мелким шрифтом. Sub-caption про stateless."
---

# Visible content

## Title bar
«Предсказали токен → дописали в контекст → предсказываем следующий»

## Body
[Замкнутый цикл 5 шагов, Ocean rounded boxes, явная стрелка возврата]

**(1) Текущий контекст**
системный промпт + история + запрос + уже сгенерированная часть ответа

**(2) Прямой проход** *(gold)*
токенизация → эмбеддинги → все слои внимания

**(3) Распределение**
вероятности на все ~200 тыс. токенов словаря

**(4) Сэмплинг**
один токен — по правилам температуры / top-p / схемы

**(5) Токен дописан в контекст**
⟲ возврат к (1)

[Stop-условие мелким — как цикл останавливается]
До специального стоп-токена **или** до `max_tokens` — обрыв мгновенный, хоть на середине JSON-поля.

[Мини-иллюстрация «горшочек, не вари!» — как цикл ломается, угловое размещение]
Вырожденный цикл повторов: модель «зацикливается» на одном токене/фразе и генерирует полотно повторов вместо остановки.

[Строка практики]
**Практика:** `repetition_penalty` / `frequency_penalty` снижают вероятность буквальных повторов; `max_tokens` — страховка от бесконечного цикла, если стоп-токен не сработал.

[Sub-caption]
*Каждый шаг — без состояния: вся «память» живёт в контексте, который подаётся целиком (KV-cache делает повторную подачу дешёвой, не отменяя её логически).*

## Speaker notes

Соберём конвейер в цикл — это ядро всей картины. Авторегрессионная генерация: текущий контекст — системный промпт, история, запрос и уже сгенерированная часть ответа — проходит прямым проходом через токенизацию, эмбеддинги и все слои внимания; на выходе — распределение вероятностей следующего токена; сэмплинг выбирает один токен; токен дописывается в контекст; цикл повторяется до специального стоп-токена или лимита max_tokens. Каждый шаг — без состояния: вся «память» живёт в контексте, который подаётся целиком, — с поправкой на KV-cache, который делает повторную подачу дешёвой, не отменяя её логически. Ответ печатается на глазах не для красоты — это физический темп: токен за проход.

Цикл может остановиться правильно — а может сломаться. Правильная остановка — специальный стоп-токен или лимит max_tokens; обрыв по лимиту мгновенный, хоть на середине JSON-поля, и постобработка обязана этот случай ловить. Поломка — вырожденный цикл повторов: модель зацикливается на одном токене или короткой фразе и генерирует полотно повторов вместо содержательного продолжения — «горшочек, не вари!» из сказки, только горшочек — это модель, а каша — токены. [Место для личной истории лектора: если у вас был реальный случай, когда модель ушла в такой цикл на проде или в тесте, — расскажите его здесь своими словами.] Механизм тот же цикл: если распределение на каком-то шаге сильно смещено в сторону уже сгенерированного текста (например, из-за низкой температуры или структуры промпта), модель раз за разом выбирает похожий токен, и авторегрессия закрепляет паттерн. Практика: repetition_penalty и frequency_penalty снижают вероятность буквальных повторов уже сгенерированных токенов; max_tokens в любом случае остаётся страховкой — даже если модель зациклилась, генерация не продолжится бесконечно.

Сквозная трассировка — соберём весь конвейер на одном примере. Вы отправляете «Столица Франции —». Токенизация: текст режется на три-четыре токена по таблице, зафиксированной до обучения. Эмбеддинги: каждый идентификатор забирает свой вектор из входной таблицы. Внимание: на позиции после тире Query «спрашивает», Key и Value слов «столица» и «Франции» отвечают весами — и они, кстати, уже лежат в кэше и не пересчитываются. Выход: распределение, где «Париж» получил, скажем, 0.93. Сэмплинг: при нуле температуры берём максимум — «Париж»; цикл делает ещё один оборот и выбирает стоп-токен. Если бы был включён режим рассуждения — перед «Парижем» цикл намотал бы черновые токены; если бы стояла JSON-схема — распределение на каждом шаге фильтровалось бы грамматикой. Один запрос — все стадии, все ручки. Терминологическая норма курса: «авторегрессионный», не «авторегрессивный».
