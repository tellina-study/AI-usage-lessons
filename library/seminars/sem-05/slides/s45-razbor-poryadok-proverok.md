---
id: s45
type: answer_breakdown
duration_min: 1.5
assertion: "Правильный ответ — не одна проверка, а последовательность из трёх, и порядок в ней имеет значение"
learning_goal: "Разбор пяти позиций ответа зала (R3-4, новый слайд), сводящийся к порядку: нужно ли вообще → с какими правами → по какой цене"
visual:
  pattern: answer_breakdown_table
  primary: "Мелким шрифтом вверху — сам вопрос для контекста. Ниже — таблица разбора пяти позиций: вариант · где это работает · почему здесь ещё рано — и что вместо. Нижняя плашка — не таблица, а ПОСЛЕДОВАТЕЛЬНОСТЬ из трёх пронумерованных проверок: 1 нужно ли вообще · 2 с какими правами · 3 по какой цене."
---

# Разбор: порядок трёх проверок

## Assertion

Правильный ответ — не одна проверка, а последовательность из трёх, и порядок в ней имеет значение.

## Visual

Мелким шрифтом вверху — вопрос для контекста: «Что вы проверите до подключения?»

Таблица разбора:

| Вариант | Где это работает | Почему здесь ещё рано — и что вместо |
|---|---|---|
| «Ничего — сервер официальный, вендорский» | Как эвристика снижения риска — да; как единственная проверка — нигде | Официальность не иммунитет: у эталонного файлового сервера самого разработчика протокола нашли две уязвимости выхода за пределы песочницы. Вместо: проверить права и цену, даже если сервер вендорский |
| «Посмотрю, сколько у сервера звёзд» | Нигде: популярность и безопасность здесь не коррелируют | Сканирования находят проблемы у трети-двух третей случайных выборок. Вместо: смотреть на права, а не на звёзды |
| «Посчитаю, сколько токенов это добавит» | **Всегда стоит: величина измеримая** | Не рано — но это **третий** вопрос, а не первый: можно отлично оптимизировать стоимость того, что вообще не следовало подключать |
| «Проверю, какие права даю токену» | **Всегда: главная из проверок** | Не рано — но не отвечает на вопрос, нужно ли подключение вообще |
| «Спрошу, не хватит ли обычной команды в терминале» | **Именно то, с чего надо начинать** | **Не рано: это первый вопрос из трёх.** Перестаёт хватать, когда доступ нужен регулярно, из нескольких сессий, с сохранением состояния |

Нижняя плашка — не таблица, а последовательность:

**1.** а нужно ли подключение вообще → **2.** с какими правами → **3.** по какой цене в контексте

## Speaker notes

Два ответа разбираются подробнее прочих.

«Ничего, сервер официальный» — не высмеивать, это разумная эвристика в большинстве областей: «Обычно это хорошая эвристика. Здесь — недостаточная: у эталонного файлового сервера самого разработчика протокола нашли две уязвимости выхода за пределы объявленной песочницы. Неограниченный доступ к файлам, в части сценариев — выполнение кода. Официальность снижает вероятность, но не делает проверку ненужной.»

«Посмотрю на звёзды» — здесь хороши цифры: «Сканирования публичных реестров: треть выборки в тысячу серверов — с критическими уязвимостями; две трети выборки в тысячу восемьсот — хотя бы с одной проблемой; тридцать шесть и семь процентов из выборки в семь тысяч уязвимы к подделке серверных запросов. Список серверов — это список ссылок, а не реестр с проверкой. Популярность и безопасность в этой области просто не коррелируют.»

И свести разбор к порядку, а не к перечню:

«Правильный ответ — это не одна проверка, а последовательность из трёх, и порядок в ней имеет значение. Первое: а нужно ли подключение вообще — не хватит ли обычной команды. Второе: с какими правами. Третье: по какой цене в контексте. Начинать с третьего — популярная ошибка: можно отлично оптимизировать стоимость того, что вообще не следовало подключать.»
