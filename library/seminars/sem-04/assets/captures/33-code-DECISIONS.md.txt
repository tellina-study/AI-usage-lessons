$ git clone https://github.com/tellina-study/signup-landing-demo.git && cd signup-landing-demo
$ git switch seminar-4-arc
$ git show 94c5378:DECISIONS.md   # первая содержательная запись, кейс 2.1

# Decisions

Append-only log of why, in date order.

## 2026-09-24 — сторонние виджеты в форму не добавляем
Форма — два поля (имя, email), хватает нативных `required`/`pattern` в `index.html`.
Сторонний виджет — лишняя зависимость и лишние килобайты в сборке ради этого объёма.

# Комментарий: настоящий файл настоящего репозитория, переносить на слайд s35 дословно.
# Дата записи (2026-09-24) — реальная дата коммита 94c5378, а не дата по сквозному календарю
# занятия: календарь занятия («конец первой недели») — условность повествования и с датами
# git намеренно не сводится. Снято 2026-09-24.
