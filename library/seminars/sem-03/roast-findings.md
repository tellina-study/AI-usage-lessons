---
title: "Семинар 3 — сводные находки прожарки (6 точек зрения)"
status: working
date: 2026-09-15
panel: [methodology-critic, fact-checker(параллельно), senior-engineer, student-simulator, presentation-critic, consistency-checker]
note: "Консолидированный список для фикс-пасса. Дек↔источники — чисто (P0=0 у consistency). Основные фиксы: код 3.2, вёрстка-переполнение, [VERIFY-DAY-OF] в видимом слое, рассинхрон brief/guide/rubric."
---

# Сводные находки прожарки Семинара 3

## P0 — обязательно (ошибки/нарушения правил)

- **P0-1. Битый код в case-3.2 §3 (Фича 3): `cut.rrindex(" ")`** — метода нет, `AttributeError`. → `cut.rindex(" ")`/`rfind`. Продублировано в `spec_sem03.py` (слайды 3.2). Подрывает тезис кейса «код надёжен». [methodology, senior]
- **P0-2. case-3.2: бюджет многоточия без гарантии длины** — `text[:limit-1] + "…"` не гарантирует `len(result) ≤ limit`; нет `assert`. Считать `limit - len("…")` + assert. [senior]
- **P0-3. case-3.2: единицы длины не разведены** — `len()` (кодовые точки) выдаётся за «символы», но SMS-сегмент = GSM-7/UCS-2, а `\d` в маскировании карты ловит Unicode-цифры (арабо-индийские/полноширинные). Для PCI/SMS это вредный совет. → развести кодовая точка ≠ графема ≠ UTF-16/GSM-юнит; `\d`→`[0-9]`/`re.ASCII`; упомянуть NFC-нормализацию. [senior]
- **P0-4. case-3.2 §Фича 1: `[A-ZА-Я]` в валидаторе пароля** теряет `Ё/ё`, не-ASCII буквы → ложные отказы. → Unicode-классы (`regex \p{Lu}/\p{Ll}`) либо явная ASCII-only оговорка. [senior]
- **P0-5. `[VERIFY-DAY-OF]` виден в visible body слайдов** — `spec_sem03.py`: case-2.1 `resolution.example.text` и case-3.2 `complication.body`. Нарушение анти-паттерна CLAUDE.md. → убрать из всех visible-полей (title/lead/body/sample/example.text/cols), перенести флаг в `notes`. [presentation, student]
- **P0-6. case-3.1 concept: механика температуры только жаргоном на слайде** (логиты/softmax), понятная арифметика — только в заметках; concept стоит ДО наглядного variants. → на слайде дать понятную суть (или поставить variants раньше/усилить concept), убрать «пустой» жаргон без пояснения. [student]

## P1 — важное

### Вёрстка (presentation) — переполнения (libreoffice нет, посчитано по геометрии)
- **P1-L1. breakdown с 6 компонентами не влезает (~1.5″ перелив)** — s09(1.1? — проверить), 2.1, 3.1. → движок: ограничить показ 5 компонентами / уменьшить шрифт / role ≤50 симв; или разнести.
- **P1-L2. variants sample >3 строк** (case-1.1 «многоязычная…», case-3.1 T=1.3) → sample ≤100 симв, вынести имена моделей в use.
- **P1-L3. scheme из 5 нод — подписи не влезают в ноду 2.09″** → подписи ≤20 симв или `label_size=11`, `node_h` больше.
- **P1-L4. excard text длинный (>150 симв) вылезает** (case-3.2) → ≤150 симв, длинное в notes; или h больше.
- **P1-L5. context: длинные label карточек (>22 симв) переносятся, съедают value; callout впритык** → короче label, поднять карточки.
- **P1-L6. concept steps overflow при длинных пунктах** (3.2) → 13.5pt / короче пункты.

### Структура/дидактика
- **P1-S1. Тройной повтор scheme→breakdown→resolution** (nodes ≈ logic ≈ howto) утомляет. → в breakdown убрать дублирующую «Логику реализации» (=scheme), оставить компоненты+комментарии. [student, presentation]
- **P1-S2. Аббревиатуры без расшифровки на видимых слайдах**: MTEB/ruMTEB, ANN, BPE, WinoBias, F1, LLM-as-judge. → расшифровка при первом появлении в visible body. [student]
- **P1-S3. structured output только назван, без «что это»/параметра API** (2.1, 3.1). → одна строка сути на слайде (strict = модель не вернёт токен вне схемы; параметр response_format/json_schema). [student]
- **P1-S4. closing не замыкает арку** — три механизма не сведены в один принцип «механизм = граница, которую знаешь в обе стороны». → одна строка-замок. [methodology]
- **P1-S5. eval-размер: «функция задачи, не 200» декларируется, но 1.1/2.2 сводят к «50–100» без «как считать под задачу»** → 1–2 фразы по образцу case-1.2 §3.3. [methodology]

### Инженерная реалистичность (senior) — доработать в case-*.md
- **P1-E1. span-проверка точным вхождением ломается на OCR/PDF** (2.1, 3.1) → нормализация обеих сторон (whitespace/NFC/casefold) перед `in`, иначе всплеск ложных эскалаций.
- **P1-E2. L2-нормализация без защиты от нулевого вектора** (1.1/1.2) + не сказано про pgvector-оператор и HNSW `ef_search`/recall↔латентность.
- **P1-E3. recall меряется brute-force, латентность обещается на ANN** — мерить на той же ANN-конфигурации.
- **P1-E4. MRR/recall@k при нескольких релевантных доках и транзитивность кластера дедупа** — развести попарные и кластерные метрики.
- **P1-E5. `error_code` как гейт противоречит собственному провалу «слишком общий ключ»** — оговорить связку + мониторинг дрейфа формата.
- **P1-E6. logprobs как «сигнал уверенности» недоступны у части провайдеров** (в т.ч. РФ-контур GigaChat/YandexGPT) → деградация (span+кросс-проверки без logprobs).
- **P1-E7. сверка правовой ссылки «нормализацией строк» занижена** — нужен парсер в каноническую форму (статья/пункт/кодекс).
- **P1-E8. `num2words` подан как парсер прописи, а он число→слова** — назвать корректный инструмент для «пропись→число».
- **P1-E9. schema `required`+`additionalProperties:false` конфликтует с советом «null для отсутствующих»** — тип `["number","null"]`.
- **P1-E10. temperature=0 + top_p=0.9 в запуске А противоречит собственному совету не комбинировать**; у reasoning-моделей T/top_p могут игнорироваться — оговорка рядом с кодом.

### Согласованность (consistency) — обёртки отстали от дека
- **P1-C1. facilitator-guide.md — старые кейсы + НЕВЕРНАЯ нумерация слайдов** (ссылается на s04-s18 старой раскладки; 3.2 = T=0-аудит вместо токенизации). Полная перезапись под 43-слайдовый дек (блоки s03-s16 / s17-s28 / s29-s42).
- **P1-C2. brief.md — старый состав** (вики-поиск 1.1, T=0-аудит 3.2, Блок 3 «детерминизм/воспроизводимость», термин «пивот», домашнее чтение про T=0). Переписать §«Что вас ждёт»/§«Домашнее чтение», переименовать Блок 3.
- **P1-C3. rubric.md — примеры границ ссылаются на удалённые кейсы** (стр. 40 «T=0 в 3.2», стр. 59 «антонимы-поиск/недетерминизм T=0»). Обновить под новый роестр.

### Методические мелочи (в case-*.md)
- **P1-M1. case-2.1 §4: рваная нумерация 7→10→8→9** → перенумеровать 1–9.
- **P1-M2. stray-теги `</content></invoke>` в хвостах case-2.1, case-2.2** → удалить.

## P2 — полировка
- case-3.2 forward-dated модели (GPT-5.5/5.6) — подавать как иллюстрацию паттерна, [VERIFY-DAY-OF] в notes. [methodology, fact-checker зона]
- case-design.md/outline.md — снять «proposal/ЗАМЕНА», пометить принятый состав (токенизация). [consistency]
- concept scheme-нода протыкает контейнер на 0.03″; gold-контраст проверить при рендере; few-shot плейсхолдер без примера (3.1). 
- Все пороги (τ, T, recall) честно помечены «калибруй на своих» — не трогать.

## Fact-check — получен (технический слой ЧИСТ; галлюцинированных arXiv нет)

**P0 (инверсия источника — ОБЯЗАТЕЛЬНО):**
- **FC-P0-1. case-1.1 §5.2 инвертирует arXiv 2409.18511** («Do We Need Domain-Specific…»): реальный вывод — доменные модели НУЖНЫ (general проваливается на домене), а кейс подаёт как «общая обходит специализированную». Убрать эту ссылку из аргумента или переписать §5.2 корректно.
- **FC-P0-2. case-1.1 §5.2 инвертирует/упрощает ChEmbed (arXiv 2508.01643)**: доменная ВЫИГРЫВАЕТ на retrieval (nDCG@10 0.82→0.91); общая лучше только на не-retrieval. Переписать: «доменная лучше на retrieval, общая — на classification/clustering → eval под свою функцию».

**P1 (fact):**
- **FC-P1-1. case-1.1 §5.1: анекдот «5/7/2 на юр-домене» misattributed** к arXiv 2607.23507 (он про англ. T3EM). Реальный источник — Nayak, Medium «Stop Picking Embedding Models Off the MTEB Leaderboard» (сен 2026). Переприписать; arXiv оставить как general framework.
- **FC-P1-2. case-2.2 §5.3: verbosity bias** подкреплён 2606.19544, которая его почти НЕ находит (<0.011). Взять корректный источник или смягчить. Position/self-enhancement — оставить.
- **FC-P1-3. case-2.1 §5.2: Deloitte** — ~A$290k это ВОЗВРАТ (AUD), не «стоимость контракта» (~A$439k). Уточнить.
- **FC-P1-4. case-2.1/2.2 §5.3: «самооценки схлопываются в 0.9/1.0»** сильнее, чем 2412.14737 (надёжность зависит от способа запроса). Смягчить до «ненадёжна по умолчанию, зависит от prompt; pre-2025 склонна к переоценке».

**Подтверждено верным (не трогать):** Zheng 2311.10054; Dziri 2305.18654 (59/4/0); WinoBias 21.1 F1; Stanford 17/33/43%; Air Canada $812.02; OpenAI 100% vs ~86%; ruMTEB/GigaEmbeddings; BPE 1508.07909; Grafana #26863; SemHash 1.8M/83с; арифметика температуры (2.0/1.0/0.0→0.67/0.24/0.09); MRR-пример 0.566. Технический слой (температура/токенизация/structured output/cosine/MRR) — корректен.

Все `[VERIFY-DAY-OF]` держать в `notes`, НЕ в visible body (см. P0-5).
