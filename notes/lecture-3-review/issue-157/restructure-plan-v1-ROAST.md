# Issue #157 — Лекция 3 структурный ревью: план реструктуризации + ROAST

Статус: **draft для approval** (Roast-Before-Implement, до Phase 2 review + любой subagent-работы)
Comment ID belong to terminal a8c37452 (orchestrator) — маппинг закрытия присылается ему по завершении.

## 0. Текущая карта деки (36 слайдов, page_number = позиция)

| pos | file id | раздел |
|---|---|---|
| 1-4 | s01,s02,s03,s04 | Разд.0 Открытие |
| 5 | s04a (divider) | Разд.1 |
| 6-9 | s05,s06,s07,s08 | Разд.1 Промпт |
| 10 | s09 (divider) | Разд.2 |
| 11-14 | s10,s11,s12,s13 | Разд.2 RAG |
| 15 | s13a (divider) | Разд.3 |
| 16-20 | s13b,s14,s15,s16,s17 | Разд.3 Fine-tune |
| 21 | s18 (divider "API·MCP·агенты+безопасность") | Разд.4 |
| 22-26 | s19,s20,s21,s22,s23 | Разд.4 |
| 27 | s23a (sub-divider "Безопасность") | Разд.4 |
| 28-29 | s24,s25 | Разд.4 (security) |
| 30 | s25a (divider) | Разд.5 |
| 31-36 | s26,s27,s28,s29,s30,s31 | Разд.5 Фреймворк |

Owner comments используют page_number как "sNN" — сверено построчно со всеми 29, конфликтов с deck.yaml порядком нет.

## 1. Стратегическая проблема №1 — Раздел 4

**Диагноз (подтверждает #234/#235):** заголовок дивайдера s18 — «API · tools · MCP · агенты + безопасность», подзаголовок sub-divider s23a — «РАЗДЕЛ 4 · БЕЗОПАСНОСТЬ». Студент видит два конфликтующих фрейма одного раздела. Раздел самый большой (9 контентных слайдов), но explicit "coding agents" контента, которым реально пользуются студенты (Claude Code, Cursor, Copilot, open-source клоны типа OpenClaw), — **нет вообще**. Это content gap, не опечатка.

**Предложение — распустить текущий Раздел 4, собрать новый «Раздел 4. Агенты»:**

| Было (9 слайдов) | Стало (7 слайдов) | Что произошло |
|---|---|---|
| s18 divider "API·MCP·агенты+безопасность" | divider "Раздел 4. Агенты" | title убирает «+безопасность» — оно возвращается ниже как естественная часть нарратива, не отдельный ярлык |
| s19 API-layer (structured output/function calling/caching) | **merged** slide "От чат-бота к агенту: 4 механизма" | s19+s20 объединены в один слайд (structured output, function calling, caching, MCP) — закрывает #228 ("MCP упомянуть") и #229 ("почему отдельно?" — теперь не отдельно) |
| s20 MCP standard | → merged выше | |
| s21 agent loop | agent loop (как есть, `check`-callback — сильный слайд, не трогаем) | |
| s22 workflow vs agent | workflow vs agent + добавлена идея #231 (workflow/agent могут быть вложены друг в друга, пример) | |
| — | **NEW: обзор coding-агентов** (Claude Code / Cursor / Copilot / OpenClaw-класс) — через уже введённые понятия (agent loop, workflow vs agent, least-privilege) как «вот как это выглядит в инструменте у вас на ноутбуке» | закрывает content gap из issue + даёт #230 (сложный пример с инструментами+RAG) естественным образом — coding agent = tools+RAG(codebase search)+function calling в одном |
| s23 agent failures ($4200/reliability/multi-agent) | agent failures, + добавлена 4-я карточка **prompt injection** (перенесена из s25), смягчено «правильно» (#232), добавлена база сравнения к «$4,200» (#233) | |
| s23a sub-divider "Безопасность" | **удалён** | нет больше конфликтующего заголовка |
| s24 data-in-chain / ZDR | **свёрнут в caveat-блок** внутри merged-слайда «4 механизма» (там уже частично дублируется предупреждение про MCP-доверие) — компактно, без отдельного дивайдера | закрывает #234 напрямую |
| s25 tool attacks / prompt injection | **распределён**: правила защиты (least-privilege/isolation/HITL/allowlist) — в merged-слайд «4 механизма»; сам кейс (GitHub MCP heist) — 4-я карточка в agent failures | |

Итог: 9 → 7 слайдов, контент безопасности **не удалён** (это нарушило бы AI-Failure/Judgment Rule и cross-cutting themes order из памяти — «инфобез» обязателен), а перераспределён так, чтобы не было отдельного «раздела про безопасность» с несовпадающим заголовком. Освобождается 2 слайда бюджета — идут в Раздел 1 (см. §3).

**РИСК/ROAST этого решения:** слияние s19+s20 в один слайд — плотность контента высокая (4 механизма на одном слайде вместо 2×2). Presentation-designer должен визуально ужать (карточки компактнее), иначе слайд станет "нечитаемым" — ровно та претензия, которую владелец предъявил к матрице s27. **Verification point:** после первого рендера — explicit visual-sweep этого слайда на плотность.

## 2. Стратегическая проблема №2 — s32 (файл s27, матрица выбора)

**Диагноз:** Раздел 5 сейчас содержит ТРИ инструмента выбора подряд — лестница (s26), матрица 7×7 (s27), чек-лист 8 пунктов (s28). Матрица — 49 ячеек — вероятно и есть источник «ничего не понятно». Формат "для архитектора" (оси типа «аудируемость», «риск петель») не даёт быстрого ответа **студенту здесь и сейчас**, который чаще пользователь чата/агента, чем архитектор системы.

**Предложение — заменить матрицу (s27) на 2 слайда, чек-лист (s28) слить внутрь:**

- **Новый слайд A «Как выбрать — план решения»** (замена s27+s28): тот же 8-шаговый чек-лист, но визуализирован как **flowchart сверху вниз** (стрелки, да/нет-ветвления), а не таблица — студент трассирует свою задачу по стрелкам вживую. Worked example (задача A → RAG) остаётся как трассировка ОДНОГО пути на этой же схеме. Mini-apply (задача B) — как было.
- **Новый слайд B «Как работать с этим прямо сейчас»** (NEW, отвечает на вторую часть запроса — «не только архитекторам, а студентам, как строить взаимодействие с агентами/чатами»): переводит те же оси на бытовой словарь: «разовый вопрос → обычный чат», «нужно, чтобы модель СДЕЛАЛА (написала/отредактировала) с проверкой на каждом шаге → agent-режим (Cursor/Claude Code/Copilot Agent) + review по шагам», «нужны мои файлы/актуальные факты → приложи файл / RAG-режим инструмента», «задача необратимая — проверка ДО действия, не после». Прямая связка с новым coding-agents слайдом из Раздела 4.

Итог: 4 слайда (26,27,28,29) → 4 слайда (26, A, B, 29) — то же число, контент реорганизован + закрыт content gap "для студента, не только для архитектора".

**РИСК/ROAST этого решения — САМОЕ дорогое решение во всей переработке:** это полный визуальный + содержательный редизайн двух слайдов, которые presentation-critic/methodology-critic должны будут пройти отдельным пристальным проходом (не рутинная правка). Alternative был бы «просто упростить визуал матрицы, оставить структуру» — дешевле, но не закрывает вторую половину запроса владельца («для студентов, а не только архитекторов»). Рекомендую полный редизайн, но explicit sign-off нужен, т.к. это САМАЯ дорогая работа во всём issue.

## 3. Раздел 1 (Промпт) — бриф #218

Текущий Раздел 1: divider(s04a) + s05(дефолт-один-вызов) + s06(CoT) + s07(CoT faithfulness) + s08(context engineering) = 4 контентных слайда.

Добавить по брифу (пункты 0-3 из #218, пункт 4 в комментарии не дописан владельцем — оставляю как gap, не додумываю за него):

- **0.** Не отдельный слайд — усилить s05 явной строкой «модель знает только то, что в промпте (+ то, что было в её весах на обучении)» — это уже почти есть в s05 note неявно, сделать явным на visible-слое.
- **1. NEW слайд «Роли в промпте»** — callback на внимание (Л2), + **research-based claim**: роли не повышают точность/качество, но задают тон/направление ответа. **Требует research** (fact-checker находит источник) — не выдумывать цитату.
- **2. NEW слайд «Структура промпта»** — организация, разделители (delimiters), структурность.
- **3. NEW слайд «Чит-шит: как строить промпт»** — финальный чек-лист раздела: размер, что должно быть внутри.

Итого +3 слайда в Разделе 1 (4 → 7). Компенсируется -2 слайдами из Раздела 4 (§1) — почти нейтрально по общему таймингу (75 мин).

**РИСК:** research по «роли не повышают качество» может не найтись в чистом виде или найдётся с оговорками (dataset-specific, model-specific). Fact-checker обязан явно фреймить как «исследования показывают» с caveat, не как закон — по стандарту остальных цифр в деке (сверено — везде так и оформлено, паттерн выдержан).

## 4. RAG-раздел — разрешение конфликта #222 vs #223

Оба коммента читал полностью. **Конфликта на самом деле нет** — второй комментарий (#223, на s13/"когда RAG не надо") сам себя корректирует: «а на предыдущем слайде не надо» — то есть владелец явно говорит НЕ добавлять API/MCP-блок на s12(right), только на s13(NOT right). Логически это и правильное место: «данные доступны live через API/MCP в другой системе» — причина НЕ строить RAG, то есть негативный критерий, значит s13, не s12.

- **s12 (когда RAG — правильный выбор):** убрать «конъюнкция/дизъюнкция» жаргон (#221); **НЕ делать наивный AND→OR как буквально просили** (#222) — чистое ИЛИ создаёт противоречие с собственным правилом s13 («приватность, но малый статичный корпус → не RAG»); вместо этого — переформулировать как «сильный сигнал + отсутствие блокеров с соседнего слайда», сохраняя дух жалобы владельца («один явный признак уже повод присмотреться»), не создавая новую дыру. Это отклонение от буквальной инструкции — **explicit callout ниже**.
- **s13 (когда RAG — НЕ правильный выбор):** добавить 4-й критерий «данные доступны live через API/MCP/поиск в другой системе → не нужен RAG-индекс» (#223); убрать пункт про observability (#224) — не про выбор инструмента; content не теряется, наблюдаемость retrieval уже центральная тема соседнего слайда (провал RAG на масштабе) — переносится туда, не удаляется.

## 5. Fine-tuning/LoRA — реордер + factcheck (#225-227)

Текущий порядок: s13b(определение)→s14(сузился: что ушло/осталось, включая дистилляцию)→s15(PEFT/LoRA)→s16(catastrophic forgetting)→s17(критерии, включая строку "дистилляция(fine-tuning)").

**Предложение — реордер, не переписывание с нуля:**
s13b(определение, + явная строка "на практике почти всегда означает LoRA/PEFT, не full-FT") → **s15(PEFT/LoRA) переставлен сразу после определения** → s14(что ушло/осталось из FT, теперь с уже знакомым LoRA) → s16(catastrophic forgetting) → s17(критерии).

Закрывает #225 (LoRA сразу при определении) и #226 (LoRA не "теряется" на s17, т.к. уже введён на позицию раньше).

**Fact-check обязателен (delegated to fact-checker, не решаю сам):**
- #227: дистилляция классифицирована как fine-tuning на s14 ("осталось за fine-tuning: ...дистилляция") и на s17 (строка "дистилляция (fine-tuning)"). Моя предварительная оценка: **дистилляция — самостоятельная техника** (student модель учится имитировать teacher через distillation loss на выходах/логитах), не тождественна fine-tuning, хотя механизм обучения студента часто реализуется КАК fine-tuning на teacher-generated данных — нюанс. Прошу fact-checker дать точную формулировку и источник (Hinton et al. 2015 + текущая практика 2026).
- #225/#226 implied baseline: нужна ли статистика "% реальных FT-джобов это LoRA/PEFT, не full-FT" — fact-checker ищет актуальную цифру или явно помечает gap.

## 6. Точечные правки без структурных последствий

Дельта из issue, не требующая архитектурных решений — идут в обычный revision-batch:

- #211 (s01): убрать "что выбрали/что нужно" блоки, добавить изображение
- #212 (s02): вынести roadmap в отдельный слайд-план (сверить паттерн Л1/Л2) — **+1 слайд**
- #213/#214 (s03): убрать highlight/embeddings-комментарий, убрать параграфы Л2, сверить весь слайд с финальной версией деки после реструктуризации (делается ПОСЛЕ остальных правок, не до)
- #215/#216 (s04): визуальные anchor-правки (диаграмма крупнее) — presentation-designer
- #217 (s04): подтверждено — определение RAG остаётся на s09 (divider уже содержит one-line def), на s04 не дублируем
- #219 (s06+s07): слить в 1 слайд CoT (пример + faithfulness-limit), сохранить callback на check-шаг агента и human-validator
- #232/#233 (agent failures): смягчить "правильно", добавить базу к "$4,200" (Baseline/Counterfactual Mandate)
- #237 (human-validator+NANDA): добавить кивок к Л2, явно добавить темы "степень автономности/scope доверия/continuous monitoring"
- #238 (bridge-homework-qa): убрать recap-заполнитель, добавить тизер Л4, сохранить блок ДЗ Семинар 3
- #239 (Q&A): визуально скопировать паттерн Л1 — presentation-designer

## 7. Итоговый бюджет слайдов

36 (было) − 2 (Раздел 4 merge) + 3 (Раздел 1 брифа #218) + 1 (s02 TOC-слайд) + 0 (Раздел 5 net-zero) = **38 слайдов**. Рост на 2 слайда — умеренный, тайминг 75 мин остаётся ориентиром; presentation-designer держит duration_min budget под контролем на design-слое.

## 8. Порядок работ после approval

1. methodology-critic — полный ревью структуры (баланс разделов, глубина по агентам/кодингу, уместность нового Раздела 4) НА ЭТОМ ПЛАНЕ до реализации
2. fact-checker — 3 конкретных пункта: дистилляция-vs-FT (#227), LoRA-adoption baseline (#225/#226), roles-in-prompt research (#218)
3. book-editor — chapter.md reflow (реордер §3, новый материал §1 роли/структура/чит-лист, новый §4 "Агенты" с coding-agents обзором, новый §5.2 flow+playbook вместо матрицы)
4. presentation-designer — deck.yaml/slides/* + rendered — полная реализация с 3-iteration visual loop
5. speech-writer — синхронизация speech.md
6. QA-with-roles: presentation-critic + student-simulator + reader-simulator(rendered)
7. Pre-USER-GATE walkthrough (включая п.6 keystone-axis check и п.12 baseline-coverage check из CLAUDE.md)
8. Comment-mapping отчёт → send_message оркестратору a8c37452

## 9. Явные отклонения от буквальных инструкций владельца (требуют sign-off)

1. **s12 AND→OR**: НЕ делаю буквальный OR (см. §4) — заменяю на "сильный сигнал + нет блокеров" формулировку.
2. **Раздел 4 переименование**: убираю "+безопасность" из заголовка полностью (не просто "выпилить", а именно убрать из НАЗВАНИЯ раздела) — контент остаётся, ярлык уходит.
3. **s32 (матрица) — полный редизайн на 2 слайда**, не косметика визуала — самая дорогая единица работы во всём issue.
4. **Пункт 4 брифа #218** — не дописан владельцем в исходном комментарии, не додумываю содержание сам, оставляю как открытый пункт (могу спросить или пропустить).
