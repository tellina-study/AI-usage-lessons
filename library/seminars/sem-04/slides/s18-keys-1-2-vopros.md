---
id: s18
type: reflection_question
duration_min: 1.0
assertion: "Гейт-фраза уже есть и не сработала — выбор из пяти вариантов, что делать дальше, чтобы реально сработало в следующей сессии"
learning_goal: "Открытый вопрос кейса 1.2 — слайд-вопрос с готовым набором вариантов, БЕЗ таблицы разбора и подсветки ответа (R3-4) — разбор на следующем слайде"
visual:
  pattern: question_with_option_cards
  primary: "Вопрос крупно сверху, в gold-рамке. Ниже — пять карточек-вариантов, дословно и в этом порядке: «перепишу фразу капсом / жёстче» · «продублирую фразу вторым пунктом» · «заведу отдельный файл-чеклист рядом» · «передам проверку механизму вне текста инструкции» · «смирюсь — сработает не всегда, и это нормальная погрешность». Ни таблицы разбора, ни целевого ответа на этом слайде нет."
---

# Гейт не сработал — что дальше

## Assertion

Гейт-фраза уже есть и не сработала — выбор из пяти вариантов, что делать дальше, чтобы реально сработало в следующей сессии.

## Visual

Сверху, в gold-обведённом блоке, — вопрос дословно:

> «У вас гейт-фраза в `CLAUDE.md` уже есть, а поведение агента не изменилось — заявка снова не проверена руками. Выберите, что вы сделаете дальше — из карточек ниже — так, чтобы это реально сработало в следующей сессии.»

Ниже — пять карточек-вариантов в этом порядке:

`перепишу фразу капсом / жёстче` · `продублирую фразу вторым пунктом` · `заведу отдельный файл-чеклист рядом` · `передам проверку механизму вне текста инструкции` · `смирюсь — сработает не всегда, и это нормальная погрешность`

На слайде нет ни таблицы разбора, ни целевого ответа — они на следующем слайде.

## Speaker notes

Пауза, 2–3 голоса. Реакция на «перепишу капсом»: «Проверим числом — TDAD в точности этот путь проверило: усилили формулировку текстом, без тулинга. Регрессии выросли. Капс не превращает просьбу в закон».

На этом слайде нет ни таблицы разбора, ни подсветки правильного варианта — их место на следующем.
