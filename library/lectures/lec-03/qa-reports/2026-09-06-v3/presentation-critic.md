VERDICT: REVISE

# Presentation Critic Report — Лекция 3 (deck v5) — 2026-09-06

**Ревьюер:** presentation-critic (vision-enabled) · **issue #185** · **ветка** issue-185-lec03-v3
**Артефакт:** `library/lectures/lec-03/rendered/lec-03.pptx` (51 слайд) + `snapshots/s-01…s-51.png`
**Спека:** `notes/lecture-3-rework/deck-v5-inventory-draft.md`

> **Маппинг нумерации.** Спека использует v5-имена (s01, s01b, s02, s02a, s05c…),
> но рендер идёт сплошной нумерацией s-01…s-51. Ниже я даю РЕНДЕР-номер (s-NN, как
> на PNG) + в скобках имя из спеки, где нужно. Ключ: s-01=мем-хук(s01),
> s-02=Air Canada(s01b), s-03=cover(s02), s-04=lecture-map(s02a), s-05=hub(s03),
> s-06=лестница(s04), s-07=div Р1(s04a), s-10=роли/STI(s05c), s-12=CoT(s06),
> s-13=faithfulness(s07), s-24=FT-критерии(s17), s-27=div Р4(s18),
> s-29=экономика(s19b), s-30=MCP(s20), s-33=мульти-агент p^n(s22a-multi),
> s-42=таблица классов(s23b), s-43=deep-dive(s23c), s-39=data-map/ZDR(s24),
> s-48=итоги-таблица(s28), s-49=человек-валидатор(s29), s-50=мост(s30), s-51=Q&A(s31).

## Сводка
- Всего слайдов: **51** (49 content + 5 divider — минус 3 vs спека 54; итог укладывается, не блокер)
- **P0 (блокеры): 1**
- **P1 (важные): 6**
- **P2 (косметика): 5**

**Главный вывод.** Методически и структурно дек сильный: assertion-заголовки —
полные тезисы, «Что делать» присутствует почти везде, failure/judgment-контент
плотный (провалы агентов = 3 слайда + классы таблицей), baseline на measurable
проставлены (×1 chat-turn как база для ×N, 5M→11% каталога, p^n с исходными
95%/90%, «5M из 900M» паттерн выдержан). НО есть **один P0-блокер по Russification**
(RU студенто-видимый дек буквально усыпан англицизмами, включая Cyrillic-транслит
с чистыми русскими эквивалентами) и **overflow на s-49**, плюс мелкая
нечитаемость на s-46. Это не polish — это структурный gap по ENFORCED-правилу
§5.8, поэтому verdict **REVISE**.

---

## P0 — блокеры

### s-01…s-51 (deck-wide) — Russification провалена на видимом слое
**Severity:** P0
**Issue:** Deep latin-token scan по visible body RU-дека: **648 occurrences / 328
unique** латинских токенов. Даже за вычетом brand/acronym allowlist (Air Canada,
McCarthy, PEFT/LoRA/RAG/MCP/CoT/ZDR/QLoRA, arXiv-URL, case-names) остаётся
большой narrative-хвост непереведённых англицизмов. Отдельно — **Cyrillic-
транслит англицизмы**, которые deep-latin-scan НЕ ловит, но которые владелец МГТУ
ИУ6 замечает первым делом (ср. cost-of-omission Лекции 8 «обилие англицизмов! это
провал»):

| Cyrillic-англицизм | Слайды | Русифицировать в |
|---|---|---|
| **апгрейд** (×4) | s-33, s-34, s-01, s-48 | улучшение / выигрыш |
| **дефолт / Дефолт** (×2) | s-08, s-47 | по умолчанию / тонкий базовый набор |
| **продакшен / в прод / прод-БД** (×6) | s-25 «в прод», s-27 «продакшен-системы», s-39 «продакшена», s-43 «прод-БД» | промышленная эксплуатация / боевая БД |
| **чит-шит** (×1) | s-15 (заголовок!) | шпаргалка |
| **латентность** (×5) | s-08, s-24, s-25, s-45, s-48 | задержка |
| **парсинг** (×1) | s-28 «для парсинга» | разбор |
| **бэкапы** (×1) | s-43 | резервные копии |
| **ревью** (×1) | s-43 «ревью новых зависимостей» | проверка / обзор |
| **сэмплов** (×1) | s-43 «576 000 сэмплов» | примеров кода |
| **хопов** (×2) | s-41, s-43 | переходов |

Плюс латинский narrative-хвост, повторяющийся **untranslated** десятки раз (не
глоссирован либо глоссирован 1× и дальше сыпется голым английским):
`fine-tuning/Fine-tune` (×20+), `retrieval` (×7), `grounding` (×7), `forgetting`
(×7), `workflow/Workflow` (×11), `subagents/Subagent` (×6), `check/act/iterate/
plan` (×26 как cycle-подписи), `few-shot` (×3), `full-context`, `lookup`, `live`,
`structured output/retrieval`, `Function calling`, `Prompt caching`, `Structured
output`, `Skills/Skill`, `Least-privilege`, `Human-in-the-loop`, `Allowlist/pin`,
`deny-by-default`, `third-party`, `single-threaded`, `backoff/retry-with-backoff`,
`compounding`, `over-privilege`, `Zero-click`, `Runaway`, `slopsquatting`,
`rug-pull`, `heist`, `lossy-суммаризация`, `cross-session`, `temporal graph`,
`graph-база`, `self-hosted`, `open source`, `CLI-first`, `desktop IDE`, `coding-
агенты`, `path traversal` (частично глоссирован).

**Что здесь ОК (keep-list, не трогать):** brand-имена (Claude Code, Cursor, Aider,
OpenHands, Anthropic, OpenAI, Google, DeepSeek, Letta, Cognee, mem0, Graphiti),
аббревиатуры с расшифровкой (MCP, RAG, LLM, PEFT, LoRA, QLoRA, CoT, ZDR, API, CVE,
NL, GPU, FAQ), case-names (Air Canada, PocketOS, EchoLeak, postmark-mcp), техноним
ChatML / спецтокены `<|im_start|>` (это буквально предмет слайда s-10),
`plan→act→check→iterate` как **имя паттерна ReAct** (но продублировать русской
подписью «планирование → действие → проверка → повтор» хотя бы 1× — сейчас на
s-31 и s-06 цикл ПОЛНОСТЬЮ англоязычный в заголовке).

**Recommendation:**
1. Обязательно и в первую очередь — вычистить **Cyrillic-транслит** (таблица выше):
   это самые заметные и самые дешёвые правки, у всех есть 1-к-1 русский эквивалент.
2. Русифицировать повторяющийся narrative-хвост: `retrieval`→извлечение,
   `grounding`→опора на источник, `forgetting`→(катастрофическое) забывание,
   `workflow`→рабочий поток (уже есть глосса на s-32 — применить везде),
   `full-context`→полный контекст, `lookup`→прямой поиск/справка,
   `fine-tuning`→дообучение (глосса есть на s-22 — дальше не сыпать голым EN).
3. Заголовки-англицизмы: s-15 «Чит-шит»→«Шпаргалка», s-32 «Workflow vs Agent»→
   «Рабочий поток vs агент», s-40 «Реальные coding-агенты»→«…агенты для кода».
4. Cycle-подписи `plan/act/check/iterate`: оставить как якорь ReAct, но дать
   русскую строку-глоссу хотя бы один раз рядом (на s-06 и s-31 сейчас 100% EN).

**Visual evidence:** видно на КАЖДОМ content-слайде; концентрированные очаги —
s-08, s-10, s-20, s-28, s-34, s-35, s-38, s-40, s-42, s-43.
**Cost-of-omission benchmark:** Лекция 8 v1 = 224 unique в PPTX → owner reject
«провал» → 3 revision passes. Здесь 328 unique — тот же класс проблемы.

---

## P1 — важные (мешают обучению / читаемости)

### s-49 (s29 «Человек-валидатор») — text overflow: subtitle обрезан карточкой
**Severity:** P1
**Issue:** Курсивный подзаголовок «против независимого источника истины, НЕ по
правдоподобности рассуждения (самообъяснение модели ≠ контроль — урок про
верность рассуждения)» физически **перекрыт/обрезан** верхней рамкой карточки
«Степень автономности» — последняя строка «рассуждения)» срезана. Читается как
дефект вёрстки.
**Recommendation:** сократить subtitle до 2 строк ИЛИ сдвинуть блок из 3 карточек
(«Степень автономности» / «Область доверия» / «Непрерывный мониторинг») ниже,
дав subtitle воздух. Также donut-chart имеет микро-подписи «5»/«95» (~6pt) — на
проекторе с задних рядов невидимы (Projector 50% fail).
**Visual evidence:** s-49.png — наложение italic-текста на голубую карточку.

### s-46 (s27 flowchart) — «нет ↓» branch-подписи клиппятся между строками
**Severity:** P1
**Issue:** Между рядами вопросов слева торчат обрезанные фрагменты «нет ↓»
(ветка «нет» маршрута), которые наезжают на рамки нижних боксов и читаются как
куски «нет ↓» вполовину. 8 строк маршрута плотно упакованы, вертикальные
connector-подписи не помещаются в межстрочный зазор. На проекторе — «сломанная»
схема (§5.5 schema-pipeline: owner annotations должны быть читаемы).
**Recommendation:** либо увеличить вертикальный зазор между рядами и вынести
«нет ↓» в чистый gap, либо убрать промежуточные «нет ↓» вовсе (маршрут читается
сверху-вниз: не сработал вопрос → следующий ряд), оставив явную «да →» ветку
справа. 8-й пункт (строка «Данные чувствительны?») визуально оторван от 1–7 —
свести в единый список.
**Visual evidence:** s-46.png — фрагменты «нет ↓» на границах боксов 1–7.

### s-10 (s05c «Роли — два механизма») — плотность 5+ новых терминов на слайде
**Severity:** P1
**Issue:** Слайд несёт перенос #184 и объективно самый плотный: chat-шаблон +
ChatML `<|im_start|>` + Llama 4 + Anthropic top-level + Jinja/tokenizer_config +
приоритет system 63,8% + IH-Challenge 84,1%→94,1% + STI/role spoofing + ChatInject
ASR 5,18%→32,05% + Llama-4 88,3%. Это ~7 новых сущностей на одном слайде —
когнитивная перегрузка (правило «одна мысль на слайд»). Assertion при этом
отличный. «top-level поле» — англицизм (см. P0).
**Recommendation:** не удалять (спека фиксирует несокращаемость), но
разгрузить визуально: свернуть техническую линию «ChatML/Llama4/Anthropic/Jinja»
в одну строку-пример, оставив на слайде 2 числа-якоря (63,8% приоритет + ASR-скачок)
и один вывод. Детали chat-template — в speaker notes (там они уже есть).
**Visual evidence:** s-10.png — нижняя строка code-блока «Спецтокены разметки…
top-level поле… Jinja в tokenizer_config.json» очень мелкая, плотная.

### s-06 (s04 лестница) / s-45 (s26 лестница-повтор) — ступени лестницы 100% англоязычны
**Severity:** P1
**Issue:** Несущая ось лекции (keystone!) — «лестница 6 ступеней» — на s-06
подписана `Multi-agent / Agent / Workflow / RAG / Один вызов LLM / Обычный код`,
на s-45 то же + `plan→act→check→iterate`, `+few-shot`. Ступени 4-6 (Workflow,
Agent, Multi-agent) — голый английский на ГЛАВНОЙ схеме лекции, которую студент
видит трижды (s-06, s-45, s-48). Для keystone-оси это особенно нежелательно.
**Recommendation:** дать русскую подпись ступеням: «Рабочий поток», «Агент»,
«Мульти-агент» (термины уже введены по-русски на s-32/s-33). Латинские
`plan→act→check→iterate` — оставить как под-глоссу ReAct, но не как основную
подпись ступени.
**Visual evidence:** s-06.png, s-45.png — ступени 4/5/6 англоязычны.

### s-35 (s22c «Память агента») — правая карточка = сплошной англо-листинг
**Severity:** P1
**Issue:** Карточка «Векторная / graph-база» состоит из строк `mem0 — cross-session
память`, `Cognee — memory на графе`, `Graphiti / Zep — temporal graph: когда факт
верен…`. Brand-имена (mem0/Cognee/Graphiti/Zep) — ОК, но `cross-session`, `memory`,
`temporal graph`, `graph-база` — непереведённый narrative. Студент видит стену
англицизмов вместо «памяти между сессиями / память на графе знаний / временной
граф».
**Recommendation:** оставить бренды, русифицировать связки: «mem0 — память
пользователя между сессиями», «Cognee — память на графе знаний», «Graphiti / Zep —
временной граф: когда факт верен, когда устарел». «graph-база»→«граф-база знаний».

### s-38 (s25 «Skills, subagents, доступ») — блок правил 100% на английских терминах
**Severity:** P1
**Issue:** Заголовок «Skills, subagents, доступ», карточки `Skill / Subagent /
MCP-доступ`, «4 правила проектирования»: `Least-privilege`, `Изоляция
недоверенного (subagent)`, `Human-in-the-loop на write`, `Allowlist / pin`,
`deny-by-default`, плюс «GitHub MCP heist», «third-party/MCP вне ZDR». Это самый
англо-насыщенный слайд после s-40. Термины важны, но у большинства есть русские
эквиваленты, вводимые здесь впервые.
**Recommendation:** `Least-privilege`→«Минимум прав (least-privilege)» с глоссой
1×, дальше по-русски; `Human-in-the-loop на write`→«Человек в контуре на запись»;
`Allowlist / pin`→«Белый список / фиксация версий»; `deny-by-default`→«запрет по
умолчанию»; `heist`→«ограбление/утечка». Оставить MCP/ZDR/PAT как аббревиатуры.

---

## P2 — косметика

### s-51 (s31 Q&A) — нет плашки контактов (спека требовала placeholder)
**Severity:** P2
**Issue:** Слайд = «Q&A» + «Спасибо», без видимой плашки «контакты преподавателя
`[VFY-day-of]`» (спека §7 / решение оркестратора п.5: owner заполнит на GATE B).
В speaker notes placeholder есть («Контакты преподавателя [VFY-day-of] — заполнить
перед лекцией»), но на видимом слое пусто. Не блокер (owner заполнит), но если
задумана видимая плашка — сейчас её нет.
**Recommendation:** подтвердить с owner: нужна ли видимая строка-контакты на s-51,
или оставить чистый Q&A. Hero на closing — на s-50 (мост, реальное фото
разработки ПО с attribution), правило §5.9 выполнено там.

### s-49 — donut-chart избыточен для одной цифры «~95%»
**Severity:** P2
**Issue:** Донат отображает единственное значение 95% с почти-невидимыми
подписями «5»/«95». Одна цифра не требует круговой диаграммы — big-number «~95%»
справа уже несёт сообщение.
**Recommendation:** либо убрать donut (оставить big-number), либо увеличить
подписи сегментов до читаемых на проекторе.

### s-46 — 8-й пункт маршрута визуально оторван от 1–7
**Severity:** P2
**Issue:** Строка «8. Данные чувствительны? → …» вынесена отдельной плашкой под
списком 1–7 и читается как «сноска», хотя это полноценный шаг маршрута.
**Recommendation:** унифицировать оформление 8-го пункта со строками 1–7 (тот же
row-паттерн) либо явно пометить «на КАЖДОМ шаге» как рамку вокруг всего списка.

### Speaker notes — `[VFY-day-of]` в reference-футерах заметок (×15)
**Severity:** P2
**Issue:** 15 маркеров `[VFY-day-of]` живут в блоке источников внутри speaker
notes (строки-цитаты). Это planning-конвенция day-of-верификации волатильных
цифр (STI/ChatInject, PEFT 98,4%, MCP CVE, Letta Tier), не видна на PNG. По букве
CLAUDE.md маркеры лучше держать в frontmatter, но здесь они в reference-секции
заметок и оправданы как «проверить перед лекцией». На видимом слое студента их
НЕТ (подтверждено extract'ом). Оставляю как наблюдение, не требование.
**Recommendation:** при желании — перенести в frontmatter `deck.yaml` per-slide
`verify: [...]`; но приемлемо и как есть (не student-visible).

### s-33 / s-34 / s-48 — «апгрейд» в assertion-заголовках и итогах
**Severity:** P2 (учтено в P0, но выношу для дизайнера как точечную правку)
**Issue:** «Мульти-агент по умолчанию — не апгрейд» (s-33 заголовок), «…слот… не
апгрейд» (s-34 заголовок), «не апгрейд» в итог-таблице s-48. Заголовок-assertion
— самое видное место, англицизм там бьёт сильнее всего.
**Recommendation:** «апгрейд»→«улучшение» / «выигрыш» во всех трёх.

---

## Cross-deck проверки (результаты)

**Timing / Methodology в visible body:** ✅ ЧИСТО. Extract визимого слоя RU-pptx:
0 timing-маркеров (единственный «23 мин» — это «23 минус 7» в арифметическом
worked-example, не тайминг), 0 «методическ/педагогическ», 0 «Лектору/Вы здесь»,
0 LO-кодов, 0 `§X`, 0 `→ sNN`, 0 `[VERIFY/FACT]` в видимом. Дивайдер-теги — «3
разбора · 1 провал» БЕЗ минут (спека выдержана). Подтверждаю твой grep визуально
на всех 51 PNG.

**Baseline на measurable:** ✅ В целом выдержано.
- s-29 (экономика) — база «×1 chat-turn (десятки центов)» явно задана, ×N от неё,
  caveat «порядок величины, не точный тариф». Отлично.
- s-30 (MCP) — «до 90 000 → реально ~10 000 = 11% каталога» — база есть.
- s-33 (p^n) — исходные 95%/90% на шаг → 60%/35%, база явная.
- s-42/s-43 — «188 из 344», «61% из апстрима», «19,7% (1 из 5)», «$48k/14ч vs
  обычная сессия центы-доллары = на 3–5 порядков выше» — базы проставлены.
- s-49 — «~95% пилотов без ROI» — источник MIT NANDA + methodology footer.
  Denominator «из скольких» не на слайде, но «корпоративных GenAI-пилотов» +
  ссылка на отчёт — приемлемо.
Замечаний по missing-denominator НЕТ (в отличие от Лекции 10).

**Hero (§5.9):** ✅ s-01 (мем-хук «магическая пилюля», свой 2-панельный flat-рисунок
≥40%) + s-02 (Air Canada Boeing 787, реальное фото Wikimedia CC-BY-SA, attribution
plate). Closing hero — s-50 (реальное фото разработки ПО, Wikimedia CC-BY-SA,
attribution). Правило выполнено (closing вынесен на s-50-мост, s-51 Q&A без hero —
согласовано спекой).

**Дивайдеры (§Л2-паттерн):** ✅ 5 дивайдеров (s-07 нож, s-16 библиотека, s-21 пульт,
s-27 робот-рука, s-44 приборная панель) — реальный образ справа + большой номер +
frame-фраза + tag «X разборов · Y провалов» БЕЗ минут + roadmap-bar внизу.
Roadmap-bar только на дивайдерах + cover-map (s-04) — на content-слайдах его нет
(проверено). Метафоры буквальные и удачные. Attribution plate на каждом.

**Real images vs mocks (§5.7):** ✅ Дивайдеры и hero — реальные фото Wikimedia
CC-BY-SA (нож, библиотека, пульт, промышленный робот KUKA, кокпит, разработчик за
iMac), не stylized Ocean-cards. Air Canada — реальное фото борта. Мем s-01 — свой
flat-рисунок (легитимно, это мем-хук, не «скриншот источника»). Mock-fallback не
обнаружен.

**Cross-slide redundancy:** приемлемо. Лестница повторяется s-06→s-45→s-48, но это
намеренный keystone-callback (та же ось, разный уровень детализации), не дубль-
диаграмма. p^n на s-33 и строкой в s-48 — обзор vs итог, ОК. Провалы агентов
s-41/s-42/s-43 — петля/таблица-классов/deep-dive, разные уровни, не дубль.

**Assertion-quality:** ✅ Почти все заголовки — полные тезисы («Роль в промпте
настраивает тон — не точность», «Мульти-агент по умолчанию — не апгрейд, а
множитель риска», «"У нас ZDR" ≠ "данные защищены во всей цепочке"»). Сильно.

**«Что делать» в visible:** ✅ Присутствует на всех content-слайдах (gold callout).
Проверено пофрагментно.

**Lec-N-1 pattern (Лекция 2 v2):** ✅ lecture-map (s-04), 6 дивайдеров на разделы,
Q&A-слайд (s-51), итоги-таблица «механизм→граница→что делать» (s-48, паттерн Л2
s38), мост с якорями (s-50, паттерн Л2 s41), roadmap-bar только на
дивайдерах/cover. Соответствие выдержано.

---

## Что править перед GATE (приоритет)

1. **[P0]** Russification: вычистить Cyrillic-транслит (апгрейд/дефолт/продакшен/
   чит-шит/латентность/парсинг/бэкапы/ревью/сэмплов/хопов) + русифицировать
   повторяющийся narrative-хвост (retrieval/grounding/forgetting/workflow/
   fine-tuning/full-context/lookup) + русские подписи ступеней лестницы (s-06/s-45)
   и cycle-глоссу (s-06/s-31). Keep brands + аббревиатуры + ChatML/спецтокены.
2. **[P1]** s-49 overflow — освободить subtitle от наложения карточкой; увеличить/
   убрать donut-микроподписи.
3. **[P1]** s-46 — убрать/развести клиппящиеся «нет ↓»; унифицировать 8-й пункт.
4. **[P1]** s-10 — разгрузить (7 сущностей → 2 числа-якоря + вывод, детали в notes).
5. **[P1]** s-35 / s-38 — русифицировать связки в англо-насыщенных карточках.
6. **[P2]** s-51 контакты (подтвердить с owner), s-49 donut, s-46 п.8, «апгрейд» в
   заголовках.

После правок P0+P1 — re-render, re-snapshot, re-run deep-latin-scan (цель:
`unique − whitelist → narrative-хвост ≈ ∅`), затем pre-gate.

---

## Итоговый вердикт

**REVISE.** Дек методически и структурно готов к показу (assertion-evidence,
failure-share, baseline, hero, дивайдеры, timing/methodology-чистота — всё на
месте), НО **1 P0 (Russification на видимом слое — ENFORCED §5.8, структурный gap,
не polish) + 6 P1** не позволяют выше REVISE. Counter-check: 6 P1 > 3 → не может
быть APPROVE-WITH-POLISH. После вычистки англицизмов и фикса overflow/clipping
(s-49, s-46, s-10, s-35, s-38) дек выйдет на APPROVE-WITH-POLISH / APPROVE-CLEAN.
