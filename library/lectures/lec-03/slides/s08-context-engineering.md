---
id: s08
type: assertion_visual
section: "Раздел 1. Промпт и его границы"
duration_min: 2.5
assertion: "Минимальный высокосигнальный контекст — инженерное требование: точность падает с длиной (context rot = «lost in the middle» Лекции 2)"
learning_goal: "Контекст-инжиниринг + context rot (явная сшивка) + критерий «когда НЕ RAG»"
learning_outcomes: [LO7]
chapter_ref: "§1.4 [for-slide-s08]"
visual_brief: "Слева — нисходящая кривая «точность извлечения ↓ по мере роста токенов в контексте», подпись «context rot = lost in the middle Лекции 2 §3». Справа — критерий-плашка «когда НЕ RAG: малый стабильный корпус → full-context + кэширование». Gold — зона провала на кривой."
interaction: none
---

# Visible content

## Title bar
«Контекст-инжиниринг: минимум высокосигнального»

## Body
[Одна строка-различение, 16pt italic]
*Промпт-инжиниринг — одна инструкция. Контекст-инжиниринг — курирование всего набора токенов, видимых модели на инференсе.*

[Слева — кривая в Ocean rounded box]
**Точность извлечения нужной информации**
↓ падает по мере роста числа токенов в контексте  *(зона провала — gold-акцент)*

Подпись: **context rot** = тот же феномен, что «lost in the middle» из Лекции 2 §3 — новый практический термин, не новая сущность

[Справа — критерий-плашка]
**Когда НЕ RAG (точка 1):**
малый стабильный корпус, влезает в окно → **full-context + кэширование префикса**, а не RAG-инфраструктура

[Принцип, низ]
«Найти наименьший набор высокосигнальных токенов, максимизирующий вероятность желаемого исхода» — это инженерное требование, не эстетика.

## Speaker notes

Если chain-of-thought — это про форму одного вызова, то контекст-инжиниринг — про его содержимое. Введём различение. Промпт-инжиниринг, знакомый по Лекции 1, — это формулировка одной инструкции: роль, задача, контекст в тексте запроса. Контекст-инжиниринг — более широкая и итеративная дисциплина: курирование всего набора токенов, видимых модели на инференсе, включая системные инструкции, описания доступных инструментов, подгружаемые внешние данные и историю сообщений. Главный принцип формулируется так: найти наименьший набор высокосигнальных токенов, максимизирующий вероятность желаемого исхода.

Почему минимальность контекста — не эстетика, а инженерное требование? Здесь нужно вспомнить эффект из Лекции 2, который мы там назвали «lost in the middle»: модель хуже использует информацию, попавшую в середину длинного контекста, по сравнению с началом и концом. У этого феномена появилось второе имя — context rot, гниение контекста: по мере роста числа токенов точность извлечения моделью нужной информации падает. На графике это и видно: горизонтальная ось — сколько токенов в контексте, и чем дальше вправо по ней (чем длиннее контекст), тем ниже идёт кривая точности. Это тот же самый феномен, что «lost in the middle» из Лекции 2, под новым практическим названием — я делаю эту сшивку явно, чтобы термин не воспринимался как новая сущность. Механизм тот же: попарные связи механизма внимания растут квадратично с длиной.

Прямое следствие: «просто положить всё в контекст» — не стратегия. Большое контекстное окно даёт возможность вместить много текста, но не гарантию, что модель этим текстом воспользуется правильно. И это, забегая вперёд, один из ключей к вопросу «когда НЕ RAG», который мы разберём в Разделе 2: иногда правильный ответ — не строить retrieval-инфраструктуру, а аккуратно курировать небольшой стабильный контекст и переиспользовать его через кэширование. То есть это первый практический критерий: если корпус знаний мал, стабилен и помещается в окно — RAG здесь добавил бы хрупкость без выигрыша, и правильный ответ проще, чем кажется.
