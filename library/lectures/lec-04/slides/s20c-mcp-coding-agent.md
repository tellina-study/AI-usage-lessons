---
id: s20c
type: assertion_visual
section: "Раздел 3. Реализация — дисциплина и харнес"
duration_min: 3
assertion: "MCP убирает человека-моста между агентом и внешними системами (issue-трекер, браузер, деплой): сервер публикует инструменты, агент вызывает их как встроенные — но тот же канал закрывает сразу два угла Lethal Trifecta"
learning_goal: "Ведущее: MCP расширяет агента за пределы файловой системы (GitHub / Playwright / filesystem MCP) + мост к security (Lethal Trifecta через один сервер)"
learning_outcomes: [LO1, LO7]
chapter_ref: "§3.3c [for-slide-s20c]"
references: [github_mcp_server, playwright_mcp, willison_lethal_trifecta]
verify_day_of: true
visual_brief: >
  schema_architecture: кодинг-агент в центре с встроенным доступом (файловая система + shell, сплошная стрелка);
  MCP-серверы — 3 карточки вокруг (GitHub MCP: issues/PR/CI, read-only режим явным переключателем · Playwright MCP:
  accessibility-дерево вместо скриншота, генерирует тест · Filesystem MCP: доступ к соседним директориям вне рабочей
  копии) — пунктирные стрелки «агент решает, когда вызвать, тем же циклом, что встроенные». Мост к безопасности —
  плашка снизу: один MCP-сервер часто закрывает 2 угла Lethal Trifecta одним подключением (данные + канал действия),
  третий (недоверенный контент) прилетает через тот же сервер (issue/PR-комментарий). Задокументированный пример —
  GitHub MCP server: инструкции prompt injection в публичных issues эксфильтровали данные через создаваемые PR.
  Gold — «read-only по умолчанию; write — осознанное security-решение, а не настройка по умолчанию».
interaction: none
---

# Visible content

## Title bar
MCP убирает человека-мост между агентом и системами вне репозитория — и тем же каналом открывает риск

## Body
[В центре — агент, встроенный доступ vs MCP-расширение]

Кодинг-агент по умолчанию видит только **файловую систему рабочей копии и шелл**. Этого хватает для кода, но не для issue-трекера, живого браузера, деплой-таргета — всего, что **вне репозитория**. Без MCP инженер сам копирует контекст туда-обратно.

MCP-сервер публикует список инструментов при подключении — агент видит их как обычные функции, решает, когда вызвать, **тем же циклом**, что и встроенные.

[Три примера — карточки]

**GitHub MCP server**: issues, PR, CI, алерты безопасности — по наборам (toolsets), выборочно. Явный **режим только для чтения**: инструменты на запись пропускаются, даже если запрошены.

**Playwright MCP server**: не скриншот, а **accessibility-дерево** — структурное описание элементов. Кликает, заполняет формы, генерирует тест.

**Filesystem MCP** — не один сервер, а класс: доступ к директориям **вне** рабочей копии (соседний репозиторий, общий диск).

[Мост к безопасности]
Один MCP-сервер часто закрывает **два угла** Lethal Trifecta одним подключением: доступ к данным (issues, приватные репо) **и** канал действия наружу (создать PR). Третий угол — недоверенный контент — прилетает через тот же сервер, когда агент читает чужой issue.

Задокументированный случай: GitHub MCP server — вредоносные инструкции в публичных issues эксфильтровали данные через создаваемые PR.

[Gold callout]
MCP только для чтения по умолчанию там, где запись не нужна; доступ на запись — осознанное решение о безопасности, а не настройка, которую никто не пересматривал.

## Speaker notes

MCP — открытый протокол для обнаружения и вызова внешних инструментов, мы разбирали его механику в третьей лекции. Здесь вопрос другой: что конкретно MCP даёт именно кодинг-агенту, чего у него нет без MCP. По умолчанию агент видит только файловую систему рабочей копии и shell — этого достаточно для кода, но недостаточно для всего, что вне репозитория: трекер задач, живой браузер, деплой-таргет, API непрерывной интеграции. Без MCP инженер остаётся человеком-мостом. MCP убирает мост: сервер публикует список своих инструментов при подключении, и агент видит их как обычные функции, решая, когда их вызвать, тем же циклом, что и для встроенных.

Три примера делают это конкретным. GitHub MCP server даёт доступ к issues, pull requests, CI — но по наборам инструментов, которые администратор включает выборочно; есть реальный переключатель — read-only режим, при котором write-инструменты пропускаются, даже если явно запрошены. Playwright MCP server даёт браузер: агент получает не скриншот, а структурное accessibility-дерево — описание каждого интерактивного элемента текстом. Filesystem MCP — класс серверов: доступ к файлам вне рабочей директории агента.

Отдельный риск — разрастание контекста: в отличие от skills, где тело грузится только по вызову, tool-схемы MCP-сервера — always-on, часть контекста с момента подключения. Больше серверов — больше always-on объёма конкурирует с кодом за то же окно, то же context rot. Следствие: держать набор серверов минимальным.

Короткий мост к безопасности — подробный разбор Lethal Trifecta будет в разделе про ревью. Специфика MCP: один сервер часто закрывает сразу два угла трифекты одним подключением — доступ к данным и канал действия наружу, — а третий, недоверенный контент, прилетает через тот же сервер при чтении чужого issue. Задокументированный пример: GitHub MCP server позволял добираться до приватных репозиториев через issues с prompt injection, эксфильтровавшими данные через создаваемые pull request. Следствие: read-only MCP по умолчанию, write-доступ — осознанное решение о риске.
