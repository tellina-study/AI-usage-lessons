---
id: s14
type: assertion_visual
section: "Раздел 2. Эмбеддинги"
duration_min: 1.5
assertion: "Пространство эмбеддингов — сотни-тысячи измерений; близкие по смыслу токены лежат рядом"
learning_goal: "Интуиция пространства: размерность, выучивание координат, 2D-проекция + оговорка про сравнения, а не абсолюты"
learning_outcomes: [LO1]
chapter_ref: "§2.1–2.3 [for-slide-s14]"
visual_brief: "Слева — 2D scatter (PCA-style) с ПОДПИСАННЫМИ ОСЯМИ-ПРИЗНАКАМИ: ось X «ось ≈ признак: тематика (веб-разработка ↔ кулинария)», ось Y «ось ≈ признак: инфраструктура ↔ фронтенд». 2 кластера + выброс (те же тексты, что в heatmap s15): кластер SSL {«Как настроить SSL» / «Установка HTTPS-сертификата»} — верх-лево (инфраструктура), кластер React {«Деплой React-компонента» / «Сборка React-приложения»} — низ-лево (фронтенд), выброс «Рецепт борща» — справа (кулинария). Подпись под scatter: «каждая из 1536+ осей — выученный признак; здесь показаны две». Справа — 3 факт-карточки: размерность (1536–3072, gold), обучение (похожие контексты → близкие векторы), проекция (PCA/t-SNE теряет часть структуры)."
---

# Visible content

## Title bar
«Близкие по смыслу токены лежат рядом — в сотнях-тысячах измерений»

## Body
[Слева — 2D-проекция scatter, 5 точек: 2 кластера + выброс; оси подписаны как признаки]
- ось X: «ось ≈ признак: тематика (веб-разработка ↔ кулинария)»
- ось Y: «ось ≈ признак: инфраструктура ↔ фронтенд»
- кластер «SSL» (верх-лево): «Как настроить SSL», «Установка HTTPS-сертификата»
- кластер «React» (низ-лево): «Деплой React-компонента», «Сборка React-приложения»
- выброс справа: «Рецепт борща»
- подпись под scatter: «каждая из 1536+ осей — выученный признак; здесь показаны две»

[Справа — 3 факт-карточки]

**Размерность.** Публичные embedding-модели: **1536–3072** измерения (gold); внутренние у флагманов — порядка тысяч.

**Обучение.** Координаты не задаются вручную: похожие контексты употребления → близкие векторы.

**Проекция.** Увидеть пространство можно только через PCA/t-SNE — 2D-картинка теряет часть структуры.

[Gold callout внизу]
**Что делать:** близость в этом пространстве измерима расстоянием — значит, фильтрация похожих и кластеризация без разметки и без вызова LLM возможны прямо на векторах, дёшево.

## Speaker notes

Как устроено пространство, в котором живут векторы. Каждая точка — токен или текст; координаты — те самые сотни или тысячи чисел. Смысл измерений не задаётся вручную — это направления, которые модель выучила из статистики; постфактум многие из них читаются как признаки: тематика, формальность, домен. Цель обучения — устроить пространство так, чтобы единицы, встречавшиеся в похожих контекстах, лежали близко. «Как настроить SSL» и «Установка HTTPS-сертификата» окружены одними и теми же словами — «сертификат», «сервер», «домен» — и их векторы получаются близкими; «Деплой React-компонента» и «Сборка React-приложения» соседствуют со словами про фронтенд и сборку — и тоже оказываются рядом; а «Рецепт борща» живёт в совсем другой области пространства — одинокий выброс среди технических текстов. Структура возникает не из правил, а из миллионов примеров употребления.

На картинке слева — двумерная проекция: два смысловых кластера и выброс — те же пять текстов встретятся дальше в таблице попарных сходств. Оси проекции подписаны как признаки: одна разводит веб-тематику и кулинарию, другая — инфраструктуру и фронтенд; каждая из полутора с лишним тысяч осей реального пространства — такой же выученный признак, здесь показаны две. Помните, что это упрощение: реальная проекция получается из пространства в тысячи измерений алгоритмами вроде метода главных компонент или t-SNE, которые выбирают две оси с максимумом различий. Любая плоская картинка теряет часть многомерной структуры — она годится для интуиции, не для выводов.

И повторим оговорку, потому что она стоит денег на практике: в пространствах высокой размерности «сырые» расстояния сжимаются в узкий диапазон, и абсолютное значение сходства само по себе мало о чём говорит. Работают сравнения — «этот документ ближе того» — и распределения значений в вашей конкретной задаче. Универсального порога «выше 0.8 — значит похожи» не существует; порог калибруется эмпирически на ваших данных. Дальше посмотрим, как эта геометрия работает на целых предложениях — и где у неё проходит граница.
