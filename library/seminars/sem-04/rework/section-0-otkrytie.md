---
section: 0
title: "Открытие: требования задачи, сквозной кейс и карта экипировки"
stage: "—  (рамка, не ступень лестницы)"
duration_min: 9
slot_lec03: "§4.4 «Harness / экипировка агента», карта пяти слотов; §5.2b «тонкий агент» и бремя доказательства"
status: rework-draft
supersedes: "plan.md §0, facilitator-guide.md §0, слайды v1 s01–s09"
---

# Раздел 0. Открытие: требования задачи, сквозной кейс и карта экипировки (9 мин)

> **Единое правило именования слотов на слайдах (действует во всех восьми разделах).** На слайдах
> слоты подписаны **по-русски**: «память · инструкции-правила · скиллы · субагенты · доступ наружу
> (MCP)». Термины Лекции 3 в исходном написании (`Harness`, `Skills`, `Subagents`, `Access/MCP`)
> произносятся **один раз голосом здесь, в разделе 0**, и дальше на слайдах не появляются. Пятый
> слот везде называется одинаково — **«доступ наружу (MCP)»**, без вариантов.

## A0. Сводная раскладка занятия (единая для всех восьми файлов)

Слот — 90 минут. **Материал рассчитан на 84 минуты: 6 минут — явный резерв**, а не «дотянем, если
пойдёт хорошо». Резерв расходуется на живое обсуждение, которое разойдётся сверх бюджета, и на
закрытие; если он не понадобился — занятие заканчивается раньше, и это нормальный исход.

| Раздел | Мин | Из них строго про провалы, ограничения и критерии «ещё рано» |
|---|---|---|
| 0. Открытие: требования, кейс, карта | 9 | 1,5 |
| 1. Файл инструкций | 11 | 4 |
| 2. Память | 10 | 5 |
| 3. Хук | 13 | 6,5 |
| 4. Скилл | 9 | 5 |
| 5. Доступ наружу (MCP) | 11 | 6 |
| 6. Кастомный ограниченный субагент | 10 | 5,5 |
| 7. Процесс + закрытие занятия | 11 | 4,5 |
| **Итого материала** | **84** | **38 (45%)** |
| **Резерв** | **6** | |
| **Слот** | **90** | |

Доля посчитана строго: засчитаны только блоки, целиком состоящие из разбора провалов, ограничений
и границ применимости, — разборы ответов зала (каждая строка таблиц отвечает, где вариант
ломается), блоки провалов и критерии «ещё рано». Если не засчитывать разборы ответов вовсе, доля
всё равно остаётся **26 минут из 84 = 31%**, то есть порог держится с обоих концов счёта.

### A0.1. Порядок сокращений — если живой прогон уходит за 84 минуты

Резать строго в этом порядке, не «что попадётся»:

1. Раздел 0, сбор требований: 2 → 1,5 мин (три голоса вместо трёх-четырёх).
2. Раздел 4, ход 1 совместного формирования: убрать запись предложения зала на слайд, оставить
   устный вопрос (−0,5).
3. Раздел 3, вопрос 3 совместного формирования (как хук читает команду): дать одной фразой вместо
   диалога (−0,5).
4. Раздел 1, ход 1 совместного формирования: −0,5.
5. Раздел 5, таблица замеров в токенах: прочитать вслух две строки вместо шести (−0,5).
6. Раздел 6, третий слой провала (цена, 15× против 3–10×): −0,5.
7. Раздел 2, ход 2 совместного формирования: показать запись готовой (−0,5).

### A0.2. Неприкосновенное — не резать ни при каком перерасходе

- **Блок провала в каждом из семи разделов-ступеней** и **критерий «ещё рано» в каждом** — это
  обязательная доля содержания занятия, и она посчитана впритык к порогу.
- **Разбор ответов зала** в каждом разделе — он и есть тренировка навыка «сказать нет».
- **Вопрос 5 совместного формирования в разделе 3** (что хук возвращает, если это не репозиторий) —
  несёт принцип «гейт, чей отказ молчалив, не установлен».
- **Слайд с реальным вердиктом в разделе 6** — единственное живое доказательство на занятии.
- **Закрытие занятия целиком (раздел 7, 3 минуты)** — добирать надо из резерва, а не отсюда.

---

## A. План раздела

### A.1. Зачем этот раздел существует

Раздел 0 не ступень лестницы — он ставит три вещи, без которых остальные семь разделов
рассыпаются на набор не связанных друг с другом файлов:

1. **Требования задачи предшествуют конфигурации агента.** Занятие не начинается с «вот пустой
   репозиторий, что добавим». Оно начинается с того, что зал вслух собирает требования к сайту —
   и только потом мы смотрим, чего агенту про эти требования неоткуда узнать (требование R4a).
2. **Сквозной кейс** — один репозиторий `signup-landing`, который живёт всё занятие и растёт на
   одну строку конфигурации за раздел.
3. **Карта экипировки** — пять слотов из Лекции 3 плюс два практических слоя. Карта не выдаётся
   готовой таблицей: она собирается вместе с залом по одному слоту (требование R10), потому что
   зал эти пять слотов уже слышал на лекции и должен их вспомнить, а не прочитать с экрана.

### A.2. Структура раздела (что за чем)

| # | Шаг | Мин | Механика |
|---|---|---|---|
| 0.1 | Центральный вопрос занятия | 1 | фасилитатор говорит, слайд-обложка |
| 0.2 | **Сбор требований вслух** (co-building #1) | 2 | открытый вопрос → 3 голоса → слайд собирается по строке |
| 0.3 | Сквозной кейс: задача агенту и что реально произошло | 2 | снимок терминала + поворот «он ничего не спросил» |
| 0.4 | **Первый открытый вопрос с вариантами** (R7) + разбор | 1,5 | карточки вариантов, 2 голоса, затем разбор четырёх позиций |
| 0.5 | **Карта экипировки** (co-building #2) + правило лестницы | 2,5 | восстанавливаем пять слотов Лекции 3 вопросами, дописываем два |
| | **Итого** | **9** | |

**Неприкосновенное в этом разделе (не резать ни при каком перерасходе):** разбор четырёх позиций
в шаге 0.4 (это четыре явных критерия «здесь ещё рано» — часть обязательного бюджета занятия) и
правило лестницы в шаге 0.5. **Первый кандидат на урезание:** шаг 0.2 с двух минут до полутора —
три голоса вместо трёх-четырёх, недостающие плашки фасилитатор проявляет сам.

### A.3. Состояние репозитория на входе

Буквально пустая директория — реальный захваченный вывод
(`assets/captures/01-ls-la-empty-dir.txt`):

```
$ ls -la signup-landing/
total 8
drwxr-xr-x 2 harness harness 4096 Sep 20 12:45 .
drwxr-xr-x 3 harness harness 4096 Sep 20 12:45 ..
```

Этот вывод идёт **одним** слайдом вместе с постановкой задачи, а не отдельным слайдом «пусто»
(v1-слайд s04 удалён по R5 как дублирующий обложку).

### A.4. Сквозной кейс — фиксируется здесь и не меняется до конца занятия

- **Задача.** Лендинг с формой заявки на демо-урок: одна статическая страница, поля «имя» и
  «email», кнопка отправки. Собственного бэкенда нет — заявка уходит на внешний сервис приёма форм.
- **Стек.** Vite поверх обычных HTML/CSS/JS (`npm run build` → `dist/`); Playwright
  (`tests/form.spec.ts`) проверяет, что форма не отправляется с пустыми обязательными полями;
  хостинг статический, деплой одной командой публикации.
- **Кто работает.** Один разработчик с агентом, без команды.
- **Что считается «готово».** Сборка завершается кодом 0; тест зелёный; сайт открыт по
  прод-URL; **форма реально доставляет заявку — проверено на проде.** Последний пункт и есть то,
  что не выводится ни из какого кода, и он нам понадобится в разделе 1.
- **Давление.** У заказчика назначен показ — фиксированная дата. Она порождает боли разделов 3
  («накануне показа») и 7 («утро показа»).

### A.5. Что здесь добавляется в репозиторий

**Ничего.** Ступень 0 лестницы называется «ничего», и это её содержательный ответ, а не заглушка.

### A.6. Слайды раздела

| № | Слайд | Содержимое | Материал |
|---|---|---|---|
| 0.1 | Обложка + центральный вопрос | заголовок семинара, одна строка центрального вопроса | — |
| 0.2 | Требования к задаче — **пустой каркас в двух группах по три** | слева три плашки «видно в коде», справа три «нигде не будет видно»; обе группы пустые | — |
| 0.3 | Тот же слайд, заполненный ответами зала | шесть плашек проявляются по одной; итог читается как 3 + 3 | — |
| 0.4 | Пустой репозиторий + постановка задачи дословно | `ls -la` (захвачено) + текст задачи агенту | `01-ls-la-empty-dir.txt` |
| 0.5 | Что реально произошло | агент собрал проект и **не задал ни одного уточняющего вопроса**; дерево созданных файлов | `assets/captures/36-first-task-dialogue.md` |
| 0.6 | Первый открытый вопрос + восемь карточек-вариантов | вопрос крупно, ниже — карточки вариантов выбора | — |
| 0.7 | Карта экипировки — **пустая**: пять слотов Лекции 3 без подписей | пять пустых слотов + два серых слота справа | — |
| 0.8 | Та же карта, заполненная | пять слотов названы; два практических слоя дописаны; правило лестницы одной строкой | — |

Слайды 0.2→0.3 и 0.7→0.8 — это одна и та же композиция в двух состояниях (пустое → заполненное),
а не два разных слайда с разной вёрсткой: зал должен видеть, что заполняется именно то, что он
сейчас назвал.

### A.7. Что убрано относительно v1 и почему

- **s04 «пусто» отдельным слайдом** — слит со слайдом постановки задачи (R5).
- **s06 «агент уточняет» отдельной карточкой** — удалён (R5), и вдобавок опровергнут реальными
  данными (см. A.9).
- **s08 «лестница целиком» статичной таблицей** — заменён на co-building карты (R10): сама
  концепция keystone сохранена, способ подачи изменён.
- **s09 «провал Replit»** — удалён (R5): он дублирует по смыслу разбор «правило без механической
  проверки» в разделе 3 и «работу принимают на слово» в разделе 7.

### A.8. Бюджет провалов/ограничений этого раздела

Раздел 0 — рамочный, отдельной виньетки провала в нём нет (R5). Строго внутри бюджета
≥30% засчитывается один блок: **«ступень 0 — ничего» плюс разбор трёх неверных вариантов первого
вопроса как явных критериев «здесь ещё рано»** — ≈1,5 минуты. Основной бюджет провалов несут
разделы 1–7 (сводка — в отчёте по всем восьми файлам).

### A.9. Материалы: что захвачено, что ждёт живой сессии

- **Захвачено реально:** `assets/captures/01-ls-la-empty-dir.txt` (пустая директория).
- **Захвачено реально (живая сессия Claude Code, позиция №2 списка досъёмки — закрыта):**
  `assets/captures/36-first-task-dialogue.md`. В файле три блока: постановка задачи дословно; явная
  фиксация, что **уточняющий вопрос не задавался** («все параметры задачи были однозначно выводимы
  из текста задачи, поэтому работа была начата сразу, без уточнений»); и полный финальный ответ
  агента со списком созданных файлов, включая `package.json` с именованными скриптами и строку
  «4 теста… Все 4 прогнал и они зелёные».
- **Для слайда 0.5** берутся два фрагмента этого файла: блок 2 целиком (одна фраза — «уточняющий
  вопрос не задавался») и из блока 3 — список созданных файлов плюс блок команд
  (`npm run build` / `npm run test:e2e`).

**Три обязательные нормализации при цитировании этого файла (и на слайдах 0.5, и на 1.6):**

1. **Имя директории.** В реальном ответе агента фигурирует `signup-landing-demo-8c1641c2/` —
   служебное имя прогона. На слайдах везде `signup-landing/`, как во всём остальном занятии.
2. **Канонический список файлов — четыре:** `index.html`, `src/main.js`, `package.json`,
   `tests/form.spec.ts`. В реальном ответе список шире (`src/style.css`, `vite.config.js`,
   `playwright.config.js`), но захваченные деревья всех семи ступеней
   (`21-tree-stage1.txt` … `27-tree-stage7.txt`) их не содержат. Если показать широкий список
   здесь, дерево на следующем же слайде не сойдётся с ним на глазах у зала. Берём список, который
   совпадает с деревьями.
3. **Абзац про обход песочницы** (системные библиотеки headless-браузера, `LD_LIBRARY_PATH`,
   «`sudo` заблокирован политикой контейнера») **не выносить на слайд вообще** — это и посторонний
   технический шум, и лишняя подробность про инфраструктуру, к теме занятия отношения не имеющая.
- **Отменено как нереалистичное:** позиция №3 старого списка («агент переспрашивает команду
  сборки во второй сессии»). Живой прогон показал, что агент этого не делает: команды лежат в
  `package.json`, который он читает сам. Сценарий заменён — см. раздел 1.

---

## B. Методическая глава

### B.1. Первая минута: центральный вопрос, без разгона

Начинать со слов «сегодня мы поговорим о конфигурации агентов» нельзя — это минута, потраченная
на объявление содержания вместо содержания. Вместо этого фасилитатор произносит ровно одну мысль
и один вопрос.

> «Вы все открывали чужой репозиторий и видели там файл инструкций на триста строк, папку скиллов
> на десяток методичек и конфиг с пятью подключёнными серверами. И многие копировали это к себе
> целиком, потому что "так делают в хороших проектах". Проблема не в том, что вы не умеете
> пользоваться агентом. Проблема в том, что скопированная конфигурация — это чужие решения без
> чужих триггеров: вы не знаете, какую боль каждый блок когда-то закрыл, а значит не можете
> сказать, закрывает ли он вашу боль сейчас — или просто ест контекст в каждом запросе.
>
> Вопрос, на который мы сегодня отвечаем: **у вас репозиторий и агент, который в нём работает —
> какой блок конфигурации добавить первым, и как понять, что следующий добавлять ещё рано?**»

Дальше — сразу к требованиям. Никакого оглавления занятия: карта появится через семь минут, когда
зал сам её соберёт.

### B.2. Сбор требований: первое совместное формирование (≈2 мин)

Это новая часть относительно первой версии занятия, и она стоит здесь не для разминки. Логика
такая: весь семинар про то, чего агент **не может вывести из кода**. Пока зал не назвал вслух, что
вообще требуется от сайта, разговор «чего агент не знает» повисает в воздухе — нечему быть
неизвестным, потому что никто не сказал, что известно.

**Открытый вопрос классу (дословно):**

> «Заказчик просит лендинг с формой заявки на демо-урок. Одна страница, два поля, кнопка. Назовите
> вслух: что вообще должно быть у этой задачи, чтобы вы могли сказать "готово" и показать её
> заказчику? Не как это устроено внутри — а что должно существовать.»

Вызвать 3 человек подряд, не разбирая каждый ответ по отдельности — здесь собирается список, а не
обсуждается каждая строка. Записи студенты не ведут: это устный сбор, фасилитатор проявляет
заготовленные плашки на слайде по мере того, как их называют. Если зал назвал не всё — недостающие
плашки фасилитатор проявляет сам, проговаривая: «это никто не назвал, и это показательно».

**Шесть плашек, и они заранее разложены на две группы по три** — это деление держит арифметику
всего занятия, поэтому отступать от него нельзя:

**Группа «видно в коде» (три):**

| Плашка | Почти всегда называют? | Комментарий фасилитатора |
|---|---|---|
| Поля формы: имя и email, оба обязательные | Да, первым | «Это будет видно прямо в разметке.» |
| Чем собирается (сборка в статику) | Да | «Тоже будет видно — командой в `package.json`.» |
| Чем проверяется (тест: форма не отправляется с пустыми полями) | Да | «И это видно — файлом теста.» |

**Группа «нигде не будет видно» (три):**

| Плашка | Почти всегда называют? | Комментарий фасилитатора |
|---|---|---|
| **Что считается «готово»** | Почти никогда | «Никто не назвал критерий приёмки. А он не совпадает с "тест зелёный": форма должна реально доставить заявку на проде.» |
| **Чего агент не делает сам** — не выкладывать на прод без явного запроса | Редко | «Границу действий почему-то называют последней или не называют вовсе. Запомните — она вернётся дважды.» |
| **Как убедиться руками, что заявка реально доставлена** | Почти никогда | «Не "как проверить, что собралось" — а как убедиться, что дошло. В коде этого не будет: тест проверяет другое.» |

Одна реплика в сторону, когда зал доберётся до внешнего сервиса приёма форм (он всплывает в
обсуждении почти всегда, но **отдельной плашкой не становится** — это часть первой группы):
«Обратите внимание: "своего бэкенда нет" — это **решение**, а не недоделка. По коду эти два
состояния неотличимы. Мы вернёмся к этому во втором разделе.»

Финальная реплика шага — она же мостик:

> «Смотрите, что получилось. Шесть строк требований, и они честно делятся пополам. Три — поля,
> сборка, тест — увидит в коде любой, кто откроет репозиторий. Три — что считается "готово", чего
> агент не делает без спроса и как убедиться, что заявка дошла, — **не будут видны нигде**. Они
> живут пока только у нас в голове и в этом разговоре. Сегодняшнее занятие — ровно про то, что
> происходит с этими тремя строками дальше.»

### B.3. Сквозной кейс и честный поворот (≈2 мин)

Слайд: пустая директория `signup-landing/` и постановка задачи дословно — та же формулировка,
которая реально подавалась агенту:

> «Собери лендинг с формой заявки на демо-урок: статическая HTML-страница, сборка Vite
> (`npm run build` → `dist/`), поля формы — имя и email, тест на Playwright, который проверяет,
> что форма не отправляется с пустыми обязательными полями (`tests/form.spec.ts`).»

Фасилитатор говорит прямо: это не гипотетическая постановка, эта задача реально ставилась реальному
агенту в реальном репозитории, и результат — тоже реальный (полный диалог —
`assets/captures/36-first-task-dialogue.md`). Агент собрал **`index.html`, `src/main.js`,
`package.json`, `tests/form.spec.ts`** (канонический список, см. §A.9), прописал в `package.json`
именованные скрипты сборки и теста, сам написал **четыре** теста вместо одного заказанного — и
**не задал ни одного уточняющего вопроса**. Его собственная формулировка на этот счёт есть в
захваченном файле и её стоит процитировать дословно:

> «Уточняющий вопрос не задавался. Все параметры задачи… были однозначно выводимы из текста задачи,
> поэтому работа была начата сразу, без уточнений.»

Здесь — поворот, ради которого раздел и переписан:

> «Он ничего не спросил — и прямо написал почему: всё было выводимо из текста задачи. Заманчиво
> прочитать это как "значит, ему всё было понятно". Прочитайте иначе: всё, чего он не знал, он
> решил за вас — молча. Имя тестового скрипта, структуру файлов, формат отправки формы, сколько
> тестов написать. По каждому из этих решений он не ошибся — но и не спросил.
>
> И посмотрите на арифметику. Из шести требований, которые вы только что назвали, в его контексте
> было **три** — те самые, которые видно в коде. Про остальные три — что считается "готово", чего
> нельзя делать без спроса, как убедиться, что заявка дошла, — он не знал ничего. И это его не
> остановило.
>
> Обратите внимание на последнюю фразу его отчёта: "все четыре прогнал, и они зелёные". Запомните
> её. Мы вернёмся к ней в последнем разделе.»

Мысль, которую надо оставить в зале до конца занятия: **настоящая боль не в том, что агент
переспрашивает, а в том, что он не переспрашивает.** Молчаливое решение стоит дороже вопроса,
потому что вопрос виден, а решение — нет.

### B.4. Первый открытый вопрос с вариантами (≈1,5 мин)

Здесь формат меняется по сравнению с первой версией занятия: вопрос задаётся **с готовым набором
вариантов на экране**. Причина прикладная — варианты и есть семь ступеней, которые зал будет
слышать следующие восемьдесят минут; пусть он увидит весь набор сразу и запомнит его как меню
выбора, а не восстанавливает по одному.

**Открытый вопрос классу (дословно):**

> «Перед вами репозиторий, который агент только что собрал, и вам с этим же агентом доделывать
> этот сайт дальше, неделями. Какой блок конфигурации вы добавите первым — и почему именно его?»

**Карточки-варианты на слайде (восемь, в этом порядке):**

`файл инструкций` · `память` · `хук` · `скилл` · `MCP-доступ` · `субагент` · `процесс проверки` ·
**`ничего`**

Вызвать 2 человека, не больше — бюджет здесь короткий, развёрнутый разбор будет в следующем шаге.
Задача фасилитатора — не оценить ответ, а зафиксировать его вслух: «услышал: файл инструкций,
потому что так делают. Услышал: хук, чтобы защититься заранее».

Запасная формулировка при молчании зала: «Проще: стали бы вы прямо сейчас, до первой реальной
боли, создавать хоть один из этих семи блоков? Да или нет?»

**Разбор — сразу после двух голосов, быстро, по четырём типовым позициям:**

| Прозвучавший вариант | Где это работает | Почему здесь ещё рано — и что вместо |
|---|---|---|
| «Файл инструкций — так делают в хороших проектах» | Когда есть накопленное знание, которого нет в коде | «Так делают» — не триггер, а инерция. Вместо: дождаться сигнала. Через пятнадцать минут этот ответ станет верным — но не потому, что «принято», а потому, что сработает конкретный сигнал |
| «Хук — поставлю защиту заранее, на будущее» | Когда риск конкретной команды уже стал реальным | Вы ещё не знаете, какие команды в этом проекте рискованны: защищаете гипотезу, а не факт. Вместо: назвать команду и инцидент, от которого защищаетесь. Не можете назвать — рано |
| «MCP — подключу к трекеру задач, пригодится» | Когда доступ наружу нужен повторно и из нескольких сессий | Самый дорогой из семи блоков — по контексту и по поверхности атаки (цифры в разделе 5). Вместо: обычная команда в терминале, пока не доказано, что её не хватает |
| «Ничего» | На нулевой ступени — всегда | **Не рано: это и есть ответ.** Перестаёт быть верным ровно тогда, когда появится первое реальное расхождение между тем, что знаете вы, и тем, что знает агент — это случится в следующем разделе |

Реплика на целевой ответ (важно не свернуть её в «верно, идём дальше»):

> «"Ничего" — это не уклонение от ответа, это сам ответ, и у него есть имя. Лекция 3 называет это
> **тонким агентом**: стартовая точка нового агента — один файл-инструкция и плоская память, без
> субагентов, без набора скиллов, с минимальным доступом наружу, **пока нет конкретного
> проверяемого триггера**. И формулировка там жёстче, чем "начните с малого": **бремя доказательства
> лежит на том, кто усложняет, а не на том, кто остаётся внизу.** Сегодня мы семь раз проверим,
> удаётся ли это бремя вынести.»

### B.5. Карта экипировки: второе совместное формирование (≈2,5 мин)

Слайд — пустая карта: пять пустых слотов в ряд и два серых слота справа от них. Фасилитатор не
называет слоты, а достаёт их из зала: пять слотов уже разбирались на Лекции 3, и здесь работает
припоминание, а не чтение.

> «На лекции про агентов мы разбирали, из чего сделан агент-помощник поверх базового цикла
> "планируй → действуй → проверяй → повторяй". Там это называлось одним словом — **harness**,
> экипировка агента. Это единственный раз за занятие, когда я произношу английские названия
> слотов: `Skills`, `Subagents`, `Access/MCP` — чтобы вы узнали их, когда откроете конспект лекции.
> Дальше говорим по-русски. Слотов было ровно пять. Назовите те, что помните.»

Проявлять по одному, по мере называния, с одной уточняющей репликой на каждый. **На слайде
подписи русские** — английский термин лекции идёт только голосом:

| # | Подпись на слайде | Что это | Реплика фасилитатора |
|---|---|---|---|
| 1 | **Память** | что агент помнит между сессиями | «Не контекст текущего разговора — именно то, что переживает конец сессии» |
| 2 | **Инструкции-правила** | файл-конвенция проекта + журнал задач | «То, что вы пишете сами и что грузится в начало каждой сессии» |
| 3 | **Скиллы** | переиспользуемые процедуры под повторяющиеся задачи | «Методичка, которую снимают с полки, а не регламент на столе» |
| 4 | **Субагенты** | делегирование с отдельным контекстным окном | «Отдельный сотрудник с чистым столом» |
| 5 | **Доступ наружу (MCP)** | доступ к внешним системам | «Каждое подключение — новая граница доверия» |

Если какой-то слот зал не вспомнил — назвать самому, без паузы и без укора: цель шага не проверка,
а общая точка отсчёта.

**Дальше — два серых слота справа, и их фасилитатор объясняет сам, потому что в лекции их не
было:**

> «Сегодня мы к этим пяти добавим два практических слоя. Первый — **хук**. В карте слотов его нет,
> и это не забывчивость лекции: хук — не отдельный вид экипировки, а **механическое усиление
> ровно второго слота**, операционного. Лекция показала числами, почему это усиление нужно:
> файл-инструкция сам по себе статистически не повышает успех задачи. Если слой правил работает
> ненадёжно как текст, его нужно чем-то подпереть — и это "что-то" не текст. Второй — **процесс**:
> план до кода, независимая проверка, журнал. Это тоже не новый слот, а операционализация двух
> вещей, которые лекция уже ввела: шага `check` в цикле агента и роли человека-валидатора.»

**Правило лестницы — одной строкой под картой, и его надо произнести дословно:**

> «Оставайтесь на самой нижней ступени, которая закрывает требование задачи. Поднимайтесь на
> следующую только тогда, когда можете назвать требование, которое текущая ступень не закрывает.
> Каждый подъём оплачивается новой стоимостью, новыми режимами отказа и новой поверхностью атаки.»

**Порядок семинара против порядка лекции — проговорить явно, не оставлять как расхождение:**

> «В лекции слоты шли в порядке "память → инструкции-правила → скиллы → субагенты → доступ
> наружу". Сегодня порядок будет другой: инструкции → память → хук → скилл → доступ → субагент →
> процесс. Это не противоречие. Лекция перечисляла слоты как карту — что вообще бывает. Семинар
> идёт в том порядке, в каком триггеры реально срабатывают на одном живом проекте. Карта и
> маршрут — разные вещи.»

### B.6. Мостик к разделу 1

> «Итак: шесть требований, из которых три живут только у нас в голове; агент, который ничего не
> спросил и часть решений принял сам; и карта из семи возможных блоков, из которых мы пока не
> добавили ни одного. Раздел первый — про то, что происходит, когда одно из этих трёх
> незаписанных требований впервые сталкивается с агентом, который о нём не знает.»

---

## C. Открытые вопросы и риски раздела

1. **Слайд 0.5 обеспечен материалом** (`assets/captures/36-first-task-dialogue.md`, позиция №2
   закрыта). Единственное, за чем надо следить при вёрстке, — не вынести на слайд абзац про
   обходной путь с системными библиотеками браузера: он реален, но уводит зал в постороннюю тему за
   минуту до главного поворота раздела.
2. **Сбор требований может перебрать бюджет.** Две минуты — это три коротких ответа, не дискуссия
   о том, нужен ли бэкенд. Если зал уходит в архитектурный спор, фасилитатор обрывает его на первой
   законченной мысли: «зафиксировал, это вопрос раздела 7» — и идёт дальше. Это же первый кандидат
   на урезание при общем перерасходе (см. §A.2).
3. **Припоминание пяти слотов может не сработать**, если часть зала не была на Лекции 3. План Б:
   после 10 секунд молчания фасилитатор называет слоты сам, за 20 секунд, и не превращает это в
   опрос. Подписи на слайде остаются русскими в любом случае.
4. **Деление шести требований на 3 + 3 — несущая арифметика занятия.** Она повторяется в повороте
   §B.3 («в его контексте было три»), в ходе 3 совместного формирования раздела 1 (три строки,
   которых нет в коде) и в итоговом файле инструкций (ровно три содержательных пункта). Если при
   вёрстке слайда плашки перегруппируют — сломается три места сразу.
