---
id: s10
type: process
section: "Раздел 2. RAG"
duration_min: 3
assertion: "RAG = индексация → retrieval (semantic search Лекции 2) → генерация с опорой; «не знаю» — корректный ответ, выдумка — дефект"
learning_goal: "Принцип RAG в 3 шага + инвариант grounding"
learning_outcomes: [LO7]
chapter_ref: "§2.1 [for-slide-s10]"
visual_brief: "Горизонтальный 3-stage pipeline (RIGHT_ARROW): индексация → retrieval (помечено «= semantic search Л2 §2») → генерация с опорой (ответ + цитата). Под pipeline — gold-плашка инвариант: «ответ с цитатой ≠ правдоподобный текст; „не знаю“ — корректно»."
interaction: none
---

# Visible content

## Title bar
«Принцип RAG — три шага»

## Body
[Горизонтальный 3-stage pipeline с RIGHT_ARROW, Ocean rounded boxes]

**(1) Индексация** *(заранее, офлайн)*
корпус → чанки → эмбеддинг каждого → векторное хранилище

→

**(2) Retrieval** *(на запрос)*
вопрос → эмбеддинг → top-k ближайших фрагментов
*= тот самый semantic search из Лекции 2 — не переобъясняем*

→

**(3) Генерация с опорой (grounding)**
фрагменты + вопрос → ответ **со ссылкой на источник**

[Gold callout — инвариант, низ]
**«Не знаю» / «см. источник X» — корректный ответ RAG-системы. Правдоподобный ответ при нерелевантном retrieval — это дефект, а не «лучше, чем ничего».**

## Speaker notes

RAG — retrieval-augmented generation, поиск-дополненная генерация — это архитектура, в которой перед вызовом модели система сначала извлекает релевантные фрагменты из внешнего хранилища, кладёт их в контекст вместе с вопросом, и только затем модель генерирует ответ, опираясь на эти фрагменты[1]. Введём термин: retrieval — этап поиска и извлечения релевантных фрагментов; именно он отличает RAG от «положить документ в промпт вручную».

Ключевое: механизм retrieval в RAG — это ровно тот семантический поиск на эмбеддингах, который мы построили в Лекции 2, и переобъяснять его не будем. Напомню одной фразой: текст превращается в вектор, близость векторов означает смысловую близость, поэтому фрагмент можно найти по смыслу вопроса, а не по совпадению слов. RAG надстраивает три шага. Первый — индексация, заранее, офлайн: корпус режется на фрагменты, каждый превращается в эмбеддинг и кладётся в векторное хранилище. Второй — retrieval, на запрос: вопрос превращается в эмбеддинг, по близости векторов достаются k релевантных фрагментов. Третий — генерация с опорой: фрагменты кладутся в контекст вместе с вопросом, модель отвечает, опираясь на них, и в хорошей реализации — со ссылкой на источник.

Введём ещё один термин, несущий в разделе основную нагрузку. Grounding, опора на источник, — свойство ответа быть выведенным из конкретных извлечённых фрагментов, а не сочинённым моделью из головы, с возможностью показать, из какого фрагмента взят каждый факт. Хорошая RAG-система проектируется так, чтобы ответ был grounded, и чтобы на вопрос, на который в извлечённых фрагментах ответа нет, система говорила «не знаю» или «см. источник», а не сочиняла. Сформулируем как инженерный инвариант: «не знаю» — корректный ответ RAG-системы; правдоподобный ответ при нерелевантном retrieval — дефект, а не «лучше, чем ничего». Различение «ответ с опорой и цитатой» против «правдоподобный текст» окажется центральным дальше и прямо объясняет дело Air Canada.

Источники:
[1] Lewis et al. 2020 — Retrieval-Augmented Generation (NeurIPS) — каноническая статья RAG: индексация → retrieval → генерация с опорой. https://arxiv.org/abs/2005.11401
