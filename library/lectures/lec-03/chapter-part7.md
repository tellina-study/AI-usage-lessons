---
lecture: 3
title: "Лекция 3. Архитектуры AI-систем — Часть 7 (§2 RAG, окончание)"
status: draft
version: v5.0
part: 7
of_parts: 8
---

# Глава 3 — Часть 7

> **Это Раздел 2 главы (окончание, теория).** Начало главы — [`chapter.md`](chapter.md); §2.0–§2.5 — [`chapter-part6.md`](chapter-part6.md). Здесь — §2.6 (гибридный поиск), §2.7 (векторные БД и обвязки), §2.8 (Elastic/OpenSearch и «нужна ли векторная БД»), §2.9–§2.10 (чанкирование), §2.11 (изменение системного дизайна), Deep-dive box 2, вопросы аудитории и self-check Раздела 2. Типовые прикладные RAG-кейсы (§2.12) — в [`chapter-part8.md`](chapter-part8.md). Раздел 3 — [`chapter-part2.md`](chapter-part2.md); Раздел 4 — [`chapter-part3.md`](chapter-part3.md)/[`chapter-part4.md`](chapter-part4.md); Раздел 5 — [`chapter-part5.md`](chapter-part5.md). Читать подряд; деление на файлы — следствие ENFORCED-ограничения размера документа.

## Оглавление (Часть 7)

- Раздел 2. RAG: поиск-дополненная генерация (окончание)
  - [2.6. Гибридный поиск: BM25 + плотные векторы + RRF + reranking](#26-гибридный-поиск-bm25--плотные-векторы--rrf--reranking)
  - [2.7. Векторные БД и обвязки: что где живёт](#27-векторные-бд-и-обвязки-что-где-живёт)
  - [2.8. Elastic, OpenSearch и вопрос «нужна ли мне вообще векторная БД»](#28-elastic-opensearch-и-вопрос-нужна-ли-мне-вообще-векторная-бд)
  - [2.9. Чанкирование: зачем оно существует и какие есть стратегии](#29-чанкирование-зачем-оно-существует-и-какие-есть-стратегии)
  - [2.10. Чанкирование: конфликтующие исследования, worked example и взаимодействия с пайплайном](#210-чанкирование-конфликтующие-исследования-worked-example-и-взаимодействия-с-пайплайном)
  - [2.11. Как меняется системный дизайн: индексация, свежесть, eval, стоимость на масштабе](#211-как-меняется-системный-дизайн-индексация-свежесть-eval-стоимость-на-масштабе)
  - [Deep-dive box 2](#deep-dive-box-2--что-не-вошло-в-лекцию-graphrag-stale-index-agentic-rag-мини-лестница-rag)
  - [Вероятные вопросы аудитории (Раздел 2)](#вероятные-вопросы-аудитории-раздел-2)
  - [Self-check (Раздел 2)](#self-check-раздел-2)
- §2.12 «Типовые RAG-задачи» (прикладные кейсы) — в [`chapter-part8.md`](chapter-part8.md).

---

## Раздел 2. RAG: поиск-дополненная генерация (окончание)

> Начало Раздела 2 (§2.0–§2.5) — в [`chapter-part6.md`](chapter-part6.md). Здесь — внутреннее устройство «правильного RAG 2026 года», обещанное в §2.4: как строится гибридный поиск, где живут векторы, когда выделенная векторная БД избыточна, как резать документы и что ломается при переходе прототипа в продакшн.

### 2.6. Гибридный поиск: BM25 + плотные векторы + RRF + reranking

[for-slide-rag-hybrid-disambig]
Прежде чем строить гибрид, надо снять неоднозначность самого слова — иначе «гибрид» означает три разные вещи, и инженеры спорят, не замечая, что говорят о разном. **Определения sparse и dense мы уже дали в §2.0** (разреженный — во весь словарь, почти нули, инвертированный индекс, BM25/SPLADE; плотный — 384–1024 ненулевых float, ANN-индекс, эмбеддинги); без них «гибрид чего с чем» повисает, поэтому здесь опираемся на них как на известное. Три вещи называют «гибридом», и все три легитимны:

- **(a) Лексика + dense через слияние (fusion) — главное значение в RAG.** Слить в один ранжированный список результаты BM25 (или sparse) и dense-эмбеддингов. Ключевое слово — **fusion**, обычно через **RRF** (см. ниже). Это то, что имеют в виду в подавляющем большинстве RAG-статей под «hybrid search», и именно этот смысл разворачивает весь §2.6.
- **(b) Семантика + фильтры по метаданным/атрибутам — другой рычаг.** Плотный поиск **плюс** структурные фильтры (`автор = X`, `год > 2023`, `отдел = legal`), которые сужают пул кандидатов до ранжирования. Это распространённо называют «гибридом» (особенно вендоры), но строго это **не слияние двух ранкеров**, а «поиск с предикатом» — **ортогональный** рычаг, который **композируется** с (a): можно одновременно делать BM25+dense fusion **и** фильтровать по метаданным. Смешивать (a) и (b) не нужно — это разные явления, не альтернативы.
- **(c) Sparse + dense** — частный случай (a), где «лексику» даёт не BM25, а обучаемый разреженный энкодер (SPLADE/ELSER, §2.0) плюс dense.

Дисциплина именования на слайд: «гибрид» сам по себе неоднозначен — всегда уточнять, **что** сливается (full-text BM25 + dense / sparse + dense / фильтр по метаданным + dense); **RRF — это метод слияния, а не синоним гибрида**; **фильтрация по метаданным — отдельный рычаг**, сочетающийся со всеми вариантами (Weaviate, 2026; Pinecone Docs; Qdrant, 2026). Дальше §2.6 разбирает смысл (a) — fusion лексики и dense; фильтры (b) вернутся в §2.7–§2.8 как отдельная ось выбора хранилища.

[for-slide-rag-hybrid-why]
§2.4 закончился выводом «лучший RAG 2026 года — объединение классической и семантической веток поиска». §2.6 разворачивает этот вывод в конкретную инженерную конструкцию — **гибридный поиск в смысле (a)**, — потому что именно она стала дефолтом продакшена, а «чистый dense-only» — тем самым «запахом», о котором говорилось выше. Начнём с вопроса «зачем вообще комбинировать», потому что комбинация не бесплатна. Классический **BM25** (разреженный, лексический, §2.0) находит документы по точному совпадению слов — кодов, идентификаторов, имён, редких терминов, номеров ошибок; но он бессилен перед перифразой и синонимом («car» ≠ «automobile», «сломанный вход» ≠ «authentication failure»). Плотный векторный поиск — наоборот: ловит смысл, но размывается на редких точных токенах (артикул, имя функции) и дрейфует, когда две команды называют одно и то же по-разному. Точка, которая выталкивает инженера с чистого BM25, конкретна: **дрейф синонимов и аббревиатур** — момент, когда две команды дают одному понятию разные имена, и лексическое пересечение рушится. Точка, которая выталкивает с чистого dense, — потеря лексической точности на идентификаторах. Гибрид берёт оба источника recall'а сразу. Скептическая оговорка сразу же: гибрид **не бесплатен** — вы держите два индекса, два пути запроса и шаг слияния; берите его только когда **измеренный** разрыв существует, а не по умолчанию (Maven, 2026; InfoQ, 2026; DEV «you probably don't need a vector DB», 2026).

[for-slide-rag-hybrid-rrf]
**Как слить два списка — Reciprocal Rank Fusion (RRF).** Проблема слияния в том, что BM25-оценки (неограниченные, зависят от корпуса) и косинусная близость (0–1) живут на **несовместимых шкалах** — нормировать сырые оценки хрупко. RRF обходит это, сливая **по рангам**, а не по сырым оценкам: `score(d) = Σ_по_ретриверам 1 / (k + rank_i(d))` — каждый ретривер вносит вклад по тому **рангу**, который он дал документу, а не по абсолютной оценке. Параметр `k` — сглаживающая константа, **по умолчанию 60** в большинстве реализаций (дефолт Elastic и OpenSearch); больший `k` уплощает вклад топовых рангов (демократизирует между ретриверами), меньший (иногда ~10) заостряет упор на топ каждого ретривера. `k=60` — конвенция, а не закон, её стоит подбирать `[VFY-day-of]` (dev.to RRF, 2026; Weaviate, 2026). Альтернатива RRF — **взвешенная линейная комбинация** нормированных оценок: даёт более тонкий контроль ценой необходимости нормировать и подбирать веса (больше настройки, больше хрупкости). И Elastic, и OpenSearch поддерживают оба способа. Практический дефолт — RRF: параметр-лёгкий и устойчивый.

[for-slide-rag-rerank]
**Reranking — вторая ступень.** Первичный retrieval (bi-encoder) кодирует запрос и документ **раздельно** — быстро, предвычислимо, но грубо по точности ранжирования. **Cross-encoder** прогоняет пару `[запрос, документ]` **совместно** через трансформер — заметно точнее оценивает релевантность, но его надо считать на запросе для каждого кандидата, поэтому он дорог и применяется только к переупорядочиванию top-N (например, top 50–100). Дефолт 2026 — cross-encoder-реранкер поверх гибридного retrieval (Towards Data Science, 2026; Thread Transfer, 2026). Модели-ориентиры (все `[VFY-day-of]`): **Cohere Rerank** — управляемый API, широкое языковое покрытие, брать когда не нужен свой GPU-ops; **BGE-reranker-v2-m3** — open (Apache-2.0), многоязычный, ~568M параметров, ~80 мс на батч из 50 пар запрос-документ на одном GPU класса A10G, брать для self-hosting; **ColBERT / ColBERTv2** (late interaction, вектор на токен, scoring через MaxSim) — хорош под жёсткий latency-бюджет self-hosted, но **нишевый в 2026**: несколько источников считают связку bi-encoder + cross-encoder проще и обычно эквивалентной по качеству, а per-token-векторы ColBERT раздувают размер индекса. Отдельная скептическая оговорка про бенчмарки реранкеров (BEIR, MTEB, MIRACL): **место в лидерборде не переносится на ваш корпус** — у вашего корпуса своё распределение терминов, оценивать надо на своих данных (futureagi, 2026).

[for-slide-rag-hybrid-deltas]
**Измеренные приросты — с базой (иначе цифра не интерпретируется).** Три замера, каждый с явной базовой линией:

| Настройка | Метрика | Гибрид / reranked | База(ы) | Прирост |
|---|---|---|---|---|
| WANDS (e-commerce) | NDCG | **0,7497** (настроенный гибрид) | BM25 **0,6983** / чистый вектор **0,6953** | ~7,4% над **любым** из двух |
| Финтекст + таблицы | Recall@5 | **0,816** (гибрид + neural rerank) | dense-only **0,587** | +0,229 абс. |
| Anthropic Contextual Retrieval (промахи top-20, ниже — лучше) | доля промахов | контекстные+BM25 **2,9%**; +реранкинг **1,9%** | база **5,7%** | −49%, затем −67% |

Читать дельты скептически: прирост на WANDS ~7,4% NDCG — реальный, но **скромный**, и это над уже настроенной базой; числа по финтаблицам и Anthropic крупнее именно потому, что там лексика/таблицы/перекрёстные ссылки — ровно место, где чистый BM25 **или** чистый dense ломается по отдельности. **Выигрыш гибрида зависит от корпуса**: на чистой прозе с хорошо сформулированными запросами разрыв может быть мал (denser.ai, 2026; Anthropic, 2024). Отсюда граница-«когда НЕ»: не стройте гибрид рефлекторно на корпусе, где лексика и запросы совпадают (общий язык, чистая проза) — там прирост может не окупить второй индекс и шаг слияния; сначала измерьте разрыв на своих запросах, потом усложняйте.

### 2.7. Векторные БД и обвязки: что где живёт

[for-slide-rag-vectordb]
Когда гибрид оправдан и корпус вырос, встаёт вопрос «в чём хранить и искать векторы». Здесь легко переусложнить: под-каталог движков большой, а правильный выбор для большинства систем — скучный. И сразу снимем частый вопрос: **Elasticsearch и OpenSearch — это тоже полноценные векторные хранилища**, а не что-то «вне» ландшафта. Оба держат dense-векторы и ищут по ним: Elasticsearch — тип поля `dense_vector` + kNN через HNSW (движок Lucene); OpenSearch — k-NN plugin с выбором движка (Lucene / FAISS / nmslib) и алгоритмов (HNSW / IVF); оба делают гибрид (BM25 + kNN) нативно (§2.8). Просто их честнее ставить в ландшафт под своей осью — **«поисковый движок, который ещё умеет вектора»** (search-engine-first), в отличие от **vector-native** движков (Qdrant / Milvus / Weaviate), которые строились вокруг ANN с первого дня. Обе категории — векторные хранилища; различает их **происхождение**, а оно тянет за собой набор сильных сторон (таблица ниже; детальный разбор выбора между этими двумя категориями — §2.8):

| Движок | Категория | Сильная сторона | Брать когда | Ориентир потолка |
|---|---|---|---|---|
| **pgvector (Postgres)** | реляционная БД + вектора | RAG поверх данных, которые уже в Postgres; IVFFlat + HNSW | у вас уже есть Postgres, нужна одна система и транзакционная согласованность | **~50M векторов** комфортно; деградирует за ~50–100M без pgvectorscale |
| **Elasticsearch / OpenSearch** | search-engine-first | `dense_vector`/k-NN + зрелый BM25 + **гибрид и фильтры из коробки** | у вас уже есть кластер логов/поиска того же масштаба; нужен сильный гибрид | миллионы–десятки миллионов; за ~100M — специализированные системы |
| **Qdrant** | vector-native | выделенный движок, минимум ops, нативные sparse + multi-vector, **filterable HNSW** (фильтр внутри обхода графа) | нужен целевой движок без большой платформы, богатые vector-фичи | крупный одиночный узел + кластер |
| **Weaviate** | vector-native | встроенный **гибрид** (vector + BM25 + фильтры по метаданным) | нужен гибрид + фильтрация «из коробки», без ручной обвязки | средне-крупный |
| **Milvus** | vector-native | миллиарды векторов, разделённые compute/storage | **100M+ векторов**, тяжёлое горизонтальное масштабирование | миллиарды (требует ops-инвестиций) |
| **FAISS** | библиотека, не БД | in-process ANN; только вектора, без payload/фильтров/CRUD | нужна сырая скорость + полный контроль, persistence/CRUD/фильтры пишете сами | огромный, но шардинг/persistence на вас |
| **Chroma** | vector-native (lite) | прототип/MVP, object-storage backend | ранняя разработка, ноутбуки, лёгкий прод | прототип → лёгкий прод |
| **LanceDB** | embedded | встраиваемый, local-first, колоночный (формат Lance) | без сервера; локально/edge; DS-воркфлоу | локальный/embedded масштаб |

[for-slide-rag-vectordb-axes]
По каким осям реально расходятся эти две категории — коротко, потому что именно эти оси и есть критерий выбора в §2.8. **Full-text / BM25:** у search-engine-first он зрелый и «из коробки», гибрид нативен; у vector-native он есть, но не главная сила. **Фильтрация по метаданным** (рычаг (b) из §2.6): у Elastic/OpenSearch — через Lucene-движок; у Qdrant — filterable HNSW (фильтр применяется **внутри** обхода графа, одностадийно, а не пред-/пост-фильтрацией — это заметно точнее на жёстких фильтрах). **Квантизация:** Elastic/OpenSearch догоняют скалярной/product-квантизацией (§2.8); у vector-native (особенно Qdrant) компрессия зрелее и богаче. **Multi-vector / late-interaction** (ColBERT-подобное): у vector-native активно, у search-engine-first ограниченно. **Payload с вектором:** у Elastic модель «документ + поля», у vector-native JSON-payload на вектор — first-class. И операционная ось, решающая на практике: если у вас **уже** есть текстовый/лог-поиск того же масштаба — Elastic/OpenSearch дают вектора «бесплатно» рядом и сильный гибрид; для чистой векторной нагрузки с нуля vector-native обычно проще операционно и мощнее по vector-специфичным фичам (Opster, 2026; Qdrant, 2026; Firecrawl, 2026).

[for-slide-rag-pgvector]
Отдельно — pgvector, потому что вокруг него сходятся и цифры, и вендор-война, и это учит читать бенчмарки. Бенчмарк pgvectorscale (май 2025, `[VFY-day-of]`) заявляет **471 QPS при 99% recall на 50M векторов** — и приводит это как **11,4× от 41 QPS Qdrant** при том же recall (база сравнения — QPS при фиксированном recall на 50M векторов). Но этот бенчмарк опубликован TigerData/Timescale — **вендором pgvectorscale**, поэтому читать его нужно как вендор-благоприятный; независимые сравнения (в том числе собственные бенчмарки Qdrant) ставят Qdrant наверх. **Оба верны и оба — маркетинг**; урок — не «кто быстрее», а «бенчмаркайте на своих данных и своём миксе запросов». И физические ограничения, которые реально бьют (не зависят от вендора): HNSW-индекс должен держаться **в RAM**, чтобы быть быстрым; overhead индекса ≈ **2–3× от базового размера векторов**; построение HNSW на миллиард векторов может требовать **>1,5 ТБ RAM**. За ~50–100M векторов расширения общего назначения упираются в стены пропускной способности и латентности, которых целевые системы избегают (Instaclustr, 2026; firecrawl, 2026; TigerData, 2025).

[for-slide-rag-orchestration]
**Обвязки: LlamaIndex, LangChain/LangGraph, observability — они дополняют, а не конкурируют.** Консенсус 2026 года: LlamaIndex и LangChain — **не конкуренты, а взаимодополняющие** слои (nutrient.io; Applied AI Studio). **LlamaIndex** — сильная стартовая точка, когда трудная часть задачи — retrieval по грязным приватным документам (ingestion, индексация, retrieval с разумными дефолтами). **LangGraph** (низкоуровневый stateful-runtime LangChain) стал в 2026 одним из способов строить агентов: durable execution, checkpointing, human-in-the-loop с паузой/возобновлением на часы-дни. Типовая продакшн-форма: **LlamaIndex для ingestion + retrieval, LangGraph для оркестрации, LangSmith/Langfuse для observability** (rahulkolekar, 2026). Скептическая оговорка, замыкающая на правило лестницы: для **простого RAG-эндпоинта ни один фреймворк не нужен** — несколько сотен строк своей склейки избавляют от version churn и утечек абстракций; фреймворки окупаются на агентной/многошаговой сложности, а не на «retrieve → generate». Это тот же тезис §4.3 и Anthropic *Building Effective Agents* про фреймворки агентов, применённый к RAG-слою: обвязку добавляют под сложность, а не по умолчанию.

### 2.8. Elastic, OpenSearch и вопрос «нужна ли мне вообще векторная БД»

[for-slide-rag-elastic]
Прежде чем ставить выделенную векторную БД, стоит задать вопрос, который в 2026 году отсеивает большую долю RAG-систем: **а не хватит ли того, что у меня уже есть?** Очень многие организации уже эксплуатируют Elasticsearch или OpenSearch для логов и поиска — и оба нативно умеют **RRF-гибрид** и взвешенную линейную комбинацию keyword + vector (bigdataboutique, 2026). Что несёт каждый: **Elasticsearch** — тип поля `dense_vector` + HNSW kNN; **ELSER** (Elastic Learned Sparse EncodeR) — разреженная *нейронная* модель, работает в кластере **без внешнего GPU-инференса**, стоит между BM25 и dense, заявлена превосходящей recall BM25 на **10–20%** на доменном контенте (`[VFY-day-of]`, вендор-число Elastic); **ESRE** собирает ELSER + многоязычный E5 dense + **BBQ (Better Binary Quantization)**: 32-битные float → ~2 бита, заявлено **~16× сокращение памяти при <1% потери recall** (`[VFY-day-of]`, вендор-заявление). **OpenSearch** — k-NN через движки FAISS/Lucene/Nmslib + neural search плагин; в версии 3.0 concurrent segment search для k-NN по умолчанию (до ~2,5× быстрее векторные запросы), максимум размерности вектора **16 000 через FAISS** против **4 096** у Elasticsearch (`[VFY-day-of]`). Лицензионная линия (OpenSearch — Apache-2.0, форк AWS после смены лицензии Elastic; Elastic позже вернул AGPL) — стоит знать, но не переоценивать при выборе.

[for-slide-rag-need-vectordb]
**Граница решения — конкретно, три уровня.** (1) **Классического BM25 достаточно**, когда запросы и документы делят словарь: внутренние инструменты, поиск по логам/коду/идентификаторам, хорошо курированные базы знаний, ранние прототипы. Никаких эмбеддингов, никакого векторного хранилища, а **переиндексация дёшева и наблюдаема** — это реальное операционное преимущество (Medium «ditch vector DB for BM25»; SQLServerCentral). (2) **Встроенного гибрида Elastic/OpenSearch достаточно**, когда вы уже эксплуатируете один из них и ваш масштаб — **миллионы–десятки миллионов** векторов; ELSER особенно даёт семантический прирост **без поднятия GPU-сервиса эмбеддингов и второго хранилища**. Это случай «у вас уже есть молоток». (3) **Выделенная векторная БД / спец-подход реально нужны**, когда: (а) **100M+ векторов** или миллиарды (→ Milvus); (б) нужны нативные multi-vector / late-interaction / sparse-примитивы или агрессивная настройка квантования (→ Qdrant/Milvus); (в) латентность/QPS векторного поиска при высокой конкуренции — горячий путь продукта, и движок общего назначения не тянет SLA; (г) вы хотите развязать векторное хранилище от ops вашего кластера логов/поиска. Anti-hype-вывод, замыкающий на правило лестницы: огромная доля RAG-систем **никогда** не превышает несколько миллионов чанков — на этом масштабе pgvector или гибрид Elastic/OpenSearch — **скучный правильный ответ**, а выделенная векторная БД часто преждевременная оптимизация (Pureinsights, 2026). Это ровно §2.3-логика «не строй RAG-инфраструктуру без требования», поднятая на уровень выбора хранилища: не ставь выделенную векторную БД, пока измеримый масштаб/латентность/multi-vector этого не потребовали.

### 2.9. Чанкирование: зачем оно существует и какие есть стратегии

[for-slide-rag-chunk-why]
Из трёх шагов RAG (§2.1) чанкирование недооценивают чаще прочего, а рычаг у него высокий: как вы режете документ на фрагменты, определяет, что вообще может быть извлечено. §2.4 назвал плохой chunking одним из трёх технических корней провалов. Прежде чем перечислять стратегии, ответим на вопрос от первых принципов — **почему нельзя просто засунуть документ целиком** (это база, без которой числа стратегий читаются как магия). Три независимые причины, каждая — самостоятельная. **Первая — лимит токенов эмбеддинг-модели.** Модель превращает текст в один вектор фиксированной длины и имеет максимальную длину входа: классические (BERT-семейство, многие OpenAI/Sentence-Transformers) — около **512 токенов** (~350–400 слов), long-context эмбеддеры (Jina и родственные) — до **8192 токенов** (~10 страниц). Документ на 50 страниц физически не влезает; значит, текст **обязан** быть нарезан хотя бы до окна модели — это нижняя, неустранимая причина. **Вторая — ретриву и генератору нужен текст разного размера, и это конфликт.** Ретривер (векторный поиск) точнее на **мелких** кусках: один вектор — одна «мысль»; вектор целой главы — «среднее по больнице», не близкий ни к одному конкретному вопросу. Генератор (LLM, пишущий ответ) лучше работает на **крупных** кусках: ему нужен связный контекст (определение + пример + следствие рядом), обрывок в 40 токенов он не использует, даже если тот идеально «нашёлся». Формулировка на слайд дословно: **«мелкие чанки выигрывают retrieval, крупные — генерацию»** (ZeroEntropy, 2026). Из этого конфликта растут все «умные» стратегии (sentence-window, parent-document): они индексируют мелким, а отдают крупным. **Третья — компромисс precision/recall (и dilution).** Слишком крупный чанк → *разбавление* (dilution): в векторе намешано много тем, релевантная деталь тонет (растёт recall, падает precision). Слишком мелкий → *фрагментация*: аргумент разрезан пополам, обе половины бесполезны (precision высокая на попадании, recall падает). Это классический precision/recall trade-off, управляемый размером чанка и перекрытием: «правильного» размера нет — есть точка на кривой, зависящая от ваших документов и вопросов.

[for-slide-rag-chunk-strategies]
Теперь — стратегии, от простой к сложной; для каждой: **механизм → параметры → стоимость → когда выигрывает → как ломается.** Их девять, и это не «зоопарк ради полноты» — каждая закрывает конкретную из трёх причин выше.

- **1. Fixed-size (по токенам/символам).** *Механизм:* режем каждые N единиц подряд, без оглядки на смысл, опционально с перекрытием. *Параметры:* N (256–512 токенов), overlap (0–15%). *Стоимость:* ~0 (просто счётчик). *Когда выигрывает:* однородный плоский текст (транскрипты, логи, чат); предсказуемость и дешевизна на миллионах документов; NAACL-2025 нашёл fixed-size «надёжнее и эффективнее» semantic во многих задачах. *Как ломается:* режет по живому — посреди предложения, таблицы, блока кода (тихий провал таблиц, §2.10).
- **2. Recursive / character (по иерархии разделителей).** *Механизм:* режет по самой крупной естественной границе, укладывающейся в лимит: абзац (`\n\n`) → строка → предложение → слово; дефолт LangChain. *Параметры:* список разделителей, целевой размер (~512), overlap. *Стоимость:* почти 0. *Когда выигрывает:* **прагматичный дефолт большинства систем** — на end-to-end бенчмарке дал 69% точности, обойдя даже semantic (§2.10), потому что чанки связнее и LLM реально может ими воспользоваться. *Как ломается:* слепа к семантике внутри абзаца; длинные таблицы/код всё ещё режет.
- **3. Sentence-window (эмбеддим предложение, отдаём соседей).** *Механизм:* small-to-big — индексируем отдельные предложения (мелко → точный retrieval), но LLM отдаём окно ±k предложений вокруг. *Параметры:* размер окна (±3). *Стоимость:* низкая на индексации, чуть больше хранение. *Когда выигрывает:* плотные fact-heavy тексты (документация, справочники), где ответ в одном предложении, но LLM нужен контекст вокруг. *Как ломается:* фиксированное окно может недотянуть до нужного контекста или притащить нерелевантных соседей; на таблицах «окно в предложениях» плохо определено.
- **4. Parent-document / small-to-big (эмбеддим мелкий, отдаём родителя).** *Механизм:* индексируем мелкие дочерние чанки со ссылкой на родителя (крупный блок/раздел); нашли по мелкому → отдали LLM родителя целиком. *Параметры:* размер дочернего (~256) и родительского (~2048 или «весь раздел»). *Стоимость:* низкая на индексации, больше хранение/память. *Когда выигрывает:* ответ требует широкого контекста (юр. документы, руководства с разделами) — самый «чистый» способ снять конфликт из «второй причины» без переплаты. *Как ломается:* слишком большой родитель → dilution уже на генерации; много дочерних на одного родителя → k родителей не влезают в окно.
- **5. Semantic chunking (границы по сходству эмбеддингов).** *Механизм:* эмбеддинг каждого предложения; граница там, где сходство соседних резко падает (смена темы). *Параметры:* порог падения (percentile), буфер соседей. *Стоимость:* **дорого на индексации** — эмбеддинг на каждое предложение ещё до нарезки. *Когда выигрывает:* теоретически — разнородные документы с чёткой сменой тем. *Как ломается — важно для курса:* NAACL-2025 «вычислительные затраты редко окупаются»; в одном замере дал recall 91,9% в изоляции, но лишь **54% end-to-end** — средний чанк вышел ~43 токена, слишком мало для LLM (§2.10). Самый спорный приём.
- **6. Late chunking (Jina, 2024 — эмбеддим весь документ, потом пулим по чанкам).** *Механизм:* переворачивает порядок — сначала прогоняем **весь** документ через трансформер (каждый token-эмбеддинг «знает» про весь документ), **потом** mean-pooling по границам чанков; эмбеддинги чанков «обусловлены» соседями, а не изолированы. *Параметры:* нужна long-context модель (до 8192 токенов); границы всё равно задаём. *Стоимость:* один forward-pass на документ (дешевле LLM-контекстуализации из п.7), но упирается в 8192 токена. *Когда выигрывает:* **чем длиннее документ, тем сильнее эффект** (nDCG@10 на NFCorpus +6,52% при ср. документе 1590 символов). *Как ломается:* на коротких документах эффекта нет (Quora, ~62 символа: разницы 0); документы длиннее 8192 всё равно надо предварительно резать.
- **7. Contextual retrieval (Anthropic, 2024 — LLM дописывает контекст к каждому чанку).** *Механизм:* перед эмбеддингом LLM дописывает к каждому чанку 50–100 токенов пояснения, откуда он («фрагмент из отчёта Q2 2023, раздел про выручку ACME…»); индексируем **обогащённый** чанк и в вектор, и в BM25. *Параметры:* промпт контекста, 50–100 токенов на чанк, обычно 800-токенные чанки, top-k=20. *Стоимость:* ~**$1,02 на миллион токенов документов** с prompt caching (без кэша дороже). *Когда выигрывает — с базой:* базовый provал retrieval 5,7% → 3,7% (эмбеддинги) → 2,9% (+BM25) → 1,9% (+реранкинг) (§2.1). *Как ломается:* платите LLM-инференс на каждый чанк при индексации; не спасает, если чанкинг уже разрезал таблицу/код (контекст допишется, данные потеряны).
- **8. Proposition / agentic (LLM решает границы).** *Механизм:* *proposition* (Dense-X) — LLM переписывает текст в атомарные самодостаточные утверждения с раскрытыми местоимениями; *agentic* — LLM-агент читает документ и сам расставляет границы по смысловым единицам. *Параметры:* промпты, модель — фактически LLM-инференс на весь корпус. *Стоимость:* **в 10–50× дороже fixed-size**. *Когда выигрывает:* high-stakes корпуса (медицина, право), где качество ретрива критично, а объём индексации управляем — но простые методы остаются конкурентоспособны, и преимущество LLM-чанкинга трактовать осторожно. *Как ломается:* непрактично для миллионов страниц; LLM может «додумать» пропозицию (галлюцинация уже в индексе); дорого переиндексировать при обновлениях.
- **9. Table-aware / layout-aware (структуро-осознанная нарезка).** *Механизм:* режем по **структуре** (заголовки Markdown, границы разделов, ячейки/строки таблиц, блоки кода не рвём); таблицу — отдельным чанком, при необходимости по строкам **с повтором заголовков**. *Параметры:* парсер структуры (Docling/Unstructured), fallback для слишком больших элементов. *Стоимость:* средняя (нужен качественный парсер; для PDF/сканов дороже — OCR + детекция таблиц). *Когда выигрывает:* документы с богатой структурой (техруководства с таблицами, код, статьи, финотчёты) — часто **самый большой выигрыш «за дёшево»**, потому что устраняет тихие провалы (§2.10, worked example). *Как ломается:* качество упирается в парсер — многостраничные таблицы, объединённые ячейки, вложенные структуры до сих пор ломаются.

[for-slide-rag-chunk-tradeoffs]
**Три параметра, которые реально решают** (все — стартовые точки, не законы; зависят от корпуса). Первый — **размер чанка**, центральный: слишком маленький → *фрагментация* (retrieval достаёт шаг 6 без шагов 1–5, модель выдумывает пробел); слишком большой → *разбавление* (один вектор представляет несколько тем и ни одной чисто). Дефолт-старт — **~512 токенов** (баланс precision и полноты контекста); 43-токенные чанки semantic-нарезки «ломают» генерацию даже при идеальном retrieval (§2.10). Второй — **перекрытие (overlap)**: смысл — чтобы факт на границе не потерялся целиком; старт **~10–15%** (50–75 токенов на 512). Слишком большой overlap дублирует факт в индексе (раздувает хранилище, один факт занимает несколько слотов top-k); ноль overlap рискует разрезать факт по границе. Третий — **метаданные на чанк** (источник, раздел/заголовок, дата, версия, тип: текст/таблица/код): включают (а) фильтрацию (§2.6, рычаг (b)), (б) свежесть (отбросить устаревшее), (в) провенанс (показать источник) — и дают маленьким чанкам нести атрибуцию (§2.4, кейсы legal/medical). Ни один из трёх не имеет «правильного» значения по умолчанию — подбор без метрики (§2.11) вслепую.

### 2.10. Чанкирование: конфликтующие исследования, worked example и взаимодействия с пайплайном

[for-slide-rag-chunk-studies]
Здесь — методическое ядро темы: исследования эффекта чанкирования **открыто расходятся друг с другом**, и это не ошибка, а суть — результат зависит от корпуса и метрики. Четыре замера, каждый с базой:

| Исследование | Что мерили | Результат (с базой) | Вывод |
|---|---|---|---|
| FloTorch/Vecta end-to-end (firecrawl, 2026, `[VFY-day-of]`) | recursive-512 vs semantic, end-to-end accuracy | recursive **69%** vs semantic **54%** (+15 п.п.); semantic имел recall 91,9% в изоляции, но чанки ~43 токена | хороший ретрив ≠ хороший ответ; связность важнее «умных» границ |
| NAACL 2025 Findings «Is Semantic Chunking Worth the Computational Cost?» | semantic vs fixed на 3 задачах (retrieval + генерация) | издержки semantic **редко окупаются**; fixed-200-word на уровне semantic или лучше | дорогой приём ≠ лучший; peer-reviewed |
| Клиника, ноя-2025 (MDPI Bioengineering, `[VFY-day-of]`) | adaptive/topic-boundary vs fixed baseline | adaptive **87%** vs fixed **13%** (p=0,001) | на узком высокоструктурированном домене выравнивание по границам даёт большой выигрыш; узкая выборка |
| Systematic study 2026 (arXiv:2606.00881, `[VFY-day-of]`) | LLM-методы (DenseX/GraphSeg) vs простые | LLM-методы «среди лучших», НО простые «остаются конкурентоспособны» | универсального победителя нет |

Читать это надо буквально так, как оно есть: **semantic chunking выигрывает 74 пункта в клинике (87 vs 13) и проигрывает 15 пунктов на общем корпусе (54 vs 69)** — один и тот же приём и выигрывает, и проигрывает. Почему они противоречат (объяснение, а не «странно»): разные **корпуса** (клиника — узкий, с реальными границами тем; общий бенчмарк — разнородный, границы размыты); разные **метрики** (recall в изоляции vs end-to-end accuracy — semantic оптимизирует не то, что видит пользователь); разный **размер чанка на выходе** (43 токена ломают генерацию даже при идеальном ретриве). Инженерный вывод дословно на слайд: **не карго-культите чужой рецепт** — recursive fixed-size дёшев и силён как отправная точка; единственный честный ответ — **измерить на своих данных**, а не скопировать чужой бенчмарк (смежная литература по оценке — arXiv:2603.06976, `[VFY-day-of]`).

[for-slide-rag-chunk-worked]
**Worked example: руководства по продукту с таблицами (тихий провал и его фикс).** Возьмём конкретный корпус — 300 PDF-руководств по промышленному оборудованию: пояснительный текст + таблицы спецификаций («Модель / Напряжение / Ток / Момент затяжки») + предупреждения по безопасности. Типичный вопрос: «какой момент затяжки для модели X-500?» — ответ живёт **в ячейке таблицы**. *Что выбрал бы наивный инженер:* `RecursiveCharacterTextSplitter`, 512 токенов, overlap 50 — работает для текста. Но парсер детектит таблицу и **расплющивает** её в поток markdown, а символьный сплиттер режет её по произвольному байту на границе 512 токенов. Результат: первый кусок содержит заголовки колонок + первые строки, а **все последующие — ряды цифр БЕЗ заголовков**: «12 / 480 / 8.5 / 34» без «Модель / Напряжение / Ток / Момент». Почему это **тихий** провал: система не падает — эмбеддинг считается, ретрив «находит» чанк с «34», ответ генерируется. Он просто **неправильный** — LLM не знает, что 34 — это момент затяжки для X-500, а не ток для X-700; на дашборде «retrieval работает», в проде — травма на производстве. *Что выбрать на самом деле:* (1) **table-aware нарезка** (стратегия 9, §2.9) — таблицу отдельным чанком целиком, а если больше лимита — по строкам, **повторяя заголовки в каждом куске**, чтобы каждая строка стала самодостаточной: «Модель X-500 | Напряжение 480 | Ток 8.5 | Момент 34»; (2) **parent-document** (стратегия 4) для текста — чтобы предупреждения по безопасности не отрывались от процедуры; (3) **метаданные** `{модель, раздел, тип: table/text, версия}` — фильтр «только X-500» и «только актуальная ревизия»; (4) overlap ~10% для текста, для таблиц не нужен (строки самодостаточны). *Как проверить, что не поймали тихий провал:* сгенерировать 20–30 вопросов, ответы на которые в таблицах, и убедиться руками, что в top-k приходит чанк **с заголовками колонок** (а не «голый ряд цифр»); мерить recall@k по табличным вопросам **отдельно** от текстовых — иначе провал невидим. Мораль, эхо §2.4: самый большой выигрыш здесь дала не «умная» semantic-нарезка, а **банальное уважение структуры** — дешёвый структурный фикс бьёт дорогой «умный» приём (Ragie, 2026; Docling case, 2026).

[for-slide-rag-chunk-fail]
**Ещё два тихих провала и взаимодействия с пайплайном.** Помимо таблиц: **анафора и сломанные ссылки** — «оно», «этот город», «данная политика» теряют антецедент при разрезании, чанк эмбеддится неоднозначно (лечение — parent-document, late chunking или контекстное дописывание, §2.9); и **устаревшие дефолты вроде 200 символов** — копипастнутый из туториала размер оставляет эмбеддеру почти нечего представлять. И три взаимодействия chunking с остальным пайплайном, которые нельзя забыть. Первое — **re-chunk при смене эмбеддинг-модели**: размер чанка привязан к окну модели; сменили эмбеддер → старая нарезка не оптимальна, а иногда физически несовместима (чанки длиннее нового лимита); late chunking вообще **требует** long-context модели. Практически: смена эмбеддинг-модели ≈ пере-индексация всего корпуса (§2.11), это деньги и время, а не «поменяли строчку». Второе — **chunk-level метаданные для фильтрации и свежести** отсекают до векторного поиска (только свежее, только нужный раздел), иначе RAG уверенно цитирует прошлогоднюю цену. Третье — **оценка самого chunking чувствительна к k**: chunking нельзя оценить в вакууме, только через recall@k И end-to-end accuracy (recall@k может врать — 91,9% recall при 54% ответов); при этом мелкие чанки требуют большего k, крупные — меньшего, поэтому, меняя нарезку, надо перепроверять оптимальный k (Anthropic мерили top-5/10/20 и выбрали top-20). Мета-урок сильнейших команд, замыкающий §2.9–§2.10: те, кто прошёл путь от 3–4 месяцев на RAG-кейс до надёжного деплоя, **«перестали крутить размер чанка и начали управлять слоем знаний»** — качество документов, обработка таблиц, свежесть и метаданные весят больше идеального размера чанка (atlan, 2026; Towards Data Science, 2026). Это прямое эхо §2.4 «данные важнее архитектуры retrieval».

### 2.11. Как меняется системный дизайн: индексация, свежесть, eval, стоимость на масштабе

[for-slide-rag-design-pipeline]
До сих пор RAG обсуждался как логика; §2.11 — про то, что ломается, когда логика становится продакшн-системой на миллионах документов и тысячах запросов. Первое: конвейер индексации `ingest → chunk → embed → index` в проде — это **двойной конвейер**: офлайн/батч для массовой индексации и инкрементальный для обновлений. **Первое узкое место — обычно ingestion, а не поиск**: на миллионах документов chunk+embed не помещается на одну машину (Redis; Introl, 2026). То есть узкое место сдвинуто относительно интуиции «медленный поиск»: медленна и дорога подготовка индекса, а не запрос по нему.

[for-slide-rag-freshness]
**Свежесть и переиндексация — реальная стоимость, не сноска.** **Батч-переиндексация** проста, но даёт устаревание; **CDC (change-data-capture) синхронизация** близка к реальному времени, но «утраивает операционную сложность». Один приведённый кейс: переход с батча на CDC улучшил SLA свежести **с 24 часов до субминуты** — ценой реальных ops-затрат (`[VFY-day-of]`, Redis). Специфичный для векторного хранилища режим отказа: каждая правка документа заставляет **пере-чанковать + пере-эмбеддить**; если этот джоб тихо отстаёт, retrieval деградирует **невидимо** — без ошибки, просто хуже ответы. У keyword-индексов этого режима нет (переиндексация дёшева и наблюдаема) — это отдельный аргумент **против** прыжка на векторы (Maven, 2026), эхо §2.4 «stale index». И отдельный migration-event: **смена embedding-модели требует пере-эмбеддить *все* векторы** — старые и новые несравнимы; апгрейд модели — не «поменяли строку в конфиге», а миграция всего индекса.

[for-slide-rag-eval]
**Eval retrieval — иначе система деградирует невидимо (§2.4).** Две группы метрик. **Retrieval-сторона (нужны метки):** Recall@k (ориентир ~**0,8+** при k=20 на широких датасетах — база: доля запросов, где нужный документ попал в top-k), Precision@k (**0,7+** узкий домен / **0,5+** широкий), MRR (1/ранг первого релевантного), nDCG (взвешенное по релевантности ранжирование, ориентир **0,8+** при k=10). **End-to-end (LLM-судья, метки не нужны) — RAGAS:** *Faithfulness* (обоснованы ли утверждения ответа извлечённым контекстом), *Context Precision* (доля релевантных среди извлечённых чанков), *Context Recall* (доля нужной информации, реально извлечённой) (futureagi, 2026; Label Your Data). Практика 2026: считать **обе** группы и **сверять** их, оценивать **непрерывно** (батч + онлайн A/B), а не однократно. Скептическая оговорка, замыкающая на §1.10 (LLM-судья) и §3.2 (eval fine-tune): метрики LLM-судьи дёшевы, но шумны — их надо выборочно сверять с человеческими метками, иначе они тихо награждают уверенно-неверные ответы (futureagi, 2026; arXiv:2607.07302, `[VFY-day-of]`).

[for-slide-rag-cost-scale]
**Латентность, стоимость и counterfactual «может, RAG вообще не нужен».** Ядро урока: «прототип отлично работает на 1000 документов и нескольких пользователях; ломается на миллионах векторов и тысячах конкурентных запросов» (Redis). Драйверы стоимости: ingestion/эмбеддинг, векторный поиск, **reranking** (cross-encoder на запросе — реальная per-query стоимость) и **токены LLM** (доминируют). SLA retrieval < 50 мс толкают инфраструктурные расходы вверх. Митигации с базой: **семантическое кэширование** заявлено срезающим стоимость LLM **до ~69%** в типовых продакшн-нагрузках (`[VFY-day-of]`, Redis — база: доля от стоимости без кэша); препроцессинг Contextual Retrieval стоит ~**$1,02 на миллион токенов документов** *с prompt caching* (единоразово, но нетривиально на масштабе корпуса — Anthropic). И несущий counterfactual, эхо §2.3: для базы знаний **под ~200k токенов** всё целиком кладётся в промпт с кэшированием (до ~90% сокращения стоимости) — и retrieval **пропускается вовсе**; длинный контекст + кэш — легитимная *альтернатива* RAG-конвейеру на малом масштабе (Anthropic). Общий вывод §2.11, замыкающий раздел на правило лестницы: переход POC→прод — это **другая архитектура** (двойные конвейеры, гибридный retrieval, семантический кэш, мониторинг/governance), а не подкрутка параметров; и первый вопрос перед всем этим — «а нужен ли RAG вообще, или корпус мал и стабилен настолько, что его хватает положить в окно» (Redis; ragie.ai, 2026).

### Deep-dive box 2 — что не вошло в лекцию: GraphRAG, stale index, agentic RAG, мини-лестница RAG

> **Назначение блока.** Внутреннее устройство «правильного RAG» сверх лекционного среза. Устройство гибридного поиска, реранкинга и чанкирования уже разобрано в основном нарративе (§2.6–§2.10); здесь — то, что в лекционный срез не помещается: GraphRAG, stale index как отдельный режим отказа, agentic RAG и сборка всего RAG-инструментария в мини-лестницу.

**GraphRAG (Microsoft, arXiv:2404.16130, 2024).** Обычный RAG плохо отвечает на «глобальные» вопросы («какие основные темы во всём корпусе», «собери все изменения по проекту»), потому что ответ не локализован в нескольких фрагментах. GraphRAG строит граф сущностей и связей по корпусу и работает с этим графом, а не только с плоскими чанками. Цена — дорогая индексация: исторически индексация одного датасета стоила существенно (порядка десятков тысяч долларов в начале 2024), к 2025 Microsoft радикально снизила стоимость (ориентир — единицы долларов за книгу ~32k слов) `[FACT-CHECK: GraphRAG cost figures, freshness cadence yearly; источник Microsoft Azure AI Foundry blog 2025]`. Положительный ROI у GraphRAG — только для синтеза по многим источникам, не для простых lookup. Это сам по себе пример решения по матрице: дорогая архитектура оправдана только классом задач, который дешёвая не закрывает.

**Stale index — это RAG, который вернулся к проблеме, ради которой его строили.** Тонкий, но важный момент: RAG строят, чтобы знание было свежим без переобучения модели. Но если конвейер переиндексации не настроен или сломался, индекс отстаёт от корпуса, и система начинает уверенно отвечать по устаревшим данным — то есть воспроизводит ровно тот дефект (устаревшее знание), от которого RAG должен был спасать, только теперь он скрыт за «свежим» интерфейсом. Это типовой провал и аргумент, почему observability (§2.3) включает мониторинг «возраста» индекса, а не только релевантности (полный разбор свежести/переиндексации — в §2.11).

**Agentic RAG.** Итеративный цикл retrieve → reason → retrieve (модель сама решает, что доискать, по результатам предыдущего шага). Мощнее на сложных мульти-хоп вопросах («сравни политику A и политику B и скажи, что изменилось»), потому что один проход retrieval такой вопрос не закрывает. Цена — **новые** режимы отказа поверх обычных RAG-провалов: *retrieval thrash* (модель крутит почти одинаковые запросы по кругу, термины осциллируют, качество плоское — аналог петли агента, §4.10); *tool storms* (лавина вызовов retrieval); *context bloat* (контекст распухает от накопленных фрагментов — прямой триггер context rot, §1.6); распространение галлюцинации по итерациям (ошибочный промежуточный вывод становится входом следующего retrieval); отравление памяти. Архитектурно agentic RAG — это уже **гибрид RAG и агента** (§4, Часть 3), и к нему применимы **обе** группы режимов отказа: и RAG (этот раздел), и агентов (§4.10, Часть 4). Поэтому по правилу лестницы (§5.1, Часть 5) к нему поднимаются только когда обычный RAG доказанно не закрывает класс мульти-хоп вопросов, а их ценность оправдывает кратный рост стоимости и риска.

**Резюме «правильного RAG» как лестницы внутри лестницы.** Даже внутри ступени «RAG» есть своя мини-лестница сложности: (1) базовый dense-retrieval + строгий grounding + observability — дефолт; (2) + structure-aware chunking — когда документы имеют структуру (а они почти всегда имеют, §2.9); (3) + hybrid search — когда в корпусе есть коды/идентификаторы/редкие термины (§2.6); (4) + reranking — когда нужна высокая точность top-k и есть бюджет на вторую модель (§2.6); (5) GraphRAG — только под глобальные/мульти-хоп вопросы; (6) agentic RAG — только под итеративные мульти-хоп задачи высокой ценности. Правило то же: не подниматься без требования. Самая частая ошибка — прыгнуть сразу к (5)/(6), потому что «звучит мощно», пропустив observability на (1), без которой даже базовый RAG — скрытая бомба (§2.4).

### Вероятные вопросы аудитории (Раздел 2)

**В1. Если контекстные окна растут (миллион токенов и больше), не умрёт ли RAG сам собой — просто положим весь корпус в окно?**
Нет, и причина — context rot из §1.6, теперь с прямой эмпирикой. NoLiMa (§2.1) показал, что при заявленных окнах 128K–2M эффективная длина большинства моделей — всего 2–4K токенов; работа arXiv:2510.05381 (2026) добавляет: удлинение контекста роняет качество **даже при идеальном retrieval**. Плюс стоимость и латентность растут с числом токенов в каждом запросе — RAG выходит на порядки дешевле за запрос (по одному отраслевому замеру — до ~1000×, порядок величины, §2.3) и решительно обгоняет full-context, когда доля релевантного падает ниже ~20%. Есть и симметричный counterfactual (§2.11): для корпуса **под ~200k токенов** правильный ответ — положить всё в окно с кэшированием и **не строить RAG вовсе**. Корректная формулировка: длинный контекст и RAG — не «победитель и проигравший», а две архитектуры под разные режимы, и консенсус 2026 — гибрид (retrieval сужает базу → длинное окно рассуждает по ней). Универсального победителя нет.

**В2. Как на практике понять, что retrieval «тихо сломался», если система не падает?**
Только измерением — это и есть смысл observability из §2.3 и eval-метрик §2.11. Минимальный набор: golden set (определён в §2.4 — репрезентативный набор вопросов с известными правильными источниками); регулярно гонять его и мерить recall@k и nDCG (§2.11); алерт на падение метрики; мониторинг распределения «уверенности» retrieval (близости векторов) — рост доли запросов, где даже лучший кандидат далеко, — ранний сигнал деградации; отдельно — мониторинг «возраста» индекса (stale index, §2.11). Если ничего из этого не настроено, ответ на вопрос «сломался ли retrieval» звучит как «мы не знаем» — а это и есть скрытая бомба.

**В3. Где проходит граница «корпус помещается в окно»? Откуда взялись ~200k токенов?**
Это ориентир Anthropic для их моделей, а не закон природы. Смысл порога не в точном числе, а в принципе: если корпус целиком влезает в окно и при этом стоимость/латентность «класть его весь каждый раз» приемлемы, а кэширование префикса снимает повторную оплату — отдельная RAG-инфраструктура не окупается. Конкретное число зависит от модели, бюджета и частоты запросов. Инженерный навык — не запомнить «200k», а уметь оценить: влезает ли, сколько стоит класть весь корпус в каждый запрос, окупит ли это сложность retrieval.

**В4. Если RAG так легко ломается на масштабе, может, для важных доменов лучше fine-tuning на корпусе?**
Это распространённая и опасная инверсия, и ответ на неё — весь §3 (Часть 2). Коротко: fine-tuning на знаниях не решает проблему свежести (знание устаревает, переобучать дорого) и добавляет свою — catastrophic forgetting. Для меняющегося знания правильный ответ — чинить RAG (observability, chunking, hybrid+rerank) или, для малого корпуса, длинный контекст, но не «зашить знание в веса». Подробный разбор — §3.

**В5. Что отвечать заказчику, который говорит «просто сделайте RAG по нашим документам, это же стандартно»?**
Тремя вопросами, которые есть в §2.2–§2.3: (1) Сколько документов и как часто они меняются — может, корпус влезает в окно и RAG не нужен (§2.3, пункт 1)? (2) Это вопросы со свободными ответами или выдача фиксированных регламентов — во втором случае нужен детерминированный lookup, а не RAG (§2.3, пункт 2; Air Canada, §2.5)? (3) Кто и как будет измерять качество retrieval после роста корпуса — без ответа на этот вопрос RAG станет скрытой бомбой (§2.3, пункт 3; §2.4)? «Стандартно» — не инженерный аргумент; инженерный аргумент — ответы на эти три вопроса.

**В6. Гибрид «почти всегда лучше» — значит, ставить его по умолчанию?**
Нет — это тот же карго-культ, что «строить RAG на всё». Гибрид не бесплатен: два индекса, два пути запроса, шаг слияния (§2.6). Прирост зависит от корпуса: на WANDS это ~7,4% NDCG над уже настроенной базой (скромно), а крупные приросты (Anthropic 5,7%→1,9%, финтаблицы 0,587→0,816) получены там, где чистый BM25 **или** чистый dense ломается — жаргон, таблицы, перекрёстные ссылки. На чистой прозе с хорошо сформулированными запросами разрыв может быть мал. Правило: сначала измерить разрыв на своих запросах (BM25 против dense против гибрида), потом добавлять второй индекс — а не «гибрид, потому что 2026».

**В7. Как выбрать векторную БД — Qdrant, Milvus, pgvector или Elastic?**
Начать с вопроса «а нужна ли выделенная векторная БД вообще» (§2.8). Под ~несколько миллионов чанков скучный правильный ответ — pgvector (если у вас уже Postgres) или встроенный гибрид Elastic/OpenSearch (если вы уже их эксплуатируете): ELSER даёт семантический прирост без GPU-сервиса эмбеддингов и второго хранилища. Выделенная БД оправдана под конкретный триггер: 100M+ векторов → Milvus; нужны multi-vector/late-interaction/агрессивное квантование → Qdrant/Milvus; латентность/QPS при высокой конкуренции — горячий путь продукта; развязка ops векторного хранилища от кластера логов. И читать бенчмарки скептически: pgvectorscale-vs-Qdrant — вендор-война (§2.7), «оба верны и оба маркетинг», мерить на своих данных. Это §2.3-логика «не строй инфраструктуру без требования», поднятая на выбор хранилища.

**В8. Semantic chunking звучит умнее fixed-size — почему не использовать его всегда?**
Потому что исследования открыто расходятся, и на многих корпусах semantic chunking проигрывает и по качеству, и по стоимости (§2.10). В Feb-2026-бенчмарке recursive 512-token был №1 при 69% против 54% у semantic (который породил крошечные ~43-токенные фрагменты); NAACL 2025 прямо пишет «вычислительные издержки не оправданы стабильным приростом». Клинический 87% vs 13% — реальный, но узкодоменный. Дефолт — recursive fixed-size (дёшев и силён); усложнять стратегию только под измеренный на своём корпусе разрыв. И держать в уме тихий провал — таблицы: расплющивание таблицы в текст рушит связи строк/столбцов без единой ошибки (§2.10).

**В9. Что важнее для качества RAG — модель эмбеддинга, чанкинг или данные?**
Данные и их обработка — раньше и сильнее, чем «лучший эмбеддинг». JMIR (§2.4): один и тот же RAG дал 6% галлюцинаций на кураторской базе и 35% на веб-шуме — разница только в данных. Мета-урок сильнейших команд (§2.10): они «перестали крутить размер чанка и начали управлять слоем знаний» — качество документов, обработка таблиц, свежесть, метаданные. Порядок инженерных усилий: сначала курировать корпус и чинить ingestion/таблицы/свежесть (§2.11), потом гибрид+реранкинг (§2.6), и только под измеренный разрыв — экзотика (GraphRAG, agentic). Смена embedding-модели — вообще migration-event (пере-эмбеддить всё, §2.11), а не «подкрутка».

### Self-check (Раздел 2)

1. Опишите три шага RAG (индексация, retrieval, генерация с опорой) и явно укажите, какой из них — это семантический поиск из Лекции 2, §2, который мы НЕ переобъясняем. Что такое grounding и почему «ответ с цитатой» принципиально отличается от «правдоподобного текста»?
2. Перечислите четыре признака «когда RAG — правильный выбор» и три критерия «когда RAG — НЕ правильный выбор». Для каждого из трёх «не» приведите пример задачи из вашей предметной области.
3. Объясните фразу «вернул что-то ≠ вернул правильное» через отсутствие у RAG встроенного сигнала «не нашёл». Почему RAG без observability — это «скрытая бомба», а не просто «менее качественная система»?
4. Разберите дело Air Canada в терминах раздела: какой архитектуры требовала задача, какая была выбрана, в чём именно состоял отказ grounding, и какие две архитектуры были бы правильными (одна — если RAG не нужен, другая — если нужен).
5. Объясните тезис «сходство ≠ релевантность» через результат NoLiMa: почему «эффективная длина 2–4K при заявленных 128K–2M» — это аргумент против стратегии «положить всё в большое окно» и одновременно против «наивного RAG» на чистой векторной близости? Как это связывает §1.6 (context rot) и §2.4?
6. На примере JMIR (6% vs 35% галлюцинаций при одном и том же RAG) объясните, почему разрыв в 29 п.п. нельзя закрыть более крупной моделью или более строгим промптом. Что означает формулировка «RAG не устранил галлюцинации, а переместил проблему в retrieval-слой»?
7. Как искали в тексте **до** эмбеддингов (§2.0)? Объясните роль инвертированного индекса, булева поиска и разницу TF-IDF/BM25. Почему **неверно** говорить «семантический поиск построен на BM25» — какова реальная родословная (две независимые ветки, кто пришёл первым, что к чему «добавили»)? Дайте определения разреженного и плотного вектора (по одному свойству из таблицы §2.0). Почему dense-only в 2026 — «запах», а гибрид — **слияние (fuse)** двух независимых веток, а не наследование одной из другой?
8. Разведите три смысла слова «гибрид» (§2.6): (a) fusion лексики и dense, (b) семантика + фильтры по метаданным, (c) sparse + dense. Какой из них — главный в RAG и почему (b) — «ортогональный рычаг», а не альтернатива (a)? Зачем сливать по рангам (RRF), а не по сырым оценкам? Чем bi-encoder отличается от cross-encoder-реранкера, и почему реранкер применяют только к top-N? Приведите один измеренный прирост гибрида **с его базой**.
9. По §2.7–§2.8: почему Elasticsearch и OpenSearch — это **тоже** векторные хранилища (что конкретно они хранят и как ищут)? В чём разница осей «search-engine-first» (Elastic/OpenSearch) и «vector-native» (Qdrant/Milvus/Weaviate)? На каком масштабе выделенная векторная БД — преждевременная оптимизация, и какие четыре триггера её всё же оправдывают? Почему бенчмарк pgvectorscale-vs-Qdrant нельзя принимать за истину?
10. По §2.9–§2.10: назовите компромисс размера чанка (фрагментация vs разбавление) и объясните, почему recursive fixed-size — сильная базовая линия, а semantic chunking часто не окупается (приведите конфликтующие замеры с базами). Почему таблицы — «тихий» провал retrieval, и как его лечить?
11. По §2.11: почему первое узкое место продакшн-RAG — обычно ingestion, а не поиск? Что означает «смена embedding-модели — migration-event»? Назовите две группы eval-метрик (label-based и LLM-судья) и объясните, почему их надо сверять. В чём counterfactual «под ~200k токенов RAG не нужен»?

---

> **Продолжение — Часть 8.** Прикладной раздел §2.12 «Типовые RAG-задачи» (шесть реальных архетипов: поддержка/база знаний, Q&A по документации, поиск по коду, юридический поиск, deep-research, enterprise смешанные данные + разбор «когда RAG — не тот инструмент») — в [`chapter-part8.md`](chapter-part8.md). Это прикладное завершение Раздела 2: теория §2.0–§2.11 разложена на конкретные кейсы. Раздел 3 («Fine-tune vs промпт vs RAG») — в [`chapter-part2.md`](chapter-part2.md). Раздел 4 («Агенты») — [`chapter-part3.md`](chapter-part3.md)/[`chapter-part4.md`](chapter-part4.md). Раздел 5 («Фреймворк решения»), Заключение и Источники — [`chapter-part5.md`](chapter-part5.md).

---

**Конец Части 7 (главы 3).**
