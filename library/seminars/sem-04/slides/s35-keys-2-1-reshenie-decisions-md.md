---
id: s35
type: code_artifact
duration_min: 0.75
assertion: "Новое — не файл (он заведён на дне 0), а первая содержательная запись в нём, датированная днём, когда решение записали"
learning_goal: "Конкретное решение кейса 2.1 — содержимое уже существующего файла, без diff создания: файл существовал с дня 0, новая в нём только запись"
visual:
  pattern: code_artifact
  primary: "DECISIONS.md целиком, реальный файл. Справа — состояние репозитория на конец первой недели, строка DECISIONS.md выделена gold. БЕЗ diff-панели: файл существовал с дня 0, диффа создания на слайде нет."
  backup: "Продуктовый слой дерева — assets/captures/22-tree-stage2.txt; spec.md и README.md заведены на дне 0 и в тот git-снимок не вошли. На слайде состояние репозитория показывается целиком. Дата записи (2026-09-26) — конец первой недели по сквозному календарю занятия, не день создания репозитория (2026-09-20)."
---

# Первая запись в уже заведённом файле

## Assertion

Новое — не файл (он заведён на дне 0), а первая содержательная запись в нём, датированная днём, когда решение записали.

## Visual

Слева — файл целиком, реальный:

```markdown
# Decisions

Append-only log of why, in date order.

## 2026-09-26 — сторонние виджеты в форму не добавляем
Форма — два поля (имя, email), хватает нативных `required`/`pattern` в `index.html`.
Сторонний виджет — лишняя зависимость и лишние килобайты в сборке ради этого объёма.
```

Справа — состояние репозитория на конец первой недели, строка `DECISIONS.md` выделена gold:

```
README.md
spec.md
DECISIONS.md
CLAUDE.md
AGENTS.md
index.html
package.json
src/main.js
tests/form.spec.ts
```

## Speaker notes

Два механизма, не один. Авто-память рантайма живёт вне репозитория, в каталоге памяти на машине разработчика: индекс из первых 200 строк или 25 КБ плюс тематические файлы, которые агент создаёт сам. Команда «запомни: сторонние виджеты в форму не добавляем» создаёт тематический файл именно там — в дерево справа он не попадает.

`DECISIONS.md` — второй механизм, git-трекнутый append-only лог, заведённый на дне 0. Новое здесь — не файл, а первая содержательная запись в нём, и дата у неё сегодняшняя: решение принято несколько дней назад, а записано только сейчас. Это реальный файл реального демо-репозитория, не пример, сочинённый для слайда.
