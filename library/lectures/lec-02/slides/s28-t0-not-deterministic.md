---
id: s28
type: case_study
section: "Раздел 4. Сэмплинг и генерация"
duration_min: 3.5
assertion: "T=0 не даёт детерминизма: на стандартном vLLM — 80 уникальных ответов из 1000 одинаковых запросов"
learning_goal: "Механизм недетерминизма при T=0 (batch-инвариантность, не «floating-point вообще»); цена решения и следствия для тестов"
learning_outcomes: [LO4, LO6]
chapter_ref: "§4.3 (chapter-part2.md) [for-slide-s28]"
visual_brief: "Слева 45% — крупная цифра-экспонат в Ocean rounded box: «80 / 1000» (gold, 96pt) с подписью «уникальных ответов при T=0, стандартный vLLM (Thinking Machines Lab, 2025)». Справа 55% — схема причины: три плашки-запроса разных пользователей сливаются в «серверный батч» переменного размера → плашка «другой порядок суммирования» → «младший разряд решает выбор токена» → авторегрессия разносит расхождение. Внизу две плашки: «Решение существует: 1000/1000 побитово» и «Цена: ~35% пропускной способности → выключено по умолчанию». Gold callout про тесты."
---

# Visible content

## Title bar
«T=0 не даёт детерминизма: 80 уникальных ответов из 1000»

## Body
[Слева — экспонат, Ocean rounded box]

**80 / 1000**
уникальных вариантов ответа на идентичный запрос при T=0 — стандартный vLLM (открытый инференс-сервер; Thinking Machines Lab, сентябрь 2025)

[Справа — механизм, цепочка плашек]

**Причина — не «floating-point вообще», а отсутствие batch-инвариантности ядер:**
1. Сервер группирует одновременные запросы **разных пользователей** в батчи
2. Размер батча зависит от чужой нагрузки в эту миллисекунду
3. Разный размер батча → **разный порядок суммирования** → младшие разряды другие
4. Два близких кандидата в argmax → младший разряд решает выбор токена → авторегрессия разносит расхождение по всему ответу

[Две плашки внизу]
- **Решение существует:** batch-инвариантные ядра — 1000/1000 побитово идентичны
- **Цена:** ~35% пропускной способности → провайдеры не включают; `seed` у OpenAI — официально «mostly deterministic», в основном детерминировано

[Gold callout]
**Получить гарантированно детерминированный ответ от облачной LLM сегодня нельзя — стройте процессы с учётом этого. Следствие: не сравнивайте ответы побитово, сравнивайте семантически или по структуре.**

## Speaker notes

Это утверждение — самое коварное из шести, потому что оно почти правда, и на нём строят тесты и конвейеры. Вы ставите ноль температуры, ожидая: одинаковый запрос — одинаковый ответ. Проверка на реальной инфраструктуре: стандартный vLLM, тысяча запусков идентичного запроса — восемьдесят уникальных вариантов ответа. Ноль действительно делает выбор argmax-детерминированным — но само распределение, из которого берётся argmax, оказывается чуть разным от запуска к запуску.

Причина глубже, чем расхожее «floating-point на GPU». Главный виновник — отсутствие batch-инвариантности вычислительных ядер. Сервер провайдера динамически группирует одновременные запросы разных пользователей в батчи; размер батча зависит от нагрузки в конкретную миллисекунду; а многие ядра при разном размере батча используют разный порядок суммирования. Сложение чисел с плавающей точкой неассоциативно — порядок меняет младшие разряды, — и когда два кандидата в argmax близки, младший разряд решает выбор токена; дальше авторегрессия разносит расхождение по всему ответу. Ваш «детерминированный» запрос недетерминирован, потому что вы делите сервер с другими пользователями, и их трафик меняет размер вашего батча.

Самое поучительное: проблема решаема — и решение отвергнуто экономикой. Те же авторы написали batch-инвариантные ядра: тысяча из тысячи запусков побитово идентичны. Цена — около 35 процентов пропускной способности, поэтому провайдеры этот режим по умолчанию не включают; отсюда и статус параметра seed у OpenAI — официально «в основном детерминировано», без гарантий.

Главный вывод формулируем так, и порядок здесь важен: получить гарантированно детерминированный ответ от облачной LLM сегодня нельзя — это не временный баг, а следствие экономического выбора провайдеров, и проектировать процессы нужно с учётом этого факта, а не в надежде, что «в следующей версии починят». Отсюда, как следствие, а не как главный тезис: не стройте тесты на побитовом сравнении ответов — сравнивайте семантически или по структуре; воспроизводимость фиксируйте датасетом и метрикой. А если строгий детерминизм действительно нужен — аудит, регуляторика, — это отдельное требование к инфраструктуре с ценником в треть пропускной способности, заложенное в бюджет с самого начала.
