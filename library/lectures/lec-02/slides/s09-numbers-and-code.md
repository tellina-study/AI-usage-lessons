---
id: s09
type: comparison
section: "Раздел 1. Токенизация"
duration_min: 2.5
assertion: "Токенизатор режет по частоте, а не по структуре: числа — чанками по 3 цифры, код — целыми отступами"
learning_goal: "Числа и код как второй/третий случай расхождения нарезки со структурой; формат compact comparison (не case-study, чтобы не дублировать s08)"
learning_outcomes: [LO6]
chapter_ref: "§1.5 [for-slide-s09]"
visual_brief: "Строка-мотивация под заголовком: числа и код — самые частые «нетекстовые» входы; от нарезки зависят арифметика модели и бюджет токенов. Колонка «Числа»: 1000000 → [100][000][0] — чанки слева направо ≠ разряды; right-to-left нарезка улучшает арифметику, спецсхемы до +33% (gold). Колонка «Код»: GPT-2 — 16 токенов на отступ 4-го уровня; GPT-4 — пробелы группами (словарь чинится — но не под все задачи сразу). Внизу — блок «Что делать»: разделители разрядов, вычисления в инструмент, единообразные отступы, + строка про авто-роутинг в готовых чат-продуктах."
---

# Visible content

## Title bar
«Токенизатор режет по частоте, а не по структуре»

## Body
[Строка-мотивация под заголовком]
Числа и код — самые частые «нетекстовые» входы: от их нарезки зависят арифметика модели и ваш бюджет токенов.

[2 колонки в Ocean rounded boxes]

**Числа**
`1000000` → `[100][000][0]`
Чанки по 3 цифры слева направо ≠ разряды.
Нарезка справа налево улучшает арифметику; задача-специфичные схемы — **до +33% точности** к стандартной нарезке (gold).

**Код**
GPT-2: **16 токенов** на отступ 4-го уровня.
GPT-4: пробелы группами — словарь чинится, но не под все задачи сразу.

[Внизу — 4 приёма одной строкой, с заголовком группы]
**Что делать:**
**Разделители разрядов** («1 234 567») · **вычисления — в инструмент** · **единообразные отступы** · **в готовых чат-продуктах счёт уже уходит в code interpreter автоматически — звать инструмент самому нужно в нестандартных случаях и в своих приложениях поверх API**

## Speaker notes

Зачем инженеру знать, как режутся числа и код: это самые частые «нетекстовые» входы, и от их нарезки напрямую зависят арифметика модели и ваш бюджет токенов на код. Два компактных примера того, как частотная нарезка расходится со структурой данных.

Числа. Токенизатор cl100k_base стандартизировал нарезку чисел чанками по три цифры слева направо: миллион превращается в группы, границы которых не совпадают с разрядами. Человек читает разряды справа налево — тысячи, миллионы; модель получает нерегулярные блоки — и это прямой источник части арифметических ошибок. Исследования подтверждают диагноз с обеих сторон: принудительная нарезка справа налево заметно улучшает численное рассуждение, а задача-специфичные схемы токенизации чисел давали до плюс тридцати трёх процентов точности на арифметике с большими числами относительно стандартной нарезки.

Код. GPT-2 кодировал каждый пробел отступа отдельным токеном: строка на четвёртом уровне вложенности тратила шестнадцать токенов только на отступ. Токенизаторы поколения GPT-4 группируют пробелы в единые токены, целясь именно в Python-стиль. Это редкий случай, когда проблему токенизации починили изменением словаря, — полезный контраст к strawberry: словарь можно оптимизировать под частый класс входов, но нельзя совместить нарезку со структурой всех задач сразу.

Четыре приёма на вынос. Значимые числа записывайте с разделителями разрядов — «1 234 567» вместо сплошной строки: разделители выравнивают границы токенов по разрядам. Любую арифметику важнее прикидки выносите в инструмент — модели прекрасно пишут код для вычислений, которые не могут выполнить сами. Для кода — единообразное форматирование и оценка бюджета по измерению на реальных файлах: средний ориентир «четыре символа на токен» для кода систематически врёт. И важная поправка для практики: в готовых чат-продуктах — ChatGPT, Claude в веб-интерфейсе — счёт и арифметика уже автоматически уходят во встроенный инструмент вроде code interpreter, минуя прямой проход модели; то, что мы сейчас разобрали, становится критичным именно тогда, когда вы вызываете модель через API напрямую — в своих приложениях и агентах, где такого автоматического роутинга нет, и в нестандартных случаях, которые готовый продукт не распознал как «нужен инструмент».
