VERDICT: APPROVE-CLEAN

# Methodology Critic Report — Лекция 4 deck **v3.1 delta** — 2026-05-17

**Артефакт:** `library/lectures/lec-04/rendered/lec-04.pptx`+`.pdf` (32 слайда, видимый слой re-rendered из v3.1 pdf 07:05 + speaker notes) + `deck.yaml`/`deck-part2.yaml`.
**Issue:** #99 · **Branch:** issue-99-lec-04-software-production · **Phase:** 7 re-QA **delta** (post-v3 APPROVE-WITH-POLISH 2 P1).
**Скоуп delta:** только то, что меняла v3.1 — НЕ пере-ревью A→D-хребта (подтверждён YES в v3, не изменён). Проверено: (1) LO7-s29 5 осей; (2) strict-in counter консистентность 3 артефактов; (3) v3.1 polish не повредил методику; (4) tone-strip полнота + научная честность точек возврата.
**Метод:** v3-snapshots устарели (06:36, pre-v3.1); re-рендер delta-слайдов из v3.1 pdf @145dpi + независимый zip-grep видимого слоя XML (notesSlide исключены) + grep speaker notes.

## Severity counts
- **P0: 0**
- **P1: 0** (оба v3 P1 — P1-1/P1-2 LO7-s29 — **CLOSED**; новых P1 v3.1-polish не внёс)
- **P2: 1** (унаследованный s06 gold-доминанта — НЕ блокер, информационный; v3.1 его не трогала)

**Counter-check (mandatory):** 0 P1 → APPROVE-CLEAN корректен (не форсируется REVISE; strict-in ≥30% holistic подтверждён, single-artifact concentration отсутствует, ни одна DoD-метрика не провалена).

---

## 1. v3 P1-1/P1-2 (LO7 s29) — **CLOSED**

Проверка по запросу п.1 (a)/(b)/(c) на **видимом** re-rendered s29 (не только source):

**(a) Все 5 канонических осей §6.1 присутствуют на видимом s29 — YES.** Рендер v3.1 s29 несёт именованную ленту из 5 осей ≥14pt bold:
1. **Незнакомость кода** — знакомый типовой → AI надёжен · приватный/легаси → надёжность падает
2. **Обратимость операции** — обратимо → выше автономия · необратимо → жёсткий человеческий гейт
3. **Критичность / прод** — некритичное → шире автономия · прод и пользователи → строгий гейт
4. **Аудит / ответственность** — нет следа → можно · нужен владелец решения → человек на merge
5. **Цена ошибки** (gold-выделена как вето/приоритет-ось) — задаёт приоритет осей — она вето: одна высокая опускает потолок

Точное соответствие chapter §6.1 (source of truth, НЕ правилась — book-first сохранён). Заголовок слайда «Чем мерить требование задачи — пять осей выбора» эксплицитно фиксирует «пять». Полная 5×4-сетка корректно ушла в speaker notes (читаемость из зала — owner-decision v1 SYNTHESIS соблюдён, не регрессировал).

**(b) «Повторяемость» больше НЕ 6-я ось матрицы — YES, семантически корректно.** Перемещена в нижний teal pre-фильтр callout «Сначала отсев: детерминированная, проверяемая, **повторяемая** задача (парсинг, валидация по схеме, арифметика) → обычный код, без AI вовсе — остальные оси не нужны». Это точный перенос §6 «не AI вовсе» (предсказуемое+верифицируемое+повторяемое → обычный код). build_lec04.py L1954 несёт явный design-комментарий «"Повторяемость" — НЕ ось матрицы (pre-фильтр)». v3-дефект «3 оси + чужая 3-я» устранён по существу, не косметически.

**(c) LO7 success-критерий достижим по видимому s29+s31 — YES.** Success-критерий (plan §3 L77 + chapter §6.1 L168): «обосновать уровень+конфиг по 5 осям + когда было бы иначе». Видимый s29 даёт 5 named-осей + правило «назови решающую ось по цене ошибки, проверь остальные на блокеры» (sub-caption + dominant-плашка). Видимый s31 (re-rendered) даёт 8-вопросный чек-лист, операционализирующий те же оси (Q1 pre-фильтр · Q2 обратимость · Q3 тест-оракул · Q4 кто ревьюит/мержит=аудит · Q5 секреты · Q6 знакомость · Q7 цена/навык · Q8 конфиг solo/team по обратимости/аудиту) + worked example задача A (→ C + senior-ревью + hard-гейт + команда+AI, в формате success-критерия) + mini-apply задача B think-pair-share (LO4). Студент, видящий только слайды, теперь получает полный 5-осевой LO7-аппарат — v3 «осей-thinning на payoff» снят.

**Вывод:** v3 P1-1 (LO7-осей-thinning) и P1-2 (s29↔chapter §6.1 матрица-mismatch) — **оба CLOSED одной правкой**, как и рекомендовал v3 Топ-1. Глава НЕ менялась (book-first), слайд приведён к source of truth.

---

## 2. strict-in counter — **ПОДТВЕРЖДАЮ финально (консистентен, мой v3-пересчёт без корректировок)**

Проверка консистентности по 3 артефактам (запрос п.2):

| Источник | s05 | count | share | partial_out | Статус |
|---|---|---|---|---|---|
| `deck.yaml` s05 L186 | `in_bucket: true` (Решение #100; v3 re-QA 14→15) | — | — | — | ✓ |
| `deck.yaml` s06 | `case_study`, НЕ in_bucket (level-A калибровка+caveat) | — | — | — | ✓ корректно partial→out |
| `deck-part2.yaml ai_failure_judgment` | в `in_bucket_slides` | **15** | «15/32 ≈ 47% / 54.5% минут (42/77)» | s05 **убран** из partial_out | ✓ |
| `notes/.../plan-v2-final.md` §5 L140 | `[Решение #100 — ЗАКРЫТО]` | 15/32 = 46.9% / 54.5% мин | single-cluster снят (Р1–Р6=41–82%, max Р4=14.3%); ≥40% holistic +15 п.п. | — | ✓ |

Все 3 артефакта синхронны и **точно соответствуют моему v3-пересчёту**: 15/32 = 46.9% слайдов / 54.5% минут (42/77); распределение Р1–Р6 = 41–82% секции; max-кластер Р4 = 14.3% deck (<40% → single-cluster СНЯТ); ≥40% holistic держится с запасом ~15 п.п.; chapter strict-in ~69% не затронут (глава не правилась); single-artifact concentration отсутствует. **Финально подтверждаю без корректировок.** CLAUDE-минимум strict-in ≥30% — пройден с двойным запасом; L4 owner-waiver НЕдоступен (Решение #82) и НЕ требуется. Пересчёт НЕ меняет failure-share вердикт (был ≥порога и без s05; s05 — усиление распределения Р1, не спасение от gap).

---

## 3. v3.1 polish НЕ повредил методику — **ПОДТВЕРЖДЕНО (per-slide визуальная проверка)**

| Polish | Слайд(ы) | Проверка видимого слоя v3.1 | Вердикт |
|---|---|---|---|
| s06 METR-дубль снят | s06 | Рендер: только Copilot RCT (+56% лаб / +7–22% поле / −19% знакомое легаси). METR-стат отсутствует. METR-якорь корректно остаётся s01(hook)+s17. s06 = partial→out (level-A калибровка+caveat «лаб числа как лаб») — strict-in count НЕ затронут. Урок контекст-зависимости («на знакомом коде у экспертов выигрыш исчезает») сохранён. | ✅ не повредил; strict-in нетронут |
| s19/s21 term-density → notes | s21 (in-bucket, strict-in security), s19 (partial→out) | **s21:** judgment-аппарат ПОЛНОСТЬЮ на видимом слое — NYU 89-сценариев ~40% уязвимы, 7703-файла 12.1% CWE, Stanford false-confidence («вносят больше уязвимостей И увереннее»), SAST/DAST/SCA/secret-scan, критерий «SAST+secret-scan as gate (не опция); threat-modeling не делегируется». Inline-define «CWE — каталог типов уязвимостей» присутствует (grey-italic, не удалён). **strict-in s21 НЕ выхолощен.** **s19:** judgment «mutation score, не coverage; "что считать корректным" — спецификация, не делегируется» на видимом слое; inline-defines grey-italic, не удалены. | ✅ strict-in s21 нетронут; s19 ОК |
| s11/s15 серый-курсив в notes-стиле | s11 (partial→out, level-C), s15 (partial→out, level-D, НЕ in-bucket) | **s11:** C-цикл plan→act→check→iterate интактен; ключевой урок «check не должен быть самооценкой модели» на видимом слое; grey-italic «Рамка C» (линза s05) present, де-эмфазирован, не удалён. **s15:** level-D цикл интактен; критерий «человек обязателен на любом необратимом/прод-действии» на видимом слое. s15 корректно НЕ in-bucket (autonomy-mechanics, не failure/judgment) — ОК, подтверждаю. | ✅ оба partial→out нетронуты; s15 не-in-bucket корректно |

Вывод: ни одна polish-правка не редуцировала judgment/limits-аппарат in-bucket-слайдов и не сломала концептуальную последовательность. strict-in count (15) устойчив к polish.

---

## 4. tone-strip полнота + научная честность точек возврата — **ПОДТВЕРЖДЕНО**

**Видимый слой (запрос п.4):** независимый zip-grep по 32 видимым slide-XML (notesSlide исключены by pattern), расширенный паттерн (scaffold «N-я точка возврата»/«возвращаемся»/«разберём каждый» + анти-паттерн-каталог + §-коды/LO-коды/`→ sNN`/VFY/FACT-CHECK/insider-phrasing): **TOTAL=0**. Совпадает с независимым grep оркестратора (TOTAL=0). s03-keystone callout scaffold-fix («разберём каждый» → «линза, как читать каждый уровень») применён — подтверждён в grep. Визуальная проверка s08/s13/s17/s23/s30: маркер «N-я точка возврата» на видимом слое отсутствует.

**Научная честность НЕ выхолощена (migrated, not lost):** grep speaker notes — 62 phrase-hit «обязателен человек / не делегируется / человеческая ревью / критерий / измерять» по notesSlides. Spot-check s08/s13/s17/s23/s30 notes: каждый 287–290 слов (в DoD 150–300), scientific-honesty content present=True. Содержательный урок точек возврата сохранён И на видимом слое как **контент** (не scaffold-рамка): s08 «AI написал за минуту без проверки = долг записан за минуту → человеческая ревью до интеграции»; s13 «merge = решение об ответственности, не делегируется»; s17 «измеряйте, не ощущайте — измеряемое решение для команды»; s23 4 правила защиты + архитектурный критерий; s30 «между AI-карго-культом и AI-отрицанием — инженер, называющий уровень+конфиг+условие». Tone-strip убрал лектор-scaffold-рамку, перенёс нарратив в notes/речь (iteration-log: «23 return-connectives moved, not lost») — научная честность ЦВ-возвратов **мигрировала, не исчезла**.

---

## Cross-cutting (delta)
- **LO coverage:** LO1 — усилен keystone (v3, не изменён). LO4 — достижим (формат success-критерия, s31). **LO7 — теперь достижим ПОЛНОСТЬЮ** (v3 «осей-thinning» снят: видимый s29 = 5 named-осей §6.1 + s31 чек-лист операционализирует). Нет висящих LO.
- **Cognitive load:** s29 5 осей-лента + dominant-плашка + pre-фильтр на ~3 мин — в пределах 3–5 концептов; оси — re-use аппарата s04/s12/s13/s17 (амортизация, не новый груз). Перегруза нет.
- **Sequence:** A→D-хребет не трогался v3.1 (подтверждён YES в v3). Термин-сирот на видимом слое 0 (grep). Полиш не внёс разрывов.
- **Tone:** видимый слой TOTAL=0 scaffold/anti-pattern; научная честность в notes сохранена. Решение #100 tone-принцип соблюдён полностью.
- **DoD:** speaker notes delta-слайдов 287–290 слов (150–300 ✓), без layout/«Лектору». strict-in ≥40% holistic ✓. LO7 payoff операционализирован ✓. Ни одна DoD-метрика не провалена → вердикт НЕ форсируется в REVISE.

## P2 (унаследованный, информационный — v3.1 НЕ трогала, не блокер)
- **s06 gold-доминанта «+56% лаб»** vs caveat: визуальная доминанта лаб-числа может закрепиться сильнее caveat'а. Owner/designer judgment, не блокер GATE. (Перенесён из v3 P2 без изменений; не влияет на verdict.)

---

## Сводка для оркестратора

- **VERDICT: APPROVE-CLEAN** (0 P0, **0 P1**, 1 унаследованный P2-информационный). Counter-check: 0 P1 → APPROVE-CLEAN корректен; strict-in ≥30% holistic + не single-artifact + 0 проваленных DoD → НЕ форсируется REVISE.
- **LO7-s29: CLOSED.** Видимый re-rendered s29 несёт все 5 канонических осей §6.1 по имени (≥14pt); «повторяемость» корректно в pre-фильтре «не AI вовсе», НЕ 6-я ось; LO7 success-критерий достижим по видимому s29+s31. Оба v3 P1 (P1-1 осей-thinning + P1-2 матрица-mismatch) закрыты одной правкой, глава не менялась (book-first).
- **strict-in counter: ПОДТВЕРЖДЁН финально, без корректировок.** deck.yaml s05 `in_bucket:true`; deck-part2.yaml count=15, s05 в in_bucket_slides и убран из partial_out; plan §5 ЗАКРЫТО. 15/32 = 46.9% слайдов / 54.5% минут; Р1–Р6 = 41–82%; max-кластер Р4 14.3%; single-cluster снят; ≥40% holistic +15 п.п.; chapter ~69% нетронут; single-artifact concentration отсутствует. Точно соответствует моему v3-пересчёту.
- **polish-impact: НЕ повредил.** s06 METR-дубль снят (strict-in нетронут, METR-якорь s01+s17); s21 strict-in security НЕ выхолощен (term-density в notes, judgment-аппарат на видимом слое полон); s19/s11/s15 partial→out нетронуты; s15 корректно не-in-bucket.
- **tone-strip: полон, научная честность сохранена.** Видимый слой TOTAL=0 (независимый grep подтверждает оркестраторский TOTAL=0); scaffold-нарратив точек возврата мигрировал в speaker notes (62 phrase-hit, notes 287–290 слов), содержательный урок сохранён и на видимом слое как контент — выхолащивания нет.
- **GATE B:** методических блокеров нет. Унаследованный s06-P2 — owner/designer judgment, не препятствует GATE.
