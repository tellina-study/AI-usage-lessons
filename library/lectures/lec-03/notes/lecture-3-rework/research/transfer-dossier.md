# Досье переноса: тема «роли протокола и chat-шаблоны» из Лекции 2 v2 → Лекцию 3

**Назначение.** Собрать в одном месте полные тексты блоков про chat-шаблоны / протокольные роли / специальные токены / Special Token Injection (STI), перенесённых из Лекции 2 в Лекцию 3 (issue #184, #185). Плюс — сжатый остаток, который сохранён в Лекции 2 (для анти-дубляжа), и требования владельца.

**Источник материала.** Ветка `issue-183-lec02-v2` (Лекция 2 v2). Собрано read-only через `git show`; ветка не переключалась.

**Дата сборки досье.** 2026-09-06.

---

## 0. Карта источников (коммит : путь)

| Блок | Коммит : путь | Статус в Л2 сейчас |
|---|---|---|
| Слайд s07-chat-templates (полный) | `ef5dcf1^` : `library/lectures/lec-02/slides/s07-chat-templates.md` | **удалён** (ef5dcf1 «роли→Лекция 3») |
| Chapter §1.3 — оригинал (до сжатия) | `ef5dcf1^` : `library/lectures/lec-02/chapter.md` (стр. 142–153) | сжат до абзаца-упоминания |
| Chapter §1.3 — сжатый остаток | `097cac8` (HEAD ветки) : `library/lectures/lec-02/chapter.md` (стр. 162–166) | **актуальный в Л2** |
| Research §5 + библио | `a55bd15` : `notes/lecture-2-review/v2-research/tech-nuances-2026.md` (стр. 79–93, 213–222) | файл существовал до a55bd15; удалён позже (**не в HEAD**) |
| Слайд s41-bridge-lecture3 (полный) | `4264315` : `library/lectures/lec-02/slides/s41-bridge-lecture3.md` | актуальный (deck v3.1) |
| Chapter §5.8 «Мост к Лекции 3» | `4ea80d8` : `library/lectures/lec-02/chapter-part3.md` (стр. 134–141) | актуальный |
| Issue #184 | `gh issue view 184` (tellina-study/AI-usage-lessons) | OPEN |

> Примечание к пути research-файла: в задаче он указан как `library/lectures/lec-02/notes/...`, но фактически лежит по `notes/lecture-2-review/v2-research/tech-nuances-2026.md` (в корне репо, не под `library/lectures/lec-02/`). Файл был добавлен в `263b463`, жив в `a55bd15`, удалён в более позднем коммите ветки — в HEAD его нет; текст взят из `a55bd15`.

---

## 1. Слайд s07-chat-templates (полный текст) — `ef5dcf1^`

### Frontmatter

```yaml
id: s07
type: assertion_visual
section: "Раздел 1. Токенизация"
duration_min: 2.5
assertion: "Chat-формат — соглашение поверх модели: роли system/user/assistant — те же токены в общем потоке"
learning_goal: "Chat-шаблоны и специальные токены; протокольные роли ≠ «роли» из текста промпта; практический вывод (фильтрация входа, проверка шаблона); слой 1 закрытия M2 (полное закрытие — s20, forward-ref в notes)"
learning_outcomes: [LO1, LO6]
chapter_ref: "§1.3 [for-slide-s07]"
visual_brief: "Строка-тезис под заголовком: «модель принимает один плоский поток токенов; chat-формат — соглашение поверх него». Слева — структурированный диалог {role, content}; стрелка «chat-шаблон» → плоский поток токенов с выделенными спецтокенами <|im_start|>system … <|im_end|> (моноширинно, спецтокены teal). Справа — 2 карточки: (1) «Протокольные роли ≠ „роли“ из текста»: system/user/assistant — структура диалога через спецтокены; «ты — лучший разработчик» — просто содержимое сообщения; (2) карточка риска «Подделка» (gold-обводка): внешний контент со строкой, похожей на разметку роли, вливается в тот же поток. Внизу — блок «Что с этого»: фильтруйте внешний контент до контекста; при локальном запуске проверяйте chat-шаблон."
```

### Visible body

**Title bar:** «Роли system/user/assistant — те же токены в общем потоке»

**Body:**

[Строка-тезис под заголовком] Модель принимает один плоский поток токенов. Chat-формат — соглашение поверх него, а не часть архитектуры.

[Слева — схема преобразования] Структурированный диалог `{role, content}` ↓ **chat-шаблон** → Плоский поток токенов (моноширинно, спецтокены teal): `<|im_start|>system` Ты помощник… `<|im_end|>` · `<|im_start|>user` Объясни… `<|im_end|>`

[Справа — карточка различения] **Протокольные роли ≠ «роли» из текста.** system/user/assistant — структура диалога, собранная спецтокенами. «Ты — лучший разработчик» в тексте промпта — просто содержимое сообщения, не протокольная роль.

[Справа — карточка риска, gold-обводка] **Подделка:** внешний контент (веб-страница, файл, письмо) со строкой, похожей на разметку роли, вливается в тот же поток токенов — отдельного «защищённого канала» для ролей нет.

[Внизу — блок «Что с этого»] **Что с этого:** фильтруйте внешний контент до попадания в контекст (экранирование, детекция спецтокенов) · при локальном запуске проверяйте chat-шаблон — модель с чужим шаблоном молча глупеет.

[Caption мелким] *Почему роль при этом работает — и почему подделанная работает так же — в разделе про внимание.*

### Speaker notes (дословно)

> Сообщения в API имеют роли: system, user, assistant. Роли часто представляют как структурные поля — метаданные, которые модель получает «отдельным каналом». Такого канала не существует: модель принимает один плоский поток токенов, а chat-формат — соглашение поверх него, надстройка, а не часть архитектуры. Прежде чем список сообщений попадает в модель, он прогоняется через chat-шаблон — правило сборки одной плоской последовательности токенов из структурированного диалога. В формате ChatML, ставшем фактическим стандартом, сообщение оборачивается специальными токенами — служебными записями словаря, не встречающимися в обычном тексте. В экосистеме Hugging Face шаблон хранится буквально как Jinja-шаблон в файле настроек токенизатора — его можно открыть и прочитать.
>
> Разведём два значения слова «роль», которые постоянно смешиваются. Протокольные роли system/user/assistant — это структура диалога, «кто говорит»; они собираются спецтокенами chat-шаблона. А «роль» из текста промпта — «ты — лучший разработчик» — это просто содержимое сообщения: обычные токены внутри реплики, никакой протокольной метки у них нет; их влияние на поведение модели — эффект внимания к содержимому контекста, и мы разберём его в разделе про внимание. Деталь протокола: имя роли — обычная строка после спецтокена, а не отдельный спецтокен; именно поэтому провайдеры добавляли новые роли без перевыпуска словаря.
>
> Что с этого на практике — два вывода. Первый: если роль — последовательность токенов, то текст, похожий на разметку роли, может попасть в поток из внешнего контента. Атаки класса Special Token Injection — подделка роли: в страницу или файл вставляют строку вида «начало реплики assistant», и модель в части случаев верит — «кто говорит» она определяет в значительной мере стилистически, а не по формальной метке. Поэтому внешний контент — веб-страницы, файлы, письма — фильтруйте до попадания в контекст: экранирование и детекция спецтокенов. Второй: при локальном запуске проверяйте chat-шаблон — модель, запущенная с чужим шаблоном, не выдаёт ошибку, она просто заметно глупеет; проверка шаблона — первый пункт диагностики. Здесь мы закрыли половину вопроса: роль — не защищённая структура. Почему она вообще работает — увидим в разделе про внимание; разбор prompt injection целиком — в Лекции 3.

---

## 2. Chapter §1.3 — ОРИГИНАЛ (до сжатия), `ef5dcf1^`

### 1.3. Chat-шаблоны и специальные токены: роли — это тоже токены

Сообщения в API имеют роли: system, user, assistant. Роли часто представляют как структурные поля — что-то вроде метаданных запроса, которые модель получает «отдельным каналом». Это заблуждение M2 в его исходной форме, и здесь мы закроем его первую половину: **ролей как отдельного канала не существует. Роли — это те же токены в общем линейном потоке.**

Прежде чем список сообщений `{role, content}` попадает в модель, он прогоняется через **chat-шаблон (chat template)** — правило сборки одной плоской последовательности токенов из структурированного диалога. В формате ChatML, предложенном OpenAI и ставшем фактическим стандартом, сообщение оборачивается **специальными токенами** — служебными записями словаря, не встречающимися в обычном тексте: `<|im_start|>system ... <|im_end|>`, `<|im_start|>user ... <|im_end|>`, и так далее. У Llama исторически использовались текстовые маркеры `[INST]...[/INST]`. В экосистеме Hugging Face шаблон хранится буквально как Jinja-шаблон в поле `chat_template` файла `tokenizer_config.json` — это обычный текстовый шаблонизатор, который можно открыть и прочитать. Формат чата — не свойство модели «из коробки»: один и тот же checkpoint с другим chat-шаблоном будет вести себя иначе.

Две детали устройства, полезные практикам. Первая: в ChatML имя роли — это **обычная строка** после специального токена `<|im_start|>`, а не отдельный специальный токен на каждую роль. Именно поэтому провайдеры смогли добавлять новые роли (`tool`, `developer`) без перевыпуска словаря: спецтокены размечают границы, роли — текст между ними. Вторая: для локальных моделей chat-шаблон — регулярный источник тихих багов. Модель, обученная под один формат разметки и запущенная с другим (например, checkpoint под ChatML, поданный с Llama-разметкой), не выдаёт ошибку — она просто заметно глупеет: инструкции размечены «не так, как на обучении», и выученный приоритет системной роли не срабатывает. Если локальная модель ведёт себя странно хуже заявленного — проверка соответствия chat-шаблона идёт первым пунктом диагностики.

Следствие, важное для любого, кто строит агентов: если роль — это последовательность токенов в общем потоке, то текст, *похожий* на разметку роли, может попасть в этот поток из пользовательского контента. Атаки класса **Special Token Injection (STI)**, они же role spoofing: во внешний контент (веб-страницу, которую агент читает через инструмент; файл; письмо) вставляются символы вида `<|im_start|>assistant`, и модель в части случаев интерпретирует последующий текст как собственную реплику или системную инструкцию (Sentry Security, 2026; arXiv 2603.12277). Исследования механизма показывают неприятную деталь: модель определяет «кто говорит» в значительной мере стилистически — по тому, как текст звучит, — а не по формальной метке (HiddenLayer, 2025). Роль не является криптографически защищённой границей; в боевых системах защита строится на экранировании и детекции специальных токенов во входящем внешнем контенте **до** подачи в модель, а не на надежде, что модель сама разберётся.

Здесь мы закрыли только половину M2: показали, что роль — не защищённая структура, а конвенция из токенов. Вторая половина вопроса — *почему тогда роль вообще работает*, и почему подделанная роль работает так же хорошо, как настоящая, — требует механизма внимания и разбирается в §3.2 (часть 2). Детальный разбор prompt injection как класса атак — в Лекции 3.

---

## 3. Chapter §1.3 — СЖАТЫЙ ОСТАТОК в Л2 (анти-дубляж), `097cac8` (HEAD)

> **Это остаётся в Лекции 2. Лекция 3 НЕ должна дублировать этот абзац — только развивать.**

### 1.3. Chat-шаблоны: роли — это тоже токены

Сообщения в API имеют роли: system, user, assistant. Прежде чем список сообщений `{role, content}` попадает в модель, он прогоняется через **chat-шаблон (chat template)** — соглашение поверх плоского потока токенов, а не отдельный защищённый канал: правило сборки одной плоской последовательности токенов из структурированного диалога, оборачивающее каждое сообщение специальными токенами разметки (например, `<|im_start|>system ... <|im_end|>` в формате ChatML). Роли (system/user/assistant), устройство специальных токенов и то, чем опасна подделка роли через инъекцию в чужой контент (Special Token Injection, STI), подробно разбираются в Лекции 3 (issue #184).

**Self-check Л2 (пункт 3, тоже остаётся в Л2):**
> Роль (system/user/assistant) — это не защищённое поле, а условность из токенов chat-шаблона. Почему это значимо для того, кто строит приложение поверх LLM, даже без подробностей атак — они разбираются в Лекции 3?

**Changelog-запись v2.2 → v2.3 (фиксация решения):** «§1.3 сжат до абзаца-упоминания: chat-шаблоны, роли, специальные токены и подделка роли (STI) переносятся в Лекцию 3 (issue #184 backlog).»

**Глоссарий Л2 (chapter-part3, тоже остаётся):** «**Системный промпт** — сообщение с ролью и ограничениями, подаваемое до пользовательских; технически — токены в общем потоке с выученным приоритетом (§1.3, §3.2).»

---

## 4. Research §5 — `tech-nuances-2026.md` (`a55bd15`)

### 5. Chat-шаблоны и специальные токены

**Механизм:** Роли system/user/assistant не являются "инструкциями", которые модель волшебно понимает — они реализованы как последовательности специальных токенов (`<|im_start|>system`, `<|im_start|>user`, `<|im_start|>assistant`, `<|im_end|>` в ChatML) или текстовых маркеров (`[INST]...[/INST]` у Llama), вставляемых в единую линейную последовательность токенов перед подачей в модель. Формат преобразования списка сообщений {role, content} в эту последовательность задаётся chat template — обычно Jinja2-шаблоном, хранящимся в поле `chat_template` файла `tokenizer_config.json` (HuggingFace).

**Цифры/примеры:**
- ChatML (`<|im_start|>`/`<|im_end|>`) — фактический "стандарт" формата, изначально предложен OpenAI; роль вставляется как обычная строка после `<|im_start|>`, что позволяет добавлять новые роли без новых спецтокенов. [AI/TLDR](https://ai-tldr.dev/learn/llm-fundamentals/tokens-and-tokenization/chat-templates-special-tokens/)
- Chat template хранится как Jinja-шаблон в `tokenizer_config.json` под ключом `chat_template` (пример: модели семейства Qwen3). [HuggingFace blog](https://huggingface.co/blog/chat-templates)
- Уязвимость "role spoofing" / Special Token Injection (STI): если модель воспринимает весь вход как один плоский поток токенов, а не структурированные метаданные, то атакующий может вставить в user-контент (например, в текст веб-страницы, которую агент читает через tool) буквальные символы, похожие на `<|im_start|>assistant`, и модель может интерпретировать последующий текст как "свой собственный" ответ или системную инструкцию — это описано как класс атак Special Token Injection / "role confusion". [Sentry blog](https://blog.sentry.security/special-token-injection-sti-attack-guide/) / [arXiv 2603.12277 "Prompt Injection as Role Confusion"](https://arxiv.org/pdf/2603.12277)
- Ключевой механизм уязвимости: модель определяет источник текста по тому, "как он звучит" (стилистически), а не по формальной метке роли — то есть роль не является криптографически защищённой границей. [HiddenLayer research](https://www.hiddenlayer.com/research/inside-the-prompt-how-llms-learn-roles-follow-instructions-and-get-exploited)
- Защита в проде обычно строится на экранировании/детекции спецтокенов во входящем внешнем контенте до подачи в модель, а не только на обучении модели устойчивости к этому.

**Для слайда:**
- Роль "system" — это не привилегия, а просто последовательность токенов в общем потоке; если приложение позволяет внешнему контенту (веб-странице, файлу) содержать текст, похожий на `<|im_start|>assistant`, модель в некоторых случаях может "поверить", что дальше идёт её собственная реплика.
- Формат чата — не свойство модели "из коробки", а обучаемый Jinja-шаблон в конфиге токенизатора; один и тот же checkpoint с разным chat_template может вести себя иначе.

### Библиография к §5 (из блока «Источники» файла)

По теме §5 явных пометок `[НЕ ПОДТВЕРЖДЕНО]` в общем своде источников файла **нет** (тема 5 в списке неподтверждённых не значится). Все четыре внешние ссылки (AI/TLDR, HuggingFace blog, Sentry blog, arXiv 2603.12277, HiddenLayer) даны inline выше как подтверждённые на 2026-09-05.

> Общая шапка research-файла: «Дата сбора: 2026-09-05. Все цифры и утверждения проверены через WebSearch/WebFetch в сентябре 2026 — там, где проверить не удалось, стоит явная пометка "[НЕ ПОДТВЕРЖДЕНО]".»

---

## 5. Мост Л2 → Л3: что именно Лекция 2 обещает про Лекцию 3

### 5.1. Слайд s41-bridge-lecture3 (полный текст) — `4264315`

**Frontmatter:**

```yaml
id: s41
type: summary
section: "Раздел 6. Финал"
duration_min: 2
assertion: "Лекция 3 — как модель выходит за пределы контекста: RAG, инструменты, MCP, агентный цикл"
learning_goal: "Мост к Лекции 3 с якорями из этой лекции (роль = токены с весом внимания → injection, граница similarity → RAG, кэш → экономика агента, thinking → бюджет)"
learning_outcomes: [LO1]
chapter_ref: "§5.7 (chapter-part3.md) [for-slide-s41]"
```
(visual_brief — hero-полоса «мост» + 4 Ocean rounded boxes 2×2 с иконками и строками-якорями; полный brief в исходном файле.)

**Visible body — Title:** «Лекция 3: как модель выходит за пределы контекста»

4 бокса 2×2 (иконка + строка-якорь):
1. **RAG** — Семантический поиск по вашей базе → найденные фрагменты в контекст. *Якорь: сходство ≠ релевантность — главная причина разочарований наивного поиска.*
2. **Инструменты / вызов функций** — Модель генерирует структурированный вызов → внешняя система исполняет. *Якорь: надёжность формата вызова обеспечивают structured outputs.*
3. **MCP** — Открытый протокол подключения инструментов. *Якорь: стабильный префикс с описаниями инструментов → кэш промптов, экономика агента.*
4. **Агентный цикл** — Действие → наблюдение → коррекция. *Якорь: инструкции для модели — те же токены с весом внимания; агент читает внешний контент → prompt injection; невидимые токены рассуждения умножаются на число шагов.*

**Speaker notes (дословно):**

> У собранного конвейера есть жёсткая граница: модель видит только контекст и не может выйти за него — ни за свежими данными, ни за действием в мире. Следующая лекция — «Агенты, RAG, API» — про то, как эта граница преодолевается: семантический поиск по вашей базе знаний с подстановкой найденных фрагментов в контекст — прямая надстройка над эмбеддингами из сегодняшнего второго раздела; вызов инструментов, где модель генерирует структурированный вызов функции, а надёжность формата обеспечивает уже знакомый вам механизм структурированного вывода; открытый протокол MCP для подключения инструментов; и агентный цикл «действие — наблюдение — коррекция».
>
> Четыре якоря из сегодняшней лекции, которые там понадобятся. Раз инструкции для модели — те же токены, получающие вес во внимании наравне с любым другим текстом, а агент читает внешний контент, prompt injection — не экзотика, а базовый режим угроз агентных систем; подробный разбор ролей протокола и их подделки — в следующей лекции. Сходство не равно релевантности — главная причина, по которой наивный семантический поиск разочаровывает, и отправная точка разговора о качестве. Экономика агента строится на кэшировании промптов — многошаговый цикл с нестабильным префиксом разоряет. И каждый шаг цикла может тащить невидимые токены рассуждения — бюджет агента без их учёта ошибается в разы.
>
> До следующей встречи — четыре коротких эксперимента (буквы на 3 моделях; 10 запусков при T=0 с побайтовым сравнением; косинус «настроить SSL — отключить SSL»; аудит своего проекта — кэш и лишнее рассуждение). Результаты понадобятся в разговоре про агентов — там цена каждой из этих границ умножается на число шагов.

### 5.2. Chapter §5.8 «Мост к Лекции 3» — `4ea80d8` chapter-part3.md

**Обещание Л3:** «Лекция 3 "Агенты, RAG, API" — про то, как эта граница преодолевается: **RAG** (семантический поиск по вашей базе + найденные фрагменты в контекст — прямая надстройка над эмбеддингами из Раздела 2), **вызов инструментов** (модель генерирует структурированный вызов функции — надёжность этого формата обеспечивают structured outputs из §4.5), **MCP** (открытый протокол подключения инструментов), **агентный цикл** (действие → наблюдение → коррекция).»

**Четыре якоря (дословная формулировка обязательств Л2 перед Л3):**
- **Из §1.3 (инъекция/роли):** «роль — это токены в общем потоке, а агент читает внешний контент — почему это создаёт риск инъекции промпта и как от него защищаться, разбираем в Лекции 3».
- **Из §2.4 (similarity ≠ релевантность → RAG):** «сходство ≠ релевантность — главная причина, по которой "наивный RAG" разочаровывает, и отправная точка разговора о качестве поиска».
- **Из §3.4 (кэш → MCP/экономика):** «экономика агента строится на кэшировании промптов — многошаговый цикл с нестабильным префиксом разоряет».
- **Из §4.7 (reasoning-токены × шаги):** «каждый шаг агентного цикла может тащить невидимые токены рассуждения — бюджетирование агента без их учёта ошибается в разы».

**Дополнительные точки крепления «structured outputs → function calling»** (из §5.5 «Итоги» и таблицы конвейера §5.1, part3):
- §5.1 таблица: «Сэмплинг → structured outputs; reasoning-токены как статья расходов»; «Токенизация → роль как токены chat-шаблона (устройство и риски — Лекция 3)».
- §5.5 таблица итогов: «Роль — токены chat-шаблона в общем потоке (§1.3)… про защищённость роли — Лекция 3 (расхожее "роль защищена архитектурно" — неточно)».

**Четыре эксперимента-самопроверки Л2 → Л3** (результаты обещаны нужными в Л3): гонка патчей (r в strawberry/cranberry на 3 моделях); детерминизм (10× при T=0, побайтовое сравнение); граница similarity (косинус «настроить SSL / отключить SSL» vs «настроить SSL / рецепт борща»); экономика (аудит своего проекта — кэш промптов и лишний reasoning).

---

## 6. Требования issue #184 (tellina-study/AI-usage-lessons, OPEN)

**Заголовок:** «Лекция 3: тема "роли протокола и chat-шаблоны" (перенос из Лекции 2, owner-решение 2026-09-05)»

**Автор/решение:** klabulan (Maxim Levko); owner-решение при ревью Лекции 2 v2.2 (issue #183).

**Суть переноса:** тема «зачем нужны роли system/user/assistant, как chat-шаблон собирает плоский поток токенов, чем опасна подделка ролей (Special Token Injection)» переносится в Лекцию 3 (промптинг) — логически принадлежит блоку про промптинг/инъекции.

**Готовый материал (по issue):**
- `library/lectures/lec-02/slides`-архив — `s07-chat-templates` в истории git ветки `issue-183-lec02-v2` (собран в §1 выше);
- chapter Лекции 2 §1.3 (сжатая версия остаётся как упоминание — §3 выше);
- research: `notes/lecture-2-review/v2-research/tech-nuances-2026.md` §5 (собран в §4 выше).

**Требования при включении в Лекцию 3 (дословно):**
1. Пояснить **ЗАЧЕМ роли нужны** (структура диалога, управление поведением).
2. Чем **протокольные роли отличаются от «ролей» в тексте промпта**.
3. Чем опасна **подделка** (Special Token Injection / role spoofing).
4. **Как защищаться** (экранирование/детекция спецтокенов во внешнем контенте до подачи в модель; для локальных — проверка chat-шаблона).

---

## 7. Ключевые ссылки-источники (для fact-check в Л3)

- ChatML / chat template в `tokenizer_config.json`: HuggingFace blog «chat-templates»; AI/TLDR.
- STI / role confusion: Sentry Security blog «Special Token Injection (STI) attack guide» (2026); arXiv 2603.12277 «Prompt Injection as Role Confusion».
- Роль определяется стилистически, не защищена криптографически: HiddenLayer research «Inside the Prompt: how LLMs learn roles…».
- Атрибуции в chapter §1.3: Sentry Security 2026; arXiv 2603.12277; HiddenLayer 2025.
