# Methodology Critic Report — speech.md Лекция 5 «AI в финансовом секторе и ритейле» — 2026-05-17 (Phase 10)

VERDICT: REVISE

**Причина REVISE:** один DoD-метрик (WPM ≤95 per-fragment, hard cap, zero-tolerance) **fails** на 6 из 33 фрагментов. По правилу DoD Enforcement «если any DoD metric fails: verdict ≥ REVISE». Содержательно (методика, арка, LO, strict-in, conversational, термины, anti-hype, универсальность) речь сильная — после WPM-trim это APPROVE-уровень. Это **не** content-fail и **не** strict-in-fail; это локализованная pacing-правка + неверная самооценка автора.

## Severity counts
- **P0: 0** (методически речь пригодна; концепты введены до использования, термин-сирот нет, нет curriculum mismatch)
- **P1: 2** (WPM DoD breach на 6 фрагментах; неверная PROVEN-PASS самооценка автора вводит оркестратора/гейт в заблуждение)
- **P2: 3** (мелкие нестыковки self-assessment vs факт: «мы с вами» 8 vs заявл. 10; slide-sum 69 vs deck 70; s32 формально 97.3 vs «~94»)

Counter-check: 2 P1 → допускает APPROVE-WITH-POLISH по числу P1, НО DoD WPM fails → правило DoD Enforcement форсирует **REVISE** (не выше). Соблюдено.

---

## Подтверждения (что прошло — фиксирую как PASS)

### 1. AI-Failure ≥30% strict-in ПО МИНУТАМ — honest recount → **PASS с большим запасом, holistic**

**L5 ∈ L4–L17 → owner-waiver НЕДОСТУПЕН** (реестр `tools/lecture-production/README.md` §3.6: waiver только L1/L2; L5 не записан, не introductory). Правило ≥30% strict-in применяется в полном объёме.

**Методика подсчёта (решение #78, partial→out):** засчитан только полностью in-bucket фрагмент = (документированный провал + явный урок) ИЛИ (разбор фундаментального ограничения/риска) ИЛИ (явный критерий «не этот тип / не ИИ») ИЛИ (сравнение с более правильным альтернативным инструментом). База = активный контент 67.0 мин (33 слайда минус cover s02 + 5 дивайдеров s04a/s10/s15/s20/s25); show-total 69.0 мин.

**Засчитано STRICT-IN (строгий набор, 14 фрагментов = deck solid):**
s09 (Zillow payoff: distribution shift + asymmetry + урок «куда подключён выход» + Opendoor-альтернатива), s11 (почему anomaly, не классификация, не LLM + механизм + AML→rules), s13 (матрица ошибок + «точность лжёт при дисбалансе» + cost-sensitive — разбор фундаментального риска), s14 (FP-провал + precision/recall + катастрофический кейс + критерий автоблок/мягкий challenge + rules-engine AML), s16 (скоринг: 4-арг критерий why-not-нейросеть + объяснимость = условие законности + SHAP-альтернатива), s18 (Apple Card + прокси-bias с нуля + точная фактура NYDFS + 3-критерий), s19 (критерий «автоматизация без gate» — чистый judgment-вывод), s22 (fact-checking: 3-арг «когда LLM НЕ источник истины» + механизм галлюцинации + RAG/grounding-альтернатива + 5 классов), s23 (Air Canada + Klarna 2 провала + урок augmentation≠замена + критерий), s24 (pivot: двухуровневый judgment «выбор типа необходим, но не достаточен»), s28 (Wendy's + прокси≠цель + объединение с filter bubble + критерий), s29 (матрица + нижняя строка «правильный тип = вообще не ИИ, rules-engine»), s30 (5 критериев «когда ИИ не нужен/опасен»), s31 (ФЗ-152/PII/биометрия: разбор риска + критерий «не слать в публичный LLM» + Just Walk Out hidden-labor урок).
= **38.0 мин** strict-in.

**Дополнительно засчитано (расширенный набор +3):** s05 (why-not-LLM для рядов + альтернатива ARIMA/бустинг — фундаментальное ограничение + альт.), s06 (3-арг развёрнутый критерий + «что сломается» = Zillow-механизм + data-security альтернатива), s08 (явный разбор фундаментального ограничения экстраполяции: «модель не понимает, что мир изменился» — bucket «разбор фундаментального ограничения/риска»). = **43.5 мин**.

**Отклонено (partial → out при подсчёте %):**
- s01 (hook — провал без урока, урок отложен в s09; deck partial→out) — OUT
- s03 (тезис «LLM не универсальный молоток» — установочный, без worked failure/альтернативы) — OUT
- s04 (ЦВ + card-pattern — рамка, не bucket-контент) — OUT
- s07 (X5/Магнит — доминантно success-пример; judgment-хвост 1 строкой) — OUT (partial)
- s12 (Stripe/JPMorgan/Visa — vendor success-метрики; «точность лжёт» только намёк-хвост) — OUT (partial)
- s17 (Сбер — пример + защитная интерпретация «100% ≠ чёрный ящик»; не failure/критерий как первичный контент) — OUT (partial)
- s21 (где LLM уместен — доминантно положительная применимость; подмена базы ~1/3) — OUT (partial)
- s26 (collaborative/content с нуля — доминантно концепт-обучение; слабости названы, но без урока/критерия/альтернативы как первичного) — OUT (partial, deck out)
- s27 (hybrid + filter bubble + dynamic pricing — концепт + judgment вперемешку; deck partial→out) — OUT (partial)
- s32 (чек-лист + мост Сем5 — payoff/bridge; deck partial→out) — OUT (partial)
- s02/s04a/s10/s15/s20/s25 — cover/дивайдеры, исключены из базы

**Итог strict-in доля (по минутам):**

| Набор | strict-in мин | / контент 67.0 | / show 69.0 |
|---|---|---|---|
| строгий (14 фраг = deck solid) | 38.0 | **56.7%** | 55.1% |
| расширенный (17 фраг, +s05/s06/s08) | 43.5 | **64.9%** | 63.0% |

**Вывод:** даже по строжайшему чтению **56.7% ≥ 30%** — почти 2× порога. Разрыв до 30% = **отрицательный** (запас +26.7 п.п.). Автор заявил «≈35/55 ≥30% holistic» — по моему независимому пересчёту запас ещё больше (заявленное скромнее факта; не завышение). **Холистичность подтверждена:** strict-in распределён по всем 6 разделам — Р1: s05/s06/s08/s09 · Р2: s11/s13/s14 · Р3: s16/s18/s19 · Р4: s22/s23/s24 · Р5: s28 · Р6: s29/s30/s31. Ни один раздел не несёт >40% веса; single-cluster снят by design. Failure/judgment — несущая ось речи (каждый из 5 типов завершается «где он ломается + критерий + альтернатива»). **PASS, не single-artifact.**

### 2. Conversational, не пересказ главы → **PASS**

Речь — живой лекторский голос, не зачитывание chapter и не speaker notes. Маркеры (тело s01→резерв): 53 сценических cue (`[пауза]`/`[понизить голос]`/`[обращение к залу]`), 14 «запомните/запишите», 10 моментов прямого обращения, 11 «давайте/посмотрим/возьмём/соберём», 6 явных проговорённых аналогий, 8 callback-ов, 4 риторических «Почему …?». s09 сжимает плотную §1.5-прозу главы в устную арку с Knight-callback и Opendoor-контрфактом, произнесённую разговорно (не дословный перенос). Переходы между слайдами явные, дивайдер-bridge-фразы живые («Раздел первый из пяти … начнём с самой массовой задачи»). Не сухой конспект.

### 3. Арка / LO → **PASS**

- **ЦВ символьно идентичен chapter §0.3 / s04.** Глава (стр. 106): «…почему именно он (а не LLM везде), и где этот тип ломается?». Речь s04 (стр. 87): «…почему именно он, а не LLM везде, и где этот тип ломается?» — скобочная вставка линеаризована, вербально/семантически тождественно. ✓
- **5 точек возврата реальны:** s09/s14/s18/s23/s28 явно названы «возвращается первый/второй/третий/четвёртый/пятый, последний раз», каждая закрывается критерием-выводом («тип выбран верно, но…»). Не косметика. ✓
- **Failure-нить связна:** Zillow (s01 hook → s09 payoff) → fraud FP (s14) → Apple Card (s18) → Air Canada+Klarna (s23) → Wendy's (s28); Knight = только callback (s09/s14/s19); Just Walk Out = s31 иллюстрация. ✓
- **6 типов разведены:** прогноз рядов / anomaly / скоринг / LLM / recsys + CV-пласт, у каждого явное «почему этот тип, не LLM/нейросеть» и «где ломается». ✓
- **LO1 несущая ось слышна:** «какой тип под какую задачу» проходит через s03/s04/s05–s28 → payoff-матрица s29. ✓
- **LO6 = Understand (не Apply Семинара 5):** s22 (стр. 387) и s32 (стр. 545) явно: «лекция — распознать класс и назвать принцип; самостоятельно проверить 5 заявлений — Семинар 5, Apply». Retrieval-вопросы используют «РАСПОЗНАЕТЕ» (s23), «как проверить факт» на уровне принципа — не требуют самостоятельной верификации. Bloom-граница операционализирована, не словесна. ✓

### 4. Forward-pointer'ы → **PASS**

Л7: sensitivity/specificity (s13 «построим в Лекции 7»), Obermeyer (s18 «увидим в Лекции 7»), CV-bias (s31 «углубим в Лекции 7») — все forward, «введём здесь, углубим позже». Л3 RAG/grounding (s22 «мини-напоминание Лекции 3») и Air Canada (s23 «callback Лекции 3, не дубль») — backward, явно как напоминание, без дублирования. Семинар 5 = Apply (s22/s32). Направления корректны. ✓

### 5. Термины / anti-hype / универсальность → **PASS**

- **Canonical-validity:** все канон-термины строго по locked glossary.yaml (дрейф распределения, асимметрия цены ошибки, матрица ошибок, ошибка первого/второго рода, прокси-предвзятость, коллаборативная/контентная фильтрация, холодный старт, смещение к популярному, чрезмерная специализация, гибрид, информационный пузырь, динамическое ценообразование, прокси≠цель, grounding, KYC, liveness, скрытый человеческий труд, аварийный выключатель, cost-sensitive, precision↔recall, reason codes, SHAP, iBuying, augmentation, ФЗ-152/ПДн). 0 insider-RED-FLAG («прикладн/в режиме/рабочее определение»).
- **«паттерн карточки» insider-construct:** обработан корректно teach-not-defend (s04 стр. 93): «не отраслевой стандарт и не термин из учебника … наш с вами рабочий конструкт лекции — как лестница автономности в прошлый раз». Соответствует glossary-note. НЕ скрытое insider-phrasing.
- **Anti-hype:** 0 «магическ»/«спасёт»/«революци»; заключение s32 «между восторгом и отрицанием … провалов много намеренно». Без «магической пилюли».
- **Универсальность:** 0 «ИУ6/Бауманка»; «наша страна / наша отрасль / вы будете работать» — безопасное обобщение.
- **Англицизмы:** 0 пайплайн/фоллбэк/эдж-кейс/инсайт/онбординг/юзкейс в теле. «подключение клиента» вместо «онбординг» (исправлено).
- **РПД «>90%»:** единственное совпадение — s21 стр. 361, ТОЛЬКО как обучающий пример подмены базы (класс 5), явно «НЕ используем как факт … классическая подмена базы». Compliant.
- **Strip в теле:** `[FACT-CHECK]`=0, `[VERIFY-DAY-OF]`=0, `[VFY]`=0, LO-коды=0, `§\d.\d`=0, `→sNN`=0. ВСЕ verify/fact-check вынесены в actionable pre-flight (URL/файлы/что делать day-of). ✓

---

## P1 issues

### P1-1 · WPM DoD breach: 6 из 33 фрагментов > 95 (hard cap, zero-tolerance)

**Severity:** P1 (DoD-метрик fails → форсирует verdict ≥ REVISE по правилу DoD Enforcement; методически речь не «непригодна», поэтому не P0).

**Issue:** DoD WPM Hard Rule — «≤95 для **каждого** fragment, не average, не 8 of 10». Независимый пересчёт по официальной методике (слова в произносимом теле фрагмента, минус inline `[cue]`-вставки и гильеметы `«»`, делить на planned `duration_min` из deck) даёт **6 фрагментов > 95**:

| Фрагмент | duration_min | words (stripped) | WPM | Δ к 95 |
|---|---|---|---|---|
| **s28** | 3 | 301 | **100.3** | +5.3 |
| **s29** | 3 | 294 | **98.0** | +3.0 |
| **s32** | 1.5 | 146 | **97.3** | +2.3 |
| **s09** | 2 | 192 | **96.0** | +1.0 |
| **s23** | 3 | 288 | **96.0** | +1.0 |
| **s14** | 3 | 287 | **95.7** | +0.7 |

Остальные 27 фрагментов ≤95 (следующий — s11=94.7, s18=94.3, s22=94.0 — в пределах, но без запаса). Средний темп речи ~80 WPM (6003 слова / 75 мин) — реалистичен; проблема локальная на 6 плотных фрагментах.

**Evidence:** s28 (стр. 463–477) — 301 слово на 3 мин = 100.3 WPM, самый плотный (Wendy's + прокси≠цель + объединение с filter bubble + критерий + retrieval — много контента в один beat). s29 (стр. 483–499) — 294 слова, матрица с проговором 5 строк + нижняя плашка. s32 (стр. 541–549) — 146 слов на 1.5 мин (чек-лист 8 вопросов + мост Сем5 + anti-hype финал спрессованы).

**Противоречие с самооценкой (см. P1-2):** автор в self-assessment (стр. 571, 599) заявляет «ВСЕ 33 фрагмента ≤95, максимум s28=94.7 … Зеро-tolerance соблюдён: ни один fragment не >95 … PROVEN PASS». По моему пересчёту s28=100.3 (Δ +5.6 к заявленному), и ещё 5 фрагментов >95. Заявление PROVEN PASS **фактически неверно**.

**Recommendation:** trim-pass только на 6 фрагментах, цель ≤93 WPM (запас от cap):
- **s28** убрать ~20–25 слов: сжать повтор «Это тот же механизм, что информационный пузырь. Там прокси — клик … Здесь прокси — выручка …» (стр. 473) до одной фразы-сопоставления; критерий/retrieval/Wendy's-фактуру НЕ трогать.
- **s29** убрать ~10–15 слов: матричные строки уже телеграфны — снять одно из двух пояснений в строке «Поддержка и объяснение» либо «Рекомендации и цены»; нижнюю плашку «не ИИ вовсе» сохранить целиком (несущий judgment).
- **s32** убрать ~5–8 слов ИЛИ перенести 0.3–0.5 мин из Q&A-буфера на s32 (s32=1.5 жёсткий): сжать перечисление 8 вопросов чек-листа до телеграфной формы; мост Сем5 + anti-hype финал НЕ резать.
- **s09 / s23 / s14** убрать по 2–4 слова filler (Δ всего +0.7…+1.0): минимальная косметика, ни критерий, ни точку возврата, ни альтернативу, ни callback не трогать.
- **ENFORCED-инвариант для trim-агента:** резать ТОЛЬКО пояснительный filler; критерии, 5 точек возврата, альтернативы, аналогии, Knight/Opendoor-callback'и — неприкосновенны (preflight стр. 33–34 уже это формулирует — соблюсти).
- После trim — повторный per-fragment WPM-пересчёт скриптом, цель max ≤93, ни один >95.

### P1-2 · Неверная PROVEN-PASS самооценка вводит оркестратора/USER GATE в заблуждение

**Severity:** P1 (риск: оркестратор/pre-gate доверится «PROVEN PASS» и откроет GATE C с непрошедшим DoD; ровно класс «5+ feedback rounds после critic APPROVE», который правила пытаются предотвратить).

**Issue:** Секция «Самооценка» (стр. 567–605) утверждает: «Pacing/WPM — PROVEN PASS … ВСЕ 33 фрагмента ≤95, максимум s28 (94.7 WPM) … Зеро-tolerance соблюдён». Это не соответствует пересчёту (P1-1: 6 фрагментов >95, max s28=100.3). Вероятная причина — скрипт автора неверно извлекал многоабзацные `«…»`-блоки (non-greedy `.*?` обрывает на промежуточном `»`, недосчитывая слова → заниженный WPM). Changelog (стр. 13) тоже заявляет «16 фрагментов >95 … сжаты до ВСЕХ ≤95» — финальное состояние не достигнуто.

**Evidence:** стр. 571 «**PROVEN PASS** … максимум = s28 (94.7 WPM)»; стр. 599 «ВСЕ 33 фрагмента ≤95 (проверено скриптом, max s28=94.7) … Зеро-tolerance соблюдён». Факт: s28=100.3, s29=98.0, s32=97.3, s09=96.0, s23=96.0, s14=95.7.

**Recommendation:** после trim-pass (P1-1) переписать секцию «Самооценка» WPM-блок честно: перечислить пофрагментный WPM по корректной методике (greedy-в-пределах-фрагмента или «весь текст минус cue/гильеметы»), статус не «PROVEN PASS» до фактического прохода. Не блокирующее само по себе, но обязательно к исправлению вместе с P1-1, иначе pre-gate walkthrough получит ложный сигнал.

---

## P2 issues (мелочи, не блокируют)

### P2-1 · «мы с вами» — 8, не 10; распределён по 8 фрагментам (не «7 разделов»)
Self-assessment (changelog стр. 13) «доведён до 10, распределён по всем 7 разделам». Факт: 8 вхождений в 8 фрагментах (s03/s04/s09/s13/s18/s28/s31/s32). Распределение хорошее (живой голос подтверждён независимо — см. PASS §2), но цифра в самооценке завышена. Косметика; на методику не влияет. Привести самооценку в соответствие при правке P1-2.

### P2-2 · Сумма slide-duration = 69.0, не 70.0 (deck totals)
Мой пересчёт Σ duration_min по 33 слайдам = 69.0; deck-part2 totals заявляет 70.0 (`slide_times_sum_min`). Расхождение 1.0 мин (вероятно s31 или округление дивайдеров). На per-fragment WPM (использует индивидуальные duration) не влияет; на 75-мин бюджет с ~5–6 буфером — не влияет. Сверить с consistency-checker; самостоятельная сверка вне scope methodology.

### P2-3 · s32 формально 97.3 WPM, но «остаточный риск» назван «~94»
Стр. 599 относит s32 к граничным «~94». Фактически 97.3 (>95). Покрывается P1-1 (s32 в списке на trim) — фиксирую как P2-уточнение неверной самооценки, не отдельный дефект.

---

## Cross-cutting

- **LO coverage:** gaps нет. LO1 (тип↔задача) — несущая, payoff s29. LO2 (применимость/неприменимость) — критерий в каждом разделе + s30. LO3 (риски данных/ограничения) — s14/s18/s23/s31. LO6 (типичные ошибки + fact-check, Understand) — s09/s14/s18/s23/s28/s32, Bloom-граница с Сем5 явная. Все 4 LO покрыты конкретным контентом, нет висящих.
- **Cognitive load:** 4 новых понятия введены «с нуля» (ошибки 1/2 рода, матрица ошибок, прокси-предвзятость, дрейф распределения) — честно анонсировано в s03 («четыре понятия введём прямо сегодня … разберём на пальцах»). Введение каждого — до использования (дрейф s09 до payoff, матрица s13 до s14-провала, прокси s18). Sequence breaks нет. Темп новых концептов в пределах нормы для 75-мин лекции с аналогией на каждый тип.
- **Sequence:** концепты строго до применения; термин-сирот нет; прогрессия простое→сложное (массовый прогноз → anomaly → скоринг → LLM → recsys → сборка-матрица).
- **Tone drifts:** нет. «Вы»-тон, без familiar CTA, без «магической пилюли», без локального биндинга. Pre-flight actionable (URL/файлы/что делать).
- **Curriculum relevance:** L5 = intermediate (отраслевая); все типы релевантны «зачем инженеру финансы/ритейл»; нет forward-pointing контента, который надо DEFER (Л7-углубления явно помечены как forward, не разворачиваются здесь).

---

## Топ-5 правок (приоритизировано)

1. **[P1-1] WPM trim на 6 фрагментах** до ≤93: s28 (100.3, −20–25 слов: сжать дубль прокси-клик/прокси-выручка), s29 (98.0, −10–15 слов: одно пояснение в матричной строке), s32 (97.3, −5–8 слов ИЛИ +0.4 мин из Q&A-буфера), s09/s23/s14 (96.0/96.0/95.7, −2–4 слова filler). Инвариант: критерии / 5 точек возврата / альтернативы / аналогии / Knight-Opendoor callback'и НЕ резать.
2. **[P1-1] Повторный per-fragment WPM-пересчёт** корректным скриптом (greedy-в-пределах-фрагмента) после trim; gate-условие: max ≤93, ни один >95.
3. **[P1-2] Переписать WPM-блок самооценки честно** (убрать «PROVEN PASS / ВСЕ ≤95 / max 94.7»); привести пофрагментные числа по верной методике — иначе pre-gate получит ложный сигнал.
4. **[P2-1/P2-3] Синхронизировать самооценку** с фактом: «мы с вами» = 8 (не 10); s32 не «~94»; «зеро-tolerance соблюдён» удалить до фактического прохода.
5. **[P2-2] Передать consistency-checker** проверку Σ duration_min (69.0 мой пересчёт vs 70.0 deck totals) — вне scope methodology, но флагнуть.

---

## Резюме

Содержательно речь Лекции 5 — **сильная**: ЦВ символьно идентичен главе, 5 точек возврата реальны и закрываются критериями, failure-нить связна (Zillow→FP→Apple Card→Air Canada/Klarna→Wendy's, Knight callback), 6 типов чётко разведены, LO6=Understand-граница с Семинаром 5 операционализирована, forward-pointer'ы правильного направления, термины строго по locked glossary, anti-hype и универсальность соблюдены, conversational голос настоящий (не пересказ главы). **strict-in по минутам = 56.7% (строго) / 64.9% (расширенно) ≥ 30%, holistic по всем 6 разделам, не single-cluster** — PASS с большим запасом, waiver не нужен и недоступен.

Единственный блокер — **DoD WPM**: 6 фрагментов >95 (s28=100.3, s29=98.0, s32=97.3, s09=96.0, s23=96.0, s14=95.7) при заявленном автором «PROVEN PASS / все ≤95». По правилу DoD Enforcement любой проваленный DoD-метрик → verdict ≥ REVISE. Правка локальная (trim ~50–60 слов суммарно на 6 фрагментах + честная переписка самооценки), не структурная, не content/strict-in. После trim-pass + повторного WPM-пересчёта артефакт ожидаемо переходит в APPROVE-WITH-POLISH/CLEAN.
