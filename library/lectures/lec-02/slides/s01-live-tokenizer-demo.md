---
id: s01
type: hook
section: "Раздел 0. Введение"
duration_min: 2
assertion: "Модель видит ваш запрос не словами и не буквами — а токенами"
learning_goal: "Открывающий hook через визуализацию токенизации реального запроса"
learning_outcomes: [LO1]
chapter_ref: "§1.1 [for-slide-s01]"
visual_brief: "Beautiful token rainbow — фраза, разрезанная на цветные токен-чанки (3 параллельных примера на 3 языках). Hook на тему лекции без устаревающих тестов."
verify_day_of: false
---

# Visible content

## Title bar
«Как модель видит ваш запрос»

## Body
[Большой headline сверху, 38pt bold #21295C, центр]

> Модель видит ваш запрос не словами — а **фрагментами**

[Sub-line, 18pt italic teal]
*Эти фрагменты называются токенами. Они — единственное, что попадает в LLM.*

[3 параллельные строки с примерами в Ocean rounded boxes — token-rainbow]

**EN:** `tokenization is fascinating` → `[token][ization][ is][ fascin][ating]` (5 токенов)

**RU:** `Привет, как дела?` → `[Прив][ет][,][ как][ дела][?]` (6 токенов)

**Код:** `def hello(name):` → `[def][ hello][(][name][)][:]` (6 токенов)

[Gold callout снизу]
**Один и тот же смысл — разное число токенов: EN дешевле RU почти в 2×.**

[Footer мелким]
*Реальные разрезы в токенизаторе `o200k_base` (GPT-4o, Claude 4.x). Подробно — в Разделе 1.*

## Speaker notes

Введение лекции — простая, но контринтуитивная мысль: когда вы отправляете запрос в LLM, модель не видит ваш текст так, как видите его вы. До того, как любая «магия» — ответы, рассуждения, генерация кода — может произойти, ваш текст проходит через первую обязательную операцию, которая называется **токенизация**. Текст разрезается на единицы, которые называются токенами. И именно с этими токенами — не с буквами и не со словами — дальше работает модель.

На слайде — три параллельных примера на трёх типах входа. Английская фраза `tokenization is fascinating` режется на пять токенов: `[token]`, `[ization]`, `[ is]`, `[ fascin]`, `[ating]`. Заметьте, что пробел становится частью следующего токена, а не отдельной единицей; это особенность семейства BPE-токенизаторов. Русская фраза `Привет, как дела?` — короче по количеству символов, но даёт уже шесть токенов, потому что для русского словарь модели не содержит готовых сборок типа `[Привет]` целиком: слово приходится разрезать на `[Прив]` и `[ет]`. Третий пример — программный код: `def hello(name):` — режется на шесть токенов, причём специальные символы вроде скобок и двоеточия — это отдельные токены.

Главное практическое следствие, к которому мы вернёмся в Разделе 1: один и тот же смысл стоит **разное число токенов** в зависимости от языка и формата. Английский — почти в два раза дешевле русского, потому что в обучающем корпусе LLM английского текста на порядок больше, и BPE выучил для него более крупные сборки. Это инженерный факт со стоимостной интерпретацией: один и тот же запрос в API на русском обойдётся примерно вдвое дороже, чем на английском.

Тема сегодняшней лекции — что происходит внутри LLM между моим запросом и ответом. Мы разберём четыре внутренних этапа: токенизацию, эмбеддинги, внимание, сэмплинг. И каждый этап будет связан с инженерной практикой через конкретные следствия — вроде того, что мы только что увидели на стоимости запросов.
