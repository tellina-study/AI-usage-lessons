# Iteration log — Семинар 4, часть 2 (review-раунд 2026-09-22)

Продолжение `iteration-log.md` (файл превысил 600 строк — см. CLAUDE.md Document Size Limit).

## Review-раунд (2026-09-22) — REVISE после четырёхстороннего ревью v4 (49 слайдов)

Четырёхсторонний ревью (methodology-critic APPROVE-WITH-POLISH, presentation-critic
APPROVE-WITH-POLISH, student-simulator, reader-simulator APPROVE-CLEAN) дал 5 независимых
P1-находок (2 подтверждены двумя агентами независимо) — по правилу проекта REVISE-раунд перед
показом, не просто полировка. Применены Fix 1-7, сделано синхронно (Read/Write/Edit/Bash),
без делегирования в субагентов.

**Fix 1 (P1, структурный).** Кейс 2.1 был единственным из 6 кейсов без отдельного шага
«исследование» между проблемой и вопросом (нарушение R4-2). Слайд-вопрос (старый s28, сценарий
+ вопрос в одном) разбит на три: проблема (новый s28), исследование (новый s29 — presence
paradox из кейса 1.1, `arXiv:2602.11988`, перенесённый на `DECISIONS.md`, явная связка «мы это
уже видели», честная оговорка про перенос механизма без нового измерения), вопрос (обрезанный
старый s28 → новый s30). Детали и пересчёт тайминга — `rework/section-2-pamyat.md` §A.2b/A.10/A.12.

**Fix 2 (P1, 2 независимых агента).** Термин «presence paradox» использовался на s19/s25 без
определения. Определён на s11 (исследование кейса 1.1) — новая строка-глосса между таблицей
источников и honest-gap плашкой, плюс явное упоминание в speaker notes, что термин вернётся ещё
дважды (кейсы 1.2/1.3) и снова в разделе 2 (кейс 2.1, s29).

**Fix 3 (P1, 2 независимых агента).** s49 (текст-спецификация уже говорил «без таблицы») визуально
дублировал таблицу шести развилок s47 при рендере (`table_card` со всеми 3 колонками). Новый s53
(было s49) переписан: тонкая строка-перечень кейсов без табличной рамки + доминирующий крупный
вопрос без карточек — композиционно отличается от s51 (было s47), не уменьшенная копия.

**Fix 4 (P1).** Кейсы 2.2 и 2.3 не имели собственных divider-слайдов (в отличие от 1.1/1.2/1.3/2.1).
Добавлены `build_s37` (divider 2.2, icon `git-branch`) и `build_s44` (divider 2.3, icon
`clipboard-check`), по образцу `divider_case`. Тайминг раздела 2 вырос с 28,5 до 30,5 мин
(+1,0 Fix 1, +1,0 Fix 4 — оба divider'а по 0,5 мин).

**Fix 5 (P2).** Три overclaim исправлены: (a) s52 (было s48) — «полная карта всех семи позиций»
→ «пять слотов показаны как позиции карты, хук/процесс только названы»; (b) s01 — «подписи…
окончательно в конце занятия» смягчено (только 5 из 7 подписываются к концу); (c) формула
«правила в промпте — просьбы, правила в коде — законы» (кейс 1.2) была условной (только реакция
на ответ «капсом») — сделана безусловной частью разбора s19 (первая фраза footer'а).

**Fix 6.** Закрыты TODO в `deck.yaml`: `failure_share` пересчитан по каждому кейсу отдельно
(1.1=59%, 1.2=65%, 1.3=61%, 2.1=54%, 2.2=61%, 2.3=62%, все ≥30% индивидуально, агрегат ≈48%
от 73,0 мин материала); `learning_outcomes` оставлен `[LO7, LO6, LO3]` с explicit TODO-пометкой
подтверждения у оркестратора (не додумано самостоятельно, по инструкции).

**Fix 7.** 6 companion-файлов (`handout.md`, `rubric.md`, `plan.md`, `plan-part2.md`,
`facilitator-guide.md`, `facilitator-guide-part2.md`) помечены deprecation-баннером сразу после
frontmatter — описывают старую v3 (7 ступеней, 90 мин), полная пересборка под v4 вне объёма
этого раунда.

### Пересборка деки

- Слайды: 49 → **53** (`slides/*.md` переименованы через `mv` + пересчитан `id:` во frontmatter
  скриптом на Python; новых файлов — 4: `s28`, `s29`, `s37`, `s44-divider-...`).
- `deck.yaml` пересобран программно из frontmatter всех 53 `slides/*.md` (Python + PyYAML) —
  исключает транскрипционные ошибки; `slide_count: 53`, материал `73,0` мин (было 71,0),
  `duration_min: 75` (резерв сокращён с 4 до ~2 мин, поглощает рост от Fix 1/4).
- `build_sem04.py`: все `build_sNN` для sNN ≥28 переномерованы regex-скриптом (per-function-block,
  self-references только), добавлены 4 новых функции (`build_s28`, `build_s29`, `build_s37`,
  `build_s44`), `build_s30` (было `build_s28`) урезан до вопроса без сценария, `build_s19`
  (формула безусловно в footer), `build_s11` (presence paradox строка), `build_s53` (было
  `build_s49`, полностью новая композиция). `SEQ` расширен на `range(1, 54)`.

### Финальные числа

- `sem-04.pptx` — **53 слайда**, `sem-04.pdf` — **53 страницы** (`pdfinfo`).
- `snapshots/` — **53** `sNN.png` @150dpi, дублей `s-NN.png`/`sem-04-s-NN.png` не осталось (были
  промежуточно от `pdftoppm` дефолтного `-NN` separator — переименованы через `mv` в `sNN.png`).
- Build прогонялся дважды подряд (после правок build-скрипта) — оба раза 0 строк
  `WARNING`/`OVERFLOW`/`error` в stdout.
- Визуально проверены (Read PNG): s28 (проблема, новый), s29 (исследование, новый), s37/s44
  (оба divider'а, новые — подтверждено визуально РАЗНЫЕ: разный заголовок/tag/иконка/позиция
  gold-пилюли), s51 vs s53 (подтверждено композиционно разные — таблица vs список+крупный вопрос),
  s11 (presence paradox определение — влезает без overflow), s19 (формула безусловно, первая
  строка footer'а), s30 (вопрос без сценария), s01, s36, s52.

### Toolchain

LibreOffice портативная установка не на `$PATH` по умолчанию — нужен
`source /home/harness/.local/lo-portable-env.sh` перед `soffice`/`pdftoppm`/`pdfinfo` (см.
`notes/mcp-limitations.md` [#201-4] про таймауты — в этой сессии `soffice --convert-to pdf` и
`pdftoppm -r 150` запускались отдельными командами с 120-180с таймаутом, оба уложились).

---

## Раунд 5 (2026-09-22) — смена оси «день-0 vs жди сигнала» + R5-1..R5-4, 53→52 слайда

Вход: `REWORK-REQUIREMENTS-v5.md` (R5-0 смена центральной оси, R5-1 визуальная унификация
дивайдеров, R5-2 раздел 0 переписан, R5-3 вопрос-до-исследования во всех 6 кейсах, R5-4
README-плашка заметной) + 5 файлов `rework/section-*.md` (v5) как единственный источник истины
для новой структуры. Полная пересборка `slides/*.md` (все 53 старых файла удалены, 52 новых
написаны с нуля), `deck.yaml`, `build_sem04.py` (2285 строк, переписан целиком, helper-библиотека
~35 функций перенесена почти без изменений, все 52 `build_sNN` написаны заново под новый порядок/
контент), синхронно (Read/Write/Edit/Bash), без делегирования в субагентов — по явному указанию
задачи.

**Новая сквозная нумерация (v4 → v5).** Раздел 0: 7 слайдов (s01-s07), контент s02-s06 заменён
полностью (задача заказчика → открытая дискуссия → spec.md → сквозной кейс с новым поворотом
«структура, не критерий» → 2 режима вместо 8 карточек харнесса). Раздел 1: 16 слайдов (s08-s23,
было 19) — экономия за счёт слияния исследование+разбор в один слайд на кейс (R5-3), вопрос
переставлен раньше. Раздел 2: 26 слайдов (s24-s49, было 24) — рост за счёт двух НОВЫХ отдельных
research-слайдов у кейсов 2.2/2.3 (раньше исследование было незаученной частью речи фасилитатора
без строки в таймингах); кейс 2.1 переставлен (вопрос перед исследованием) и решение (s30) лишилось
diff-панели (R5-0: файл заведён на день 0, diff «new file mode» противоречил бы рамке). Раздел 3:
3 слайда (s50-s52, было 3) — синтез переписан под новую ось.

**Проверка «вопрос без цифр» (R5-3, обязательное требование задачи).** Все 7 слайдов-вопросов
(s05, s11, s16, s21, s26, s36, s44) проверены grep'ом по Assertion+Visual секциям исходных `.md`
на `[0-9]|arXiv|%` — 0 хитов у всех. Два вопроса потребовали явной зачистки цифр, оставшихся в
`rework/*.md` дословных формулировках (задача прямо предупреждала об этом сценарии): s36 (кейс
2.2) — убрано «тридцать восемь записей» из вопроса (осталось на предыдущем scenario-слайде s35,
где цифра уместна); s44 (кейс 2.3) — убрано «шаги 1–2» → «первые шаги».

**R5-1 визуальное решение (дивайдеры).** Введены 3 новых pattern-функции взамен старых
`divider_section`/`divider_case`/`divider_section_and_case`:
- `divider_section_macro` (раздел-уровень, s08) — широкий GOLD 4-сегментный progress-bar +
  gold-номерной значок-кружок рядом с заголовком (новый визуальный элемент, единственный маркер
  macro-уровня).
- `divider_case_micro` (кейс-уровень, s09/s14/s19/s34/s42) — узкий TEAL 3-сегментный
  progress-bar, БЕЗ значка, заголовок на 2-3pt меньше — визуально легче macro.
- `divider_hybrid` (комбинированный раздел2+кейс2.1, s24) — несёт ОБА элемента: gold-значок +
  широкая gold-полоса (macro) И узкая teal-полоса (micro) на одном слайде.
Найден и исправлен off-by-one баг при первом рендере: `divider_badge_macro(s, active_idx + 1)`
показывал «2» на слайде «Раздел 1» (s08) и «3» на слайде «Раздел 2» (s24) — исправлено на
`active_idx` без `+1` (active_idx уже 0-индексирован от 0=открытие, что совпадает с номером
раздела). Перепроверено визуально после фикса — s08 показывает «1», s24 показывает «2».

**Layout-баг найден и исправлен при визуальной проверке.** s12 (кейс 1.1, merged
исследование+разбор + README-плашка R5-4) — при первом рендере gold-плашка README накладывалась
на последнюю строку второй таблицы (недостаточный бюджет высоты: 2 таблицы по 2,15/1,9in +
заголовок 143 симв. не оставляли места на 3-строчный callout). Фикс: `th1` 2.15→1.7,
`th2` 1.9→1.65, зазоры увеличены, один длинный текст в ячейке «Сила» сокращён до однострочного
(«Умеренно «за» — не общее описание» вместо двухстрочного оригинала) — освободило ~0,9in под
callout. Перепроверено визуально — плашка теперь читается отдельным чистым блоком.

**Финальные числа.**
- `slides/*.md` — **52** файла (было 53), сквозная нумерация s01-s52 без пропусков.
- `deck.yaml` — **52** слайда, сумма `duration_min` = **78,75** (12 раздел 0 + 29,5 раздел 1 +
  32,75 раздел 2 + 4,5 раздел 3), `duration_min: 81` (материал + ~2,25 резерва).
- `sem-04.pptx`/`sem-04.pdf` — **52** слайда/страниц.
- `snapshots/` — **52** `sNN.png` @150dpi, дублей не осталось.
- Build прогонялся 4 раза подряд (после правок th1/th2/badge/overflow) — финальный прогон: 0
  строк `WARNING`/`OVERFLOW`/`error` в stdout.
- Визуально проверены (Read PNG): все 8 дивайдер-стилей (s08/s09/s14/s19/s24/s34/s42 — macro vs
  micro vs hybrid чётко различимы по весу/цвету/значку), s01 (обложка, новый центральный вопрос
  день-0/сигнал), s02-s06 (новые слайды раздела 0 — задача заказчика, spec.md, сквозной кейс,
  2 режима, разбор режимов), s11+s12 (вопрос кейса 1.1 чист от цифр + README-плашка заметна),
  s36+s37 (вопрос кейса 2.2 чист от «38 записей» + новый research-слайд с цифрами после
  голосования), s50 (recap-таблица с колонкой «Когда», teal/gold без оценочной семантики),
  s52 (closing question, композиция явно отличается от s50 — тонкий список стрелок vs таблица).

### Что нового для `notes/mcp-limitations.md`

Ничего нового не найдено в этой сессии — все известные ограничения ([#201-1..6]) актуальны и
учтены (multipara_box вместо literal `\n`+CENTER, +18% margin в terminal_card, отдельные
soffice/pdftoppm команды с щедрым таймаутом, единый `ROOT`/`OUT` без `.bak`-копий).

---

## Раунд 6 (2026-09-22) — точечный фикс-проход по 5 P1 из four-way ревью после раунда 5

Вход: комбинированные находки methodology-critic (REVISE) + presentation-critic (REVISE) +
student-simulator + reader-simulator (APPROVE-WITH-POLISH) на v5 (52 слайда) — 5 независимых P1,
лично перепроверенных оркестратором перед делегированием. Работа выполнена синхронно
(Read/Write/Edit/Bash), без делегирования в субагентов.

1. **Fix 1 (P1, ядро нарратива) — временнóе противоречие кейс 1.1 → 1.2/2.1/2.3.** `s13`
   обещал «первый сигнал через две недели» (`CLAUDE.md` пуст весь этот срок), но `s15`/`s25`/`s43`
   открываются «с первого дня в файле уже есть строка» — прямое противоречие подряд на занятии.
   Переписан мостик кейса 1.1 (`s13.md` + `build_s13()` — assertion, Visual, Speaker notes,
   frontmatter) на формулировку, которая разводит два типа решений: пусто именно для *описания
   структуры репозитория* (ждёт сигнала), но часть содержимого (гейты/лог/задачная память)
   заводится сразу на дне 0 как дешёвая гигиена — это кейс 1.2. Синхронно поправлен
   `rework/section-1-fayl-instrukciy.md` (мостик кейса 1.1) и добавлена явная уточняющая врезка в
   `rework/section-0-otkrytie.md` §A.5 (снимает конфликт на уровне методической главы, не только
   слайда). Проверено: `s04`/`s07` не содержат буквальной фразы «харнесс пуст» — точечных правок
   там для этого фикса не потребовалось.
2. **Fix 2 (P1) — таблицы разбора `s17`/`s22` урезаны до 2 строк из 5.** Корневая причина — не
   `.md`-источник (там всегда было 5 строк), а `build_s17()`/`build_s22()`: литеральные python-
   массивы `rows3`/`rows2` дублировали только 2 из 5 строк вручную при первой сборке. Дописаны
   недостающие 3 строки в каждой таблице; высоты блоков и зазоры пересчитаны (`th1/th2` в `s17`
   1.4→1.35, `th3` 1.3→1.95, gold-callout `7.0`→`7.3`; в `s22` `mh` 0.72→0.6, `th` (эмпирика)
   2.15→1.75, `th2` (разбор) 1.1→1.82, колонки `s22`-разбора перебалансированы `[0.3,0.28,0.42]`→
   `[0.25,0.26,0.49]`, чтобы длинные ячейки уложились в одну строку) — проверено построчным
   python-расчётом `_fits`-логики (chars-per-line/line-height) до рендера, не только после.
3. **Fix 3 (P1) — `s24` без превью трёх кейсов раздела 2 (асимметрия с `s08`).** `divider_hybrid()`
   получил новый опциональный параметр `case_lines` (тот же bullet-list паттерн, что уже был в
   `divider_section_macro` для `s08`); `build_s24()` передаёт три строки кейсов 2.1/2.2/2.3.
   Единственный call-site `divider_hybrid` — сам `s24`, так что сигнатура расширена без риска для
   других слайдов.
4. **Fix 4 (P1) — `s04` assertion обещает конкретику «чего spec.md не покрывает», gold-плашка её
   не подтверждала.** Вторым предложением в ту же gold-плашку (`build_s04()`) добавлена явная
   конкретика поворота (структура — имена файлов, куда положить, как разложить — spec.md по
   определению это не описывает), дословно сверенная с `rework/section-0-otkrytie.md` §B.4.
   `gold_h` 0.95→1.05, `y3` сдвинут на ту же дельту.
5. **Fix 5 (P1, системная) — spec.md/README.md заявлены «с первого дня», но ни разу не видны в
   деревьях репозитория.** Проверены все 5 мест с деревом `signup-landing/` (`s04` «что создал»,
   `s10` «дерево на входе», `s13`/`s23` «дерево репозитория — без изменений», `s30` «дерево кейса
   2.1»). Все пять либо совпадают с реальными git-капчурами (`21-tree-stage1.txt`,
   `22-tree-stage2.txt` — ни один не содержит `spec.md`/`README.md`), либо тот же канонический
   4-файловый список, привязанный к `36-first-task-dialogue.md`. Капчуры НЕ редактировались.
   Вместо этого в каждый `terminal_card` добавлена одна `CODE_MUTED`-строка-пометка
   (`# + spec.md, README.md (раздел 0) — …`), явно отделённая от содержимого капчура — тот же
   стиль, что уже использовал `s23` для `# tests/CLAUDE.md — не появляется`. Высоты боксов
   пересчитаны там, где не было запаса (`s04` `h3` 2.0→2.25, `s10` `h1` 2.1→2.3, `s13` 2.35→2.65,
   `s23` 2.65→2.95; `s30` уже имел запас, высота не менялась).

**Пересборка:** `python3 build_sem04.py` → `sem-04.pptx` (52 слайда) → `sem-04.pdf` (52 страницы,
`pdfinfo` подтверждено) → PNG для 10 затронутых слайдов (`s04/s08(reference)/s10/s13/s15/s17/s22/
s23/s24/s30`) через раздельные `soffice --convert-to pdf` + `pdftoppm -f N -l N -r 150` (с
верификацией размера файла 130–250KB на каждый — известная ловушка [`iteration-log.md` строка 305]
однажды дала почти-пустой single-page PNG; в этот раз все файлы штатного размера). Финальный
прогон сборки — **0** строк `WARNING`/`OVERFLOW`/`error` в stdout.

**Визуальная проверка (Read PNG, обязательный пункт) — все 8 затронутых слайдов подтверждены:**
`s04` (gold-плашка теперь двухпредложная с явной конкретикой про структуру; «что создал» — 5
строк, пометка видна без overflow), `s10` (дерево + пометка без overflow), `s13` (заголовок и
gold-плашка синхронны с новой формулировкой Fix 1, дерево + 2 пометки), `s15` (не менялся,
визуально сверен на согласованность с новым `s13` — конфликта больше не слышно), `s17` (таблица
разбора — полные 5 строк, без overflow), `s22` (таблица разбора — полные 5 строк, target-ответ
подсвечен gold), `s23` (дерево + пометка), `s24` (список трёх кейсов раздела 2, симметричен `s08`,
macro+micro прогресс-бары и значок не пострадали), `s30` (дерево + пометка, запас высоты был и
остался).

**Не затронуто (по прямому указанию):** `rework/section-2-pamyat-part2.md`/`-part3.md` (кроме уже
описанной точечной правки `section-0-otkrytie.md` §A.5 и `section-1-fayl-instrukciy.md`, обе входят
в фикс 1), `sem-05/`, `assets/captures/*` (только читались, не редактировались), `qa-reports/`.
