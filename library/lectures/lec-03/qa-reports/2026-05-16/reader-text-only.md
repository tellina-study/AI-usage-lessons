# Reader Text-Only Report — Лекция 3 — 2026-05-16

VERDICT: APPROVE-WITH-POLISH

**Режим:** text-only (методический контроль СЛАЙДОВ ДО рендера). Симуляция: студент 3 курса ИУ6, прошёл Л1+Л2, на самоподготовке без преподавателя, читает только markdown слайдов (visible + speaker notes). PNG ещё нет.

**Итог одной фразой:** колода читается на самоподготовке очень хорошо — speaker notes почти везде связный студенческий текст 150–300 слов, assertion'ы реально проверяемые, арка s01→s30 ведёт к payoff и центральный вопрос ощутимо закрывается. **P0-блокеров рендера НЕТ.** Есть 1 P1 (термин «инъекция» введён за 5 слайдов до определения, security-термин из §7 inline-required) и набор P2 (мелкие term-before-use на карте-лестнице s04, дисбаланс плотности s27/s28).

---

## 1. Введено-после-использования (на уровне слайдов, порядок s01→s30)

Скан каждого канон-термина: первое появление в visible/notes vs. место определения.

### P1 — требует фикса в md (до рендера желательно, но можно и заметками)

- **«инъекция / prompt injection» — s20 → определяется s25.**
  - s20 visible: «описание попадает в контекст модели — **носитель инъекции**»; s20 notes повторяют то же.
  - Формальный inline-define только в s25 («Prompt injection (инъекция в промпт) — текст во внешних данных...»).
  - Между ними s23/s24/s27 косвенно опираются на «поверхность инъекции».
  - План §7 явно помечает `prompt injection` как **inline-required при первом упоминании (§4.1 ≈ s19/s20)**. Сейчас правило нарушено: студент на самоподготовке встречает названный вектор атаки за 5 слайдов до его определения. Контекст s20 («описание в контексте модели — носитель инъекции») частично самоочевиден, поэтому это **P1, не P0** — но это security-термин и явное требование плана.
  - **Recommendation:** в s20 visible добавить 1 строку-микроглоссу к слову «инъекция», например: *«инъекция = подсунутый в контекст текст, который модель исполнит как команду; разберём как класс атак в Разделе 4»*. Альтернатива (слабее): усилить эту фразу в s20 speaker notes. Это не структурная переделка — одна вставка.

### P2 — мелочи, можно оставить или поправить заметкой

- **«CoT» и «few-shot» — s04 visible (ладдер ступень 2: «Один вызов LLM (промпт; + CoT, + few-shot)») → CoT определяется только s06; few-shot нигде в слайдах inline не определяется** (есть в glossary, но слайды не вводят).
  - Митигировано: s04 несёт disclaimer «лестница — карта лекции, не требование понять всё сейчас». На карте сокращения допустимы как маркеры будущих тем.
  - few-shot — пререквизит из Л1, в Л3 не разбирается; на видимом слое появляется только как пометка на карте. Низкий риск, но строго по §7 это «inline-required» — для самоподготовщика «+ few-shot» без расшифровки = пустой токен.
  - **Recommendation (необязательно):** ничего не менять (карта-контекст оправдывает) ЛИБО в s04 sub-caption добавить «CoT, few-shot — приёмы промпта из Л1/Раздела 1, разберём в Разделе 1». Не P0/P1.

- **«catastrophic forgetting» — s15 visible (причина №3: «↓ Риск catastrophic forgetting») → определяется s16.**
  - Митигировано: визуальная карточка s15 №3 несёт пояснительную клаузу «база заморожена, физически не переписывается под новый сигнал», а s15 notes делают явный forward-ref «к которому мы сейчас перейдём». Для reader-самоподготовщика смысл реконструируется из клаузы.
  - **Recommendation (необязательно):** в s15 visible к «catastrophic forgetting» добавить 2 слова-глоссу «(деградация общих способностей при дообучении)». P2.

- **«дистилляция / distillation» — s14 visible перечисляет «дистилляция» в зоне «Осталось за fine-tuning» БЕЗ inline-глоссы на видимом слое; определение только в speaker notes s14.**
  - Для режима «слайд + notes» (наш режим) — покрыто заметками. Для будущего student-simulator (только PNG + видимые notes) — на видимом слое термин висит голым. Notes-covered → P2.
  - **Recommendation (необязательно):** s14 visible — к «дистилляция» дать 4-словную глоссу «(обучить меньшую модель повторять большую)».

### Чисто (term defined at/before first use) — подтверждено

single-shot (s03, inline + Л2-ref) · RAG (s03 gloss + «детали Раздел 2», full s10 — митигация плана reader-P1 работает) · retrieval (s10 inline) · function calling (s03 gloss, full s19) · MCP (s03 gloss, full s20) · faithfulness (s07 inline, при первом use) · context rot (s08 inline + сшивка с «lost in the middle» Л2) · grounding (s10 inline) · observability (s12 inline, при первом use; s13 assertion опирается уже после) · fine-tuning (s14 inline — пробел пререквизита закрыт как обещал план) · PEFT/LoRA/QLoRA (s15 inline) · structured output (s19 inline) · prompt caching (s19 inline) · workflow (s04 gloss, full s22) · агент (s04 gloss, full s21) · ZDR (s24 inline) · least-privilege (s24 inline) · BAA (s24 footer, тот же слайд) · golden set (на видимом слое не всплывает, в notes — русская фраза «эталонный проверочный набор», не голый термин — не проблема).

**Вывод по 1:** дисциплина введения терминов высокая. Главные обещания плана (fine-tuning gap → s14, RAG-мост → s03, context rot сшивка → s08) **выполнены в тексте слайдов**. Единственный реальный нарушитель §7 — «инъекция» (P1).

---

## 2. Assertion-test (заголовок-assertion — проверяемое утверждение или просто тема?)

Проверял frontmatter `assertion` каждого слайда: это полное предложение-тезис с проверяемым содержанием, или «4 названия без смысла».

**Сильные, проверяемые assertion'ы (подавляющее большинство):** s01 («заплатила за выдумку — потому что выбрала генеративную для задачи, требовавшей детерминированного lookup» — каузальный тезис), s05, s07 («проговорённое рассуждение не обязано отражать реальную причину → человек проверяет результат, не самообъяснение»), s08, s11 («4 признака ОДНОВРЕМЕННО» — фальсифицируемо), s12, s13 («вернул что-то ≠ вернул правильное»), s14 («не умер — сузился»), s15 («PEFT почти всегда лучше: дешевле/модульнее/↓forgetting»), s16, s17, s19, s20 («стандартизация подключения ≠ безопасность подключаемого»), s21 («check не должен быть самооценкой модели»), s22, s23 («5×99%=95%, не 99%» — конкретное проверяемое число), s24 («ZDR ≠ нигде ничего не хранится»), s25, s26, s27 («матрица — структура для аргумента, не сумма баллов»), s29. — Это эталонный assertion-evidence: каждый можно оспорить/подтвердить, не просто тема.

**Слабее как assertion (но не «голая тема»):**
- **s02** assertion = «Лекция 3. Архитектуры AI-систем: агенты, RAG, API» — это не тезис, а название. Но s02 = `cover`, для cover это допустимо по §4 README (cover не обязан нести assertion-тезис). Не дефект.
- **s09 / s18** (section_divider) — assertion'ы описательные («Раздел 2 — RAG: ...»). Для дивайдеров это нормативно (тип `section_divider`), не флаг.
- **s03** assertion «Вокруг одного вызова надстраиваются 4 обвязки — и одна (RAG) стоит прямо на эмбеддингах Л2» — проверяемо, но визуально это hub-and-spoke recap; тезис скорее «карта», чем спор. Приемлемо для recap-слайда Раздела 0.
- **s30** assertion «Эта рамка — база Лекций 4–17» — это обещание/мост, слабо фальсифицируемо, но для финального summary-моста допустимо.

**«4 названия без смысла» — НЕ найдено ни одного.** Даже самые «списочные» слайды (s19 «3 механизма», s25 «4 правила») несут связку-вывод, превращающую список в аргумент («ни один не делает модель надёжнее», «инъекция × широкие права — убери любой → атака не проходит»).

**Вывод по 2:** assertion-дисциплина — одна из сильнейших сторон колоды. Нет slide'ов, где заголовок = просто тема контента.

---

## 3. Speaker notes — связный читаемый текст, помогает понять БЕЗ лектора?

Прочитал все 30. Notes написаны как **студенческий конспект**, а не layout-проза («слева donut, справа bar») и не обрывки. Везде 150–300 слов связного текста (дивайдеры s09/s18 короче — ~150–180, что для дивайдера оправдано). Логика «утверждение → механизм → пример → граница/урок» выдержана.

**Особенно сильные notes (понятны самоподготовщику полностью без лектора):** s01, s07, s08, s10, s12, s13, s16, s21, s22, s23, s24, s25, s26, s27, s28, s29 — каждый можно читать как абзац методички, термин вводится в самом тексте, «и что?» закрыт явным уроком/критерием.

**Слабых / layout-проза / обрывочных notes — НЕ найдено.** Это заметно лучше типичной колоды на этой фазе.

**Мелкие замечания (P2, не блокеры):**
- **s27 notes — самые плотные в колоде** (~360 слов, 7 осей × 7 архитектур + порядок приоритезации осей + 2 примера обоснования). Текстуально связно и понятно, но это пик когнитивной нагрузки на самоподготовке: 49-клеточная матрица + методология «как читать» + Air Canada callback в один слайд (duration 3 мин). Не дефект текста, но **на самоподготовке s27 — место, где читатель замедлится сильнее всего**. Notes сами по себе вытягивают (там есть «не сумма баллов, а назови решающую ось»), поэтому P2, не P1. Рекомендация: при рендере убедиться, что визуальная матрица не перегружает (это уже задача presentation-critic/designer, render-fix).
- **s28 notes** — worked example (задача A) прогоняется через 8 шагов очень убедительно; mini-apply (задача B) дан с явным «сначала попытка, потом сверка». Текст хороший. Замечание: для самоподготовщика задача B остаётся **без разбора в этой колоде** (разбор — «на Семинаре 3 / позже»). Это намеренный методический выбор (LO4 mastery вынесен в С3, см. план), не дефект, но самоподготовщик не получит обратной связи на свой ответ из материалов лекции. Зафиксировать как осознанный trade-off, не флаг.
- **s14 notes** — определение «дистилляция» спрятано в середине второго абзаца notes, на видимом слое термин голый (см. §1 P2). Notes покрывают для нашего режима.

**Вывод по 3:** notes — самодостаточны для самоподготовки практически везде. Это сильнейший аргумент за APPROVE-WITH-POLISH, а не REVISE.

---

## 4. Арка / связность (s01→s30 ведёт к payoff? ЦВ закрывается? дивайдеры не рвут нить?)

**Центральный вопрос (s04):** «У меня есть задача и доступ к LLM. Какую архитектуру выбрать — и когда правильный ответ "не ИИ"?». 

**Возвраты ЦВ — отслеживаемы и явные.** Notes сами маркируют точки возврата: s12 «вторая точка возврата центрального вопроса», s17 «третья точка возврата», s22 «четвёртая точка возврата», s23 «пятая точка возврата». Это редкая для черновика явная нумерация арки — самоподготовщик не теряет нить. ЦВ ставится s04 → проходит через каждое «когда НЕ» (s08/s12/s17/s22) → закрывается payoff s26 (лестница) + s27 (матрица) + s28 (чек-лист + apply).

**Payoff-связка s26–s28 — работает.** s26 явно «соберём всё пройденное в лестницу» и перечисляет требование на каждой стрелке-переходе (карта → инструмент). s27 «лестница говорит "не поднимайся без нужды", но не говорит по каким осям мерить — это даёт матрица». s28 «лестница и матрица сворачиваются в 8-шаговый чек-лист». Каждый payoff-слайд явно ссылается на предыдущий — лестница строится инкрементально, а не вываливается. Air Canada замыкается дважды: s13 (revisited как grounding) и s27 notes («эта строка объясняет открытие: Air Canada — выбор генеративной для задачи из нижней плашки»). Кольцо s01→s27 закрыто чисто.

**Дивайдеры s09 / s18 — НЕ рвут нить, наоборот сшивают.** s09 notes начинаются с явного recap Раздела 1 («мы закончили первый раздел: дефолт — один вызов...») и анонсируют 3 сюжета Раздела 2, включая «замыкая раздел обратно на дело Air Canada». s18 notes делают то же для Раздела 4 (5 сюжетов + «впервые встаёт сквозная тема безопасность»). Дивайдеры работают как мост, а не как разрыв — для самоподготовщика это «где я на карте» + «что дальше и зачем».

**Межразделовые переходы внутри:** s08→s09 (context rot → «когда НЕ RAG» прямо ведёт в раздел RAG), s13→s14 (Air Canada закрыт → fine-tuning), s17→s18 (критерии знание/поведение → API-слой), s21→s23 (4 места отказа цикла → ровно эти режимы в датированных кейсах — прямой мост, notes s23 явно «три класса провалов ниже — ровно эти режимы»), s25→s26 (безопасность → собираем лестницу). Сквозная линия «человек-валидатор» прошита s07→s21 (check)→s29 (callback s07) — три явных callback'а, самоподготовщик видит единую нить.

**Вывод по 4:** арка — одна из сильнейших сторон. ЦВ ощутимо закрывается, payoff-лестница строится инкрементально, дивайдеры сшивают. Связность для самоподготовки отличная.

---

## 5. Failure-слайды (урок + альтернатива понятны со слайда, не «страшилка»?)

Проверял bucket плана (s07, s12, s13, s16, s22, s23, s24, s25, s27, s28) + s01:

- **s01 (hook, partial→out)** — урок отложен в s13, но даже здесь visible несёт «не сбой модели — неправильный выбор архитектуры» + контраст «что выбрали vs что было нужно». Не голая страшилка. ✔
- **s07** — урок: «контроль на самообъяснении — не контроль; проверяй результат против внешнего источника». Альтернатива явная (внешний источник истины). Числа 25%/39% поданы с методом эксперимента в notes — не «модель иногда врёт», а структурный вывод. ✔ Сильно.
- **s12** — каждый из 3 критериев несёт правильную альтернативу прямо в visible (full-context+кэш / детерм. lookup / observability-процесс). «Скрытая бомба» объяснена («не падает, а уверенно отвечает неправильно»). Урок+альт явные. ✔
- **s13** — 3 кейса деградации + диагноз Air Canada + правильная альтернатива («не убрать RAG, а спроектировать как наблюдаемую систему»). Gold-урок «вернул что-то ≠ вернул правильное» проверяем. ✔
- **s16** — урок: «нет eval-петли + версий → это не риск, а критерий НЕ делай FT». Альтернатива (PEFT / RAG) на visible. Механизм объяснён. ✔
- **s22** — не failure-кейс, а критерий-различение; «самая практически дорогая ошибка отрасли: строить агента где хватило бы workflow» — урок есть, диагностический вопрос = инструмент. ✔
- **s23** — 3 кейса с числами ($4,200/63ч, 5×99%≈95%, мульти-агент хрупкость), каждый с «Почему» + «Правильно». Не страшилка: каждый кейс даёт альтернативу (retry-скрипт / меньше хопов / single-threaded). ✔ Сильно.
- **s24** — 2 факта (судебный приказ, ZDR покрывает не всё) + правило «карта данных по фиче, не галочка». Альтернатива (on-prem, не слать без ZDR/BAA). Урок инженерный, не FUD. ✔
- **s25** — механизм инъекции + 2 кейса + «катастрофа = инъекция × широкие права, убери любой → не проходит» + 4 правила защиты. Конструктивно, не страшилка. ✔
- **s27** — нижняя плашка «детерминированная задача → код, без ИИ; ИИ добавил бы только недетерминизм+стоимость+латентность+инъекцию» — это критерий, не страшилка. ✔
- **s28** — чек-лист = свёрнутые «когда НЕ» как вопросы. Конструктивный инструмент. ✔

**Вывод по 5:** ни один failure-слайд не вырождается в «страшилку». Везде паттерн «провал → почему (механизм) → правильная альтернатива/критерий». Это прямо обслуживает миссию курса (говорить «нет» неподходящему ИИ). Самоподготовщик из текста выносит критерий, а не страх.

---

## 6. Связь с Л2 (s03 мост): RAG не «чёрный ящик» до s10?

- **s03 visible + notes** делают мост явно: «RAG = retrieval = semantic search Лекции 2 §2 + LLM сверху», стрелка от «эмбеддинги Л2 §2» к RAG-узлу, sub-caption «детали — Раздел 2; single-shot и semantic search не переобъясняем — готовые блоки». s03 notes даже советуют «если semantic search/single-shot вспоминаются смутно — перечитать Л2 до этой лекции». Для самоподготовщика это идеально: он знает, что RAG стоит на знакомом, и куда смотреть если забыл.
- Между s03 и s10 RAG упоминается s04 (ступень лестницы), s05 (что добавляет RAG — конвейер/хранилище/деградация retrieval), s08 (когда НЕ RAG, точка 1), s09 (дивайдер, one-phrase «извлечь → положить в контекст → ответить с опорой»). На каждом из этих появлений RAG используется **функционально** (что он добавляет / когда не нужен), а не как магия — и каждый раз есть явная пометка «детали — Раздел 2». s09 notes дают полную one-phrase механику до s10.
- **s10** даёт полный 3-шаговый принцип + inline-define retrieval + grounding.

**RAG НЕ «чёрный ящик» до s10.** К моменту s10 самоподготовщик уже понимает: (а) RAG = semantic search Л2 + генерация, (б) что он добавляет инфраструктурно (s05), (в) когда он НЕ нужен (s08). s10 затем формализует. Мост Л2 выполнен ровно как требовал план reader-P1. ✔

---

## 7. Structural blocker vs notes-fix vs render-fix (что чинить в md ДО рендера)

### P0 — фиксить md ДО рендера (блокеры рендера)
**НЕТ.** Ни одного structural blocker. Текст слайдов работает на самоподготовке без преподавателя по всем 7 пунктам чек-листа.

### P1 — желательно поправить в md (или явной заметкой)
1. **s20 — «инъекция» введена без inline-глоссы, определена только s25** (нарушение §7 inline-required для security-термина). **Fix = notes-fix или 1 строка в visible** (одна вставка-микроглосса в s20 visible ИЛИ усиление в s20 speaker notes). НЕ структурный, НЕ блокирует рендер, но рекомендуется до рендера, т.к. дешевле поправить markdown сейчас, чем после.

### P2 — можно оставить / поправить заметкой / отложить до визуала
2. **s04 — «CoT»/«few-shot» на карте-лестнице без глоссы** (митигировано disclaimer'ом «карта, не требование понять сейчас»). Notes-fix опционально.
3. **s15 — «catastrophic forgetting» в visible до определения s16** (митигировано пояснительной клаузой + forward-ref в notes). Notes-fix опционально.
4. **s14 — «дистилляция» на visible без глоссы** (покрыто speaker notes; для будущего PNG-only режима — добавить 4-словную глоссу). Notes/visible micro-fix опционально.
5. **s27 — пиковая когнитивная плотность** (49-клеточная матрица + методология приоритезации осей). Текст notes вытягивает; основной риск — визуальная читаемость матрицы → **render-fix**, передать presentation-critic/designer, не текстовый дефект.
6. **s28 задача B без разбора в колоде** — осознанный методический trade-off (LO4 mastery → Семинар 3), НЕ дефект; зафиксировать как намеренное.

---

## Сводка

- **Слайдов с P0 issues (текст совершенно не работает без живого слова):** **0 / 30.** Нет structural blockers, нет рваных notes, нет «голых тем» вместо assertion.
- **Слайдов с P1 (понятно, но нужен фикс):** **1** — s20 («инъекция» introduced-before-defined, security-термин из §7; fix = 1 строка / notes).
- **Слайдов с P2 (мелочи):** **4–5** — s04 (CoT/few-shot на карте), s15 (catastrophic forgetting до s16), s14 (дистилляция на visible), s27 (когнитивная плотность → render-fix), s28 (B без разбора — намеренный trade-off).
- **Self-study читаемость:** очень высокая. Notes — связный студенческий текст 150–300 слов везде; assertion'ы проверяемые; арка s01→s30 с явными нумерованными точками возврата ЦВ; payoff-лестница s26–s28 строится инкрементально; мост Л2 (s03) и сшивка context rot (s08) выполнены как обещал план; failure-слайды дают критерий+альтернативу, не страшилку.

### Топ-5 фиксов до рендера

1. **(P1) s20** — добавить 1 строку-микроглоссу к «инъекция» в visible: *«инъекция = подсунутый в контекст текст, который модель исполняет как команду; класс атак — Раздел 4»*. Закрывает единственное реальное нарушение §7 inline-required.
2. **(P2) s14** — к «дистилляция» на видимом слое дать 4-словную глоссу «(обучить меньшую модель повторять большую)» — снимет голый термин на visible для будущего PNG-only прохода.
3. **(P2) s15** — к «catastrophic forgetting» в карточке №3 добавить 3 слова «(деградация общих способностей)» — устранит forward-зависимость от s16 на видимом слое.
4. **(P2) s04** — в sub-caption добавить «CoT, few-shot — приёмы промпта, разберём в Разделе 1» (опционально; disclaimer уже частично закрывает).
5. **(render-fix, не md) s27** — передать presentation-critic/designer как watch-item: 49-клеточная матрица — пик плотности, нужен особый контроль читаемости при рендере (не текстовый дефект, текст notes сам вытягивает).

**Вердикт: APPROVE-WITH-POLISH.** Колода методически готова к рендеру; P0-блокеров нет; единственный P1 (s20) и P2-список чинятся точечными вставками в markdown без структурной переработки.
