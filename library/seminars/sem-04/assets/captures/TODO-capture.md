# TODO: материалы, требующие живой сессии Claude Code

Я (эта сессия) не имею доступа к интерактивной Claude Code-сессии с подключёнными MCP-серверами —
только к `Bash`. Всё, что требует REPL slash-команд (`/memory`, `/context`, `/skills`, `/mcp`),
реального диалога с агентом, реального вызова субагента или экрана plan mode — **не имитировано**
(рисованная имитация терминала недопустима — аудитория семинара сама работает в терминале).
Ниже — точная инструкция по каждой позиции: что снять, где, какой командой, что должно быть видно
в кадре, на какой слайд идёт.

Репозиторий для досъёмки существует публично и воспроизводим одной командой:
`git clone https://github.com/tellina-study/signup-landing-demo.git && git switch seminar-4-arc`
(см. `README.md` в этой же папке, таблицу опорных коммитов дуги). Досъёмка должна идти **на клоне
этой ветки**, чтобы дерево файлов на скриншотах совпадало с уже снятыми (`21-tree-stage1.txt`,
`22-tree-stage2.txt`).

---

## Позиция 2 (раздел 0) — первая задача агенту + уточняющий вопрос

**Машина:** любая с установленным `claude` CLI и реальной интерактивной сессией.
**Что делать:** в пустой директории `signup-landing/` (см. `01-ls-la-empty-dir.txt` — она уже
пуста, можно использовать этот же путь) запустить `claude`, одним запросом поставить задачу
дословно: «собери лендинг с формой заявки на демо-урок: статическая HTML-страница, сборка Vite
(`npm run build` → `dist/`), поля формы — имя и email, тест на Playwright, который проверяет, что
форма не отправляется с пустыми обязательными полями (`tests/form.spec.ts`)».
**Что должно быть видно в кадре:** текст задачи дословно + весь ответ агента, включая момент, где
он задаёт уточняющий вопрос про что-то не выводимое из кода (например, хостинг или команду
проверки сборки) — не отвечать на него развёрнуто, просто зафиксировать.
**Слайд:** Раздел 0, слайд 2 и слайд 3 (фрагмент диалога с уточняющим вопросом).

## Позиция 3 (раздел 1) — повторный вопрос про сборку/тест во второй сессии

**Что делать:** новая сессия того же репозитория (`CLAUDE.md` ещё нет), попросить добавить второе
поле в форму и прогнать проверку. Ожидается, что агент снова спросит команду сборки/теста.
**Что должно быть видно:** сам вопрос агента дословно, второй раз подряд в новой сессии.
**Слайд:** Раздел 1, слайд 1.

## Позиция 4 (раздел 1) — новая сессия после появления CLAUDE.md отвечает без переспроса

**Что делать:** после того как `CLAUDE.md` реально существует в репозитории (он уже там —
`git show 1019546:CLAUDE.md`, он же `28-code-CLAUDE.md.txt` в этой папке), открыть новую
сессию, спросить «как собрать этот сайт и проверить форму».
**Что должно быть видно:** вопрос + мгновенный ответ агента из `CLAUDE.md`, без переспроса.
**Слайд:** Раздел 1, слайд 3.

## Позиция 5 (раздел 2) — `/memory`

**Что делать:** в сессии того же репозитория сказать агенту «запомни: не подключаем стороннюю
библиотеку валидации форм — форма из двух полей, хватает нативного HTML5», затем выполнить
`/memory`.
**Что должно быть видно:** список файлов памяти, включая созданный
`feedback_native-form-validation.md`.
**Слайд:** Раздел 2, слайд 2. Позиция списка №5.

## Позиция 6 (раздел 2) — `/context` в новой сессии

**Что делать:** открыть новую сессию того же репозитория (после того как позиция 5 отработала),
выполнить `/context`.
**Что должно быть видно:** `MEMORY.md` виден среди загруженного контекста (не только на диске —
именно загружен).
**Слайд:** Раздел 2, слайд 4. Позиция списка №6.

## Позиция 10 (раздел 4) — `/skills`

**Что делать:** в любой сессии `lessons-3bb49d40` (не нужен `signup-landing`, воспроизводимо уже
сейчас) выполнить `/skills`.
**Что должно быть видно:** строки `pre-user-gate` (с description) и `build-deck` (без description
— просто имя) рядом, для контраста. Реальные тексты description уже проверены и лежат в
`10-skills-frontmatter-head5.txt` / `11-skills-frontmatter-table.md` этой папки — но сам вид
панели `/skills` может форматировать иначе, поэтому нужен реальный скриншот UI.
**Слайд:** Раздел 4, слайд 3.

## Позиция 11 (раздел 5) — `claude mcp add` + `claude mcp list`

**Что делал я:** пробовала `claude mcp list` в demo-окружении — команда зависла («Checking MCP
server health…») и не дала ответа за 15 секунд (нет реально сконфигурированных серверов). Это не
подставной результат — я его не включила в captures, чтобы не выдать зависание за «настоящий»
рабочий вывод.
**Что делать на реальной машине:** `claude mcp add github ...` (реальный сервер, например
GitHub MCP, ключ — переменная окружения, не хардкодить), затем `claude mcp list`.
**Что должно быть видно:** сама команда подключения + строка `✓ Connected` в `claude mcp list`.
**Слайд:** Раздел 5, слайд 2. Позиция списка №11.

## Позиция 12 (раздел 5) — `/mcp`

**Что делать:** в той же сессии, где сервер реально подключён (позиция 11), выполнить `/mcp`.
**Что должно быть видно:** панель с числом доступных инструментов у подключённого сервера.
**Слайд:** Раздел 5, слайд 3. Позиция списка №12.

## Позиция 15 (раздел 6) — ответ субагента `diff-reviewer`

**Что уже готово:** реальный diff лежит в `14-diff-validate-js.txt` (правка `src/validate.js`,
ужесточение email-regex) — этого диффа достаточно, чтобы ревьюер мог его оценить. Файл
`.claude/agents/diff-reviewer.md` тоже реально существует (`31-code-diff-reviewer.md.txt`,
скопировать в `signup-landing/.claude/agents/diff-reviewer.md`).
**Что делать:** в реальной сессии Claude Code с установленным субагентом `diff-reviewer` дать
задачу «вызови diff-reviewer на этот diff» (или явно попросить делегировать ревью).
**Что должно быть видно:** ответ субагента, начинающийся строкой `VERDICT: PASS` или
`VERDICT: BLOCK`, раздел «что проверено» с конкретными файлами.
**Слайд:** Раздел 6, слайд 3.
**Важно:** я не стала подменять это своим собственным «ревью» того же диффа — план явно требует
именно отчёт субагента внутри Claude Code (изолированный контекст, не мои рассуждения), а не
рецензию произвольного ИИ-агента вообще.

## Позиция 16 (раздел 7) — баннер `plan mode on`

**Что делать:** в реальной сессии, `Shift+Tab` дважды перед нетривиальной задачей.
**Что должно быть видно:** баннер `plan mode on` в интерфейсе + попытка редактирования файла,
которая отклоняется (режим только читает/планирует).
**Слайд:** Раздел 7, слайд 1. Позиция списка №16.

## Позиция 18 (раздел 7) — заполненный `review.md` с вердиктом

**Блокер:** зависит от позиции 15 — вердикт (`PASS`/`BLOCK`) должен быть тем же, что реально выдал
`diff-reviewer` на реальный diff, иначе `review.md` будет выдумкой поверх невыполненного ревью.
**Что делать:** после позиции 15 скопировать `Tasks/review.md.template` (уже в демо-репозитории,
`signup-landing/Tasks/review.md.template`) в
`Tasks/2026-09-20_form-endpoint-fix/review.md`, вписать реальный вердикт субагента и раздел «что
проверено» со ссылкой на реально выполненные команды (например, тот же коммит `cf58408`,
см. `17-log-md-form-endpoint-fix.txt`).
**Слайд:** Раздел 7, слайд 3. Позиция списка №18.

---

## Сводка

| # | Позиция | Статус | Требует |
|---|---|---|---|
| 2 | Терминал: первая задача агенту | TODO | живая сессия, пустой `signup-landing/` |
| 3 | Диалог: повторный вопрос до CLAUDE.md | TODO | живая сессия, 2-я сессия того же репо |
| 4 | Терминал: новая сессия после CLAUDE.md | TODO | живая сессия |
| 5 | `/memory` | TODO | живая сессия |
| 6 | `/context` | TODO | живая сессия |
| 10 | `/skills` | TODO | живая сессия (репозиторий курса, без signup-landing) |
| 11 | `claude mcp add`/`claude mcp list` | TODO | живая сессия + реально настроенный MCP-сервер |
| 12 | `/mcp` | TODO | живая сессия + подключённый сервер |
| 15 | Ответ `diff-reviewer` | TODO | живая сессия с субагентом `diff-reviewer` |
| 16 | Баннер `plan mode on` | TODO | живая сессия |
| 18 | Заполненный `review.md` | TODO (блокирован позицией 15) | живая сессия |

**Итого 11 позиций из 18 остаются на досъёмку** — все требуют реальной интерактивной сессии
Claude Code (частично — с подключёнными MCP-серверами), которой у этой сессии нет. Остальные 7
(плюс отдельно запрошенные хук/symbolic-ref/фронтматтер-скиллы) сняты по-настоящему, см.
`README.md`.
